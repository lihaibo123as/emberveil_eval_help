# EVAL_HELP 插件项目记忆（EmberVeil 全职业施法工具）

- **★★★★★【用户要求：模版加入猎人分类】顺带挖出「导出→导入」的静默丢弃缺口（1.70.43，本分支，用户截图）**：用户原话「将以上模版加入案例模版: 猎人分类」。**① 新增模版**：`EVAL_IO_TEMPLATES` 加 `{ cls = "猎人", ... }`，内容照抄截图 6 条（选取目标:最近敌人 → 猎人印记 → 毒蛇钉刺 → 宠物:攻击 → 猛禽一击 → 奥术射击）——★**顺序即优先级，必须忠实照抄**。模版 UI（`EVAL_HELP_TPL_BUILD`）自动遍历该表出【分组】标题+行，窗口高度按 `34 + rows*20 + 34` 动态算，**新增分组无需改别处**。**② 挖出两个真 bug（都是「不报错的空操作」）**：ⓐ **11 个取反条件导出后无法导入**——`EVAL_COND_STR` 一直会写 `不可攻击/不可流血/非精英/非Boss/目标非战斗/非友善/非敌对/非中立` 与 `未就绪/不可用/已排队`，而解析表 `COND_BOOL`/+COND_FLAG` 里**只有正向写法**（取反原本只认 `!` 前缀）→ `EVAL_PARSE_ONE` 返回 nil → `EVAL_PARSE_CONDS` 只 push 非 nil → **条件被静默丢弃**。影响：任何用到这些条件的方案，「导出→导入」或经案例模版导入都会掉条件。修复=补齐 8 条 `COND_BOOL` 取反项 + 新增 `COND_FLAG_INV` 表（flag 的 inv 语义用「键名」表达不了，必须单独一张表）。ⓑ **无条件技能行被静默丢行**——`string.match(body, "^([^|]+)|(.*)$")` 里的竖线是**字面量**（Lua 模式没有 | 交替语义！），于是「行内必须有竖线」是隐含要求；导出侧 `EVAL_PROFILE_TO_TEXT` 总补一个竖线所以自家往返没问题，但**手写模版漏竖线那一行就无声消失**（我第一版模版写 `- 宠物:攻击`，6 条只剩 5 条）。修复=无竖线时兜底为「技能名 + 空条件」。**★方法论（本轮最大收获）：序列化器的「输出域」必须用一条【导出→导入往返】断言整体锁死**——写出组 59：遍历全部条件类型（正/反/flag 两态），对每个断言 `EVAL_PARSE_ONE(EVAL_COND_STR(cd))` 能读回且 k/v/inv 一致；它一次列出**全部 11 个缺口**，远胜我手写几个病案。**★另加组 60：把**每一个模版**都过一遍 `EVAL_PROFILE_FROM_TEXT`**——因为模版文本在**加载期不会被解析**，写错（拼成未定义变量/条件不合法）不会在 luacheck 或启动时报错，只在用户点「导入」那一刻爆或静默掉条件。**★变异矩阵（4/4 全捕获）**：`drop-negation`（删 不可攻击 项）→组 59 broken=1；`drop-flaginv`（删反向 flag 查表）→组 59 broken=3；`drop-bareline`（删无竖线兜底）→组 60「a bare skill line still parses」；`drop-hunter`（改分组名）→组 60「a 猎人 group exists」。**★坑**：变异脚本里跨行锚点在 **CRLF** 文件上匹配不上（`\n` ≠ `\r\n`，两个变异体一度「未落地」）→ **CRLF 源码的多行锚点要么带 `\r\n`，要么改用单行锚点**。**★另一个坑**：run_code 的模板字面量里写反引号（``!``）会提前终止字符串；写 `[".."]` 之类含引号的 Lua 代码时，用数组 join 拼行比长字符串更安全。

- **★★★★★【用户实测两个缺陷】①「类型: 全部」过滤钮整颗不显示（第 10 次 local 作用域坑）② 检索 NPC 的小圆点要「随机彩色」（1.70.41，本分支，用户截图）**：用户原话「1. 应该有个按钮没了. 2. 检索的npc/怪 地图数据没有绘制圆形的随机彩色的标注.」——**注意截图标题显示 `v1.69.2`，但那是「源码里的 VERSION 常量」，不是真的旧构建**（发版时我只改了 .toc 的 `## Version`，漏了 `EvalHelp.lua` 的 `local VERSION`），所以游戏跑的确实是新代码、两个问题都真实存在。**① 按钮消失的根因**：`DS.filterBtn` 用 `DSL_ROW_BTN_Y` 定位，而该 local 在 **35 行之后**才声明 → 那里读到的是**全局 nil** → `SetPoint("TOPLEFT", parent, "TOPLEFT", x, nil)` → **本客户端对 nil 锚点不做任何定位、也不报错** → 按钮落在未定义位置 = 看不见。**这是本项目第 10 次 local 作用域坑，也是「不报错的空操作」的又一实例**（Lua 不报错、pcall 全成功、测试桩也不校验 SetPoint 的坐标参数 → 三层都测不到）。**修复**：把一组布局常量（`DSL_ROW_CY/BTN_H/BOX_H/BTN_Y/BOX_Y`）**集中上移到 EVAL_DS_BUILD 函数开头**，一次性消除「相互引用的局部量声明顺序」这类陷阱。**② 随机彩色的关键设计判断**：用户要「每只怪一个随机颜色，便于区分」——但**颜色必须由实体 id 派生（确定性哈希），不能每次绘制现摇随机数**；否则每 0.25s 一次重绘、或只是切图回来，颜色就会全变，**用户根本无法「靠颜色记住哪只是哪只」，随机色反而沦为视觉噪音**。实现 `dsEntityColor(id)`：32 位整数哈希 → 色相（黄金角铺满色轮）+ 受限饱和度/亮度 → HSV→RGB，纯函数、跨会话稳定。**★变异矩阵（最终 3/3 全捕获）**：`color-random`（调用点改成 `math.random()`）→「the drawn colour IS the id-derived colour」；`color-flat`（hue 写死成常量 0.11）→「hue actually varies across ids（got 1 deciles）」；`layout-nil`（重新引入未声明 local）→ LAYOUT CHECK 直接报行号。**★★本轮又踩两次方法论老坑（本文件反复记过）**：ⓐ **「断言解析函数 ≠ 断言调用点」第 3 次发作**——最初只断言 `EVAL_DS_ENTITY_COLOR(id)`（解析函数），于是 `color-random` **存活**（调用点现摇，解析函数依然正确）；修复=扩展 `EVAL_DS_TEST_CAPTURE_DRAW` 记录**调用点实际传的 r/g/b**，并断言「drawn colour == dsEntityColor(id)」+「二次重绘同色」。ⓑ **「断言存在性 ≠ 断言实质」**——distinctness 断言用 `%.2f` 格式化 RGB 去重，**hue 写死时饱和/亮度仍让三元组两两不同，distinct 依然达标**，于是 `color-flat` **存活**；修复=把 RGB 转回**色相**并断言「色相跨 id 落在 ≥3 个十分位」。**教训：断言要盯住「要保的那条性质本身」（这里=色相多样性），而不是它的粗糙代理（三元组不相等）。** **★新增两道源码级检查（放在能读文件的一侧）**：`LAYOUT CHECK`（EVAL_DS_BUILD 内布局常量必须声明早于首次使用；**必须跳过注释行**，否则解释该 bug 的注释自己会被误报成「使用」——首版即如此）；`VERSION CHECK`（`EvalHelp.lua` 的 `local VERSION` 必须等于 .toc 的 `## Version`——**版本号有两份就必须有断言盯着它们相等**，这次正是漏改源码那份导致标题显示旧版本）。**★编辑工具坑（第 3 次踩）**：用 run_code 写含正则转义的 JS 字符串时**反斜杠被吞**，生成出语法错误代码。**规避：写进文件的正则一律用 `new RegExp(...)` 拼接或 `String.fromCharCode(10)`，绝不在待写内容里放反斜杠转义。**

- **★★★★★【A 方案落地】删掉「定时强刷 1.5s / 慢速兜底 5s / 稳定期闸门 0.8s」三件套，改为「签名一变立刻重绘」（1.70.40，本分支，用户选定 A）**：用户问「那先地图切换能正确是（被）地图事件（驱动）.就能更快的正确渲染当前地图标注了吗?哪些 5s 兜底渲染、1.5s 渲染还有必要吗?」→ 我实测取证后用户选择 **A 方案：每张图都画，中间态可以闪一下**。**① 决定性事实：本客户端没有「地图事件」这条路可走**——`grep` 遍 UnrealQuest **全部 6 处 `RegisterEvent`，没有任何一个是地图/区域事件**（唯一区域相关的是 `PLAYER_ENTERING_WORLD`）；它的地图标注层**完全靠轮询**（`WorldMapPins.lua:140 REFRESH_INTERVAL = 0.25`）。所以「任务插件切图能正确显示」靠的是 ① tick 帧挂 `WorldFrame`（1.70.39 才修对）② 签名一变就画。**照做即可，不需要自己发明事件层**。**② 三件套全部删除，各自的成立前提都已被证伪**：ⓐ **1.5s 快速修复**：全文日志**一次都没触发过**（只有「定时兜底」，没有一条「强刷自愈」）→ tick 父级修好后它是死代码。ⓑ **5s 慢速兜底**：唯一依据是「客户端上报区域滞后 13.7 秒」，而**这条证据本身是坏构建产生的**——`13.7 ÷ 0.25 = 54.8 ≈ 55`，原文正说「期间 tick 跑了 55 次」，**55 次恰好等于 13.7 秒满速 tick**；可当时地图是开着的，而地图开着时 tick 根本不跑（父级 bug）。**该数字自相矛盾，不能作为「上报滞后」的证据**；基于它加的兜底 = 用每 5 秒一次全量重建（12 次/分）换一个不存在的收益。ⓒ **0.8s 稳定期闸门**：当初为「大陆过渡时把区域坐标画到大陆画布」而加，但真凶同样是父级 bug；**更糟的是它成了「切图不更新」的主因**——日志铁证 `t=3922342.934 视图变化 → 17|Barrens（等待稳定）` … `t=3922477.306 视图变化 → 215|Mulgore（等待稳定）`，**134 秒里一次都没「稳定」过 → 一次都不重绘**，屏幕停在上一次兜底画的旧图上。**这正是用户报了很久的「滞后一拍」的残余来源：不是检测慢，是闸门把重绘全吞了。** **③ 新语义**：签名一变**立刻画**，只保留「先记签名再画」（避免绘制中途出错时每 tick 反复重建）；频率仍由 `DS_ANN_INTERVAL=0.25` 节流，且重绘只在签名**真的变化**时发生。**④ 实测响应速度**：`视图变化 map=nil → 14|Durotar` → `视图稳定` = **1.0 秒**（那 1 秒还是闸门自己造成的），现在闸门删掉后就是**下一拍（≤0.25s）**。**★变异矩阵（3 个全部被捕获）**：`regress-settle`（重新引入稳定期闸门）→「a signature change redraws on the VERY FIRST tick」；`no-sig-guard`（去掉签名未变快路径）→「unchanged signature: 20 ticks cause ZERO queries」（got=40），这条同时守住了频率防护；`no-hide`（无视图时不收起整层）→「the whole layer is hidden when the map has no area」。**★教训一（本轮最普适）**：**一个兜底机制在「前提被证伪」之后必须被删掉，不能因为「留着也无害」而保留**——这三件套不但「无害」不成立（5s 兜底每分钟 12 次全量重建、闸门直接吞掉重绘），还会让后来的人以为「这里有保护」。**先证伪前提，再决定去留。** **★教训二：兜底机制的「证据」必须复核来源**——5s 兜底挂在一条自相矛盾的数字上，我却沿用了两个版本（1.70.33→1.70.40）。**把证据本身做一次算术/逻辑自检**，比多写三个兜底有用得多。**★教训三：旧测试会固化旧语义，必须随语义改写**——本组共改了 **3 处**（原 53 组「未稳定就不画」断言、原 51 组「定时强刷/慢速兜底」整组、以及首版闸门断言），若不改就会「测试全绿但测的是已废弃的行为」。**★测试自身的坑（本轮第 2 次踩）**：我写的「连续切图」用例序列**从 17 开始**，而前一条用例刚好也停在 17 → 签名未变 → 不重绘 → 断言失败，**是测试自己写错而不是产品 bug**；改成从「与前一状态真正不同」的区域开始才有效。**构造「连续变化」类用例时，首项必须与当前状态不同，否则第一拍根本不是在测「变化」。**

- **★★★★★【根因终定案】「切图不更新 / 滞后一拍」= tick 帧挂在 UIParent，而本客户端打开全屏世界地图时会隐藏 UIParent → OnUpdate 停发 → 整个标注层在地图开着时停摆（1.70.39，本分支，用户第三次取证 + 参照任务插件）**：用户原话「可以查看任务插件 地图切换之后能正确的显示当前地图任务图标」——**这句话就是破案的关键线索**。**决定性证据（两张截图）**：① 凄凉之地截图的浮层写着 `客户端报: area=17 zone=3 how=unique` / `mapFile=Barrens 名字=贫瘠之地` / `GetMapInfo=Barrens 256x256` / `已画签名=405esolace 池=303 可见=259` / `稳定中=true` —— **客户端明确报的是贫瘠之地(17)，我们画的却是凄凉之地(405)**；② 血蹄村截图（对应 area=405）钉子分布完全正确。**注意「贫瘠之地」截图把 256x256 的小图铺满了整个窗口、标注糊在所有区域上**：`GetMapInfo=Barrens **256x256**` 而之前杜隆塔尔是 `1002x668`——**这就是「区域百分比被画到大陆画布上」的直接观测**。**根因（UnrealQuest `Core/Driver.lua:467-482` 有明文，我此前一直没读到）**：「A frame receives no OnUpdate while it or any ancestor is hidden. **UIParent is part of the game UI and gets hidden -- the fullscreen map presentation being the case that bit us**: the whole addon's periodic work stopped for as long as the map was open. **WorldFrame is the 3D viewport and is never hidden by the UI**.」对方因此把驱动帧挂 `WorldFrame`，并点明 **pfQuest 也是这么干的**（`pfMap = CreateFrame("Frame", "pfQuestMap", WorldFrame)`）。而 EvalHelp 的 tick 帧是 `CreateFrame("Frame", "EVAL_DATASEARCH_TICK", **UIParent**)`——**全项目从未出现 WorldFrame（grep 零命中）**。**这一个父级错误完整解释了用户报了很久的三个症状**：① **地图开着时 tick 不跑** → 切图当场不重绘；② **关图瞬间 UIParent 恢复 → tick 恢复**，此时客户端早已把画布重建清空，我们才补画**上一次**的图 → 恰好表现为「滞后一拍」；③ 再开图时画布又是新的、而已画签名还记着旧图 → 「数据还是上次那张图的」。**★★为什么前几轮全部失败（最值得记住的一条）**：我加的「轮询+签名比对」「定时强刷 DS_ANN_FORCE=1.5s」「慢速兜底 5s」「稳定期闸门 0.8s」**全都建立在一个从未被验证的假设上——tick 一直在跑**。这个假设**在地图打开时是假的**，所以那三层加固全在空转；更要命的是，**tick 不跑 → 日志也不产生**，于是我继续用「日志里没有 X」去推断，越推越偏。**★教训一（本轮最普适）：「定时刷新」类修复之前，先确认「定时器在地图打开时是否真的在跑」**——我连续用日志推断五轮，却从没问过「这些日志本身是不是在地图打开时根本不产生」。**沉默的日志有两种含义：「没触发」和「根本没跑」，必须能区分**。**★教训二：对标「同类功能正常的插件」时，要读它的**驱动/生命周期**代码，不只是业务代码**——用户那句「任务插件切图能正确显示」是决定性提示，我若早半小时去读对方的 Driver.lua（而不是反复读 WorldMapPins 的业务逻辑），一轮就能结束。**★教训三：本项目记忆里早有「开图绝不 Toggle —— IsShown 不可靠」这条**，而同一条记忆的**下一条**就写着「全屏地图呈现会隐藏 UI」的完整机理——**答案一直在我自己的记忆文件里，我只是没往下读**。**修复**：① tick 帧父级改为 `WorldFrame`（pcall 探测 + 拿不到退回 UIParent，聊胜于无）；② 新增 `EVAL_DS_TEST_TICK_FRAME()` 暴露帧本身供断言检查**父级**；③ **测试桩补上「祖先隐藏 → 不派发 OnUpdate」这条铁律**（旧桩把 `CreateFrame` 的 parent 参数**直接丢掉**，于是「挂 UIParent」和「挂 WorldFrame」长得一模一样——**这个 bug 在测试里完全不可见**，又是「桩太宽松」的第 N 次发作）。**★变异矩阵（2 个全部被捕获）**：`parent-revert`（父级改回 UIParent，即原 bug）→ 断言在「the annotation tick is parented to WorldFrame」处失败 ✅；`nofallback`（WorldFrame 探测去掉、恒用 UIParent）→ 同上 ✅。**★★本轮又踩「local 作用域」坑（第 9 次）**：我把 `EVAL_DS_TEST_TICK_FRAME()` 写在前面、`local dsTick` 写在 300 行后 → 访问器读到全局 nil，**测试当场抓到**（got=false want=true）；修复=在文件前部加 `local dsTick = nil` 前向声明、原处改为赋值。**★备份污染（第 3 次，教训完全一致）**：我在**加访问器之前**拍的 `_DS_good.lua`，回滚一次就把新写的三处修改全部抹掉（测试立刻报 `attempt to call a nil value (global 'EVAL_DS_TEST_TICK_FRAME')`）。**铁律重申：变异基线必须取自「跑绿之后」的快照，且回滚后必须核对哈希 + 重跑测试**——本轮最后用哈希 `0B11F67F...` 对齐才止住。

- **★★地图诊断浮层 + 稳定期闸门 + 诚实放置（1.70.36~38，本分支，用户建议）**：用户原话「要不你添加个添加绘制日志信息在地图上面就和在地图上面绘制方块以下.在切换地图之后.直接绘制输出当前地图地名.标签数据.等信息」。**① 地图诊断浮层 `/eh ds hud`（1.70.38）**：把诊断状态**直接画在世界地图左上角**（半透明底衬 + FontString），内容含「客户端报的 area/zone/how」「mapFile/名字」「GetMapInfo」「已画签名/池/可见数」「本次显示」「稳定中」「图层/随机点」。用 `Client.GetWorldMapCanvas()` 当父级（与钉子同画布 → 随地图显隐）；**areaId 为 nil 时也刷新**（用户要能看到「客户端报 nil」这件事本身）；文本只在变化时 SetText（频率防护）。**★★实现时的关键坑（第 8 次 Lua local 作用域）**：我把 `EVAL_DS_HUD*` 函数写在前面、状态变量 `local dsHudOn` 写在后面 → 函数读写的是**全局**、local 永远 false，`EVAL_DS_HUD_STATE()` 恒返回 nil（测试当场抓到 got=nil）。**状态变量必须声明在使用它的函数之前**。**② 稳定期闸门（1.70.37）**：签名连续保持 `DS_ANN_SETTLE=0.8s` 才允许重绘，过渡/缩放/大陆状态下一律只登记不绘制。**根因**：本客户端在**大陆/缩放过渡**下 `zoneIndex` 仍报非 0 → UnrealQuest 的 `zoneIndex==0` 守卫不生效 → 我们把区域百分比坐标画到**大陆画布**上 → 钉子糊满整张图（用户截图「迷雾之海」即大陆视角）。日志证据：14 次视图变化里 **7 次是 `map=nil → X`**。**③ 诚实放置（1.70.35）**：`dsAnnPlace` 原来 `pcall(PositionWorldMapPin) 后丢弃返回值 + 无条件 Show + return true` —— 画布不在时仍报告「放了 303 个钉子」，`dsAnnShownCount` 也数得出 303，**「信号都说健康、屏幕却是空的」**。现在定位失败就不 Show、不算成功。**④ 随机点测试 `/eh ds rnd`（1.70.35）**：每 2 秒往随机位置放 8 个随机色钉子并**如实统计 `PositionWorldMapPin` 成败**。**实测 9 轮 72 点 成功=8 失败=0，跨区域 14→17 都可见** → **证明绘制链路完全正常、不是「只在地图打开时能画」**，把问题收敛到「我们问的区域 ≠ 画布显示的区域」。**⑤ 画布贴图校验（1.70.36）**：本想读 `WorldMapButton:GetTexture()` 作为独立判据，实测**恒为 nil**（该客户端不暴露），故**只观测不拦截**——若直接拿它当守卫，功能会整个失效。**这是「先验证信号再依赖它」的实例**。**★方法论教训（本轮最大收获）**：连续 5 轮靠日志推断都失败，瓶颈是**无法把「日志状态」与「屏幕画面」对齐**——把状态直接画在画面上（浮层）+ 让用户跑一次随机点探针，立刻定案。**当排查反复卡住时，先造「让证据自己出现在现场」的工具，而不是继续加日志**。**★变异矩阵（6 个全部被捕获）**：稳定期闸门 `nogate`/`zerothreshold`/`noreset`；浮层 `nohud`/`nilonly`（areaId=nil 时跳过刷新）/`noarea`。**★测试坑（第 3、4 次同类）**：ⓐ 浮层用例的 stub **硬编码 `return 14`**，完全不理会 `TEST.viewArea=false` 哨兵 → 「无视图」用例静默测错对象；ⓑ 断言只查 `type(text)=="string"`，而旧文本会一直留在变量里，**跳过刷新也能通过** → 必须断言文本**内容**（`area=nil`）而非仅存在性。**★备份污染（第 2 次）**：`Copy-Item` 拍 `_DS_good.lua` 时恰逢变异体生效，导致备份与源码都带着 `noarea` 变异，回滚后功能仍是坏的（HUD 显示 area=?），**靠 grep 源码核对才发现**。**教训：备份后必须校验哈希/关键行，回滚后必须重跑测试且检查「失败是否来自预期断言」**。
- **★★★强刷从「盲目重建」改为「安全网自愈」+ 大陆视图判据澄清（1.70.32，本分支，用户实测取证）**：用户第二次取证后日志给出**决定性数据**，推翻了我 1.70.31 的两个假设。**① 强刷本身是生效的**——`定时强刷 map=14 共 303 点` 每 1.500 秒一条、持续不断（上次显示 0 条是因为取证时旧构建/稳定窗口未覆盖）。**② 但盲目重建是严重设计错误**：每分钟 **40 次完整重建**（清空全部钉子 → 重新查库 → 重新摆 303 个钉子），必然掉帧闪烁；而它想解决的「检测被漏掉」在日志里**从未发生**（16 次重绘全部由「视图变化」正常触发）。**→ 修正语义：强刷是安全网，只在「层本该有钉子却一个都没显示」（`dsAnnShownCount()==0`）时才重建**；正常情况零开销，真被漏掉最多 1.5s 自愈。**★教训：兜底机制必须「先校验、再修复」，不能无条件重做——无条件重做等于用性能换一个不存在的收益**。**③ 大陆视图判据澄清（我此前推断错误）**：实测 `report: area=14 mapFile=Durotar continent=1 zoneIndex=1 how=unique 可见钉子=303` —— **客户端在用户看来像「卡利姆多大陆图」时，报的仍是具体区域（zoneIndex=1）**；而 UnrealQuest 的 `MapContext:GetViewedZone`（MapContext.lua:504-508）**本身就处理大陆视图**（`zoneIndex == 0` → 返回 `nil, report, "continentView"`）。所以**非 nil 的 areaId 就是「具体区域视图」**，我们不需要、也不应该再自己判大陆——`dsViewSig` 的 `type(areaId) ~= "number"` 判据是对的。**★教训：客户端报的 areaId 与用户「看到的画面」可能不一致，此时不该用画面去反推代码逻辑，而要看客户端自己暴露的语义**。**④ 新增诊断**：重绘日志追加 `dsViewReportStr()`，原样打印 `report.area/mapFile/continent/zoneIndex/how` + 可见钉子数——**这次正是靠它一行定案**；以后遇到「显示不对」先看这一行。**★变异矩阵（3 个全部被捕获）**：`blind`（退回盲目重建）→「healthy layer: the force check is a no-op」；`neverheal`（安全网不修）→「a wiped layer is repaired by the safety net」；`noforce`（去掉强刷）→ 同上。**★★测试桩第三次踩同类坑（1.70.25 / 1.70.27 之后）**：`dsAnnShownCount()` 依赖 `f:IsShown()`，而**测试桩的钉子是无方法纯表**，恒返回 0 → 安全网每次都误判「钉子丢了」（用例假失败）；补上真实 shown 状态后又发现**第二个更隐蔽的坑**：`dsAnnPins` 是**模块级状态、跨用例残留**，`dsAnnPlace` 按序号复用（存在即不新建）→ 本用例的桩**根本没被用到**，仍在使用前面用例的旧帧。加 `EVAL_DS_TEST_RESET_PINS()` 在用例开头清池才解决。**教训：模块级缓存/池子必须先重置，否则用例测的是上一个用例留下的对象；「桩没被用到」比「桩不对」更难发现**。
- **★★★切图不更新/滞后一拍：视图检测（轮询+签名比对）被客户端的瞬时 nil/旧值骗过 → 改为「定时强刷」自愈（1.70.31，本分支，用户建议并采纳）**：用户原话「首次打开地图标注正确。鼠标点击其他地图，数据不更新。然后关闭地图，重新打开地图，这次的数据才更新是上次点击切换的那个地图的数据。如果地图切换事件没法正确捕捉的话，能否做个在打开地图之后定时刷新这个点数据，地图关闭就关闭这个机制」。**日志铁证**（按时间戳重排后一目了然）：`t=3918005 视图变化 14|Durotar → 17|Barrens` → `重绘 map=17 共 500 点`（**数据其实是对的**）→ 之后**连续 287 秒**全是 `地图=17 未变 → 跳过重绘` → 突然 `t=3918292 视图变化 map=nil → 14|Durotar|1|1`。**关键在最后那一行的 `map=nil`**：签名在中间被**某次 nil 清掉了**，而清签名只有 `dsAnnHideAll()` 一条路——即某个 tick 里 `dsViewSig()` 返回了「无视图」。**根因**：本客户端在**区域切换瞬间**（世界地图正被重新呈现）`GetViewedZone()` 会短暂返回 **nil 或仍是旧值**，而我们的重绘**完全依赖「轮询 + 签名比对」**：返回 nil → 走 `dsAnnHideAll()` 把刚画好的钉子清掉；返回旧值 → 判「未变」跳过重绘，可客户端此刻已把画布重建/清空。**两种情况下我们都不会再画** → 看到旧数据且**滞后一拍**；关图重开时客户端重建画布，我们**上一次**画的钉子才显出来——正是用户描述的「更新成上次点的那张图」。**修复（采纳用户的建议）**：新增 `DS_ANN_FORCE = 1.5` 秒的**定时强刷**——只要 `dsViewSig()` 还给出 areaId（地图可见）就**无条件重绘**，**完全不依赖变化检测**；地图关闭时上面的 `if not areaId` 分支已 return，机制自然停用，正合「关图就关掉这机制」。**★★位置很关键（我自己先写错了一次）**：强刷必须放在**「签名未变」分支内部**、即**变化检测快路径之后**。我第一版放在 `sig == dsAnnSig` 判断**之前**并 `return`，结果每次切图都会被强刷路径抢先处理，**切图延迟从 0.25s 变成 1.5s**——功能对但体感变钝。放对位置后：检测到变化 → 0.25s 内立刻重绘；检测被骗过 → 最多 1.5s 自愈。**教训：「兜底机制」必须排在「快路径」之后，否则兜底会吞掉快路径的延迟优势**。**★频率防护**：取 1.5s 而非每 tick 0.25s——重绘 = 查一次库 + 最多摆 500 个钉子，是重活（见记忆体频率防护总则）。**★变异矩阵（3 个全部被捕获）**：`noforce`（去掉强刷，退回纯变化检测）→「periodic force redraws even when the view signature looks unchanged」；`forcewhenclosed`（强刷不受「地图不可见」约束）→「nothing is drawn with no viewable zone」；`forceeverytick`（周期改 0，等于每 tick 重绘）→「no redraw before the force interval elapses（频率防护回归）」。**★测试桩又踩同一个坑（1.70.27 记过，这是第 2 次）**：测「关图」分支时我写 `TEST.viewArea = nil`，但该 stub 是 `return TEST.viewArea or 14` —— **nil 被 `or 14` 兜成了 14，用例静默测错对象**（断言 got=1 want=0）。改用**显式关闭哨兵 `false`**（stub 内特判返回真 nil）才测到。**教训重申：桩里任何 `or 默认值` 的兜底都会让「传入空值」的用例失效；要测空值必须传一个不会被兜的哨兵，并在桩里显式处理它**。
- **★★数据检索控制行「位置错行」：同行控件混用两套垂直基准（1.70.30，本分支，用户截图）**：截图红线段指出「占位提示比输入框文字低一截」。**根因**：同一行里两套基准并存——`dsBtn` 的按钮文字用 `SetPoint("CENTER")`（**中线**）、EditBox 用 `SetJustifyV("MIDDLE")`（**中线**，中线 -64），而占位提示 `ph` 用 `SetPoint("TOPLEFT", ..., -62)`（**文字块顶边**）。顶边 -62 的 11px 文字实际中线约 -67.5，**比输入框文字中线低 3.5px**——正是用户看到的那条偏移。**修复**：① `ph` 改为**按中线对齐**（`SetPoint("LEFT", ...)` + 中线 y）；② 该行所有 y 收敛为**同源常量** `DSL_ROW_CY`（整行中线 -64）/ `DSL_ROW_BTN_Y` / `DSL_ROW_BOX_Y`，各控件由它推导，不再各写魔法数字（**该行已两次出布局问题**，根因都是散落的硬编码 y）；③ 构建期把预期几何记进 `DS.controlRow`，测试断言「整行中线一致」。**★教训（通用，值得反复看）：同一行/同一栏里混用「顶边对齐」与「中线对齐」必然错位**——要么全顶边、要么全中线。**按中线对齐更稳**：字体链不同（enUS/ruRU）或字号变化时行高会变，顶边锚定会随之漂移，中线锚定不会。**★坐标系易错点（我本轮亲手踩）**：TOPLEFT 锚点的 y 是**顶边**，且 **y 越往下越负**，所以 `中线 = 顶边 - 高度/2`、`顶边 = 中线 + 高度/2`；我第一版写成 `中线 = 顶边 + 高度/2`（得 -48），**测试当场抓到**（got=-48 want=-64）。**写这类推导时把两条公式都写进注释并各带一个数值例子**，否则下次还会翻。**★变异矩阵（3 个变异体全部被捕获）**：`oldoffset`（把 ph 改回按顶边 -62 锚定，即原 bug）→「placeholder shares the search box center line」；`btnoff`（把按钮整体下移 4px）→「button 类型 shares the same center line」；`centersign`（中线公式符号写反）→ 同上。**★测试手法说明**：桩不记录 SetPoint 参数，所以断言走的是「构建期把与 SetPoint 同源的常量记下来」这条路——**关键是同源**：若测试自己再抄一份坐标，变异就测不出来（本项目已在此栽过 3 次，见 1.70.25/26/28）。
- **★★光环/buff 条件下拉加搜索框 + 「列表没有就用自己输入的名称」（1.70.29，本分支，用户要求）**：用户原话「debuff 和buff 条件类型下拉选择增加个输入框支持检索,没有检索信息.可以让用户自己输入名称」。**实现**：① `EVAL_DD_OPEN` 增加可选 `opts.search`——面板顶部一个 EditBox，输入即过滤（照抄 1.70.3 的 EditBox 教训：**必须显式 `SetJustifyH("LEFT")` + `SetTextInsets`**，本客户端默认居中偏右）；② 过滤与「自由文本行」判定抽成**纯函数 `DD_FILTER(items, locked, kw, allowFree)`**，返回「按显示顺序排列的**原始下标**」——UI 与测试共用（吸取前三轮的教训：测试里复刻逻辑会让变异不可见）；③ `opts.onFreeText`：关键字非空且表中无**完全同名**项时，末尾追加一行哨兵 **-1**，渲染为「✎ 使用输入的名称 "xxx"」，点击即把用户输入当作条件值——这正是用户要的「没有检索信息就自己输入名称」；④ 光环条件下拉启用 `search = true` + `onFreeText`。**★★本轮靠「先写断言再跑」抓到两个真 bug（都不是测试问题，是产品问题）**：ⓐ **重入被 toggle 吃掉**——输入触发 `OnTextChanged → ddUI.refilter() → 再次 EVAL_DD_OPEN`，而该函数开头的「重复点击同一锚点=收起」判断会把这次重入当成用户又点了一次按钮，**于是每打一个字下拉就关闭**；修复=加 `ddUI.reentrant` 标志并在 toggle 判断里排除。ⓑ **`ddUI.locked` 被 multi 门控**——原写法 `ddUI.locked = (ddUI.multi and opts.locked) or nil`，而**光环菜单是非多选**，于是它拿不到 locked，一旦输入关键字**分组标题行会被过滤掉**（用户正在输入时分组结构整个消失）；修复=locked 独立于 multi 存储（它的语义是「该行不可选」，与是否多选无关）。**★教训（第 4 次同源，务必内化）：凡是「打开面板」的函数被用于「刷新面板」，它开头的 toggle/尺寸/清空类副作用都会在重入时二次触发**——重入路径必须显式标记（`reentrant`）并让副作用分支让路。同轮还踩到 `EVAL_DD_OPEN` 打开时会清空搜索框，导致「打一个字就被自己清掉」，同样是重入副作用，用同一个标志解决。**★变异矩阵（10 个变异体全部被捕获）**：过滤层 `noheaderkeep`（分组标题也参与过滤）/ `wrongindex`（返回显示位置而非原始下标——会让 onPick/sel/icons 全错位）/ `nofree` / `alwaysfree`（已有同名项仍加哨兵）/ `casesensitive`；接线层 `toggle`（去掉重入例外）/ `lockgate`（locked 退回 multi 门控）/ `noteretain`（重入时清空关键字）/ `freealways` / `nofree`。**★仍要记的老教训**：**「原始下标 vs 显示位置」是过滤类功能的头号错位源**——我特意让 `pi` 始终是原始下标、另用 `shownList[slot]` 记录显示顺序，并专门加了 `wrongindex` 变异体守住这条；**任何在列表上叠加过滤的功能，都要先问「回调收到的下标是哪一个坐标系」**。**★测试基建小坑**：桩里 `dd:GetWidth()` 返回 nil → `math.max(60, nil-8)` 直接炸；改 `pcall` + 兜底宽度。
- **★★新增「目标类型」条件 + 光环下拉顺序大改（1.70.28，本分支，用户要求）**：用户原话「① 条件类型 添加: 目标类型: 野兽,元素 这种,支持下拉 支持 是/否；② 目标debuff 下拉信息优化.优先显示目标身上实时监测出来的debuff」。**② 的直接根因**：`EvalHelp.lua` 的光环下拉把 `EVAL_GO_SKILL_CHOICES()`（动作条**全部技能名**）**先 push**，实时项（◆/○/●/▲）再追加——下拉第一屏全是技能名，用户看到的正是「全是技能名称，不是 debuff 效果」。**修复**：顺序改为 ① 实时项置顶 → ② 已记录名 ◇ → ③ 其余技能名垫底，三组之间插入**不可点分组标题**（复用 1.70.25 的 `opts.locked`）。**① 的实现**：`SE_TYPES` 加 `kind="creature"` 的 `tCreature`（多选或关系 + 是/否，入 CTG_2 组）；`CREATURE_TYPES` 表（12 类：id 稳定入库 / loc 中文 / tok 英文）；求值 `condOne` 的 `tCreature` 分支；文本格式 `目标类型:野兽/元素` 与 `目标类型非:元素` 双向解析；下拉首行显示**客户端此刻实际报告的类型**。**★★最重要的设计判断：不要盲信也不要盲弃一个「被记为不可靠」的 API**——记忆里早有「UnitCreatureType 不可靠」一条，但**逐字读它的出处**后发现：那条结论针对的是「用类型**推断**可否流血」（黑暗犬被误判），**不等于返回值本身是垃圾**；而且它此前**只用于调试串、从未参与判定**，所以根本没有可信实测。于是本轮按「假设可能不准，但**绝不静默失败**」来设计：宽松匹配 + 未知值可见 + 兜底项。**★这个判断当场就救了功能**：`test_stub.lua` 里早已硬编码 `UnitCreatureType = "人型生物"`——**本客户端返回的是带「生物」后缀的本地化名，而非标准名「人型」**；若只按标准名比对，`人型` 将**永远匹配不上且毫无报错**（正是我预设要防的失败模式）。**修复**：`creatureCore()` 剥离词尾后再比一次。**★此处又踩一个自造的坑**：首版把后缀表写成 `{生物,类,型,系}`，结果 `人型` 被剥成 `人` 而 `人型生物` 剥成 `人型`，**两边永远对不上**——因为「型/类」是**名字本身的组成部分**（人型/龙类），不是后缀；改成**只剥「生物」**后通过。**教训：做「容错匹配」时，剥离规则必须只针对确定的外来修饰，绝不能把可能属于本体名字的字也列入**。**★变异矩阵（7 个变异体全部被捕获）**：`strip-type`（把「型」也当后缀）→「suffixed label 人型生物 matches 人型」；`nosuffix`（去掉宽松匹配）→ 同上；`nov`（忽略是/否方向）→「目标类型非:元素 passes for 野兽」；`noother`（去掉未列出兜底）→「unlisted type matches the 其他 catch-all」；`noimport`（去掉文本解析）→「creature condition parses」；`noheader`（删掉实时分组标题）→「the VERY FIRST row is the live-section header」；`livelast`（把实时项挪到技能之后，即原 bug）→「live aura entries occupy the very top」。**★★本轮第三次踩同一类方法论错误，必须记牢：断言必须观察【真代码】，不能观察【测试里复刻的一份】**——我为下拉顺序写的首个访问器**自己重新实现了一遍顺序逻辑**，于是 `noheader`/`livelast` **两个变异体全部存活**（测试看的是自己的副本）。修复=把菜单结构抽成文件作用域 `SE_AURA_MENU(k0)`，**UI 与断言共用同一份**，抽完两个变异立刻被捕获。**这与 1.70.25（断言引擎≠断言接线）、1.70.26（断言解析函数≠断言调用点）是同一根源的三次发作**：**测试里任何「复刻逻辑」都会让被测代码的变异不可见**。**★另外两个「假通过 / 等价变异」教训**：ⓐ 顺序用例最初 `firstSkill=nil`（动作条为空）→「技能垫底」无从谈起，断言因**缺少对照组**而失败/无意义；补 `EVAL_GO_RESCAN` 造技能后 `livelast` 才变成可判别的真变异。**构造顺序类断言必须先确保「两组都非空」**。ⓑ 我第一版直接改 `TEST.slotNames` 以为能造出技能，但 `EVAL_GO_SKILL_CHOICES` 读的是 Engine **私有的** `wscanned/wslots`，必须走 `EVAL_GO_RESCAN` 才生效——**改测试桩不等于改了被测模块的内部状态**。**★其他实现坑**：① `EVAL_RULE_RUN` **会跳过「不在动作条」的技能**，本组断言必须改用新导出的 `EVAL_COND_EVAL(cd)`（单条件直调），否则恒定 false 且失败原因与条件无关；② 抽取 `SE_AURA_MENU` 时顺带清掉了一段**遗留的重复 ◇已学习 代码块**（旧实现残留，会把已记录项 push 两遍）；③ 三语言同步补 `CT_TCREATURE`/`CRE_*`/`SE_LIVE_AURA` 等键。
- **★★★切图后标注不更新（一直是旧数据）：图层开关「派生」与「持久化恢复」两套规则打架（1.70.27，本分支，用户日志取证）**：用户报「标注数据地图切换没正确更新数据」。**日志铁证**：每一次心跳都是 `分类层=false`，而存档里 `cfg.ds.nodes=true`、`cfg.ds.cats={herbs,mines}` —— **而我上一版刚把「总开关由类别数派生」定为核心设计（1.70.23）**，两者正好互相矛盾；层若真关着，屏幕上不可能有钉子（用户截图密密麻麻）。**根因**：`EVAL_DS_RESTORE()` **独立读 `cfg.ds.nodes` 恢复图层开关**，可 1.70.23 之后这个字段只是**派生结果的缓存镜像**、会过期。故障链：恢复读到过期 false → `dsAnnOn=false` → `dsAnnDraw` 的类别分支被 `if dsAnnOn` 挡住 → **每次重绘 0 个点** → 但 tick **在重绘之前**就把 `dsAnnSig` 记成了本图 → 此后每 tick 判「本图已画过」→ 切图时新签名虽触发重绘，**依旧 0 点** → **上一批钉子原样留在屏幕上** = 用户看到的「旧数据」。**修复三处**：① **恢复时以「类别集合」为唯一权威重新派生** `dsAnnOn`（`cfg.ds.nodes` 只作镜像回写，保持向后兼容）——**凡是「派生值」，恢复时都必须重算，绝不能读它的缓存**；② **图层关闭时 tick 必须「收起已有钉子」**（`dsAnnHideAll`）而不是「什么都不做」——旧实现跳过重绘，于是旧钉子永远挂在屏幕上，这正是「旧数据」的直接来源；③ 顺带把签名提交点调回「先记后画」（见下方教训）。**★教训一（最普适的一条）：一个状态若有「真值」和「派生值」两份，恢复/序列化时只能信真值**——我把 `dsAnnOn` 同时当作「可持久化的开关」和「由类别派生的结果」用了两轮，两轮都出 bug（1.70.23 派生、1.70.27 恢复读缓存）。**★教训二：关闭一个图层，必须同时回收它已经画出去的东西**——「不再绘制新的」和「清理已绘制的」是两件事，漏掉后者就是「关不掉/旧数据」（与 1.70.20 的 `TOGGLE` 坑同源，第 3 次出现）。**★教训三：「池子」的隐藏 ≠ 清空，断言要盯可观测契约**——`dsAnnHidePins` 只 `Hide()`、帧留在数组里复用，所以断言 `getn(pins)==0` 是错的（测试首版就错在这）；应断言 `IsShown()` 全为 false + 签名被清。**★变异验证（3 个变异体全部被捕获）**：`restore-stale`（恢复改回读 `cfg.ds.nodes`）→「restore derives the layer ON from the category set」；`nostalehide`（去掉关闭时的收起守卫）→「a tick with the layer OFF hides the leftover pins」；`areaid-only`（签名退回只用 areaId，即 1.70.24 那个回归）→「signature starts with areaId」。**★本轮两个「假通过」断言（都靠变异测试才暴露，值得单记）**：ⓐ **测试桩把变量写死了**——我的 46c 用例设 `TEST.viewArea=99` 想换图，但该用例自建的 stub 里 `GetViewedZone` **硬编码 return 14**，视图根本没变，断言却因「99≠14」而「通过」——**它测的不是我想测的东西**；改成 `return TEST.viewArea or 14` 后立刻暴露真问题。**教训：桩里任何硬编码的返回值都可能让用例静默测错对象**。ⓑ **我自己加的守卫是废话**——为「0 点重绘不得记签名」写了 `if n > 0 or dsAnnOn then dsAnnSig = sig end`，可 `or dsAnnOn` 让它恒真；深究后发现**「不记签名」本身就不是对的设计**（空地图会变成每 tick 重复查询，违反频率防护总则），于是**删掉该守卫并把断言改写成真正要保的不变量**：「换图必须推进签名 + 签名反映新区域」。**教训：写守卫前先问「这个分支真的可能为假吗」——恒真的守卫等于没有，还会让后来的人以为这里有保护**。
- **★★每个类别改用专属图标 + 自定义检索才用小圆点（1.70.26，本分支，用户截图要求）**：用户原话「查看每个类别的标注是否有对应的图标显示。自定义检索的数据(npc/无掉落等)才用小圆点显示」。**截图证据**：满屏绿色小圆点，灵魂医者（spirithealer）也只是一个圆点——**16 个类别全部用同一个 QuestDot 圆点，只靠颜色区分**。**根因两处**：① `dsAnnPin` 在建钉时**一次性固定** `DS_NODE_DOT_TEXTURE`（创建时定死）；② `dsAnnPlace` **只设颜色、从不设素材**。**修复**：① `DS_ANN_CATS` 每项加 `icon` 字段（素材名**逐个抄自 UnrealQuest `Map/NpcPins.lua` 的 CATEGORIES 表**，不自行拼写——本客户端素材路径写错是**静默不画**）；② 新增 `DS_ANN_ICON_ROOT = "Interface\\AddOns\\unrealQuest\\media\\icons\\"`（**抄 ClientAPI.lua:10226 的 NPC_SERVICE_ICON_ROOT**，无扩展名+小写插件名）；③ `dsAnnIconFor(l)` 照抄对方 `IconForLocation`：稀有怪按 rank 换脸（elite/boss/rare-mobs，照抄 RANK_ICONS）、草药/矿脉等**物件类优先用该物件自己的美术**（`UQ.nodeIcons`——Peacebloom 就该长得像 Peacebloom）、表里没有的（Incendicite/Indurium）回落类别图标；④ `dsAnnPlace` 改为**每次绘制重设素材与尺寸**（池子复用，创建时定死会素材残留——与颜色残留同一个坑）；⑤ **有图标时不染色**（照抄对方 NpcPins:1108-1113 的 if/else：有 icon 只设贴图，无 icon 才"圆点+颜色"）；⑥ 素材尺寸照抄对方（图标=WORLD_PIN_SIZE 15，采集/节点类=半尺寸 NODE_WORLD_PIN_SIZE≈8，否则上千点糊成一片）；⑦ **搜索结果定位（自定义检索）显式传 `icon=nil` → 小圆点+金色**，正好对上用户「自定义检索的数据才用小圆点」。**★教训一：素材是否存在必须用真实文件系统验证**——本客户端路径写错**不报错、pcall 全成功、纹理就是空的**，Lua 侧永远查不出来；我在 `test_engine.js` 里加了 **ICON CHECK**（读 DataSearch.lua 的 icon 清单 → 逐个 `fs.existsSync` 真实 .tga），启动即报 `16 category icons, 0 missing`，路径错/大小写错/加 .tga 后缀三种变异全部当场拦下。**★教训二：fengari 沙箱里 `io` 不可用**——首版把磁盘校验写进 Lua 断言，`io.open` 拿不到 → 断言假失败（got=0）；**校验真实文件系统的事就该放在能读盘的那一侧（node harness）**，Lua 侧只钉清单。**★变异矩阵（7 个变异体全部被捕获，且各自被对应断言捕获）**：`noicon`（删光所有 icon 字段）→「every category has an icon」；`badpath`（根路径加 .tga）→「icon root has NO .tga」+ ICON CHECK 直接 skipped；`upper`（插件名改大写 UnrealQuest）→「uses lowercase addon name」；`overlayicon`（搜索结果也塞图标）→「search-result pins are placed with NO icon」；`tinticon`（图标也染色）→「icon pins are not colour-tinted」；`nopooltex`（素材只在创建时设）→「place() sets the texture on every draw」；`noresolver`（分类调用点漏传图标）→「category annotations are placed WITH an icon」。**★教训三（本轮第二次踩，与 1.70.25 同型）：断言解析函数 ≠ 断言接线**——`overlayicon` 首版存活，因为我只断言 `dsAnnIconFor`（解析器）而**调用点写死的 `nil` 没人测**；`noresolver` 同理。补了 `EVAL_DS_TEST_CAPTURE_DRAW`/`EVAL_DS_TEST_CAPTURE_OVERLAY`：**包一层 dsAnnPlace 记录真实重绘时每条标注实际传了什么素材**，这才覆盖到调用点。**教训：凡是「函数 A 产出配置、调用点 B 消费配置」的分工，两边都要断言——只测 A 的话，B 写死一个常量就能让功能整体失效而测试全绿**。**★编辑工具坑**：我用 edit 工具写入含反斜杠的 Lua 字符串常量时，**双反斜杠被吃成了单反斜杠** → `invalid escape sequence near '"Interface\A'`；**含 Windows 路径的字符串常量写完必须回读核对原始字节**（或直接用 `String.fromCharCode(92)` 拼），别相信写进去的转义。
- **★★★下拉多选的两处实测缺陷：宿主忘传 opts.multi（面板点一下就关）+ 处理函数不信任面板回传值（点 A 却像点了 B）（1.70.25，本分支，用户截图实测）**：用户原话「①每次点击一个 下拉窗不要关闭——关闭只在按钮重复点击时触发开/关；②我选择的是草药，但会把矿脉也勾选上」。**决定性取证**：直接读存档 `cfg.ds.cats`，内容是 `{ herbs = true }` **只有草药一项**——**数据层完全正确**，所以缺陷 ② 纯属显示层，不必去查 `EVAL_DS_SET_CAT` 的逻辑。**两个缺陷同一个根因**：`DataSearch.lua` 调 `EVAL_DD_OPEN(btn, items, handler)` 时**没传第 4 个参数 opts** → `ddUI.multi = false` → 面板走**非多选分支**：① 该分支的 OnClick 是 `function() dd:Hide() onPick(pi) end` → **点任一项都立刻关闭**（缺陷 ①）；② 该分支不做方框绘制，于是我此前**把 ■/□ 拼进标签文本**的做法成了唯一渲染源，而面板内部 `ddUI.sel` 又是另一套状态 → 两者一旦不同步，用户读到的勾选就与数据不符（缺陷 ②）。**修复**：① `opts = { multi=true, selected=<逐行派生的勾选表>, locked=<分组标题行> }`；② 勾选态**只由面板按 selected 绘制**，标签里**绝不再拼 ■/□**；③ `onPick(pi, nowOn)` 的回传值**必须被采信**——旧写法 `EVAL_DS_SET_CAT(k, not EVAL_DS_CAT_ON(k))` 是「回过头再读一次数据源取反」，在面板与数据不一致时会连续同向翻转，**这正是「点 A 却像点了 B」的机理**；④ 新增 `opts.locked`（多选模式下分组标题行不画方框、不挂 OnClick、点击不回调），并把菜单结构从闭包**提到文件作用域** `dsCatMenu()/dsCatMenuSel()/dsCatMenuPick()`，让测试能直接断言；⑤ 新增 `EVAL_DD_SYNC(sel)`：批量动作（全部开启/全部关闭）后**整体重绘所有行**——只重绘被点那行会让其余 16 行停在旧方框上。**★本轮最重要教训（两条，都会重犯）**：**(a) 「测试的桩太宽松」会把真实断言变成假通过**——`test_stub.lua` 的 mock 帧 `Show/Hide` 是空操作、`IsShown` 恒返回 nil，于是「点选后面板是否仍打开」这类**行为级断言永远是 false**；我给 mock 帧补上真实的 shown 状态与 `SetScript/GetScript` 存取后才测得动。**写 UI 行为断言前先确认桩有状态**。**(b) 回归断言必须覆盖「接线」，不能只覆盖「引擎」**——首版测试直接调 `EVAL_DD_TEST_OPEN_MULTI`（引擎）与 `dsCatMenu()`（数据），**变异测试证明：把 DataSearch 里的 `opts.multi` 删掉，两条断言依然全绿**——因为引擎本身没问题，坏的是宿主忘了传参。补上「点真实 `DS.catBtn` → 断言面板确为多选态 → 点行不关闭 → 断言被点行渲染出实心方框、邻行仍是空心方框」后才捕获。**★变异矩阵（5 个变异体全部被捕获，这是本项目第二次系统性变异验证）**：`nomulti`（去掉 opts）→ 捕获；`toggle-read`（处理函数改回读数据源取反）→ 捕获；`nolocked`（分组标题不再锁定）→ 捕获；`baked`（把标记重新拼进标签）→ 捕获；`nosync`（批量后不整体重绘）→ 捕获。**★变异过程中的两个真实性陷阱（值得单独记住）**：**ⓐ 等价变异体**——`toggle-read` 首版「没被捕获」并非断言不够，而是**该变异在设计良好的新实现下是等价变异**（面板与数据恒同步时，「取 nowOn」与「读数据源取反」结果相同）；要判别必须构造**面板与数据故意矛盾**的输入（面板=开 且 数据本来就是开 → 正确实现保持开，旧实现会关掉）。**能捕获的测试用例必须落在「新旧实现结果不同」的输入上**，否则再多的断言也只是重复覆盖同一路径。**ⓑ 变异体没真正落地**——PowerShell 里 `node -e "...` 的引号被吃掉，`baked` 一度显示「未被捕获」，实为**替换根本没执行**；改用**独立 .js 脚本做替换**并**回读文件确认变异确实写入**，才发现真正原因（断言只查「含 ■」而变异体产生的是「■ X 草药」，方框仍在）。**教训：变异验证必须确认变异体真的应用了，否则会得出「断言有效」的错误结论**。**★测试助手自身的 Lua 陷阱**：`EVAL_DS_TEST_DD_PICK` 里写 `(nowOn ~= nil) and nowOn or not EVAL_DS_CAT_ON(k)` —— **and/or 惯用法无法表达 false**（nowOn=false 时左侧求值为 false，直接落到 or 的默认分支），导致「传入 false」被静默当成「自动取反」；必须显式 `if nowOn == nil then ... end`。**同类陷阱在断言里尤其危险：它让「测了 false 分支」变成「根本没测」**。**★文件安全教训**：本轮为跑变异用 `Copy-Item` 做备份，但**备份是在某次变异之后拍的**，随后 `Copy-Item` 还原**把已经在文件里的修复（`opts` 行与 `nowOn` 行）覆盖回旧代码**，一度让我以为「断言在正确代码上失败」。**教训：变异基线必须来自一次「跑绿之后」的快照，且用不会与变异路径重名的文件（如 `_DS_good.lua`）**；本轮最后改用固定 `_DS_good.lua` 并在还原后**先跑测试确认绿**，才止住这次来回。
- **★★★「勾选了类型但地图上不显示」= 视图签名只含 areaId，关图再开同图被判「未变」（1.70.24，本分支，用户实测 + 日志取证）**：用户报「选中的类型没有在地图上显示」。**日志是决定性的**：`[131] 心跳：分类层=true 当前地图=14 钉子池=230 本层=草药215 旅店1 邮箱1 修理8 兽栏1 灵魂医者4` —— **数据取到了、钉子也建了 230 枚**；随后却是连绵的 `地图=14 未变 → 跳过重绘`。**根因**：`dsViewSig` 只返回 `areaId`，于是「关图 → 再开同一张图」（areaId 仍是 14）被判定为「未变」→ tick 永不重绘 → 钉子停留在关图时被客户端清掉的状态 → **地图看起来是空的**。**修复**：签名并入 `mapFile|continent|zoneIndex`，**照抄 UnrealQuest `WorldMapPins.lua:678` 的 ViewSignature**（它正是为此把 mapFile/continent/zoneIndex 全部纳入，注释明写「a signature that ignored it would leave the old shape on screen until something else happened to dirty the layer」）。`EVAL_DS_ANN_REFRESH`/tick 改存签名而非裸 areaId。**★这是与 1.70.17 同一类错误的两端**：那次错用**不可靠的** `IsShown` 当可见性判据（关图后它可能恒 true）；这次走向另一极端——**一个能反映「视图变化」的信号都没留**。**教训：判断「视图是否需要重绘」应当用「视图自身的完整标识」（区域+地图文件+大陆+zoneIndex），既不要依赖帧可见性，也不要退化成单一 ID**。**教训：调别的插件前先读它同类功能的实现**——UnrealQuest 的 ViewSignature 早把这件事做对了，我若先看它就不会踩。**顺带修的布局**：① 按钮与输入框**不同行**（按钮 y=-78、输入框 y=-54）→ 按钮上移到 y=-56 使两者纵向居中于同一行（框中心 -64、按钮高 16 → 顶边 -56）；② 按钮加宽到 214（x=LX+310..542）填补与输入框之间的空隙；③ 占位提示原在 y=-82（**框外下方**）→ 移入框内 y=-62 垂直居中。**测试坑**：`string.find(s, "^14|", 1, true)` —— **plain=true 会连 `^` 锚点也当普通字符**，断言假失败；改用 `string.sub(s,1,3)=="14|"`。新增回归断言「同区域不同视图必须产生不同签名 + 触发重绘」并**变异验证**（签名退回 areaId-only → 断言立刻失败）。
- **★地图标注按钮合并 + 方框勾选样式 + 删除清理按钮（1.70.23，本分支，用户截图要求）**：用户要求「合并两个按钮，标注类别(N) 内部下拉项目支持第二张图的方式多选；删除清理标注的功能，开关已经在列表内具备了」。**改动**：① **三按钮 → 一按钮**：原「地图标注」(总开关) + 「类别(N)」+ 「清理标注」合并为单个 `地图标注(N)`（`DS.catBtn`，x=LX+330 w=194，右缘仍 542）——点它直接展开下拉。② **勾选样式改方框**：`EvalHelp.lua` 的 `EVAL_DD_OPEN` multi 模式原来用文字勾 `|cffffd100√|r`，改为 `■`(选中/金) 与 `□`(未选/灰)，与配置窗自绘勾选框 `cfgCheck`（金边+暗底+金色填充块）观感一致。③ **删除清理按钮**，但**必须保留「清除覆盖层」能力**——这是本轮最容易漏的点：`dsAnnOverlay`（搜索结果定位钉）在 `dsAnnDraw` 里**独立于 `dsAnnOn` 约束**（只要有 areaId 匹配就画），原「清理标注」是它**唯一的移除入口**；直接删干净会让这些钉一直挂到切图/重载。**处理**：新增 `EVAL_DS_ANN_CLEAR_OVERLAY()`，让下拉里的「**全部关闭**」顺带调用它——正好对应「开关已经在列表内具备了」的用户心智（全部关闭 = 关掉一切）。**★关键设计：总开关改为「跟随类别数」派生**——`EVAL_DS_SET_CAT` 里统计 `dsCatOn` 数量：任一类别开 → `dsAnnOn=true` + REFRESH；全关 → `dsAnnOn=false` + `dsAnnHideAll()`。**这正是让「开关在列表内」成立的前提**，否则删掉总开关按钮后图层会永远卡在某状态。**教训一：删除一个 UI 控件前，先 grep 它调用的函数被谁依赖**——「清理标注」看着只是个按钮，实际承载了 overlay 的唯一回收路径。**教训二：合并按钮时，原来两个按钮各自承担的语义必须重新指派**（总开关语义 → 派生；清理语义 → 挪进「全部关闭」）。**教训三：测试断言会随语义变化而失效**——旧断言「关着时改类别仍不画」在新模型下**必须反过来**（改类别 = 开图层），已改写成新语义并补「全关 = 图层关」+「清理覆盖层真的清掉」两条。**变异验证**：去掉派生逻辑 → 断言在「enabling a category turns the layer on」处失败 ✅。**顺带清理**：孤儿语言键 `DS_CATS`/`DS_CLEAR` 三语言全删（按钮没了，键也就没用了）。
- **★地图标注控制行布局收紧（1.70.22，本分支，用户截图「缩短按钮宽度.布局优化」）**：截图暴露两处——① **占位提示溢出**：`ph` 用 `dsText` 创建后**没设宽度**，原文「点击输入名称，停笔即搜（前 10 项匹配）」渲染为整串，横向压到右侧「地图标注」按钮上；② **按钮偏宽**（110/96/82 装 4 字标签）。**修复**：占位提示 `SetWidth(172)` 并**夹进输入框内**（框 120..300、提示 124..296）+ 文案缩短为「输入名称，停笔即搜」（`SetWidth` 是 FontString 的必要步骤——不设就会按内容无限延伸，这是自绘 UI 的常见坑）。**按钮收窄并重新排布**（右缘仍 542）：类型 18..114(96) / 输入框 120..300(180) / 地图标注 304..392(88) / 类别 396..472(76) / 清理标注 476..542(66)，间隙 4~6px。**同时发现 `focusBtn`（点击输入区聚焦的热区）仍是旧值 304 宽**，会在新布局下**盖住右侧三个按钮**（热区无视觉，点击被它吃掉，极难排查）——改为 180 跟随输入框。**教训：改布局时凡是「跟随某个控件尺寸」的隐形热区/背景纹理都要一起改**（grep 旧宽度值 `304` 是发现它的手段）；**教训：FontString 一律显式 SetWidth**，否则宽度由内容决定，换语言/改文案就会溢出——多语言插件尤其危险（enUS/ruRU 文案长度差异大）。
- **★地图标注 UI 调整：控件靠右 + 分组多选下拉 + 密度默认降档（1.70.21，本分支，用户要求）**：用户要求「移动到右侧」「支持下拉筛选 矿石/草药/宝箱之类的」「显示要支持地图当前点数据动态更新而不是把整个世界的数据都显示出来」。**① 控件靠右**：控制行从 `LX+104/226/324`（y=-78）移到 `LX+228/342/442`（右缘 542=560-18），搜索输入框从 w304 收到 w124（120..244）——**控件与输入框同行不同 y，但视觉上原来是压在输入框下方，属于「看起来挤在一起」的观感问题**。**② 下拉形式经用户确认=多选勾选**（我先后做了两版：先多选→以为要单选→问过后确认还是多选），现在是**分组显示**（— 节点（采集物）— / — 服务（城镇）— ）+ 顶部**全开/全关**快捷项；分组标题行用 `keys[pi]=nil` 实现不可点。**③ 密度默认降档**（应对「整个世界的数据」的观感）：服务类默认只留 flight/innkeeper/mailbox/repair/stablemaster/spirithealer 六项，**vendor/banker/auctioneer/meetingstone/battlemaster 默认关**（城镇里最密集的几类）。**④ 截断可见化**：达到 `DS_ANN_MAX=500` 时置 `dsAnnTruncated` 并在 `/eh ds` 里显示「已触顶，本图还有更多点未显示」——**静默截断会让用户以为「显示了整个世界的数据」或数据不对**（用户实测反馈正是这类观感）。**⑤ 澄清与验证**：用户确认第 3 条看到的其实是**旧构建**的行为（数据源 `GetAreaServiceLocations` 本就按 `coordinate[3]==areaId` 严格过滤，Database.lua:1572），并补了断言「layer queries the CURRENTLY VIEWED area only」+ **变异验证**（把 areaId 写死成 9999 → 断言立刻失败）。**教训：用户报「显示了整个世界的数据」时，先分清是「数据源没过滤」「我们传错参数」还是「点太密看起来像全图」——本次是第三种（观感），靠降默认密度+截断提示解决，而不是改过滤逻辑**。
- **★★「点开就关不掉 / 切图后都是旧数据」：关开关时不能走「强制刷新」路径（1.70.20，本分支，用户实测截图）**：用户反馈「点击之后采集点都显示了，但是无法关闭。切换地图之后都是旧数据」。**两个症状同一个根因**：`EVAL_DS_TOGGLE_NODES` 关掉时也调用 `EVAL_DS_ANN_REFRESH()`——而 REFRESH 是「无视开关、按当前地图重绘」，它会 ① 把刚关掉的层又画回来（关不掉）；② 把 `dsAnnSig` 设成当前地图，于是 tick 认为「这张图已画过」永不再重建（旧数据残留 + 再打开也不刷新）。**修复**：关闭分支改走 `dsAnnHideAll()`（隐藏 + 清签名），绝不 REFRESH；REFRESH 开头加 `if not dsAnnOn then dsAnnHideAll() ... end` 守卫。**教训：语义相反的两种操作必须走不同函数——「强制重绘」与「关闭并清理」是反义动作，用一个入口分派就会互相打架**。同类：`dsAnnDraw` 内部调 `dsAnnHideAll`（会清签名）也是这个坑的变体，已拆成 `dsAnnHidePins`/`dsAnnHideAll`。**★★★本轮最重要的方法论教训：我写的回归断言当时是「绿的」，但它根本抓不到这个 bug——用变异测试验证才发现**。过程：我把修复分别回退（去掉 REFRESH 守卫 / 去掉 TOGGLE 的关闭分支）后重跑测试，**两次都是 ALL TESTS PASS**。原因：`dsAnnDraw` 自身也有 `if dsAnnOn then` 门槛，所以「层关着→不画」这件事被两处实现互相兜住了，我的断言只观察到最终效果（没画），无法区分「是守卫起作用」还是「别处兜住了」。**最终有效的做法**：直接把 `dsAnnDraw` 的 `if dsAnnOn and ...` 门槛变异掉，断言立刻在「nothing is drawn after toggling off」处失败（got=1 want=0）——**这才证明断言真的覆盖了「关闭后不得再画」这条不变量**。**教训：回归测试写完必须做一次变异验证（把 bug 人为放回去，确认断言变红）**——否则「绿」只证明「当前实现能过」，不证明「以后坏了会被发现」。本项目此前从未做过变异测试，建议对关键不变量（开关语义、地图绑定、清理）都补上。
- **★★统一地图标注层：一套池子 + 地图绑定 + 全类别 + 可清理（1.70.19，本分支，用户要求）**：用户原话「所有在地图上的标注信息都要和地图绑定——切换之后标注要更新隐藏，切回地图要根据当前地图又重新显示；不单是矿石，所有标注信息都可能是跨地图的；然后再增加个清理标注的功能」。**旧结构是两套互不相通的钉子**：`dsPins`（搜索结果定位，12 枚，**60s 自熄、不绑地图**）+ `dsNodePins`（采集点，200 枚，绑地图）——表现为「搜索定位的钉切图不隐藏、只能等一分钟」，且节点层写死 `DS_NODE_CATS={herbs,mines,chests,fish}` 只认 4 类。**新结构**：① **单一池 `dsAnnPins`** 承接全部标注（分类层 + 搜索结果覆盖 `dsAnnOverlay`），不再有发射后不管的孤儿钉；② **每次重绘 = 清空整层 → 按当前地图重建**，天然满足「切走隐藏、切回重建」；③ **类别表 `DS_ANN_CATS` 覆盖 UnrealQuest 全部 16 类**（节点类 5：herbs/mines/chests/fish/rares + 服务类 11：flight/innkeeper/mailbox/banker/auctioneer/vendor/repair/stablemaster/spirithealer/meetingstone/battlemaster），**节点类默认关**（一个区域可达上千点）、服务类默认开；④ **清理标注** `/eh ds clear` + Tab 按钮（同时关图层开关，否则下一 tick 立刻铺回来，看起来像没生效）。**关键 API 认知**：`Database:GetAreaServiceLocations(areaId, classId, raceId, wanted)` 的 `wanted` **只 gate 节点类**（`NODE_META_KEYS`，Database.lua:1478），**服务类是默认就返回的**、靠调用方本地过滤——所以「支持全部类别」几乎零成本，只要把写死的 wanted 换成按开关组装。上限 `DS_ANN_MAX` 200→500（用户实测：杜隆塔尔采集点实际 466 个，200 被截断）。**★本轮踩的两个自己的坑**：ⓐ **`dsAnnDraw` 内部调用 `dsAnnHideAll()`，而后者会把调用方刚设好的 `dsAnnSig` 清成 nil** → 表现为「画了但签名丢失、状态查询恒 nil」；修复=拆成 `dsAnnHidePins`（只隐藏）与 `dsAnnHideAll`（隐藏+清签名），明确各自语义；**教训：一个函数同时做「清资源」和「改状态」两件事时，复用点一定会误伤**。ⓑ **`dsPersist` 写在 `dsAnnOn` 声明之前**（第 6 次踩 Lua local 作用域）→ `c.ds.nodes = dsAnnOn` 读到全局 nil，**不报错但持久化静默失效**；教训：**「不报错的空操作」比崩溃更难查，状态变量声明必须集中在文件靠前位置**。
- **★★★「地图切换后标注不更新」已实测解决（1.70.18，本分支，用户实测日志实证）**：修复后用户实测日志完整跑通全链路——`心跳（IsShown=false 仅参考）` → `viewsig 变化 nil → 14（area=14）` → `draw area=14 取到 466 点（上限 200）` → `完成：显示 200 个` → 切区域 `viewsig 变化 14 → 15（area=15）` → `draw area=15 取到 629 点` → `显示 200 个`。**区域切换检测与重绘完全正常，原报障解决**。最终根因链条（四层，前三层各是真问题但都被下一层挡住）：① UELog 通道不落盘 → 换 SavedVariables 环形缓冲；② 标注层开关没持久化（/reload 归零）；③ trace 开关也没持久化；④ **恢复逻辑写在 EVAL_DS_BUILD 里，而用户从未打开 Tab4（cfgTab=1）→ 恢复从未执行**；⑤ **我用 IsShown 当可见性总闸，而它在本客户端不可靠**（已单独记条）。**★本轮新踩的两个「日志本身」问题（频率防护总则同样适用于日志）**：ⓐ 「viewsig 未变 → 跳过重绘」是每 tick（0.25s）必然发生的常态，无条件记录 = **4 条/秒**，300 条环形缓冲约 75 秒即被刷满，**把真正有价值的「viewsig 变化」挤掉**（实测日志里 26 条连续「未变」，切换那几行几乎没留住）→ 降频到 ≥10s 一条；ⓑ trace 是调试开关，忘了关就永久刷写 → 加 **10 分钟自动过期**（`DS_TRACE_TTL`），关闭时记一条说明。**教训：设计环形缓冲容量时，必须按「最高频那条日志 × 缓冲条数」反推能保留多久的历史**——4 条/秒 + 300 条 = 75 秒，对「用户切几次图再看」的取证场景远远不够。**教训：debug 开关一律要能自动退场**，否则一次忘记开启就长期污染缓冲与磁盘。**教训（本文件第 5 次）：`DS_TRACE_TTL`/`dsTraceUntil` 声明必须在 `dsNodeTick` 之前**——我放在使用点附近，结果 tick 读到全局 nil 直接 `attempt to compare number with nil`；**测试当场抓到**（这就是必须有冒烟测试的原因）。
## 项目位置与现状
- **★★工作流铁律（用户明确定，1.70.15）：在用户实测验证通过之前，一律不提交、不发版、不推送**——在途功能（如本分支的 DataSearch/日志通道/采集点标注）改动只落工作区文件，让用户 `/reload` 直接实测；版本号、CHANGELOG、README 速览表、git commit/push 全部等用户确认「验证通过」后再做。不要自作主张提交，也不要在报告里催促提交。
- **★★根治取证开关失效：恢复逻辑不能放在「Tab 构建」里（1.70.16，本分支，实测三轮定案）**：日志始终只有一行「trace 开启」、连心跳都没有。**决定性证据**：存档里 `cfgTab = 1`（用户配置窗停在全局页）且 `cfg.ds` 被客户端重写后消失。根因：我把「恢复持久化开关」的代码放进了 `EVAL_DS_BUILD()`，而 **`EVAL_DS_BUILD` 只在用户打开数据检索 Tab（Tab4）时执行一次**；用户没打开过它 → 恢复从未执行 → `dsTrace` 始终 false → tick 里 `if not dsTrace` 不记录（且当时心跳写在 `if not dsNodeOn then return end` **之后**，同样被跳过）→ 整条链路静默。**修复三层**：① 恢复搬到**文件作用域**（载入即执行，此时 SavedVariables 已由客户端还原）+ `EVAL_DS_RESTORE()` 导出，主文件 VARIABLES_LOADED 里再补一次（首装/边缘时序保险）；② **心跳上移到 tick 最前面**（在 `if not dsNodeOn then return end` 之前），这样任何开关没满足都会留痕，并打印`标注层=`/`地图可见=`当前值；③ 测试钉住「从未打开 Tab4 也能恢复」。**教训一（本项目第三次同类）：凡是「A 开关 + B 前置开关」的诊断链路，恢复/初始化绝不能挂在 UI 构建回调上——UI 可能永不构建，而 tick 挂在 OnUpdate 上独立运行，两者生命周期不同**。**教训二：诊断输出必须能区分「代码没跑」和「条件没满足」**——我为此来回猜了三轮（通道死→开关没持久化→恢复没执行），如果第一版就有心跳+开关状态打印，一轮即可定位。**教训三：用户实测前先确认他打开了哪个 Tab**——cfgTab 是关键状态位，能直接暴露「模块根本没构建」。
- **★★★撤回并纠正 1.70.14 的错误结论：WorldMapFrame:IsShown() 在本客户端【不可用】，我一度把它当总开关导致标注层完全不出（1.70.17 定案）**：1.70.14 我看到诊断输出「地图可见=false 但对方当前区域=14」，判定「GetViewedZone 关图后不清 → 必须改用帧可见性判断」，于是把 `if dsMapShown()==false then return nil end` 写成视图签名的总闸。**这是错的，而且是我自己引入的回归**——实测日志连续两拍 `地图可见=false` 恒定（用户明明开着地图），标注层一个点都不画。**权威依据（UnrealQuest 源码两处明文，调本客户端 API 前必查）**：① `Compatibility/ClientAPI.lua:8664`「WorldMapFrame:IsShown()/IsVisible() **do not reliably reflect this client's fullscreen presentation**」——正因如此，对方开图一律 `ShowUIPanel(WorldMapFrame)` 而**绝不 Toggle**（无法先判断是否已开）；② `Map/MapContext.lua:696`「this client's fullscreen presentation is recorded **leaving WorldMapFrame's shown state TRUE after it closes**」——关图后它可能**一直停在 true**。即 **IsShown 两个方向都会撒谎（可能恒 false，也可能关图后恒 true）**。**正确做法**：完全不看帧，只用对方 `GetViewedZone()` 自己的语义——它在 `zoneIndex==0`（大陆/世界视图、未选中具体区域）时返回 `nil, report, "continentView"`，这就是「没有可标注视图」的可靠判据；`dsViewSig` 退回纯 areaId 比较。IsShown 只保留在 `/eh ds` 里做**标注为「仅参考」**的显示，绝不进分支。**教训一：判断某个 UI 的状态，优先用该 UI 所属系统自己暴露的语义（GetViewedZone），而不是通用帧 API（IsShown）**——通用 API 在魔改客户端上兼容性最差。**教训二：本项目记忆里早有一条「开图绝不 Toggle——IsShown 不可靠」，我修 bug 时却忘了它，反而把 IsShown 提升成核心判据**——**改代码前先搜一遍记忆里关于该 API 的既有结论**（同一句话当时就在「在途分支」条目里）。**教训三：测试断言会固化错误假设**——我在 1.70.14 加的「关图必须隐藏钉子」断言，正是基于错误前提写死的，本次不得不反过来重写成「IsShown 谎报关闭时仍要照画」。**断言绿 ≠ 假设对**，前提变了断言必须一起改。
- **★★UELog 在本客户端不落盘 → 日志通道改 SavedVariables 环形缓冲（1.70.12，本分支，实测取证）**：用户反馈「UELog 好像有异常、没法写日志」——**排查证实是通道本身死的，不是代码 bug**：① wiki Helpers 页确认 `UELog(message)` 存在且签名正确，文档原文「Writes message to the **Lua log category**. Does not print to chat」——所以 pcall 全成功、不报错；② 但 `%LOCALAPPDATA%\Azeroth\Saved\Logs` 是**空目录**，全盘搜 `*log*` 只有启动器的 `launcher.log`/`launcher-prev.log`；③ 游戏进程命令行 `Azeroth.exe -launcherauth -launcherpid=4384`——**Shipped 构建且无 `-log`**，日志进内存/输出窗口就没了。同页的 `RLog` 更差（「仅 FrameXML debug 编译进去时有效，否则 no-op」）。**结论：本客户端所有落盘日志通道都是死的**；`print()`(DEFAULT_CHAT_FRAME) 永远可用。**新方案（A+B）**：主通道=`EVAL_HELP_CONFIG.log` 环形缓冲（`EH_LOG_MAX=300`，行格式 `"%.3f 消息"` 带相对时间戳便于对时间轴），随 SavedVariables 落盘 → 开发侧**直接读** `%LOCALAPPDATA%\Azeroth\Saved\Account\<账号>\SavedVariables\EvalHelp.lua`（实测该文件真实存在且可读，账号 LIHAIBOAS1）；兜底=`/eh logdump [N]` 打聊天框。**落盘时机只有 /reload/小退/退出**（SavedVariables 固有语义，无法更早）——取证时要先 /reload 一次。**迁移坑**：旧存档 `cfg.log` 是**布尔**（true=开），新代码要当**表**用——`logEnabled()` 必须兼容三态（nil=默认开 / false=旧开关关过要尊重 / 表=看 `.on`），`logLine` 首次写入时把布尔自动转表，**配置窗勾选框改读写 `cfg.log.on`**（不能再整体当布尔否则一次点击就把缓冲内容冲掉）。**教训：换存储通道要全量 grep 该键的每一个读写点（我这次找到 6 处：复选框设/取、/eh log 命令、默认值、语言标签、帮助文案），少改一处就是「点一下勾选框日志全没了」**。同轮加 `/eh ds trace`（标注层轨迹日志，**默认关**——tick 0.25s 一次无条件记会把 300 条缓冲刷爆，符合频率防护总则）与 `/eh log`/`/eh logdump`/`/eh logclear`；诊断命令 `/eh ds` 扩容到含地图可见性。
- **★★根因定案：「地图切换后标注不更新」= 用 GetViewedZone() 判「地图关了」是错的（1.70.14，本分支，用户截图取证）**：诊断输出一行三处关键信息——`开关=false trace=false 本层签名=nil 钉子池=0 地图可见=false 对方当前区域=14`。**「地图可见=false 但对方当前区域=14」这一对矛盾值是决定性的**：`MapContext:GetViewedZone()` 内部读 `Client.GetCurrentMapZone()`/`GetMapInfo`（**地图子系统状态**），**关闭世界地图帧不会清它**——关图后仍返回上次查看的区域。由此我的 `dsViewSig()` 有两个后果：① 「`areaId==nil` → 地图关了 → 清空标注」这条分支**几乎永不触发**（关图后 areaId 仍是旧区域号）→ 钉子残留在画布上、重开地图看到陈旧标注；② 真正该重绘的「关图/重开/切区域」三种情况里，关图与重开**都检测不到**。**修复**：视图签名从「areaId」升级为「**可见性 + 区域**」双元组（`dsViewSig` 先问 `WorldMapFrame:IsShown()`——返回 nil 表示帧不可探测则退回只比区域保持旧行为；返回 false 直接视为无视图返回 nil），`dsNodeTick` 相应改成接收两个返回值。新增三条断言：`mapShown=false` 时必须隐藏钉子（哪怕 viewArea 仍是 1）/**关图时不画任何点**/**重开同一区域要重画**。**教训一：判断「某个 UI 是否在使用中」要直接问那个 UI（IsShown），不要用它的派生数据去反推**——派生数据往往缓存/粘滞。**教训二：诊断命令要一次性打出「矛盾值」**——本次正是 `地图可见` 与 `对方当前区域` 并排显示才一眼看出根因；只打任一个都还得再猜一轮。**教训三**：debug 前先确认客户端已 /reload 到新代码——用户截图里没有新加的可见性字段且标注层仍显示未开（我预置的 cfg.ds.nodes 未生效），说明跑的是旧构建。
- **★轨迹日志「开了却没日志」= 采集前置条件没满足（1.70.13，本分支，实测取证）**：用户开了 `/eh ds trace` 切了图，落盘后日志缓冲里**只有一行「trace 开启」**——其余什么都没有。根因不是日志坏了，而是**日志挂在 tick 里，tick 第一行就判「标注层没开就 return」**：`dsNodeOn` 原本是纯内存 local（每次载入重置为 false），用户 /reload 后标注层静默变回 OFF，于是 tick 提前返回，**一行都不会记**——表现像功能坏了，实际是「取证工具自己没被满足前置条件」。三处修复：① `EVAL_DS_TOGGLE_NODES` 把状态持久化进 `cfg.ds.nodes`，`EVAL_DS_BUILD` 构建时恢复；② `EVAL_DS_TRACE(true)` **自动把标注层一起打开**并在日志里写明原因（取证意图明确，别让用户踩两个开关）；③ `/eh ds` 在标注层关着时直接输出提示文字。**教训：凡是「开关 A 才产生日志/数据」的诊断工具，开 A 以外那个前置开关时必须自动代劳或明确报错——静默空结果最容易被误判成 bug**（与「日志通道本身是死的」是两个独立问题，本次两个叠加，先排查通道再排查前置条件）。假象排除法：落盘文件的大小/时间戳能证明写入链路是通的（1 行 = 通道 OK，代码正常），据此把怀疑从「日志坏了」转向「采集没跑」。
- **在途分支**：`feature/datasearch`（DataSearch.lua 独立模块 = 配置窗 Tab4）
  - **数据源**：`UnrealQuestData` 全局表**懒惰读取**（载入顺序 EvalHelp 早于 UnrealQuest，载入期拿不到）；对方缺席时整页只显示引导提示。
  - **分层**：`EVAL_DS_SEARCH`/`DETAIL`/`SHOWMAP` 逻辑层与 UI 分离，可在 node 里直测；反查索引懒构建 + 会话缓存。
  - **交互**：输入 0.25s 去抖；**无限级联导航**（已取消 2 级上限，顶部「重置」+ 底部「返回」）；物品悬停 `GameTooltip:SetHyperlink`；行图标 = UnrealQuest media/icons 类型图标 + 物品真实图标（`GetItemInfo` 第 9 返回）。
  - **地图定位**：有坐标的行右缘有地图图标 → `EVAL_DS_SHOWMAP`。流程参照 UnrealQuest `QuestClicks:RevealOnMap`：`Client.OpenWorldMap`（内部 `ShowUIPanel(WorldMapFrame)`）→ `MapContext:ShowAreas({areaId})` → `ParkView`。**本客户端 `GetMapZones` 忽略参数只答当前选中大陆**，故对方内部先 `SetMapZoom(continent)` 再按本地化名匹配 zone 索引。**开图绝不 Toggle——`IsShown` 不可靠（见上方 1.70.17 专条）**。
  - **标注层**：1.70.19 起为**统一地图标注层**（单池 + 地图绑定 + 16 类别 + 清理），详见上方 1.70.19 条。原来的「搜索结果钉 60s 自熄」**已删除**——所有标注都由地图绑定管理。
  - **回声行**：仅在 EditBox 字体链全失败时才显示（本客户端实测 EditBox 正常渲染，同时显示会重复渲染同一文字）。
  - **测试**：组 42 系列（42a~42j）覆盖搜索/详情/地图/标注层/日志通道。
  - **状态**：用户要求**未提交未发版**，待游戏实测确认后再定（见「工作流铁律」）。
- **★条件默认值去职业化（1.70.0，本分支）**：SE_TYPES 里 4 处职业化默认技能名清除（hasBuff 战斗怒吼 / hasDebuff 断筋 / immune 撕裂 / inRange 冲锋 → 全改 `s = ""`），seDefaultCond 的 tClass 默认 `cs = { WARRIOR = true }` → `cs = {}`（这是最隐蔽的：非战士新建「目标职业」条件默认就选战士）。空名条件求值安全（恒 false 不崩）、编辑窗显示「未选择（点此选择）」、文本格式解析要求具体名（空名条件不进导入导出）。教训：**任何条件的默认参数都要问一句「这对非本职业用户成立吗」**——战士化残留会散布在 SE_TYPES 默认值和 seDefaultCond 里
- **★采集点标注：小圆点+分类色 & 切换区域的排查结论（1.70.11，本分支，实测）**：① 用户要求把黄叹号图钉换成「任务插件那种小圆点+不同颜色」——照抄 `NpcPins` 的判定：**有 icon 字段的用图标，无 icon 的节点= `SetWorldMapPinTexture(Client.MINIMAP_OBJECTIVE_TEXTURE)`（media\\QuestDot 圆点）+ `SetWorldMapPinColor(r,g,b)`**；分类配色直接取它 NpcPins 的表：草药(0.40,0.85,0.35)绿 / 矿脉(0.80,0.62,0.40)棕 / 宝箱(1.00,0.82,0.35)金 / 鱼群(0.35,0.70,0.95)蓝；尺寸照抄 `NODE_WORLD_PIN_SIZE = WORLD_PIN_SIZE/2 = 7.5≈8`。**池子复用必须每次重设颜色**（换区域后同一钉可能换类别）。② 用户反馈「地图切换后标注没更新」——**仿真断言证明逻辑是对的**（新增测试：两次 tick 换 areaId，断言 `show=1 hide=3`：新区域点显示、旧点+多余池位全部隐藏）。真正原因待实测：大概率是 `MapContext:GetViewedZone()` 读到的区域没变（同图内平移/缩放不该重绘）或切换时机未反映。为此加 **`/eh ds`** 诊断命令（`EVAL_DS_NODE_DIAG`）打印：开关 / 本层签名 / 钉子池大小 / **对方此刻报告的当前区域**——切换不生效时先跑它取证。**教训：用户报告「没更新」先写仿真断言验证逻辑，别直接改代码**——本次差点误改正确的逻辑（插桩发现 `show=1 hide=3` 完全正确，问题在别处）。
- **★跟随地图视图的采集点标注层（1.70.10，本分支，用户要求）** ——⚠️ **本条的实现已于 1.70.19 被「统一地图标注层」取代（4 类→16 类、`DS_NODE_MAX=200`→`DS_ANN_MAX=500`、独立池→单池）**，保留作沿革参考，**当前实现以下方 1.70.19 条为准**：打开地图后随区域切换自动铺出该区域的花草/矿脉/宝箱/鱼群。**全程复用 UnrealQuest，自写部分仅约 60 行**：① 读当前视图 `MapContext:GetViewedZone()` → areaId；② 取数据 `Database:GetAreaServiceLocations(areaId, classId, raceId, wanted)`，`wanted={herbs=true,mines=true,chests=true,fish=true}`（它内部已按 `coordinate[3]==areaId` 过滤、排序、带 serviceLocationCache）；③ 画点 `Client.CreateWorldMapPin` + `PositionWorldMapPin`（与单点定位共用同一套对方图钉设施）；④ **节拍照抄对方**（WorldMapPins REFRESH_INTERVAL=0.25s）+ **视图签名比对**（areaId 没变就不重绘，对应对方 ViewSignature 思路）；⑤ 单区域上限 `DS_NODE_MAX=200`（一个区域可有上千个点，全画会卡——对方也做缓存/分档）。入口：数据检索 Tab 的「地图显示采集点」开关（`EVAL_DS_TOGGLE_NODES`），tick 挂在已有的 `EVAL_DATASEARCH_TICK` OnUpdate 上（不新增帧）。**数据形态提醒**：采集点在 `meta.herbs/mines/chests/fish` 里是**负 ID**（负号=类型编码），查 objects 表必须取反（`objects[181109]`）；实测 mines 38 对象/9993 坐标、跨多区域 30 个，herbs 46/9364/39 个。测试直调 `EVAL_DS_NODE_TICK_FOR_TEST()`（清零节流跑一次）断言「查的是当前区域 / 要了采集类别 / 每点建一个钉 / 每点定位一次」。
- **★硬依赖 UnrealQuest：地图/图钉/坐标全部复用对方 API（1.70.9，本分支，用户拍板）**：数据检索的数据源本就是 `UnrealQuestData`（没它还整页引导提示），故按用户要求**删掉全部自实现回退分支**，只走对方链路，代码 1259→1203 行且行为与它一致。替换对照：建钉 `Client.CreateWorldMapPin(i,r,g,b)`（画布子帧+尺寸+层级+贴图+鼠标一步到位，替代自写 CreateFrame/CreateTexture/SetTexture/SetFrameLevel/EnableMouse/RegisterForClicks）；鼠标 `Client.SetWorldMapPinHandlers(f,enter,leave,click)`（它内部处理 RegisterForClicks 假设）；定位 `Client.PositionWorldMapPin(f, x/100, y/100)`（**收归一化 0..1**，内部处理画布尺寸+像素对齐+Show，替代自算 `x/100*width`）；tooltip `Client.ShowMapTooltip/HideMapTooltip`（内部解析正确帧）；开图 `Client.OpenWorldMap` + `MapContext:ShowAreas({zid})` + `ParkView`；坐标筛选 `Database:GetEntityLocations(sourceType, id, areaId, limit)`。**坑：`Client.SetWorldMapPinPosition` 不存在**（我按名字臆造，grep 全库确认后改用真实存在的 `PositionWorldMapPin`）——**教训：调别的插件的 API 前必须 grep 它的定义，别按命名习惯猜**。**保留自有价值的部分**：物品掉落/商人/任务反查索引（对方没有 `GetItemSources` 之类，只有 GetItem/GetItemName），以及带坐标的区域摘要 `区域名(x,y)`（对方 `GetUnitZones` 只给名称+计数，不带坐标）。
- **★搜索结果行与详情行共用同一片 y 区域 + 顶部钮改「重置」（1.70.7，本分支，实测）**：截图暴露两处——① 详情视图里**结果行的地图钮没被藏**（显隐只关了 `r.btn`），10 行结果的地图钮在右侧排成一列圆点，还压住滚动计数与 [▼]；② 行宽 `RW-40` 的行右缘正好顶到滚动列 `RW-18` 起点，视觉上贴死。改法：**显隐要按「整套控件」关（btn + iconBtn + mapBtn 一个都不能漏）**；详情行收窄到 `RW-70`、结果行 `RW-62`，滚动钮移到 `RW-18`，指示器左移 `-42/52` 让开 ▼。★顶部「返回」按用户要求改为 **「重置」= `dsResetSearch()`**：一键清空 `nav/results/detOff/lastQuery` + 清空输入框与回声行 + 恢复占位提示 + 回搜索态（底部仍保留「返回」做逐层回退）。**教训（第三次踩，务必记牢）：`EVAL_DS_*` 导出函数必须物理定义在被它引用的 `local function` 之后**——`EVAL_DS_RESET` 放前面直接 `attempt to call a nil value`（与 `EVAL_DS_PIN_TOOLTIP_LINES` 同一个坑）。
- **★★世界地图上挂 WorldMapButton 子帧的 tooltip 必须用 WorldMapTooltip（1.70.6，本分支，查 UnrealQuest 实证）**：UnrealQuest 行为库存档记录 `worldmap.custom_pin_gametooltip_never_appeared`——**GameTooltip anchored to a WorldMapButton CHILD renders nothing**，同一子帧上换 `WorldMapTooltip` 才显示。我们的地图钉（WorldMapButton 子帧）原来就用 GameTooltip → 悬停必然什么都不出。改法：**优先复用 `UQ.Client.ShowMapTooltip(frame, lines, anchor)` / `HideMapTooltip(frame)`**（它内部 ResolveMapTooltip 会优先取 WorldMapTooltip、否则 GameTooltip，并用 RenderTooltipLines 渲染行模型），对方缺席时回退到本文件用 `WorldMapTooltip or GameTooltip` 直绘。行格式与它一致：`{text=,r,g,b,wrap}` 标题/正文、`{left=,right=,rightR/G/B}` 双列属性行、`{separator=true}` 分隔。tooltip 内容=「该钉指向的实体」完整信息（青色标题→区域/坐标/等级/阵营→掉落物品/提供任务/容器开出块），与详情页同一套反查索引；定位数据因此带上 `kind/id`（物品/任务的坐标来自子实体，标注点描述的就是**那个子实体**，另存 `parentKind/parentId`）。**教训：凡在世界地图画布上画交互件，tooltip 一律先试 WorldMapTooltip；Lua local 作用域坑再现——导出函数必须定义在被引用的 local 之后**（`EVAL_DS_PIN_TOOLTIP_LINES` 放早了直接报 attempt to call a nil value）。
- **★图标点击失效根因：纹理不接收鼠标（1.70.5，本分支，实测）**：数据检索详情行/结果行的**左类型图标原本只是行按钮的一个 Texture**——本客户端纹理不是鼠标目标，点它只会穿透到行按钮（行为应与点文字一致），但配合右侧「地图钮是独立 Button 且与行按钮同层、被行按钮整个包住」→ 两个图标都表现为「看着在却点不动」（用户实测截图圈出左右两处）。改法：**两端图标都改成独立 Button**（`ib`/`mb`，各 16×16，`EnableMouse + RegisterForClicks`），并 `SetFrameLevel(行按钮 + 2)` 叠在整行按钮之上；左图标=进条目（与点文字同效）+ 物品悬停 tooltip，右图标=开地图定位；两者都进 `page.widgets` 走 Tab 显隐契约，刷新时显隐 `iconBtn`（不再是 `icon` 纹理）。**教训：可点区域一律用 Button，不要指望 Texture 转发点击；同层重叠时后创建者不一定在上，必须显式抬高 frameLevel**。同轮加 `/eh ds probe`（`EVAL_DS_PROBE`）dump 详情行几何/鼠标/脚本状态，以及 `EVAL_DS_BUILD_FOR_TEST/EVAL_DS_TEST_LINE/EVAL_DS_TEST_ROW` 供测试断言「两侧图标都是可点按钮」防回归。
- **★详情滚动 + 地图图标区分（1.70.4，本分支）**：① **内容被截断**——模型层每区块硬截（掉落8/容器4/商人4/任务4）+ 视图 14 行上限，两者叠加会把「商人出售」这类靠后区块整个吃掉（用户实测：「下面是不是还有内容」）。改法：模型层上限收进 `DS_CAP` 表并放开（掉落30/容器15/商人15/任务10…），视图加滚动（`DS.detOff` + [▲][▼] + 右下 `x-y / 总` 计数 + 滚轮），进新详情 `detOff=0`。② **★root:SetScript 是单槽位**：`OnMouseWheel` 已被 Toolbox(Tab3) 占用，DataSearch.lua 在 toc 里更靠后，直接 SetScript 会把 Toolbox 滚轮**静默顶掉**。改法=**链式接管**：build 时 `GetScript("OnMouseWheel")` 取出旧处理器，新处理器先判「本 Tab 激活且详情模式」才消费，否则 `pcall(prevWheel, a, b)` 转发。**教训：任何 `root:SetScript(事件)` 前先 grep 该事件是否已被其他 Tab 模块占用**（同文件里 EvalHelp.lua 自己也有 OnMouseWheel）。③ 地图图标与「链接指向」图标视觉撞车（都是黄色感叹号）：行尾定位钮改用 `media\QuestDot`（圆形目标点），世界地图上的钉仍用 `media\icons\quest-marker`，并导出 `EVAL_DS_MAP_ICON_PATH/EVAL_DS_MAP_PIN_PATH` 供测试断言二者不同。④ 测试坑：`string.find(s, "quest-marker")` 里 `-` 是 Lua 模式魔法字符（惰性量词），必须 `plain=true`，否则断言假失败。
- **★EditBox 文字默认不左对齐（1.70.3，本分支，实测）**：数据检索搜索框输入的文字/光标显示在框内中偏右（用户截图），显式 `SetJustifyH("LEFT") + SetJustifyV("MIDDLE") + SetTextInsets(2,0,0,0)` 才贴左。**教训：本客户端 EditBox 不继承「左对齐」默认值，凡自绘输入框都要显式压左并清内缩**；配套把背景框/聚焦热区/占位提示统一到同一起点坐标，避免各自错位。同轮：图标按 rank 精确化（`units[id].rnk`：1=精英 2=稀有精英 3=Boss 4=稀有 → elite-mobs/boss-mobs/rare-mobs，无 rank 用 `rares` 通用生物图标），与 UnrealQuest NpcPins 的 RANK_ICONS 同语义；`EVAL_DS_ICON_PATH(kind,id)` 导出供测试断言「路径无扩展名+插件名小写」防再犯。
- **★★本客户端素材路径铁律（1.70.2，本分支，实测根因）**：**附加素材（TGA）路径必须【无扩展名】且插件名小写**——`"Interface\\AddOns\\unrealQuest\\media\\icons\\quest-marker"` 这样写；我原来写 `...\\UnrealQuest\\media\\icons\\quest-marker.tga`（大写+带后缀）→ **客户端静默什么都不画**（不报错、pcall 全成功、纹理就是空的），表现为「数据检索详情行右缘该有地图图标却看不见」（用户截图圈出的正是空位）。UnrealQuest 自己把这条写进了注释与行为库：`textures.addon_tga_paths_require_extensionless`（ClientAPI.lua:4609「Extensionless addon paths: this client silently draws nothing when the suffix is included」）。**教训：凡引用别的插件素材，一律抄它的路径常量原样，别自己拼大小写和扩展名**；本插件只用自己 media\\Flags（那两处历史写法是对的）。
- **★光环层数门槛扩到四类 + 条件行按钮重叠修复（1.70.1，本分支）**：用户实测截图暴露两个缺陷——① **目标debuff检查看不到「是/否」**：根因是坐标重叠，是/否按钮 x=250 w=52（250-302）与 层按钮 x=266 w=20（266-286）完全叠在一起，层按钮后绘制把是/否盖住；改为 是/否=246 w40（246-286）、层=290 w54（290-344）、预览 348、技能名区收窄到 100 宽（sHit 同步收窄）。**教训：条件行是按 x 硬编码排版的，新增/调整参数控件必须核对与既有控件的区间不重叠**。② **层数门槛原本只给 目标debuff（isDebuff 判定）**：现扩到四类光环（自身buff/自身debuff/目标buff/目标debuff），因为 buff 同样会堆叠（战斗怒吼/破甲…）。实现三处配套：Core.lua 四个光环集合**统一存层数**（原先 playerBuffs/targetBuffs/playerDebuffs 只存 true，现 UnitBuff/UnitDebuff 第二返回值归一为数字，非堆叠=1，`GetPlayerBuff` 分支无层数保持 true）；Engine.lua condOne 四类统一 `cnt>=lim`（`lim=n>1 and n or 1`，`tonumber(cnt) or 0` 兼容旧布尔）+ EVAL_COND_STR 统一 `>=N`/`<N` 后缀；Engine.lua 解析侧把 auraStack 定义**上移到四类光环分支之前**（Lua local 作用域陷阱：原本定义在 debuff 分支之后，buff 分支拿不到——同一坑 1.41.0 出过一次）。语义：是=至少N层 / 否=不足N层，无后缀=只要有/没有；反向 op 仍降级为无层数限制（1.32.0 保持）。测试：test_stub 的 UnitBuff 桩补 apps 返回，组 35b 覆盖四类往返+求值（注意 **EVAL_RULE_RUN 会跳过「不在动作条」的技能**，光环类断言必须排在 TEST.slotNames 清空/rescan 之前）
- 插件目录：`G:\game\u5wow\Azeroth\Binaries\Win64\Games\Emberveil\live\Azeroth\Interface\AddOns\EvalHelp\`（1.21 后用户改名 EVAL_HELP→**EvalHelp**；toc 已同步 EvalHelp.toc——目录名==toc基名才加载；改名时游戏必须关闭否则 Access denied）
- 当前版本：**1.70.40**（★★★数据检索 Tab4 + 世界地图标注层；根治切图不更新——tick 帧改挂 WorldFrame（本客户端打开全屏世界地图会隐藏 UIParent → OnUpdate 停发 → 标注层在地图开着时整个停摆），这是「滞后一拍」的真凶；随后删除三个失效兑底（定时强刷 1.5s / 慢速兑底 5s / 稳定期闸门 0.8s）——它们的前提均被证伪，且闸门本身就是切图不更新的主因；现为「签名一变立刻重绘」。详见文首两条）｜**1.69.1**（自动售卖明细日志：成交验证后逐笔输出 名称×数量（种类/子类，GetItemInfo 本地缓存，未缓存兜底 ?；wiki 确认本客户端 GetItemInfo 仅 9 返回值**无售价**故不含金额））｜**1.69.0**（工具箱任务组拆 自动交接/任务通知；交接 API 全进限频队列+交接日志；任务通知：QUEST_LOG_UPDATE 差分（接取/进度/完成）→频道四档（关/自己/说/队伍），SendChatMessage Protected→RunScript 绕行入队防刷屏踢线；组 41）｜**1.68.2**（★商人/背包写动作限频队列：tbQ+OnUpdate 滴出 TB_RATE=0.3s——一帧 24 次 UseContainerItem 会被服务器反滥用踢线；出售状态机 tbPending 逐笔验证（槽位物品消失=成功，超时 2s=失败报 未能售出:X）；执行前二次校验防槽位变动；MERCHANT_HIDE 清待售/购（防卖变用）；EVAL_TB_PUMP 测试直调；修测试基建泄漏：i18n 组替换 EVAL_LOCALES 未恢复污染后续 L() 解析——测试替换全局必须 save/restore）｜1.68.1（★实测修复：MERCHANT_SHOW 本客户端连发两次——卖出提示/计数/出售尝试全翻倍；tbMerchant 加 1.5s 去重窗 tbMerchantLast；教训：事件驱动功能上线后警惕客户端事件连发，高频/连发事件一律去抖窗口）｜1.68.0（配置窗 Tab3「工具箱」：Toolbox.lua 独立载入（toc 末尾、自绘 UI 不改主程序、EVAL_TB_BUILD(root,pages[3],refreshes) 挂钩 tabNames/SETTAB 三处嵌入）；功能=商人助手(修理/卖灰/购买补足/丢弃灰白)+就位确认+公会上下线屏蔽(SetCVar guildMemberNotify)+自动交接任务；分组滚动窗 13 行切片+滚轮挂 root(判 hdr 可见)；BAG_UPDATE 0.5s 节流；cfg 直读 EVAL_HELP_CONFIG.tb；API 审计 .dsh/reports/toolbox-api-audit.md；教训：工具箱判定别强依赖 GetContainerItemInfo 纹理字段——(tex or cnt) 有物即可）｜1.67.6（案例模版行内只留名字，desc+技能行预览移入 GameTooltip（ANCHOR_RIGHT）——模版扩容不溢出；教训：run_code 里 \n 会被 JS 层吃成真实换行写进 Lua 断串，嵌套引号大改走行号脚本）｜**1.67.5**（模版分组重整：周围补buff 从法师组拆出→独立 通用法系 组（templates 序：1战士 2法师 3通用法系 4盗贼））｜**1.66.0 及更早：详见 CHANGELOG.md / git 提交历史**（版本流水占记忆预算，近期条目已足够定位）
- **文件结构（1.39.0 起已模块化，不是单文件）**：`EvalHelp.toc`（载入顺序 Locales→Core→Engine→EvalHelp→Toolbox→DataSearch）+ `Locales/{zhCN,enUS,ruRU}.lua` + `Core.lua`(输出/i18n/状态) + `Engine.lua`(规则引擎) + `EvalHelp.lua`(UI+斜杠+init) + `Toolbox.lua`(工具箱 Tab3) + `DataSearch.lua`(数据检索 Tab4)；配套 `README.md`/`CHANGELOG.md`/`DEVELOPMENT.md`/`luacheck.js`/`test_engine.js`+`test_stub.lua`+`test_assert.lua`。**行数随开发变化，别当固定值引用**

## ⚠️ 最重要教训：改完必须跑语法检查
- **Lua 整文件编译**：任何一处语法错误 → 整个插件无法载入，且 UE 日志里看不到（%LOCALAPPDATA%\Azeroth\Saved\Logs 是空目录——本客户端落盘日志通道全废，见文首 UELog 条）
- 1.9.0 曾有两处 `local function` 误用 `end)` 收尾（多一个右括号），导致 1.9.0~1.11.0 全部功能实际未生效
- 旧 balance 脚本（数 function/end 配对）**拦不住多余符号**，只能当辅助
- **每次改完 .lua 必跑**：`node luacheck.js`（工作区根目录，fengari 真实 Lua 解析，报错带行号；显示 SYNTAX OK 才算完）
- **1.26.0 起有逻辑冒烟测试**：`node test_engine.js`（test_stub.lua 打桩 WoW API/UI mock + 5.1 垫片，加载整个 EvalHelp.lua 后跑 test_assert.lua 断言：条件解析/显示回环/规则执行/副作用；改引擎必跑两条命令）
- fengari 已装在工作区 node_modules（npm 走代理 127.0.0.1:10809）

## ★★★铁律：任务/地图类问题，先读 UnrealQuest 对应流程代码（用户明确定，1.70.40）
- **用户原话**：「记住下次碰到任务地图相关的问题优先排查任务插件的对应流程代码」。
- **触发条件**：凡是涉及 **任务 / 地图 / 标注 / 世界地图 / 地图坐标 / 地图切换** 的问题——**第一个动作是去读 UnrealQuest 的对应流程代码**，不要先自己推断，也不要先往日志上加东西。
- **读哪里（按问题类型）**：
  | 问题 | 先读 |
  |---|---|
  | 定时/刷新不生效、生命周期 | `Core/Driver.lua`（**帧父级、OnUpdate 派发**——1.70.39 的坑就在这） |
  | 当前看的是哪张图 | `Map/MapContext.lua`（`GetViewedZone` / `Inspect`） |
  | 怎么画、什么时候重绘 | `Map/WorldMapPins.lua`（`ViewSignature` ~678、`Refresh` ~4449） |
  | 某个 API 在本客户端能不能用 | `Compatibility/ClientAPI.lua`（实测配方库，含大量「本客户端不支持 X」的明文） |
  | 图标素材路径 | `Map/NpcPins.lua`（CATEGORIES 表 + `IconForLocation`） |
- **怎么读**：重点是**大段注释**——对方把踩过的坑、API 的真假、为什么这么写全留在注释里，这是整个仓库信息密度最高的内容。
- **停下信号**：当你发现自己在「加日志 → 看日志 → 猜 → 再加日志」里转第二圈，**立刻停手去读对方代码**。
- **两次代价（都是绕了远路）**：**1.70.17** 我自己发明 `IsShown` 判地图可见性，而对方 `ClientAPI.lua:8664` 早写明这 API 在本客户端不可靠 → 引入回归；**1.70.39** tick 挂 `UIParent` 导致开图时 OnUpdate 停摆，而对方 `Driver.lua:467-482` 把机理和正确做法全写在注释里 —— 我用日志推断**五轮**没找到，**用户一句「可以查看任务插件…」当场破案**。

## ⚠️ 频率防护总则（高频 API 调用场景）
- **原则**：凡是可能高频触发的 API 调用流程，设计阶段就必须考虑频率防护——不要等被踢线/刷屏/翻倍后再补
- **判定方法**：设想最坏情况「一帧/一秒内会被触发多少次」；服务器写动作超过 ~1-2 次/s 的一律限频，客户端事件先假设会连发
- **三类范式**（已有实现直接复用）：
  1. **服务器写动作 → 限频队列**：tbQ + OnUpdate 滴出 TB_RATE=0.3s（1.68.2：一帧 24 次 UseContainerItem 被服务器反滥用踢线；1.69.0 交接 API/任务通知同入队）。配套：逐笔验证状态机 tbPending（槽位物品消失=成功/超时 2s=失败）、执行前二次校验防槽位变动、MERCHANT_HIDE 清待办防「卖变用」
  2. **事件驱动 → 去抖窗口/节流**：MERCHANT_SHOW 本客户端连发两次（1.68.1 加 1.5s 去重窗 tbMerchantLast）；BAG_UPDATE 0.5s 节流（1.68.0）。教训：事件驱动功能上线后一律假设事件连发，高频事件必去抖
  3. **执行入口 → 去抖窗口**：EVAL_GO 入口 0.2s 窗口（1.54.1：/run 宏走 RunScript pending 队列，连按堆积松手后陈旧调用冲刷=目标狂切）；窗口可配 cfg.goDebounce 0~1.0（1.54.2），附 EVAL_GO_TRACE 追踪器取证
- **Protected 函数**（SendChatMessage/SpellStopCasting 等）绕行=RunScript 聊天脚本（pending script 队列）——注意 RunScript 本身也是队列，连发聊天同样会被反滥用踢线，通知类输出也要入限频队列

## 本客户端已实测的 UI 配方（UnrealQuest ClientAPI.lua 验证）
1. **拖动柄必须用 Button**（Frame 的 OnDragStart 不触发）；SetFrameLevel(父+10) 抬高（用 strata 抬高是记录在案的失败做法）；EnableMouse + RegisterForClicks("LeftButtonUp") + RegisterForDrag("LeftButton")；StartMoving 前 warm-up 两步（StartMoving→StopMovingOrSizing→StartMoving）
8. **strata 层级陷阱**：配置窗是 DIALOG；其上方弹窗（技能编辑窗/导入导出窗）必须同 DIALOG + SetFrameLevel(90~100)，MEDIUM/HIGH 都会被 DIALOG 盖住（1.11.2 实测修复）；弹窗也要 uiOffscreen 越界回归
2. **纯色纹理**只有 `Interface\Buttons\WHITE8X8` + SetVertexColor 可靠
3. **无原生 Slider / 无下拉控件**：滑条=自制轨道+拇指；**下拉一律用全局件 EVAL_DD_OPEN(锚点,选项表,回调)**（1.19.0 由 SE 专用泛化，UIParent+DIALOG+level250，多列 12 行/列，贴屏底自动上翻；宿主窗口 OnHide 里调 EVAL_DD_HIDE()）；1.19.0 起插件内所有 [<][>] 循环器已全部改下拉
4. **可见性契约**：显隐走显式控件清单 Show/Hide，不靠父子传播（容器 EnableMouse(false)，交互件单独 EnableMouse(true)）
5. **位置记忆**：GetCenter/GetLeft 含缩放，存取要除 GetEffectiveScale；每个记忆位置的窗口都要 uiOffscreen 越界回归
6. **禁用 SetScale**（点击框漂移），缩放=按 z 系数几何重建；字体链 FZLBJW→FRIZQT→ARIALN 全程 pcall
7. 小地图按钮父级必须是 UIParent（不能是 Minimap）；悬停用 OnEnter/OnLeave + GameTooltip，不用 SetHighlightTexture

## 架构要点（改代码前先读这段）
- **状态表 st（EVAL_HELP_STATE）**：所有判定数据走 UPDATE_STATE() 一次刷新、各处只读（Cat 思路，不重复调 API）；含 playerBuffs/targetDebuffs 纹理集合、castLog 释放日志（最近 5 条带条件 trace）
- **规则引擎**：规则 = { skill, enabled, groups }；groups = 组内 & 、组间 | ；EVAL_RULE_RUN 顺序执行第一条全过的；兼容旧 when={} 格式；condOne/groupsOK/EVAL_PARSE_CONDS/EVAL_COND_STR/EVAL_GROUP_STR 是核心
- **方案数据**：cfg.war.profiles + activeProfile；EVAL_WAR_ENSURE_PROFILES 做缺省迁移；SavedVariables 自动持久化
- **方案切换**：配置窗侧栏按钮 / 激活方案 [<][>] 选择器 / 战斗信息UI 方案行 / Shift+按宏（Shift 已被切换占用，方案条件里别用 Shift，用 Alt/Ctrl）
- **文本格式**（导入导出/ zs_wq.md）：`# 方案: 名` + `- 技能 | 条件` 行；技能名前 `!` = 停用；解析容忍 md 杂物行
- **函数作用域陷阱**：warCfg/c() 是配置段 local，战斗信息UI 段在其之前 → UI 段用 uiWarCfg()（直接走 EVAL_HELP_CONFIG 全局）；wslots/WAR_SKILLS 是战士段 local，配置段在其后可用
- 受保护函数（CastSpellByName 等）插件**不能调**，施法一律 UseAction(slot) + pcall；谓词返回 true/false/nil，绝不 ==1
- **EVAL_GO 目标门槛教训（1.21.7）**：规则引擎化后不能有「无可攻击目标就 return」的全局前置——是否需目标交给每条规则的条件；否则纯自身buff方案（如只有战斗怒吼）永远执行不到
- 本客户端无 UnitCastingInfo（打断条件做不了）、无 SuperWoW（无精确距离数值/挥击计时）
- **射程检测已有官方 API（排查 emberveil.org/wiki/lua 确认）**：IsActionInRange(slot) 已实现——返回 true=在射程内 / 0=超出射程 / 1=自动攻击(不测距) / nil=无目标或技能解析失败；ActionHasRange(slot) 返回技能是否有射程；CheckInteractDistance(unit,idx) 已实现但客户端不分档码数（idx<4 统一交互距离，4 恒 false）——可做规则引擎「在射程内」条件，技能本来就在动作条上直接传 slot
- **物品 API（wiki 已确认签名，游戏内实测待做）**：UseContainerItem(bag,slot) 消耗品直接用/装备自动穿（BoE 未绑弹确认）；GetContainerItemInfo→texture,count,locked,quality,readable；GetContainerItemLink→|Hitem..|h[名称]|h；GetContainerItemCooldown→start,duration,enable；GetItemInfo 第9返回值=纹理（只读本地缓存）。无 EquipItemByName/UseItemByName。注意：本客户端插件保护清单未文档化，UseContainerItem 是否被禁需实测——若被禁会静默失败（pcall 兜住不炸）
- **seBtn 返回包裹表 {btn,bg,text} 不是按钮本体**：锚下拉/加 FontString 必须用 .btn（mkSmall 相反：直接返回 b,bt）——1.21.4 修复 SE 技能名按钮把包裹表当按钮用（uiText 炸 CreateFontString、SE_BUILD 半成品窗口）
- **Lua local 作用域从声明语句【之后】开始**：RHS 里的闭包捕获不到正在声明的 local 自己 → local b = mk(..., function() ...引用b... end) 的 b 是全局 nil！OnClick 要赋值完后单独 SetScript 挂——1.21.4 修复激活方案下拉（DD_OPEN anchorBtn nil）
- **EditBox 疑似不渲染**（1.15.1 用户报告 IO 窗口空白：pcall 全"成功"但什么都不可见）→ IO 窗加 FontString 保底预览区（EVAL_HELP_IO_REFRESH）；EditBox 字体链先试 SetFontObject(GameFontHighlightSmall/ChatFontNormal/GameFontNormal)；文本类 UI 一律给 FontString 保底；单行输入弹窗用**回声行**保底（OnTextChanged 镜像到 FontString，1.17.0 重命名弹窗 EVAL_HELP_RP_* 范式）

## 编辑工具注意事项
- run_code 里**没有 require**；node 脚本一律写工作区 .js 文件再 `pwsh node xx.js`（内联 -e 引号会炸），pwsh 要加 `2>&1 | Out-String` 否则 stderr 丢失
- edit 工具：本会话内须先 read 过该文件；old_string 要精确（注意缩进空格数，批量失败先 grep 核对实际状态再重试）；node 外部改过文件后必须重新 read 才能 edit
- 大块插入用「写块文件 + node 边界替换脚本」，小改动用 edit 工具逐处替换
- 写文件若报 "file no longer exists"：换文件名重建（fs-observation 缓存）
- DSH 策略 danger-full-access、审批关闭 → 可直接写游戏 AddOns 目录
- **SavedVariables 落盘路径已实测**：`%LOCALAPPDATA%\Azeroth\Saved\Account\LIHAIBOAS1\SavedVariables\EvalHelp.lua`（**注意文件名是 EvalHelp.lua 不是 EVAL_HELP.lua**，1.29.0 改名后旧文件 EVAL_HELP.lua 是历史残留；小退/reload 时客户端写入）——**这也是现在读调试日志的地方**（见文首 UELog 条）

## 当前功能地图（用户视角）
- `/eh` 状态日志 | `/eh ui` 战斗信息UI（方案切换行+方案技能行+条件tooltip+实时条件亮金）| `/eh st` 状态信息UI（状态变量+最近释放条件明细）
- `/eh cfg` 配置窗 560×420，**四个 Tab**：① 全局（日志/界面/帮助）② 一键宏设置（方案栏+技能列表+[添加技能]+[导入导出]；1.20.0 起底部图标选择器/条件输入框/EVAL_WAR_ADD_FROM_UI 已删除，新增技能走编辑窗）③ 工具箱（Toolbox.lua，1.68.0）④ 数据检索（DataSearch.lua，在途）
- 技能编辑窗：点 [编] 或技能图标打开，条件逐行独立配置（类型/参数/删/添加），关系列切 &/|
- **定位：通用职业一键宏框架**（1.18.0 起全部用户可见描述已去「战士」化；技能白名单仅作下拉置顶（1.24.0 起重扫记录全部上条技能，任意技能可入方案/条件））；一键宏 `/run EVAL_GO()`；方案函数式调用 `/run EVAL_GO(2)` 或 `EVAL_GO1~4()`（执行指定方案并且激活，绑多按键）；技能需拖上动作条，改动后 `/eh go rescan`（`/eh war` 是旧命令别名，1.21.1 起已改名 `/eh go`，handler 里仍兼容）；
- 调试：`/eh debug`（跳过原因+触发 trace）| `/eh go list` | `/eh logdump` 调试日志 | `/eh ds`、`/eh ds trace` 数据检索诊断
- **命令组 1.21.1 改名 /eh war → /eh go**（handler 入口 string.gsub(msg,"^war","go") 兼容旧命令；sub 偏移量随 war→go 长度差 -1 已同步）

## 开源仓库（1.21.0 起）
- 远程：origin=**git@gitee.com:xeval/emberveil_eval_help.git**；github=**git@github.com:lihaibo123as/emberveil_eval_help.git**（1.29.0 起双远程同步，每次推送两端都推：`git push origin master && git push github master`；本地仓库 = 插件目录本身）
- 推送用默认密钥 ~/.ssh/id_rsa（gitee_id_rsa 未被授权，别用 -i 指定它）
- 库内含：README.md（开源门面+preview/ 界面图）、CHANGELOG.md（更新日志详情）、DEVELOPMENT.md（开发者指南）、CLAUDE.md（AI 记忆体副本，改主记忆后同步过去）、luacheck.js、test_engine.js/test_stub.lua/test_assert.lua（冒烟测试）、example/zs_wq.md
- **更新日志维护规则（1.28.0 起，用户定）**：发新版本时——① 详情写 `CHANGELOG.md`（## 🎯 vX.Y.Z — 主题 小节：要点/典型用法/设计亮点，配一个 emoji）；② README 顶部「更新日志」速览表加一行（| **X.Y.Z** | emoji 主题 | 一句话亮点 |，最新在上）；③ README 不再放版本详情（保持精简，详情只进 CHANGELOG）
- **preview/ 图片只用用户提供的**（当前 main.png=全局页/cfg.png=一键宏设置页/skill_cfg.png=技能编辑窗/info.png=战斗信息UI+状态信息UI）；我加的截图已被用户要求删除——别再自行往里放图
- 帮助输出含：异常自救 /reload 提示 + gitee 反馈地址

## 官方 API 文档（用户提供 2026）
- **Lua API 清单：https://emberveil.org/wiki/lua**（1370 条目，页面内嵌全量 JSON 索引可脚本解析；分类页如 /wiki/lua/globals/Action 有签名+返回值文档）
- 自定义分类：Azeroth（20 条，含 GetUECVar/SetUECVar、语音、IsPlayerMoving 等）；无坐标类 API（GetPlayerMapPosition 在 Mapping 分类但目标坐标无入口）
- 文档列出 Slider 控件（与「无原生 Slider」实测记录矛盾——以实测为准，用前先在游戏内验证）
- 工作区排查工具：parse_api.js（关键词检索）/ dump_cat.js（按分类转储）+ api_*.html 缓存页
- 工作流说明：当前开发主机是临时的，G: 盘游戏目录在另一台主机；本机改码→检查→推送 gitee→另一台拉取实测

## 待开发计划（搁置项）
- **免疫学习器**（✅ 1.36.0 已完成实装：探针验证→事件解析→学习表→引擎跳过；探针 /eh go probe immune/dump 保留作诊断工具）：
  - 结论已排查：无 CombatLog API、无免疫查询函数、wiki 无事件文档页（/wiki/lua/events 404）；但 Frame:RegisterEvent/RegisterAllEvents 可用
  - 设计方向：订阅 CHAT_MSG_* 战斗文字事件，从消息串解析「免疫」→ cfg.war.immune["技能@怪名"] 持久化 → EVAL_RULE_RUN 对同名怪自动跳过该技能（可流血黑白名单 EVAL_BLEED_BLACKLIST 的自动化升级版）
  - 前置步骤：先做探针 /eh go probe（30s 事件抓取写调试日志缓存）拿真实事件名+免疫文本格式，再写解析器；复用 autoFrame OnEvent 三态兼容范式（事件名在 1参/2参/全局 event）
  - 相关基建：st.canBleed 判定链（白名单>黑名单>生物类型排除）

## ★★★参考代码与「先读对方」铁律（用户明确定，1.70.40）
- **规则**：**凡是「任务 / 地图 / 标注 / 世界地图」相关的问题，第一动作就是去读 UnrealQuest 的对应流程代码，不要先自己推断、也不要只往日志上加东西。** 用户原话：「记住下次碰到任务地图相关的问题优先排查任务插件的对应流程代码」。
- **为什么是硬规则（两次惨痛实例，都是绕了远路）**：
  1. **1.70.17**：我自己发明「用 `WorldMapFrame:IsShown()` 判地图可见性」，而对方 `ClientAPI.lua:8664` 与 `MapContext.lua:696` **早已明文写明这个 API 在本客户端不可靠**（且对方因此开图**绝不 Toggle**）→ 我引入了回归，标注层整个不出。
  2. **1.70.39**：tick 帧挂 `UIParent`，导致**开图时 OnUpdate 停摆**；而对方 `Core/Driver.lua:467-482` **把机理、后果、正确做法（挂 `WorldFrame`）全写在注释里**，还注明 pfQuest 也是这么干的。我为此用日志推断**五轮**，越推越偏。**用户一句「可以查看任务插件 地图切换之后能正确的显示当前地图任务图标」当场破案。**
- **正确姿势（照做）**：① 先 `grep` 对方**同功能的驱动/生命周期**代码（不只是业务逻辑）——`Core/Driver.lua`（帧父级、OnUpdate 派发、`WorldFrame` 这个坑的出处）、`Map/MapContext.lua`（**看哪个区域**）、`Map/WorldMapPins.lua`（**怎么画** + `ViewSignature`）、`Compatibility/ClientAPI.lua`（**本客户端哪些 API 可用/不可用**）；② 读它的**大段注释**——对方把踩过的坑、API 的真假、为什么这么写都留在注释里，这是本仓库信息密度最高的地方；③ 再动自己的代码。
- **判定信号**：当你发现自己在「加日志 → 看日志 → 猜 → 再加日志」循环里转第二圈时，**立刻停手去读对方代码**，而不是继续加日志。
- UnrealQuest：`...\AddOns\UnrealQuest\Compatibility\ClientAPI.lua`（本客户端实测配方库）；**任务类流程参考** `UnrealQuest\Quest\` 目录——MainQuest.lua（主逻辑）/QuestState.lua（任务状态）/Tracker.lua（追踪）/ObjectiveAnnounce.lua（进度播报）/QuestSync.lua（队伍同步）/ExtendedQuestLog.lua（日志扩展）/QuestHistory.lua，配 `Database\quests.lua`+`Database\zhCN\` 本地化任务库；处理任务类功能（接取/进度/交付/追踪/播报）先查它的实现再动手
- Cat（TurtleWoW）：`D:\game\TurtleWoW\Interface\AddOns\Cat`（状态变量设计：宏只读缓存全局）
- OneJudge：`...\AddOns\OneJudge\OneJudge.lua`（最初的参考）
