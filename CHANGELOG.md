# 更新日志 — EVAL_HELP 全职业施法工具

| 版本 | 主题 | 一句话亮点 |
| :-- | :-- | :-- |
| **1.62.0** | 🎯 冲锋疑案真凶 | 接管动作后移到规则评估之后——AttackTarget 先开弓进战，冲锋类脱战限定技能可用性瞬间翻 false |
| **1.61.4** | 🔬 可用性结案 | 探针实证 IsUsableAction 正常（noMana=true=资源不足）——跳过原因细分：不可用:资源不足 |
| **1.61.3** | 🚑 关系列漏改 | 条件关系列同换全角竖线（1.61.2 只改了 uiEsc 调用点，这处内联写死的漏网） |
| **1.61.2** | 🚑 竖线显示修复² | || 转义在本客户端渲染异常——改用全角竖线（U+FF5C，中文字体自带，视觉一致） |
| **1.61.1** | 🚑 竖线显示修复 | 条件关系 | 在 WoW 是颜色转义符被吞——显示层全面 || 转义（关系列/条件列/预览/聊天/IO预览） |
| **1.61.0** | ↕️ 下移按钮 | 技能列表每行新增 v 下移（与已有 ^ 上移成对），调序按钮文本改为箭头 |
| **1.60.1** | 🔬 可用性探针 | /eh go probe usable [技能]——dump 格子原始可用性（IsUsableAction 本客户端走缓存态） |
| **1.60.0** | 📏 列表高度衍生 | 配置窗技能列表行数按窗口高度动态算（8→12 行），延伸到底部关闭按钮上方 |
| **1.59.0** | 🎭 姿态下拉自适应+去GCD | 条件姿态下拉按角色实际姿态栏生成（带名称）；符合的规则全部顺序执行，能不能放交给游戏内置 |
| **1.58.1** | 🚑 布局双修复 | 方案栏删按钮间距 21→19（逐行下沉错位修复）；战斗UI 方案第二排不再异常拉伸 |
| **1.58.0** | 🔢 时间型步进 | 时间类条件（距攻击/读条系）步进 0.1、区间 0.0-10.0、初始 0.1，一位小数显示 |
| **1.57.0** | ⏲️ 距攻击条件 | 新条件「距下次攻击」数值比较（距攻击<0.5 卡挥击节奏出手），复用挥击计时 |
| **1.56.1** | 🔮 法师案例×2 | 模版库新增 法师一键输出/周围补buff；修导入技能名 24 字节上限丢行（带前缀名最长 39B） |
| **1.56.0** | 📐 方案行自适应 | 战斗UI 方案切换行与状态条同宽对齐；方案多时自动换行，每行按钮拉伸填满 |
| **1.55.0** | ⏲️ 挥击计时 | 施法学习同机制：平砍事件锚点+UnitAttackSpeed → 战斗UI/状态UI 显示距下次攻击时间 |
| **1.54.4** | 🔢 连击初始1 | 条件「连击点数」默认参数 3→1（域 1-5） |
| **1.54.3** | 🗡️ 连击域修正 | 条件「连击点数」参数步进限定 1-5（GetComboPoints 上限 5），不再能步进到 8/300 |
| **1.54.2** | 🎚️ 去抖可配+追踪 | 去抖窗口入全局配置（默认 0.30s，±0.05 步进，0=关闭）；/eh go trace 看最近12次执行时间戳 |
| **1.54.1** | ⏱️ 执行去抖 | 连按不再堆积——0.2s 内重复调用直接丢弃（宏走 RunScript 队列，松手后陈旧调用全失效） |
| **1.54.0** | ✨ 光环检查四型 | 自身buff/目标debuff 合并为是/否检查；新增 目标buff/自身debuff 检查（实时下拉 ◆○●▲ 全配齐） |
| **1.53.0** | ⚡ 并行执行模型 | 仅角色技能占公共CD终止按键；角色行为/宠物/选目标/物品全部可并行——一键=切目标+宠攻+喝药+法术 |
| **1.52.1** | ⛓️ 选目标续行 | 选取目标切换成功后不再吃掉按键——刷新状态继续评估后续规则（切完目标当场放技能） |
| **1.52.0** | 📏 自动攻击距离检测 | 自动射击/魔杖超程（IsActionInRange=0）自动降档近战普攻，贴脸猎人不再空挥弓 |
| **1.51.0** | 🎯 射击状态条件 | 新条件 自动射击/魔杖射击（是/否切换）；顺带修复 ! 取反对全部布尔条件失效的老 bug |
| **1.50.0** | 🏹 自动攻击 | 「自动普攻接管」改名「自动攻击」+ 优先级：自动射击>射击(魔杖)>近战普攻，不可用自动降档，带悬停说明 |
| **1.49.3** | 🚑 取消施法修复 | SpellStopCasting 本客户端 Protected 插件直调无效 → 改 RunScript 聊天脚本同路径执行 |
| **1.49.2** | 🔍 残留审计 | 接管泛化（猎人自动射击生效）+ 怒气/姿态日志动态化 + T_RANGE 重复定义删除 + 残留文件清理 |
| **1.49.1** | ▶️ 空条件直放 | 技能不配条件=无条件直接执行（旧行为：空条件技能永远不触发） |
| **1.49.0** | 🎯 选敌前置删除 | 按宏不再先抢选最近敌人——与「选取目标:最近友方」类规则打架；选敌全交给规则 |
| **1.48.0** | 🧹 白名单清零 | 技能下拉/参数下拉纯动作条扫描——法师不再看到战士技能；新装默认方案改空（案例模版起手） |
| **1.47.0** | 🏹 角色行为扩充 | 新增 取消施法/自动射击/射击；姿态支持序号（姿态:2）；修普攻状态与接管开关耦合 |
| **1.46.0** | 🧩 图标带合一 | 战斗UI 两行图标合并为一行（方案下方，8格/行超过自动换行≤16格）；修重扫后图标全 ? 的表引用 bug |
| **1.45.1** | 🔧 开关对齐 | 配置窗勾选框与文字改为共中心线锚定（旧写法方框视觉偏低） |
| **1.45.0** | 🎯 方案驱动显示 | 战斗UI技能图标行/重扫核对/状态总览全部以激活方案技能为准——旧版固定战士技能清单废弃，真·全职业 |
| **1.44.1** | 🛟 宏入口兜底 | 引擎未加载完整时 EVAL_GO 不再弹 nil 红框，改聊天框给出 /reload 与插件启用排查指引 |
| **1.44.0** | 📋 案例模版 | 导入弹窗新增「案例模版」选单：按职业分组点选直接导入（武器战模版首发）；修导入上限残留+condTrim 漏桥 |
| **1.43.0** | ⚔️ 姿态切换技能 | 角色行为新增「姿态:X」——姿态栏直切不占动作条（CastShapeshiftForm 不受保护），已激活守门 |
| **1.42.0** | 📚 方案上限12 | 方案数 4→12：配置窗紧凑排列+战斗UI切换行12窄格+EVAL_GO1~12 全套直触 |
| **1.41.2** | 🌉 漏桥终审 | 静态分析器 split_audit.js 扫出拆分 6 处漏桥全补，审计 CLEAN |
| **1.41.1** | 🚑 EVAL_GO热修 | Engine 裸 cfg 引用（直取形式漏网）致按宏崩溃，改全局直读 |
| **1.41.0** | 🎬 自身施法三条件 | 施法中支持任意施法（空参数）+ 自身读条进行/剩余秒数；修 1.39.0 拆分漏桥 |
| **1.40.0** | 🔮 目标施法状态 | 条件「目标施法中/读条已进行/读条剩余」——聊天文本事件驱动+读条时长自学习 |
| **1.39.0** | 🧱 模块化重构 | 单文件 4758 行拆成 Core/Engine/UI 三模块，跨模块全局桥导出 |
| **1.38.1** | 📊 读条进度条 | 战斗信息UI 目标条下方新增施法读条（进度+剩余秒；状态UI已有施法行） |
| **1.38.0** | 🎬 施法中条件 | SPELLCAST_* 事件驱动读条跟踪，「施法中:X」防读条截断 |
| **1.37.0** | 📏 距离分档+射程条件 | 目标距离分档显示（近战/冲锋距/远程外）+ 条件「范围内:X」IsActionInRange |
| **1.36.4** | 🚑 免疫开关热修 | immBtn 误嵌 sDrop 调用内导致刷新 nil 报错，已归位（i18n 分支） |
| **1.36.3** | 🔘 免疫开关 | 「目标免疫技能」参数区加 免疫/未免疫 切换按钮（i18n 分支） |
| **1.36.2** | 📏 预览归位 | 编辑窗预览挪到「+添加条件」右侧同行，不再压保存/取消按钮（i18n 分支） |
| **1.36.1** | 🧬 免疫条件类型 | 条件「免疫:技能/未免疫:技能」读学习表，免疫行为可编程（i18n 分支） |
| **1.36.0** | 🛡️ 免疫学习器 | 免疫文本事件自动学习（技能@怪名），同名怪不再尝试该技能（i18n 分支） |
| **1.35.2** | 🧹 编辑窗瘦身 | 条件参数 [v] 箭头全取消——参数文字本身就是按钮（i18n 分支） |
| **1.35.1** | 🛡️ 免疫探针 | /eh go probe immune 抓免疫事件原型（i18n 分支） |
| **1.35.0** | 🌐 国际化 P1 | 编辑窗/导入导出/弹窗/提示全翻译，28 条件名入包，三语 README（i18n 分支） |
| **1.34.1** | 📐 宽语言自适应 | 英/俄窗口加宽 560→700，右侧按钮列右锚定，长文不再重叠（i18n 分支） |
| **1.34.0** | 🌍 国际化 P0 | 中/英/俄三语骨架+配置窗国旗选择器，UI 文案走语言包（i18n 分支） |
| **1.33.4** | 🧽 上限残留清除 | /eh go add 的 8 技能上限漏网已删；README 简介场景全面刷新 |
| **1.33.3** | 🚑 滚动热修 | 1.33.0 ROWS 作用域报错热修（改用行池实长） |
| **1.33.2** | 🎯 药品Buff根治 | 探针破案：tooltip 返回值恒 nil 被当门槛，拆后门到 buff 全进下拉 |
| **1.33.1** | 🔬 Buff探针 | /eh go probe 打印两条 buff 枚举+读名路径原始值，定位药品 buff 缺失 |
| **1.33.0** | 📜 技能列表滚动 | 方案技能数取消上限，超高出 ^/v 滚动条+滚轮，藏旧调试提示 |
| **1.32.10** | 💊 药品Buff修复 | UnitBuff 兜底枚举并入状态表，药品食物 buff 可匹配可下拉里选 |
| **1.32.9** | 🔮 光环下拉升级 | 已学习光环名跨会话可选（◇），剔除宠物/选取/物品伪技能，配图标配图标 |
| **1.32.8** | ✂️ 编辑区裁剪 | 导入导出编辑框装进滚动框，文本不再溢出盖住说明/预览区 |
| **1.32.7** | 🧹 帮助区重整 | 清战士残留，补五分类/Shift切方案/重扫按钮等现行信息 |
| **1.32.6** | 🐾 宠物指令图标 | 宠物动作条自学习指令图标（跨会话），物品图标全配齐 |
| **1.32.5** | 🗂️ 条件下拉分组 | 条件类型按自身/目标/光环/技能四组分类，下拉再点即收起 |
| **1.32.4** | 🖼️ 下拉图标列 | 技能项下拉左侧显示图标（技能/宠物/物品全支持） |
| **1.32.3** | 🎛️ 二级下拉 UI | 技能行=[分类▾][具体项▾] 并排双下拉，删掉过时循环提示 |
| **1.32.2** | 🔄 重扫按钮 | 配置窗全局页一键重扫动作条，不用记命令 |
| **1.32.1** | 🎯 目标存在条件 | 有目标才出手/无目标才换装，一行条件管住 |
| **1.32.0** | 🧰 技能五级分类 | 技能=角色行为/角色技能/宠物行为/目标选取/物品使用，二级下拉点选 |
| **1.31.0** | 📚 Debuff层数 | 破甲够 3 层才出手，堆叠类效果可编程 |
| **1.30.0** | 🐺 宠物指令技能 | 猎人/术士一键指挥宝宝，不占动作条 |
| **1.29.0** | 🧭 选取目标扩展 | 目标的目标 + 指定名称，想打谁就打谁 |
| **1.28.0** | 🗡️ 连击点数 | 贼/德专属条件，终结技时机可配 |
| **1.27.0** | ✨ 光环实时下拉 | 目标身上的 debuff 直接点选，怪给的也能配 |
| **1.26.0** | 🎭 目标职业 | 多选下拉=或关系，专杀某职业不是梦 |
| **1.25.0** | 🎯 选取目标 | 按宏先切目标再放技能，冲锋拦截自动化 |

---

## 🎯 v1.62.0 — 冲锋疑案真凶：接管时序（接管动作后移）

- **证据三连**（用户提供）：条件「可用」跳过 × 探针 `IsUsableAction(57)=true` × 手动点击可放；存档条件 `{k="usable"}` 无 inv，与探针调用逐字节一致——唯一差异只剩**调用时序**
- **真凶**：EVAL_GO 的自动攻击接管在规则评估**之前**执行 AttackTarget——开弓/进战瞬间，冲锋这类【脱战限定】技能的可用性缓存翻 false；探针/手动点击时没有接管副作用所以一切正常
- **修法**：接管块拆分——状态采集（st.autoAttack）保留原位；**接管动作后移到规则评估之后**，只在本次按键无规则出手时补自动攻击。规则评估时的世界状态=按键瞬间，不再被自家副作用污染
- 测试组 29/32 语义更新（接管动作在无出手时执行，空方案驱动）

## 🔬 v1.61.4 — 可用性疑案结案（跳过原因细分）

- **探针实证**（用户回报 /eh go probe usable 数据）：`IsUsableAction` 判定正常——冲锋 usable=true（含无目标时）；战斗怒吼/断筋/撕裂/英勇打击 usable=false 但全部 noMana=true = **怒气不足的正确判定**（冲锋免费所以不受此影响）。早前那次「冲锋跳过： 可用性」系 /reload 或姿态切换后缓存态短暂滞后，非逻辑 bug
- **跳过原因细分**：可用性判定失败时，调试日志直接显示 `不可用:资源不足`（noMana=true）或 `不可用`——以后不用跑探针也能区分（两处判定路径同改）

## 🚑 v1.61.3 — 条件关系列同换全角竖线（1.61.2 内联漏网）

## 🚑 v1.61.2 — 竖线显示修复²（换全角竖线）

- 1.61.1 用 `||` 转义（官方字面竖线写法），但**本客户端对 `||` 渲染异常**（显示成小方块/空白）——用户实测未生效
- 改用**全角竖线 ｜（U+FF5C）**：FZLBJW 中文字体自带全角字符，视觉与 ASCII 竖线一致，且不是转义符不会被吞。uiEsc() 实现一行替换，覆盖点不变（关系列/条件列/预览/聊天/IO 预览）

## 🚑 v1.61.1 — 条件关系「|」显示修复

- 用户报：条件关系切到 `|`（组间或）后显示为空——**`|` 在 WoW 里是颜色转义起始符**（`|cffRRGGBB`/`|r`），FontString/聊天框把单个 `|` 当无效转义序列吞掉，关系列和预览的 `非战斗 | 就绪` 显示成 `非战斗  就绪`
- 修法：显示层统一 `||` 转义（WoW 的字面竖线写法）——新增 `uiEsc()` 助手，覆盖：编辑窗关系列/条件预览、技能列表条件列、保存/添加/方案列表聊天输出、IO 保底预览区
- 数据层（存储/导入导出文本）不受影响，`|` 仍是组间分隔符；EditBox 原文不转义（复制粘贴保真）

## ↕️ v1.61.0 — 技能行下移按钮

- 技能列表行操作从 [上][编][删] 扩为 **[^][v][编][删]**——新增 v 下移（与相邻下一位交换顺序），^ 为原上移（文本「上」改箭头，两键成对）
- 顺序=优先级（1.59.0 起全部顺序执行），调序即调整尝试先后

## 🔬 v1.60.1 — 可用性探针（/eh go probe usable）

- 用户报「技能可用条件在该放时跳过（冲锋跳过:可用性）」——当前判定用 `IsUsableAction(slot)`，wiki 文档明确它 **Uses a cached usable state**（走缓存态，可能滞后或初始为 false）
- 新探针命令：`/eh go probe usable 冲锋`（不带参数 = 当前方案全部动作条技能）——打印每格的 usable/noMana/inRange/冷却/HasAction 原始值
- 在「该放却跳过」的站位跑一次探针，usable=false 而 inRange=true 即可证实是缓存态误判，后续再定矫正方案（换判定通道或缓存预热）

## 📏 v1.60.0 — 技能列表高度衍生

- 配置窗技能列表从固定 8 行改为**按窗口高度动态计算**（(H-120)/24，420 高 = 12 行）——延伸到底部关闭按钮上方，旧版下半截大片空置
- 下翻滚动按钮位置同步跟随最后一行（旧硬编码 -242 只适配 8 行）；行数变化后 offset 收敛/滚动显隐逻辑不变

## 🎭 v1.59.0 — 姿态下拉自适应 + 去除公共CD终止

- **条件「当前姿态」下拉自适应**：旧版硬编码 1/2/3——法师没有姿态、德鲁伊不止 3 个。改为按角色实际姿态栏动态生成（GetNumShapeshiftForms + GetShapeshiftFormInfo），项带名称（如 `1 战斗姿态`）；无姿态栏职业兜底只给 1
- **执行流程去除公共CD机制**（用户决定）：1.53.0 的「角色技能出手即结束本次按键」删除——现在**按顺序把条件符合的规则全部执行一遍**，能不能放交给游戏内置判定（GCD 中 UseAction 静默失败，无副作用）。规则编排直觉化：排在前面的先尝试，客户端自己决定哪个落地
- 测试组 34 语义更新：GCD 技能之后的规则继续执行（宠物指令照常发出）

## 🚑 v1.58.1 — 布局双修复

- **方案栏删按钮偏移**：配置窗方案列表的 [删] 按钮纵向间距写成了 21px，而方案按钮是 19px——每往下一行 [删] 就相对下沉 2px，方案越多错位越严重（用户截图：第 8 个方案的 [删] 掉到 [+] 行上）。改为同 19px
- **战斗UI 方案第二排宽度异常**：1.56.0 自适应布局对后续行按「该行实际方案数」拉伸填满——只剩 2~3 个方案时按钮被拉得异常宽。改为按「整行容量」定宽：满行仍填满右缘，**不满的行保持同宽左对齐**

## 🔢 v1.58.0 — 时间型条件步进修正

- 时间类数值条件（**距下次攻击 / 自身读条进行 / 自身读条剩余 / 读条已进行 / 读条剩余**）参数区改为：步进 **0.1**、区间 **0.0~10.0**、初始 **0.1**——秒级场景下 ±5/0~300 的通用步进完全不可用（一步就飞出有效域）
- 数值显示一位小数（防 0.30000000004 浮点噪音）；连击（±1/1-5）与其他数值（±5/0-300）不受影响
- 文本导入的任意值（如 距攻击<0.5）不受影响，本修正只约束编辑窗步进与初始值

## ⏲️ v1.57.0 — 条件「距下次攻击」（swingLeft）

- 新增数值比较条件（自身状态组）：**距下次攻击 X 秒**——复用 1.55.0 挥击计时（平砍锚点+攻速+施法推迟），文本 `距攻击>1` / `距攻击<0.5`
- 典型用法：`英勇打击 | 怒气>30 & 距攻击<0.5`（挥击落下前瞬间泄怒，不浪费普攻计时）；`压制 | 可用 & 距攻击>1`（离下次挥击还远优先打技能）
- 无计时数据（还没挥过/无攻速）时条件不满足——不会误判放行；测试组 38 覆盖 回环/比较/无数据

## 🔮 v1.56.1 — 法师案例模版×2 + 导入守卫修复

- 模版库新增法师分组（用户实配收录）：
  - **法师一键**：`选取目标:最近敌人 | 非战斗 & 无目标` 开怪 → `寒冰箭 | 范围内:寒冰箭 & 就绪 & 可攻击` → `火球术 | 可攻击 & 就绪` → `霜甲术 | 能量%>30 & 无buff:霜甲术` → `奥术智慧 | 无buff:奥术智慧`
  - **周围补buff**（通用法系）：`选取目标:最近友方 | 目标非战斗` + `奥术智慧 | 无目标buff:奥术智慧`——1.53.0 并行模型下切友+补 buff 一键完成，挨个补一圈
- **★修复导入守卫丢行**：技能名合理性上限 24 字节（8 汉字）是 1.x 时代定的——带前缀的技能名更长（选取目标：指定名称：嗜血者=39B、物品：弱效巨魔之血药水=36B），导入时这些行被**静默丢弃**（测试组 26 扩充时抓出）；上限提至 48 字节（16 汉字），仍挡说明性文字

## 📐 v1.56.0 — 战斗UI 方案切换行自适应布局

- **宽度对齐**：方案行右缘与上方血/能量/目标条一致（旧版固定 12 等分窄格，方案少时右半空置、方案名字挤压）
- **动态换行**：单按钮最小宽 34px 保证名字可读——首行（带「方案」标签）装不下时自动折到第二行、第三行（12 方案最多 3 行），每行按钮**拉伸填满整行**；窗口高度随行数自动撑高
- 布局在窗口重建/缩放时按当前方案数重新计算

## ⏲️ v1.55.0 — 挥击计时（施法学习同机制）

- **可行性验证**：wiki 确认 `UnitAttackSpeed(unit)` 存在（主手/副手秒每击，含急速修正）；挥击时刻锚点走 `CHAT_MSG_COMBAT_SELF_HITS` 战斗文字事件（「你击中/你爆击X造成…」英文 "You hit/crit"）——与 1.40.0 施法时长自学习同一事件机制，无需 SuperWoW
- **战斗信息UI**：读条下方新增金色挥击细条——进度=已过/攻速，文本=「下次攻击 X.Xs / 攻击就绪」（无数据时空条隐藏）
- **状态信息UI**：新增攻速行「攻速 X.Xs · 距下次攻击 X.Xs（副手 X.Xs）」
- **施法推迟**：读条会推迟挥击——锚点取 max（上次挥击， 读条结束时刻），施法后计时从读条结束起算
- 事件名若与本客户端不符则静默无效（不会炸）；测试组 37 覆盖 中英文锚点/法术不误锚/施法推迟/无锚点=nil

## 🗡️ v1.54.3 — 连击点数参数域修正

- 条件「连击点数」在编辑窗用通用数值步进器（±5、0~300）——但 GetComboPoints 只可能返回 0-5，步进到 8 以上条件永远不可能满足（死条件）
- 修正：连击条件专属步进 ±1、限定 1~5；其他数值条件（怒气/血量/秒数）维持 ±5、0~300 不变

## 🎚️ v1.54.2 — 去抖窗口可配 + 执行追踪器

- 用户反馈偶发「按一下执行两次」——去抖窗口从硬编码 0.2 改为**配置项** `cfg.goDebounce`：配置窗全局 Tab「一键宏」组 **执行去抖 [-] 0.30s [+]**（步进 0.05，范围 0~1.0，0=关闭去抖），默认 0.30s
- 新增 **执行追踪器**：每次 EVAL_GO 调用（含被去抖丢弃的）记录时间戳进 `EVAL_GO_TRACE`（最近 12 条）——**`/eh go trace`** 打印相对间隔毫秒与 执行√/去抖× 标记；现象复现时按两下键 → /eh go trace → 截图发我即可看清重复执行的真实间隔（判定是按下/松开双触发还是队列冲刷）
- 注：去抖只能消除间隔小于窗口的重复；若 trace 显示重复间隔稳定大于窗口，需要换思路（如目标切换幂等化）

## ⏱️ v1.54.1 — 执行去抖（连按堆积治理）

- **问题**（用户实测）：频繁按宏后执行滞后，停止按键后目标还在快速切换——wiki 证实 `/run` 宏在本客户端走 **RunScript pending 队列**（queued，按节奏冲刷）：按得比冲得快就堆积，松手后陈旧调用还在逐个执行
- **治理**：EVAL_GO 入口加 0.2s 执行去抖——间隔不足的重复调用直接丢弃（公共 CD 1.5s，节流完全无感）；队列里堆积的过期调用冲刷时几乎全部命中去抖 → **松手即停**
- 插件无法删除客户端的队列本身，这是行为等效方案：陈旧调用不再产生可见副作用
- 去抖命中时调试日志可见（/eh debug）；EVAL_GO1~12 直触同享；测试组 36（castLog 计数信号——接管自带 2s 节流会干扰 attackTried 信号）

## ✨ v1.54.0 — 光环检查四型（合并+新增）

- **合并**：自身有buff+自身无buff → 「**自身buff检查**」（是/否切换）；目标有debuff+目标无debuff → 「**目标debuff检查**」（层数门槛保留，方向随 是/否）
- **新增**：「**目标buff检查**」（是/否 + 实时下拉列当前目标 buff，●标记）——嗜血/迅捷等敌方增益可编程（驱散时机、爆发避让）；「**自身debuff检查**」（是/否 + 实时下拉列自身 debuff，▲标记）——中了减速/沉默再交章
- 实时项四象限配齐：◆目标debuff ○自身buff ●目标buff ▲自身debuff，名称→纹理照常跨会话学习
- 存量兼容：旧 noBuff/noDebuff 数据引擎照常判定；编辑窗打开自动归并为新格式；文本格式不变（`无buff:名`/`无debuff:名<3` 解析为合并结构，回写文本一致）
- 状态表新增 st.targetBuffs / st.playerDebuffs；测试组 35 覆盖 解析回环/引擎判定/反向/缺失 全路径

## ⚡ v1.53.0 — 并行执行模型（仅角色技能占公共CD）

- 用户定义的语义正式实装：**只有「角色技能」（动作条法术）有公共CD、出手即结束本次按键**；角色行为（攻击/自动射击/射击/取消施法/姿态切换）、宠物行为、选取目标、物品使用**全部可并行**——出手后刷新状态、继续评估后续规则（1.52.1 的选取目标续行推广到全部非 GCD 类型）
- 一次按键的真实效果：`选取目标:最近敌人 + 宠物:攻击 + 物品:治疗药水 + 致死打击` = 切目标、宝宝上、喝药、致死——四件事一键完成，只有最后的法术吃 GCD
- 排序即编排：免费动作放前面、压底放法术；GCD 法术之后的规则本次不再评估（优先级语义不变）
- 回归测试组 34：五连并行链（选目标→宠攻→姿态→攻击→法术全执行）+ GCD 技能终止（其后不执行）

## ⛓️ v1.52.1 — 选取目标不消耗按键（切换成功继续评估）

- **问题**（用户实测）：方案 `选取目标:最近友方 | 目标非战斗` + `奥术智慧 | 无buff:奥术智慧`，每次按宏只切目标永远不放奥术智慧——RULE_RUN「第一条通过的规则出手即结束」，恒过的选取目标规则把按键用尽了
- **修正**：切换目标是瞬发无 GCD 动作，不该消耗一次按键——targetSel 规则出手后 **EVAL_HELP_UPDATE_STATE() 刷新状态（新目标）并继续评估后续规则**；一次按键 = 切目标 + 放技能（仍只有一个 GCD 动作）
- 后续条件按新目标判定（可攻击/目标职业/debuff 等不会拿旧目标误判）；姿态/物品/宠物/取消施法等仍是终止动作（有 GCD/终态语义）
- 回归测试组 33：选目标+施法连发 / 单发选取目标仍计动作 / 条件门控不变

## 📏 v1.52.0 — 自动攻击距离检测（超程降档）

- 「自动攻击」的两个远程档（自动射击/魔杖射击）增加**射程检测**：`IsUsableAction` 不含距离判定（自动射击 8-35 码，贴脸也显示"可用"但放不出）→ 叠加 `IsActionInRange`，明确返回 0=超程时**自动降档**，最终落到近战普攻
- nil（无目标/无法判定）放行交给客户端自己拒；测试组 32 覆盖 超程降档近战 / 在程优先远程 两路径
- 顺带：测试桩发现接管 2s 节流（Engine local）在恒定 GetTime 下会卡死后续断言——测试用时间前移绕过（同 1.40.0 读条学习测试惯例）

## 🎯 v1.51.0 — 条件「自动射击 / 魔杖射击」+ 布尔取反修复

- 新增两个条件类型（自身状态组，bool 是/否切换）：**自动射击**（猎人自动射击激活中）、**魔杖射击**（法系魔杖射击激活中）——直查动作条 IsCurrentAction，未上条=未激活；文本 `自动射击`/`未自动射击`、`魔杖射击`/`未魔杖射击`
- 典型用法：`射击 | 未魔杖射击`（魔杖停了补一发）、`奥术射击 | 自动射击 & 目标血<30`
- **★顺带修复老 bug**：布尔条件解析的 `!` 取反表达式 `neg and (not b[2]) or b[2]` 优先级错误——所有布尔条目 b[2] 恒 true，导致 `!普攻` `!可攻击` 等**取反全部静默失效**（判定同正形）；另补显示层早有但解析层缺失的 `未普攻`
- 回归测试组 31：解析回环/激活与未激活/未上条=未激活全覆盖

## 🏹 v1.50.0 — 自动攻击（接管改名+优先级制）

- **改名**：开关「自动普攻接管」→「**自动攻击**」，新增悬停提示（优先 自动射击/射击，兜底 普通攻击）——cfgCheck 通用 tooltip 参数首用
- **优先级**：自动射击（猎人）> 射击（法系魔杖）> 攻击（近战普攻）；前两档要【上条且可用】（IsUsableAction——没装备远程武器/魔杖时自动降档），近战「攻击」无条件兜底
- 典型效果：猎人按宏自动开自动射击；法师按宏自动甩魔杖；战士不变开近战普攻；猎人忘带弓 → 自动降档为近战
- 日志可见通道：`→ 开启自动攻击（自动射击）`；st.autoAttack 状态采集跟随实际选中通道

## 🚑 v1.49.3 — 取消施法修复（Protected 绕过）

- **问题**：「取消施法」行为按了日志显示已出手，但读条没断。查 wiki 原文：`SpellStopCasting` 标注 **Protected: addons cannot call this; only the default FrameXML UI can**——插件直调静默无效（pcall 兜住不炸但也不生效）
- **修复**：改走 `RunScript("SpellStopCasting()")`——把断条代码排队为 pending script，wiki 明确此路径与聊天框 /run 相同且**无 Protected 标注**；执行时机为 pending 队列冲刷时（下一帧内），断条场景可接受
- 兜底：无 RunScript 的环境回退直调；测试桩同步（断言通道=RunScript）
- 待游戏内验证：/reload 后读条中按宏，读条应立即中断

## 🔍 v1.49.2 — 战士残留审计（第二轮）

**自动普攻接管流程（审计结论）**：通用机制非硬编码——按宏时若普攻未开（IsCurrentAction 守卫）则补开（2s 节流，切换语义连按安全）；1.47.0 已解耦状态采集。本次泛化：
- **接管支持猎人**：旧版只认「攻击」（近战），猎人接管形同虚设 → 无「攻击」时自动回退「自动射击」（UseAction 通道）

**清掉的残留**：
- `EVAL_T_RANGE` **重复定义两份**（1.37.0 编辑事故遗留，后者覆盖前者）→ 删一；参照技能多职业化（近战+背刺/脚踢/猛禽一击/审判/撕碎，突进+拦截/野性冲锋）
- 出手日志/无动作日志硬编码「怒气」「战斗姿态」→ 动态能量名（法力/怒气/集中值，UnitPowerType）+ 当前姿态名；条件文本里 power 的显示名同样动态化
- `example/zs_wq.md` 文件删除（内容早已入游戏内案例模版库）；Engine 头部战士技能清单注释、ENSURE_PROFILES 旧注释更新

## ▶️ v1.49.1 — 空条件技能直接执行

- **执行流程修正**：技能不配任何条件 = 无条件直接出手。旧行为是反的——编辑窗不加条件保存、或导入文本写 `- 技能 |`（空条件串），产出的都是**空 groups 表**，groupsOK 一组都进不去 → 恒判「条件不满足」，技能变死条目永远不触发
- 修复点只有一行：groupsOK 开头「无组即过」（trace 记「无条件」，释放日志可见）
- 回归测试组 30：空表 / 空串解析 / 纯空白串 三种空条件形态全部直放

## 🎯 v1.49.0 — 删除「无目标自动选敌」硬编码前置

- 用户实测日志：方案规则是 `选取目标:最近友方 | 目标非战斗`，按宏后日志却先出现 `无有效目标 → TargetNearestEnemy` 再执行规则——**EVAL_GO 里有一段 1.x 时代遗留的硬编码前置**：无有效目标时先自动选最近敌人，然后才评估规则。方向相反时互相抢目标（先切敌再切友）
- 与 1.22.0 删除「非战斗切姿态/冲锋」前置同一性质：**选目标行为全部交给规则**，引擎不做任何方案外的动作
- 需要自动选敌的写法（规则化）：`选取目标:最近敌人 | 无目标` 或 `选取目标:最近敌人 | 非战斗 & 无目标`
- 注意：攻击类技能仍需「可攻击」条件兜底（无目标时自动跳过）；本改动不影响 1.21.7 起「无目标不硬返回」的行为

## 🧹 v1.48.0 — 战士硬编码清零（技能清单纯扫描）

- **WAR_SKILLS 战士白名单全删**（攻击/战斗姿态/冲锋/压制/断筋/撕裂/战斗怒吼/血性狂暴/猛击/英勇打击）：技能编辑下拉、光环条件参数下拉（EVAL_GO_SKILL_CHOICES）全部改为**只列动作条扫描到的技能**——法师不再看到冲锋压制，战士也不再有未上条的灰项
- **新装默认方案去战士化**：旧版新装插件自动生成一整套武器战规则（其他职业全是垃圾条目）→ 改为空「默认」方案，起手走 配置窗→导入导出→[案例模版] 或自行添加
- 连带清理：war 配置里的战士阈值缺省键（hsRage/ovRage 等，滑条早已删除后无消费者）、UI 模块 WAR_SKILLS 别名、EVAL_WAR_SKILLS 导出
- 存量用户不受影响（只影响「方案为空」时的默认生成；已有方案原样保留）

## 🏹 v1.47.0 — 角色行为扩充（取消施法/自动射击/射击/姿态序号）

- **取消施法**：新特殊行为（不占动作条），执行 `SpellStopCasting` 打断自身当前读条；未在读条时静默跳过（建议配条件：`取消施法 | 施法中:寒冰箭 & 目标施法中` 之类的打断保命场景）
- **自动射击 / 射击（魔杖）**：入「角色行为」下拉——走动作条通道同「攻击」（需拖上动作条后重扫；这两个是切换语义，规则里建议配 未普攻 类条件防反复翻转）
- **姿态序号**：`姿态:名称` 之外兼容 `姿态:2`（姿态栏第 2 格），文本导入更方便
- **★审计修复——普攻状态与接管开关解耦**：旧代码里 `st.autoAttack` 只在「自动普攻接管」开关开启时才采集 → 开关关掉时规则里 普攻/未普攻 条件恒读「未普攻」（规则放「攻击」会每按一次开/关翻转）。现在状态始终如实采集，开关只控制「自动开启」动作
- 审计结论：接管开关与规则「攻击」**不冲突但同效**——开关=无条件的 `攻击 | 未普攻`（2s 节流）；规则版可加条件更灵活，二选一即可

## 🧩 v1.46.0 — 战斗UI 图标带合一 + wslots 表引用修复

- **两行合一**：原「技能图标行」（固定战士清单改造版）与「方案技能行」功能重复 → 合并为**一条技能图标带**：位于方案切换行下方，内容=激活方案技能，**8 格/行、超过自动换第二行（≤16 格）**，空位整格隐藏
- 功能全集保留：悬停 tooltip 看触发条件、点击开技能编辑窗、亮金=条件当前满足、「停」=停用、「?」=未找到、**动作条技能带冷却倒数**（自旧图标行并入）
- **★修复 wslots 表引用 bug**：RESCAN 旧写法 `wslots = {}` 重新赋值 → 导出的 `EVAL_WSLOTS`（UI 模块别名持有）仍指向旧空表 → 重扫后 UI 读动作条全 nil（1.45.0 截图里下行图标全 ? 的根因）。改为**原地清空**，表引用永久有效；测试组 28 回归防护
- 窗口高度固定预留两行（尺寸不随技能数跳动）

## 🎯 v1.45.0 — 方案驱动显示（旧版固定战士清单清零）

- **战斗信息UI 技能图标行**：旧版写死 7 个战士技能（攻击/冲锋/压制/战斗怒吼/断筋/撕裂/血性狂暴，其他职业全显示 ?）→ 改为**实时跟随激活方案的技能列表**（8 格，空位隐藏；图标走 wicon 统一入口，宠物/物品/姿态特殊技能同样显示；亮金=条件当前满足；动作条技能保留冷却倒数；停用的显示「停」）
- **重扫动作条核对报告**：扫描本身自 1.24.0 起已是全技能泛化，但报告只核对战士白名单 → 改为核对**激活方案**的每条技能（特殊技能标注「不占动作条」，未上条的提示拖上后重扫）
- **/eh go 状态总览**：同样从固定战士清单改为激活方案技能（物品显示背包是否找到）
- 硬编码高亮逻辑（压制可用/怒吼 buff/断筋撕裂 debuff 等战士特例）全部删除——通用 groupsOK dry 预览判定替代，任何职业任何条件组合都有实时反馈

## 🛟 v1.44.1 — 宏入口兜底（EVAL_GO nil 红框治理）

- 排查结论先行：当前磁盘代码全链验证健康（Lua 5.1 语法全过、EVAL_GO 定义在 Engine.lua:1068、全链加载后 EVAL_GO() 调用成功）——游戏内弹「EVAL_GO nil」说明当时加载到的是**不完整的文件**（最常见：改码中途 /reload 读到写入一半的 Engine.lua），/reload 一次即恢复
- Core.lua 末尾新增兜底 `EVAL_GO`：Engine.lua 未成功加载时，按宏不再弹「attempt to call global 'EVAL_GO' (a nil value)」红框，改为聊天框提示「引擎未加载完整，请 /reload；反复出现检查角色插件列表」
- 正常加载时 Engine.lua 末尾的正式 `EVAL_GO` 定义覆盖兜底，行为零变化

## 📋 v1.44.0 — 案例模版（按职业分组的方案库）

- 导入/导出弹窗新增 **[案例模版]** 按钮 → 打开模版选单：**按职业分组**（金色组标题），每个职业下挂若干方案行（名称+一句话简介，悬停高亮），**点击即导入**为新方案（超 12 个上限时替换当前方案）
- 模版数据集中在 `EVAL_IO_TEMPLATES`（cls/list.name/desc/text 结构），text 与导入导出同 md 格式——后续新职业模版只加数据不加代码
- 原「武器战示例」按钮删除并入模版库（武器战模版首发，新增「姿态：战斗姿态」开怪行展示 1.43.0 姿态技能）；选单从 IO 窗弹出（frameLevel 95>90），IO 窗关闭时联动关闭
- 顺带修复两个存量 bug：① 导入按钮方案上限残留旧值 4（1.42.0 已提 12）；② `EVAL_PROFILE_FROM_TEXT` 内裸 `condTrim` 漏桥（拆分后未别名，游戏内点导入必炸——测试新断言组 26 抓出）

## ⚔️ v1.43.0 — 特殊技能「姿态切换」

- 技能编辑窗「角色行为」分类新增 **姿态:名称** 项（姿态栏实时枚举：战士三姿态/德鲁伊形态/盗贼潜行等，有多少栏位列多少项）
- **不占动作条**：出手直调官方 `CastShapeshiftForm(栏位)`——文档明确 **Not protected**，插件可调用；与「角色技能」里拖上动作条的同名姿态技能相比，省一个动作条格子
- **已激活守门**：已在该姿态时跳过不执行——战士姿态本就不可取消（重复按 no-op），但德鲁伊形态重复按会**取消** aura，必须拦
- 就绪判定走姿态栏 API：`GetShapeshiftFormInfo`（激活/可用）+ `GetShapeshiftFormCooldown`（冷却），未拥有该姿态=「无该姿态栏位」静默跳过
- 与全部条件自由组合：`姿态:防御姿态 | 目标血<20 & 战斗中`（斩杀线换盾墙姿态）、`姿态:战斗姿态 | 非战斗 & 非姿态1`
- 添加方式：编辑窗技能行 [分类▾] 选「角色行为」→ [具体项▾] 选姿态；文本导入 `- 姿态:战斗姿态 | 非战斗 & 非姿态1`
- 战斗信息UI 亮金预览/图标（GetShapeshiftFormInfo 图标）全配套；README 更新记录表裁到 1.33.3 起（更早历史见 CHANGELOG 与 git）

## 🧰 v1.32.0 — 技能五级分类（行为/技能/宠物/选目标/物品）

**背景**：社区反馈「选取目标不该只是条件」「想一键喝药/换装」。本版把方案里的「技能」升级为五类统一抽象，编辑窗技能名下拉改为**二级下拉**（先选分类、再选具体项）。

**要点**
- **角色行为**：攻击（普攻开关动作）
- **角色技能**：战士白名单 + 动作条扫描到的全部技能（1.24.0 泛化成果）
- **宠物行为**：8 种宠物指令（1.30.0，不占动作条直调 Pet API）
- **目标选取**：9 种切换方式（最近敌人/上一目标/目标的目标/指定名称…/清除目标）——**由条件类型提为技能级**，规则动作本身即切换目标；原条件类型保留兼容存量方案，新增条件下拉不再提供
- **物品使用**：`物品:名称`——背包实时扫描下拉（带数量）+ 自定义名称输入；消耗品直接使用、**装备自动穿上**（官方文档确认 UseContainerItem 不受保护）；「就绪」条件走背包冷却

**典型用法**
```
物品:超效治疗药水 | 自身血<35 & 就绪        ← 残血自动喝红
选取目标:最近敌人 | 非战斗                 ← 按宏先选怪，下按冲锋
物品:夜幕 | 非战斗 & 姿态1                 ← 一键换装（BoE 未绑定会弹确认）
```

**设计亮点**
- 引擎三路豁免统一范式：wuse/wready/wicon/RULE_RUN/亮金 五处对「宠物/选取/物品」一致性放行，特殊技能不占动作条
- 物品定位每次实时扫包（链接解析名称精确匹配），不吃缓存过期；装备/消耗品同一接口零分支
- 选取目标与「目标职业+指定名称」串联可做出「一键给周围特定职业补buff」类方案（README 头部案例）

**顺手修复（社区双代理审计 1.25.0~1.31.0 发现）**
- 🔴 1.27.0 光环学习表 `cfg.war.debuffTex` 从未创建 → 名称→纹理学习跨会话丢失（已补初始化）
- 🟡 1.31.0 层数后缀反向写法（`有debuff:x<3`）语义反转 → 降级为无层数限制
- 🟡 1.26.0 下拉多选未传 selected 时索引 nil 隐患（已兜底）
- 🟡 头注释版本号自 1.26.0 断档（已恢复三同步并改名 EvalHelp）

---

## 📚 v1.31.0 — 目标 Debuff 层数条件

- 「目标有/无debuff」条件支持**堆叠层数门槛**：基于 `UnitDebuff` 第二返回值（层数；非堆叠 debuff 归一化为 1）
- 编辑窗：debuff 条件行新增「层」下拉（不限 / ≥2~5 层），技能名实时显示 `破甲攻击 ≥3`
- 文本格式：`有debuff:破甲攻击>=3`（至少 3 层才过）、`无debuff:破甲攻击<3`（不足 3 层=需要补）
- 典型用法：`致死打击 | 有debuff:破甲攻击>=3 & 怒气>30`（等破甲叠够再爆发）
- 完全向后兼容：无层数后缀的旧条件行为不变

## 🐺 v1.30.0 — 特殊技能「宠物指令」

- 方案技能列表新增特殊类技能 **宠物:指令**，全量 8 种战斗行为：**攻击 / 跟随 / 停留 / 停止攻击 / 被动姿态 / 防御姿态 / 主动姿态 / 解散**（对应 PetAttack/PetFollow/PetWait/PetStopAttack/PetPassiveMode/PetDefensiveMode/PetAggressiveMode/PetDismiss）
- **不占动作条**：出手直接调用官方 Pet API（非保护函数，文档+经典 1.12 语义双重确认）；无宠物时静默跳过
- 与全部 26 种条件自由组合：`宠物:攻击 | 战斗中 & 可攻击`（一键宠攻）、`宠物:被动姿态 & 宠物:跟随 | 非战斗`（脱战收宠）
- 刻意不含：永久放弃（PetAbandon 太危险）、改名、兽栏系（非战斗行为）
- 添加方式：编辑窗技能下拉末尾「宠物」段直接选；文本导入 `- 宠物:攻击 | 战斗中 & 可攻击`
- 图标兜底：宠物头像（GetPetIcon）/ 问号；战斗信息UI 亮金预览同样生效

## 🧭 v1.29.0 — 选取目标扩展「目标的目标 / 指定名称」

- 新增选中类型**目标的目标**：`TargetUnit("targettarget")` 驱动（UnitID 不解析时无动作，比 AssistUnit 安全）
- 新增选中类型**指定名称**：`TargetByName(名称)` 驱动（大小写不敏感、附近单位名匹配）
- 名称参数两种给法：下拉列出**最近 5 个敌人名称**（循环选取枚举法，选完自动还原原目标）或 **✎ 自定义名称**（回声行输入弹窗，EditBox 不渲染也能盲打）
- 典型用法：`拦截 | 目标职业:战士/法师 & 选取目标:目标的目标`（帮队友拆火）、`制裁之锤 | 选取目标:指定名称:治疗者`
- 文本格式：`选取目标:目标的目标` / `选取目标:指定名称:嗜血者`（未设名称的规则不会触发，防止乱切）

## 🗡️ v1.28.0 — 条件类型「连击点数」（第 26 种）

- 基于官方 `GetComboPoints()`：数值比较型条件（`>` `>=` `<` `<=` `==` `~=`，0–5）
- 非盗贼/德鲁伊恒返回 0——条件对其他职业**天然安全**，不会误触发
- 典型用法：`剔骨 | 连击>=3 & 能量>35`
- `/eh st` 能量行对贼/德追加「· 连击 N」实时显示

## ✨ v1.27.0 — 光环条件下拉「实时项」

- 「目标有/无debuff」下拉打开时**实时扫描当前目标**，追加 ◆ 项（目标身上的 debuff 直接点选）；「自身有/无buff」同步追加 ○ 项
- 选中的光环**名称→纹理即时学习并持久化**——此后即使 debuff 不在动作条上（怪给的），规则也能正确匹配
- 提示：先选中目标再开下拉，实时项才会出现

## 🎭 v1.26.0 — 条件类型「目标职业」

- 职业**多选下拉**（猎人/牧师/法师/术士/萨满/盗贼/战士/德鲁伊）：点按切换 √ 金标不关面板，**多选=或关系**
- 比对 `UnitClass` 英文 token，跨语言稳定
- 文本格式：`致伤打击 | 目标职业:牧师/德鲁伊`（专压治疗）
- 下拉面板新增多选模式（`EVAL_DD_OPEN` 泛化件能力升级）

## 🎯 v1.25.0 — 条件类型「选取目标」

- 七种无参选取函数入下拉：最近敌人 / 最近友方 / 最近队友 / 最近团员 / 上一敌人 / 上一目标 / 清除目标
- **副作用条件**：求值通过时立即切换当前目标——`冲锋 | 非战斗 & 姿态1 & 选取目标:最近敌人` 一键开怪
- 防误切设计：战斗信息UI 亮金预览走 dry 模式，只验证不执行

---

> 📜 更早版本（1.24.x 及以前：动作条重扫泛化、目录改名 EvalHelp 等）见 git 提交历史。
