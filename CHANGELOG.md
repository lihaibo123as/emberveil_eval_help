# 更新日志 — EVAL_HELP 全职业工具

| 版本 | 主题 | 一句话亮点 |
| :-- | :-- | :-- |
| **1.75.13** | 🔍 **地图标注：右侧放大镜入口 + 分类标注下拉 + 弹出层「点外面即关」· 黑幕抗闪收口 · 删测试框架** | ① 地图右侧**放大镜入口**（素材取任务插件 UnrealQuest 的 `search-icon`，`GetTexture()` 读回自证 + 取不到退回文字；右对齐地图窗，DX/DY 独立常量）—— **左键弹分类标注多选下拉**，标签常显类别数；★点下拉窗**之外任何位置即关**（新增全屏捕获层 `EVAL_HELP_DD_CATCH`，与 `EVAL_DD_HIDE` 同一出口）；② 地图标注**同时画到小地图**（照抄任务插件自己的小地图配方 `Client.CreateMinimapPin`，图钉是 Minimap 子件、开大地图随之藏）；③ 工具箱「缩放大地图」的设置面板同款**点外面即关**（`EH_SM_CFG_CATCH` + `smCfgShow` 统一收口 + `OnHide` 兜底）；④ ★**弹出层两条硬事实**：抬高父帧**不带动子件** ⇒ 行与搜索框必须**逐个**抬高（不抬就是「背景盖住行 = 黑框 / 行被埋 = 点不动」）；⑤ **黑幕抗闪收口**（`SetAlpha(0)` 硬抗闪 + `Hide()` 保鼠标 —— 透明全屏帧照样吃点击；开图 0.6s **有界**逐帧重申；**关图不还原**任何透明度/缩放/位置，只有**关功能**才还原且先抓原值 + 读回自证）；⑥ **删测试框架**（`tests/` + `test_assert.lua` + `test_engine.js` + `check.js` + `mutate.js`），唯一闸门 = `node luacheck.js`。 |
| **1.75.5** | 🗺️ **探索层原值只在会话内存 + 关图即清空（每次开图重新读、重新缩）+ 修「0 条也算抓取完成」** | ① 原值**不再落存档**（只在会话内存按图存；老键一次性清掉、不再写版本戳）；② 用户要求「**关图即清空原值、开图重新获取并重新缩放**」⇒ 清空在关图那一刻做，且**先还回自然档并严格自验、验不过就绝不清空**（否则会把折后值当原值 = 折两遍）；③ 修「**/reload 时原值为 0 也被当成抓取完成**」—— `todo` 空必须「确有记录」才算已齐，候选全不在用 ⇒ **不落闩 + 1 秒一次有界重试**（随后自动补抓）；④ 抓原值不再每帧重跑、地图缩放不再抖、瞬态 es 不折算、两种读回口径都认；⑤ 修「**探索层贴图跑到游戏画面上**」（写锚点必须先解析成帧对象）；⑥ 新增手动适配命令 **`/ehm fitnow`**（五步逐步打印）+ 播报分**安静档/详细档**（`/ehm mapfit verbose on`，不跨会话）。 |
| **1.75.4** | 🔔 **稀有转播=通知（不受「调试日志」总闸门）+ 探索层原值带地图身份（修「坐标丢失 / 全挤在左下」）** | 详见下方同名小节。 |
| **1.75.3** | 🎯 **选取目标一族（玩家的目标 / 目标玩家:X / 目标死亡）+ 修「自动攻击抢目标」+ 围攻·交战人数条件 + 任务线数据补完** | 详见下方同名小节。 |
| **1.75.2** | 🎯 **追踪条件（自身状态 → 追踪类型）+ 聊天输出总闸门「调试日志」+ 缩放大地图探索层折算收口** | ① 一键宏条件新增「自身状态 → **追踪类型**」（**下拉单选**；候选表 `TRK.hint` **13 条实证**，纹理→种类**全等比对**；贵调用（tooltip）**只在追踪变化时做一次**；文本导出走稳定 id、界面走本地化标签）；② 配置项「**调试日志**」升级为**整个插件往聊天框说话的总闸门**：关掉 ⇒ `say`/`EVAL_SAY` **一个字都不刷**（同一判据也是日志环闸门），常开出口只留「开关自身的确认行 + 引擎未加载兜底」；③ **缩放大地图探索层折算收口**：关掉零动作、**折算前先探测客户端会不会级联**、原值**只在自然档**抓一次、**关→开不连乘**、关闭**收钩子**+开启**再武装**、折算取证环 `mapFitTrace`、配置改**懒代理** |
| **1.75.1** | 🔀 **两支合流（框拖拽/图层工具/缩放大地图 ⇄ 任务线）+ 图层拖拽宽高只给聊天窗 + 宠物动作条 + 全职业「工具」口径** | ① **合并** `probe/map-scale` → `master`：5 处冲突逐条审过（`tools/HunterHelper.lua` 取 map-scale 82 函数并补回只读口；`test_assert.lua` 两侧断言组**都保留**；`test_engine.js` 载入清单取并集），并修掉 3 个合并副作用（**OnUpdate 真零形参**：dt 只认全局 `arg1`、测试注入也走真机通道 `EVAL_TEST_FIRE_UPDATE`；`EVAL_HH_PETDECOR` 补 `hhHasPet()` 门：没宠物一律默认金边；参考卷打包脚本补回 quest/ 三行）；② **新铁律**：`TEST LOAD ORDER CHECK`（harness 里 `test_assert.lua` 必须排在所有 `tools/*.lua` 之后 —— 否则组 200 读不到载入期登记的模块行）；③ **图层拖拽：宽/高只对聊天窗开放 + 持久化**（用户 1.75.1：「只有针对聊天窗才支持长宽设置.并且持久化」）：唯一真值 `dfSizeOK`（聊天窗 = `ChatFrame1..N`）⇒ 属性弹窗对聊天窗**建并显示**宽/高两行（窗高 224→280）、其余窗口 **Hide** 两行并收回窗高；保存走「**先抓原始值 → 再写新值**」、落存档 `w/h/ow/oh` 持久化（重登/应用存档写回）；启动清理**只清不许改宽高的目标**，**聊天窗的宽高一个字段都不动**；判据 = 组 206 整组反转 + 组 220①~④ + `DF OPEN HOOK CHECK` 四道新哨兵（白名单正则唯一 / 按能力显隐+窗高 / 清理按 noSize 过滤 / 抓原值早于 SetWidth）；④ **插件口径**由「全职业**施法**工具」改为「**全职业工具**」（`Core.lua` / `EvalHelp.lua` / `EvalHelp.toc` 的 Title+Notes / 三语言包 `CFG_TITLE`·`MB_TIP_TITLE` / 三语 README / `DEVELOPMENT.md` **一起改** —— 只改一处会自相矛盾），Tab4 由「数据检索」改名「**任务线 & 装备**」（`TAB_DS` 三语键 + 页内六处文案 `DS_DEP_TITLE`/`DS_QP_ZOOM_TIP`/`DS_QP_GOTO_DS`/`DS_QP_SENT`/`PH_ZOOM_TIP`/`PH_NO_DS`）；★Tab 名变长 ⇒ **Tab 宽 74/78 → 84/88**（7 个 Tab 仍在一行：`12 + 6*88 + 84 = 624 ≤ 660` 中文窗宽）；⑤ 工具箱 → 图层拖拽**常驻层补「宠物动作条」**：`DF_TARGETS` 新增 `PetActionBarFrame`（走候选名解析，同动作条1~4 —— 帧名只能现场探，一个都没命中就**如实缺席**）；★它是**按需出现**的（没宠物时不在）⇒ 与队伍/团队同族进**有界跟随** `DF_ROSTER_EVENTS`，事件选 **`UNIT_PET`**（有本地证据：本项目 `addons/EH_DebugBox` 正在注册它），★**`PET_BAR_UPDATE` 本地零证据 ⇒ 不注册**（铁律 5④：探测名单不算注册依据），跟满 `DF_ROSTER_TRIES` 即停、不做常驻轮询；⑥ 猎人助手提示行收成**唯一来源** `EVAL_HH_TIP_LINES()`（悬浮提示与断言读同一份文本，不会两处漂移）：没选喂养技能时由含糊的「未识别」改成**明确提示 + 警示色**「技能：未选择喂养宠物技能（右键从法术书挑）」（与「食物：未设置」同一形态）；判据 = 组 207/208（宠物动作条候选解析 + 有界跟随）+ **新增组 227**（提示行明确化 + 反向哨兵「文案里不许再出现未识别」）；⑦ **修「经典任务线按步骤名搜不到」**（用户实测 60 级经典线「**爱与家庭**」找不到）：它其实**在包里** —— 是自动任务线「**救赎**」（7 步 · 56-60 · 东瘟疫之地 · 联盟）的第 4/5 步，但**步骤名被丢了** ⇒ 运行时显示成 `#5742`，而搜索匹配的是「线名 + 步骤名」⇒ 搜不到。根因 = `quest/build_bulk.js` 的步骤名收集判据写成「**列表页**里有没有」（`!sweepById[id]`），而进包的 q 表**只装带装备奖励的 751 条**（站点 4018 页）⇒「在列表页、但没有装备奖励」的步骤名字**整批丢掉**（实测 1834 个步骤里 **1585 个没名字**、604/611 条线受影响），而站点详情页的系列块**本来就带名字**。修法 = 判据改成「**不在进包任务表**里」（`shippedIds`）⇒ 重新生成后 **sn 补名 25 → 1575 · 无名步骤 0**，搜「爱与家庭」直接命中「救赎」。★另补闸门 **`quest/audit.js` H 段（生成物体检）**：直接解析**生成物** `QuestBulk.lua`，任何系列步骤既不在 q 表也不在 sn 表 ⇒ **硬错误**（阈值 0）；变异验证 = 换回旧生成物 → 1950 个无名步骤、exit 1（✅ 捕获）。★自查踩坑：`s` 表是**序列**（`s={ "名|lo|hi|…" }`）**没有 `[n]=` 键**，首版按键值行解析 ⇒ 0 条、闸门恒绿（**假绿**）—— 解析前必须先确认表的形态。★**同一坑的第二层（用户第二轮截图）**：即使 `sn` 有了名字，**运行期照样读不到** —— `EVAL_QC_SERIES_DETAIL` 把系列步骤名 `n` **覆盖成序号**、`EVAL_QC_LINES` 取名字**只认 q 表**（`(s.q and s.q.n) or "#"..id`）⇒ 弹窗 7 行仍全是 `#5742`。修法 = 详情加 `nm`（站点系列块抄来的名字，`n` 保留序号）+ 行模型**四级来源**（`nm` → q 表 → 新读值口 `EVAL_QC_STEP_NAME`（读 `sn`）→ 如实 `#id`）+ 放大镜改用行模型真名 + 装备→来源任务线那条路也补 `sn` 兜底。判据 = **新增组 228**（搜「爱与家庭」→ 7 个步骤行 → **0 行带 `#`**、两步显示真名、首行序号仍是 `1. `）；变异（名字解析改回只认 q 表）⇒ 组 228 报「实测带 # **7** 行」、exit 1（✅ 捕获，正是截图那 7 行） |
| **1.75.0** | 🧭 **数据检索新增「任务线」+「任务线推荐」弹窗（装备优先）**：经典任务线（**全量 4-60 级**）· **先按等级列可获取装备 → 反查要做哪些任务** · 阵营严格三分类（联盟/部落/共有）· **每个任务节点带放大镜**（本插件 `database` 图标，按任务名直达数据检索且**不关窗**）· **滚动区按窗高铺满**（行数现算，不写死）· 装备详情**大图标 + 大标题**（标题/副标题垂直不重叠 · 悬停即出装备信息）· 翻页改**文字按钮 [上一页][下一页]**（底部按钮顺序 **[上页][下页][← 返回]**、整体左对齐 · **两个视图都常显**）· 焦点离开装备信息**正确隐藏** | ★**最终口径（本版收口，数字从数据现算）**：**62** 条策展经典链 + **611** 条自动任务线（界面合计 **673** 条 · ≥10 步 18 · 开门/史诗 17 · 最长 24 步）+ 全量 **751** 条带装备奖励任务 / **1262** 件物品（装备视图列出 **990** 件），覆盖 **4-60 级**（首版 22 链 / 88 任务 / 62 奖励已被 1.75.9 全量采集取代）；逐条抓取自 `database.emberveil.org`；★**奖励优先**：精良武器（旋风之斧/剑/锤、维里甘之拳、西部荒野法杖、新月法杖、翼刃、墓碑节杖、逃犯弯刀）排最前，**每条链给完整上下级步骤**（按接取顺序，带等级与地区）；奖励行**悬停出原生物品详情**、步骤行**点击进任务详情**；★过滤下拉新增「任务线」，空关键词直接列出全部链；★抓取/生成脚本**独立在 `quest/`**（`fetch.js`+`chains.js`+`build.js`，与插件零耦合）；★解耦：数据自带，**UnrealQuest 缺席时任务线照样能查**；★**装备筛选种类细分 + 武器子类型 + 多选**（用户 1.75.8）：「类型」筛选由三态单选（全部/仅武器/其它）改成**多选下拉**（走 1.26.0 的 multi 件：点按切换勾选、不关面板），选项**由数据现算**（`EVAL_QC_KIND_LIST`：剑/斧/锤/匕首/法杖/弓 · 盾牌/布甲/皮甲/锁甲/其它 —— 只列数据里真有的种类），「武器」「其它」两个**分组标题不可点**（locked 行，避免点标题凭空多一个勾），末行**「全部」一键清空**，空集 = 全部；判定只写在数据层一处（`EVAL_QC_ITEM_ROWS` 的 `kinds` 集合），按钮文字随勾选变（空=全部 · 单选=种类名 · 多选=首项…(N)）；★**全量数据 + 30-60 任务线 + 审计 + 输入框修复**（用户 1.75.9）：① **全量采集**——列表页扫完 10-60 全部 81 页 4008 条任务，**修正三个站点事实**（列表页奖励图标是**截断显示** ⇒ 奖励以任务详情页为准、与列表取并集；搜索 `?name=` 只匹配**英文标题子串**（中文名查不到）；「本系列第 N/M 部分」改用**结构性解析**（块内任务链接按文档顺序 + part 号插回当前页）——旧写法在部分页面整块错位、生成过一批假任务线）⇒ 带装备奖励的任务 **751** 条、物品 **1262** 件（其中装备 **994**）、**自动任务线 688 条**（≥8 步 36 条 · 开门/史诗 19 条：斯塔文的传说 / 埃提耶什之杖 / 奥妮克希亚的血脉 / 基尔卡克的钥匙 / 安斯雷姆的钥匙 / 圣光节杖 / 梦魇的缠绕… · 覆盖到 55-60）；★**系列解析第三次返工**：站点系列块有「全链接 / **全纯文本无链接** / 混合」三形态，只按链接解析会让经典线退化成 1 步（迪菲亚兄弟会 1/5、莱恩的净化 1/9）⇒ 改成**链接 + 可见文本「数字→名字」双来源** + `quest/resolve.js` 按名补 id（候选过 part/total 交叉验证）；★**审计加三段**：E 策展链 ↔ 站点系列核对（22 条**全部一致**，并按站点补全 10 条链的缺步）、F 装备↔任务双向链接（不一致 0）、G 任务线清单 + 按区域清点（黑石塔 27 / 东瘟疫 27 / 希利苏斯 20 / 黑石深渊 19 / 斯坦索姆 17 …；唯一未命中＝灰烬使者，**1.12 本就没有这条任务线**，已如实写进报告）；② 生成 **`quest/QuestBulk.lua`**（186 KB），装备视图从 62 件 → **994 件**，每件带**来源任务**（单任务也算）与可点放大镜；③ **审计双层**：`quest/audit.js`（装备/任务链接 + 列表页↔详情页双向 + **任务线完整性**，现 **0 硬错误**）+ 游戏内 `/eh ds 任务线 审计`（逐件对账**游戏内名字/图标**，未缓存按限频补）；④ **修「图片内的输入框无法输入」**：弹窗 EditBox 补 `EnableMouse`（真凶）+ 点击即聚焦按钮 + 左对齐 + 字体链兜底 + 回车/Esc；⑤ 等级档扩到 **10-19/20-29/30-39/40-49/50+**（下拉由 `QP_LV` 现算）；判据 = `QUEST TOC/WIRING/PANEL CHECK` + 组 191 ⑤/⑤b（新口径）+ 组 192 ⑧j~⑧n + 变异 **M20b/M21~M35 全捕获**；★**1.75.10 任务线按等级排序 + 稀有度染色 + 行尾奖励图标**（用户：「任务线也按等级排序，并按稀有程度用魔兽特有的染色机制，橙色给史诗风剑之类的任务」+「任务线同行后面添加对应的装备奖励图标展示，可以预览链接」+「同个任务线装备奖励多个就都显示，并排在任务标题右边、左对齐」）：**排序**改「等级 → 稀有度 → 原序」（旧「开门优先/档位优先」口径被明确反转，组 191 ③ 断言同步翻转）；**稀有度 6 档 = 魔兽品质色**（`EVAL_QC_RARITY` 单一来源 · 列表行与详情标题都按它上色 + `[档位·稀有度]` 标签 + 三语言 6 键）：**传说 ff8000**（雷霆之怒/逐风者/埃提耶什/萨弗拉斯/灰烬使者/流沙/安其拉 —— 用户点名的「风剑之类」）、**史诗 a335ee**（团本开门/钥匙/≥10 步/职业史诗）、**稀有 0070dd**（S 档或 ≥6 步）、**优秀 1eff00**（≥4 步）、**普通 ffffff**（2-3 步）、**粗糙 9d9d9d**（数据缺失）；**行尾奖励图标**：`EVAL_QC_CHAIN_REWARDS` 取该线全部装备奖励（与详情同序），紧跟标题**左对齐**排开、与武器列表同一套 `qpItemTex`（游戏内真实路径优先 · 未缓存自动排队补），**悬停出物品链接 tooltip**、点击进该装备详情，行宽装不下的用「+N」如实提示；★宽度单一来源 `qpTextWidth`（实测 `GetStringWidth` 优先，本机 API 表没有它 ⇒ 逐字节判 UTF-8：中文整宽/ASCII 半宽 —— 绝不用 `string.len` 当宽度）；判据 = `QUEST WIRING/PANEL CHECK` 新增 20 条 + 组 192 ⑦/⑧o/⑧p + 变异 **M36~M43 全捕获**；★**1.75.11 顺滑滚动 + 等级筛选修复**（用户：「装备和任务线滚动机制参考抓宠助手的滚动方式，顺滑滚动」+ 截图「等级筛选 -> 无法点击」）：① **滚轮由「整页跳」改成「1 行/格 + 像素滑动动画」**（照抓宠帮手/工具箱那条范式：起步错位一行高 → 每帧 ×0.55 缓动 → <0.6px 归零并**摘钩**；`qpLayoutRows` 每帧 `ClearAllPoints`+`SetPoint` 摆行，`OnUpdate` **零参数**、帧引用走外层 local —— 本客户端实测回调不传参）；② 列表填充层**不再把 `qpOff` 拍回页边界**（否则刚滚一行就被拍回页首 —— 实测当场撞到），只做夹取；`[上页][下页]` 仍按页算位并带一段短促滑动；③ **详情视图也能滚**（长链 24 步、多来源装备都看得全）：正文改由**统一绘制件 `qpDrawDetailBody`** 渲染 + 页内 `X-Y / N` 计数，两视图不再各写一份；④ **修「等级筛选点不动」**：处理函数里写了 `local labels` **遮蔽**外层 `local labels, cb` ⇒ `EVAL_DD_OPEN` 收到 nil（下拉建不出来）⇒ 真机表现就是「点了毫无反应」——去掉 `local`，并补**静态 CHECK**（禁遮蔽）+ **真点按钮的行为判据**（⑧q：真按钮→真下拉 6 档→真选「10-19」→列表真的只剩该区间）；判据 = CHECK 新增 12 条 + 组 192 ⑧q/⑧r + 变异 **M44~M49 全捕获**；★**1.75.12 任务等级+感叹号 / 放大镜跳 tab 与红字兜底 / 只检索可见装备**（用户四条：①「装备→任务线信息要展示任务对应的等级，左侧图标也要显示黄色感叹号」②「点击任务放大镜报错」③「点击任务放大镜自动打开数据检索 tab 进行对应任务的搜索」④「装备检索只在列表当前页检索…不要顺序检索所有装备…添加检索频率，不要太频繁…只在当前看得见的装备进行检索」）：① 装备详情的**来源任务行 / 链步骤行**与任务线详情的**步骤行**统一标成「任务行」——左侧画**黄色感叹号**（照数据检索列表同一张 `questIcon`），文本补上 **LvNN**（新增数据层 `EVAL_QC_QUEST_LEVEL`，策展表→全量表依次找）；② 修红字 `bad argument #1 to 'getn' (table expected, got nil)`（`DD_FILTER` ← `EVAL_DD_OPEN` ← 等级处理段里 `local labels` 遮蔽外层 ⇒ 收到 nil）——根因已修，另在 `EVAL_DD_OPEN`/`DD_FILTER` 各加 **nil 守卫**（渲染层永不因调用方传 nil 糊屏）；③ 点放大镜改成**先送检索词再 `EVAL_HELP_CFG_SETTAB(4)`** 自动切到数据检索 tab（照抓宠帮手 `EVAL_PH_JUMP` 那条已验证范式）；④ **只检索当前可见装备 + 限频**：请求队列每次重绘**重建**（列表与详情各一处 `qpReqBegin()`，只装这一屏画出来的那几件，上一页遗留的排队直接丢），间隔 1.5 → **2.0 秒/件**，并把「本页已缓存/待请求/限频」摊开在 `/eh ds 任务线` 探针里（用户问「装备链接缓存是否生效」）——整页命中缓存时队列为 0；判据 = CHECK 新增 11 条 + 组 192 ⑧s（nil 守卫不报错 · 感叹号真画出来 · 步骤行带 Lv · 点放大镜切第 4 个 tab · 队列 ≤ 一页 · 限频 ≥2s · 整页命中缓存则零请求）+ 变异 **M50~M55 全捕获** ★**1.75.13~1.75.21（任务线检索的收口 + 省时闸门）**：**1.75.13/14** 任务线列表 **cap=0 全量**（写死 200 ⇒ 高等级线被截光）+ **跨块统一排序**（最低等级→稀有度）+ 系列记录单一来源 `qcSeriesRec`；**1.75.15** 策展链扩到 **30-60 经典线**（62 链）+ 策展↔自报系列去重（688→611）+ 等级**独立 60 档**（L5=50-59 / L6=60+，口径=区间重叠）；**1.75.16** 未缓存装备的**请求泵常驻开启**（泵挂弹窗子级 ⇒ OnUpdate 不触发 ⇒ 静静死掉，正是「当前页不再检索」的真因）+ 工具柄走 `EVAL_WTT_HANDLE()`、**取柄失败不消费队列**；**1.75.17/18** 种类按**部位**细分（戒指/项链/饰品站点全写「其它」）+ 等级筛选挪**最左**（任务线 F1 等级/F2 类型/F3 阵营）+ 任务线「类型」**共用装备那份种类多选** + 未扫出装备画**宏 961 占位图**（点击=插队优先扫描）；**1.75.19** 请求队列**按列排**（每行第一件先排）；**1.75.20** 队列上限从「入队即按行序截断」改到「**排序之后**截断」（`qpReqTrim`）—— 前者会让后面的行连排队资格都没有；**1.75.21 省时闸门**：新增 **`check.js`**（一条命令并行跑 luacheck + test_engine，末给闸门/结论/耗时表），`--quick` **6.5s**（跳过独占 22s 的组 192 任务线弹窗断言，输出**如实打 SKIPPED**）、`--full` 追加 `quest/audit.js`、`--selftest` 自验失败上报；`mutate.js` 改为**默认不跑全量**（M80/M81 新增、`--check` 1 秒锚点自检、**中断自动还原**——本轮实测被中断在 M72 曾把源码留在变异态）；另新增真机取证命令 **`/eh ds 任务线 种类`**（界面命中 vs 数据层命中一次摊开）。判据 = 组 192 ⑧aa/⑧bb（变异 M79/M80/M81 全捕获）。 |
| **1.74.30** | ⏱️ **射击计时**（猎人自动射击）+ 新条件「**距下次射击**」+ 消耗品助手左键不再弹窗 | 探针真机实测定案：锚点 = `CHAT_MSG_SPELL_SELF_DAMAGE` 里含「自动射击」的原文（**暴击词形也认**）、射速 = `UnitRangedDamage` 第 1 返回（实测 2.09s）；与近战**两套锚点互不干扰**，战斗UI 金色细条 / 状态UI 行 / 条件「距攻击」按「是否自动射击中」自动切换（文案分「下次射击 / 下次攻击」）；自校准只在**明显偏离**时才修（不把抖动当急速）；★新增数值条件「距下次射击」（文本 `距射击<0.5`，编辑窗初始值 **0**）；★顺带修「切换条件类型把 30.0 带进时间型」的继承 bug；★消耗品助手主图标**左键不再弹面板**（入口收归右键，左键留给拖动） |
| **1.74.29** | 🖼️ 战斗UI「**激活方案格加边框高亮**」+ 两个助手弹窗候选出**原生物品详情** + 喂食助手**快乐度边框** | 方案格挂 4 条 1px 边框（颜色 = 该方案品阶色，算不出退回暖金），换方案即时换格；选择面板候选格悬停先出**客户端原生物品 tooltip**（品质 / 类型 / 「使用：」效果 / 售价）再补帮手的行；喂食助手主图标边框按 `GetPetHappiness()` 现算：**开心=亮绿** · 一般=金 · 不开心=红 · 没宠物=默认金 |
| **1.74.28** | 🧺 两个助手的**弹窗候选加类型过滤**（用户：「弹窗选的物品项目要过滤一下.不要显示武器,装备.灰色物品,草药,矿物,任务物品,材料等等非可使用的物品,验证可行性.」） | ①**三级判定**（收在共用件 `EVAL_IG_ITEM_KIND`，两个助手同一份）：**品质**（`GetContainerItemInfo` 第 5 返回，**完全不依赖缓存**，灰色一律剔）→ **`GetItemInfo`**（★改传**物品链接**——客户端的缓存键，名字作兜底；读第 5/6/8 返回 = 主类型/子类型/**装备槽**，装备槽非空也算护甲）→ **自建隐形 tooltip 兜底**（`EVAL_HELP_WTT` + `EVAL_WTT_MAY_READ` 守卫 + `SetBagItem`；★`SetBagItem` 已核在官方 API 索引里；★★这一步会**把物品写进客户端缓存** ⇒ 下次 `GetItemInfo` 直接命中 = **自愈**，越用越快）；②**剔除表**：武器/护甲(装备)/灰色/**材料（草药·矿物·布料·皮革·附魔材料）**/任务物品/容器/箭矢弹药/钥匙/配方图纸 —— ★其中「白色草药矿物布料」正是旧实现（只看 `q==0` + 缓存主类型）**漏掉**的一档，也是用户看到的那批；③**判不出就不剔**（铁律「查不到 ≠ 没有」：宁可多显示一件，也不把真食物藏起来）+ 计入 `unknown` 统计如实报；④**过滤只在弹窗候选路径开**（`opts.classify`）——按名字解析包格那条路（`EVAL_CH_FIND`）**不过滤**，否则「判错一类 → 用户配置的物品静默找不到」；⑤**验证入口**：`/eh go 消耗品探针 过滤` ｜ `/eh go 喂食探针 过滤` —— 把每件物品的**判定依据**摊开（品质 / 缓存主类型·子类型·装备槽 / tooltip 读到的真实类型行 / 最终判定）+ 统计行，★同时是**补词表的依据**；⑥源码检查 `DECL ORDER` 当场抓到报告函数被插到 `IG_KIND_*` 声明**之前**（真机上会读到全局 nil）→ 已下移。判据 = 组 187（旧档 ①~⑥ + 新档 ⑦~⑩：白色材料/任务/容器/箭矢/钥匙/配方逐项剔、tooltip 兜底真的调 `SetBagItem`、**缓存自愈后不再探**、判不出不剔但记账） |
| **1.74.27** | 🧹 稀有提醒转播**提取到独立模块** `tools/RareWatch.lua` + **工具箱开关**（用户：「将以上功能提取到独立文件内 ./tools 然后再工具箱内设置开关.」） | ①整块（419 行）从 `EvalHelp.lua` 搬到 `tools/RareWatch.lua`，与 `tools/` 下其他助手同一形态；主文件只留**两个接线点**（`VARIABLES_LOADED` 里的 `pcall(EVAL_RW_INSTALL)` 与命令分支 `/eh go 稀有`）；②新模块只引用**全局桥**（`EVAL_SAY`/`EVAL_LOGLINE`/`EVAL_L`/`rawget(_G,"EVAL_HELP_CONFIG")`），绝不碰主程序文件内 local；③工具箱新增「稀有提醒」组 + 「稀有提醒转播」开关 —— ★读写走**实现自己的单一来源** `EVAL_RW_ENABLED`/`EVAL_RW_SET`（**不另存一份** tb.rareWatch，免得开关与实现两处真值打架；`/eh go 稀有 开|关` 与它同源），勾完顺手回执一行状态；④守卫同步扩到新文件：`RARE WATCH WIRING CHECK` 改成**分两段扫**（接线点看主文件 + 新模块，实现模式看两份的并集），另外 6 个源码检查的文件清单和测试载入清单都补上 `tools/RareWatch.lua`；⑤打包基准 68 → **69** 个文件（参考卷同步）。判据 = 组 120⑥（模型里有 `rw` 行 / 模型超一页 → 翻到第 2 页 / **真实 OnClick** 翻转单一真值 / 回执一行 / 双击复原）+ 组 199 全绿 |
| **1.74.26** | 🧹 点名字**只做一件事**：调目标函数选中它；选不中**就报错**（用户定案：「点击名字逻辑很简单.不要和那边插件的机制管理.需求只要是调用类似目标函数选中目标就可以了.如果目标不存在就报错.」） | 【删】按用户定案把所有与任务插件**机制**的耦合全部删掉：不再有坐标 / 最近刷新点 / 地图投影 `MapContext:GetCurrentZoneView` / `EVAL_DS_SHOWMAP` 开图 / 右键地图定位 / `稀有 定位` 命令 / 距离文案；链接 token 从 `EHRW:areaId:x:y:unitId` **收成 `EHRW:<名字>`**（名字是唯一需要的东西），代码里因此少了一整套坐标换算与退路分支。【留】点名字 = **一次 `TargetByName(名字)`**：选中报「已选中目标：X」，选不中报「选不中目标：X」——**就这一个动作**；唯一保留的诚实点是**必须核对结果**（调用后读一次 `UnitName("target")`，否则「点了没反应」会被报成成功——`TargetByName` 在远处是**静默失败**）。【守】源码检查 `RARE WATCH WIRING CHECK` 现在**反向守**：稀有转播里一旦再出现 `EVAL_DS_SHOWMAP`/`EVAL_RW_NEAREST_LOC`/`GetCurrentZoneView` 即 FAIL；组 199⑪⑫ 也有反向哨兵（链接里不许出现 `EHRW:14` 这类坐标、失败文案不许夹带「码」）。判据 = 组 199⑪~⑯ |
| **1.74.25** | 📏 切目标**文案纠错**：把「客户端限制」说清 + 报出距离 + 加一条一次定案的**目标探针**（用户实测：「目标存在的情况下提示说不在，无法选中目标」+ 截图里导航显示**556 码**） | 【实锤】用户截图：任务插件导航箭头显示那只稀有在 **556 码**外，而聊天框里我们写的是「它没刷 / 它不在这儿」—— **真因是客户端限制**：按名字选目标（`TargetByName`）**只认客户端附近的单位**（官方文档明文；本项目 `Engine.lua:735`、参考卷 F3 早已记过「远处会**静默失败**」）⇒ 文案犯了「把自己的限制说成怪物状态」的错，玩家会以为插件在胡说。②**改文案**：`选不到「X」：按名字选目标只认客户端**附近**的单位（它现在约 556 码）—— 走近了再点一次，或右键点名字在地图上定位`（一句话说完，上一版被聊天框折成两行 —— 截图可见）；**距离取的是**转播时对方给的那个码数，把「为什么选不到」直接摆在脸上；③**新增取证命令** `/eh go 稀有 目标探针`：拿**当前目标自己的名字**（必然存在且必然在附近）做实验 —— 清空目标 → 按名字选回来 → 报判定，**这一步是唯一能区分**「客户端只认附近」与「TargetByName 对本插件是空操作」的方法（两者从结果上长得一模一样）；同时打印库里那份名字与**字节数**，排掉「库名 ≠ 客户端名」这第三种可能；④探针两条判定都写死结论（接口正常→之前是距离问题 / 连眼前目标都选不回来→要换实现，请把那一行发我）。判据 = 组 199⑫（含**反向哨兵**：文案里绝不许再出现「它没刷」）+ ⑯（探针走真实命令入口且真的用当前目标的名字验） |
| **1.74.24** | 🎨 稀有提醒**按品阶染色 + 名字可点**：左键 = 切目标到它，右键 = 地图定位它的刷新点（用户：「对稀有精英染色」「点击他可以定位目标」「点击头像相当于目标切换到稀有精英目标」） | ①名字按**品阶染色**：精英 银 c0c0c0 · 稀有 蓝 0070dd · 稀有精英 紫 a335ee · 首领 橙 ff8000（物品品质那套玩家一眼就懂的梯度；与「方案品阶」五色是**两个维度**，本项目明文纪律要求错开，故最低档用银而非白）；★整条消息**只许一段 8 位色码**（1.73.43 实测：一条多段会整条不画）——为此把色码从语言包里的写死金移到调用方按品阶给；②名字做成**可点链接**（`EHRW:areaId:x:y:unitId`，形态照抄分享封皮那条真机配方：色码在外、链接在里）→ **左键 = `TargetByName(名字)` 把目标切到那只稀有**（名字由链接里的 unitId 现查，不靠「最后一只」），**右键 = 复用数据检索那条已跑通的定位链路 `EVAL_DS_SHOWMAP`** 开图并标出它的**最近记录刷新点**（刷新点用 `MapContext:GetCurrentZoneView` 的投影现算，与对方算距离的是同一个点；拿不到投影就退回第一点并如实记日志）；③`TargetByName` 先核过官方索引（category=Targetting）——但「附近没有这只」时它的确切行为本机无文档，**不猜**：调用前后各读一次 `UnitName("target")` 做**前后比对**再如实报（切到了 / 附近没有 / 目标被清空 / 换成了别的 / 接口不存在）；④链接形态是唯一有「整条不画」风险的改动 → 留了 `稀有 链接` 开关，一键退回**已验证的「只有色码」形态**；⑤命令 `/eh go 稀有 目标|定位|链接`；判据 = 组 199（⑩染色/⑪最近点/⑫左键切目标含反向哨兵/⑬右键开图/⑭命令/⑮链接开关）+ 源码检查 `RARE WATCH WIRING CHECK` 扩到「4 色逐项 8 位 · 链接形态 · SetItemRef 分派」 |
| **1.74.23** | 🔔 **稀有提醒转播**：任务插件发现稀有那一刻 → 聊天框一行（用户：「需求是只要知道他发现稀有的那一时刻.在对话框内输出一段文字」） | 任务插件 UnrealQuest 的稀有卡片**出现在屏幕上那一刻**，聊天框立刻报一行：名字 + 品阶（精英/稀有精英/首领/稀有）+ 距最近刷新点码数 + 同刻还有几只。做法 = **包住它的弹窗唯一出口** `RareAlert:Show`（模块注册表是公开的：全局 UnrealQuest + GetModule("RareAlert")；★安装必须等 VARIABLES_LOADED —— 目录名排序 EvalHelp(E) 先于 UnrealQuest(U)，载入期拿不到对方）→ **原函数返回值逐个透传、原函数抛错照原样抛出**（绝不吞别人的错，只在抛错时如实计数）；对方换实现（Show 不再是函数）时自动退化为**轮询它的公开读值口** GetStatus().alerts（首读只立基准，涨了才转播）；开关与状态：/eh go 稀有（状态 / 开 / 关 / 试），试 直接调对方的测试弹窗走完整链路自证。判据 = 组 199 + 三语言 19 个键 |
| **1.74.22** | 🧩 案例模版收录「神圣风暴」（骑士 · Rainbow 分享）+ 🎭 模版名称按**艾泽拉斯体系 × 自身品阶**重命名 + 📝 模版支持**作者/备注** | ①把用户配置里的「神圣风暴」（13 技能惩戒骑）收进骑士模版组：文本由插件**自己的导出器**逐字生成（读存档里那份方案表）并过 `export(parse(text)) == text` 往返校验，不是手抄；②**34 条模版全部改名 + 说明重写**，按各条**自身品阶**取艾泽拉斯名号（普通=凡人世界「旅店歇脚 / 急救药水」· 稀有=精锐「肯瑞托法师 / 无冕刺客」· 珍稀=英雄「游侠领主 / 翡翠追猎」· 绝版=传说「地狱咆哮 / 白银之手」；神级的「神圣风暴」原名已是法术名 → **保留原名**）；③模版条目新增可选 `author`/`note` 字段 —— 悬停显示「作者：Rainbow」「备注：来自龙之国度的LM-Rainbow-骑士无私分享」，导入时按该署名记方案来源（没带 author 的模版照旧记「技能学院」，行为不变）；④**神级档配色**由深红 `c00000` 改为**亮蓝 `00bfff`**（`SH_SEAL_TIERS` 唯一来源）。 |
| **1.74.21** | 🎯 神级门槛 **120 → 50**（用户：「方案评分 调整以50为神级」） | 四个低档把 0~49 分四等分：**≤11 普通 / 12-23 稀有 / 24-35 珍稀 / 36-49 绝版 / ≥50 神级**；★覆盖封顶与制定层全部**从档位表现算**，所以这次只改了四个数字：封顶自动变成 23/35/49、神级门槛自动变 50、头衔/神级示例行与礼包样例自动跟着走（旧写法要改 6 处，现在一处）—— ★这正是 1.74.19 把阈值收成单一来源的回报 |
| **1.74.20** | 👤 **配置分角色存**：消耗品助手 + 喂食助手 + 骑乘助手整块按角色（用户：「消耗品助手 + 喂食助手 + 骑乘助手整块按角色（含各自悬浮件位置）。配置按照角色存储」） | 新增**角色级存档** EVAL_HELP_CHAR（## SavedVariablesPerCharacter，本客户端实测支持 —— UnrealQuest 已在用、磁盘上三角色三文件）；三个助手**整块**（开关 / 选中物品与食物 / 贴图缓存 / 悬浮件位置，共 15 个键）搬到角色表，tbCfg() 改成**路由代理**（角色键落角色表、其余落账号表）⇒ 上百处既有调用点一行未改；老存档里那些键**一次性继承**到每个角色（账号里那份副本保留，作为还没登录过的角色的种子），迁移挂在 VARIABLES_LOADED（早调会从空表继承出空配置）；方案库 / 语言 / 日志 / 分享开关**仍是账号级**。★过程中修掉一个自己写出的 Lua 经典坑：路由的 __index 写成 at and at[k] or nil 会把所有值为 false 的开关**吞成 nil**（= 用户关掉的开关自己又开了），组 79 当场抓到 |
| **1.74.19** | 🏅 **方案评级与头衔晋升重做**（用户：「审计方案的评级标准.审计玩家头衔的评级标准.增加评级难度」） | 旧口径三个漏洞：**空技能白送 1 分**（25 条空「攻击」= 25 分 = 神级，实测复现；真机存档里「合欢宗宝典」就是 18 条技能里 **13 条是空的**、靠它从珍稀被抬到神级）· 条件不分轻重（「战斗中」与「队友血量<30＆目标debuff」同价）· 头衔 2~5 档全是「≥1 个」→ 满档只要 1 个方案。新口径：**空技能 0 分**（不占动作条的特殊技能保留 1 分）· 条件按类加权（自身/技能 1 · 目标/光环 2 · 队伍/候选 3）· 单条上限 12 分 · 完全重复的条件只算一次 · **覆盖封顶**（覆盖 1/2/3 个大类分别封顶 34/69/119，**≥4 类才不封顶** ⇒ 神级必须跨类写）· 档位改 ≤14/15-34/35-69/70-119/**≥120 神级** · 头衔门槛 {1,2,3,3,2} + **逐级满足**。★新增的 COND WEIGHT CHECK 上线当场抓到隐藏条件类型 target 没进任何分组（会被按 0 分算、也不计覆盖） |
| **1.74.18** | ⇕️ 战斗信息UI 技能图标带**自动换行 + 自适应高度**（用户截图：「战斗UI 每行8个.超过2行的时候更多的就不显示了.需求是需要能自动换行.自适应高度换行图标」） | 旧实现是**写死的 2 行 16 格** + 帧高按两行预留 → 第 17 条技能起**连格子都没有**、静默消失；现改成按当前激活方案的技能数**现算行数**：每行 8 格、池子补齐到整行、帧高 = 行数 × 行距（下面的内容跟着挪）；技能数一变（配置窗增删 / 左键切方案 / 导入分享）由 **tick 一个判据**自动重建（变多变少都跟着走）；★行数公式抽成**文件级纯函数** uiCellRows，BUILD 与 tick 共用一份 —— 两处各写一份只要不等就会变成「每 0.15s 重建一次」的无限循环（变异测试当场撞到） |
| **1.74.17** | ⇕️ 技能列表滚动条改**图标库范式**：上一页/下一页搬到**底部 [案例模版] 左侧、右对齐**（用户：「查看系统记忆和图标库的滚动条实现方式，将技能列表的滚动条方式调整下。在有滚动条的时候上一页和下一页按钮放置在底部案例模版按钮左侧、右对齐」） | 撤掉列表右栏那两个 ▲/▼（三角**只留给行的调序**，两套按钮不再长得一样）；改成底部导航行上的两个**文字按钮** [上翻][下翻]（走 TB_UP/TB_DN 语言包，三语言齐），整组右缘停在 **[案例模版] 左边**、与它**同顶边同高**（同高同顶边 ⇒ 中线必然对齐，不存在错位空间）；**只在有滚动条时出现**（技能数 > 可见行数），没有滚动条时按钮与文字一起隐藏；步进从「一行」改成**一整页**（= 可见行数，与工具箱/图标库同语义；滚轮仍 ±1 行细调） |
| **1.74.16** | 🚫 取消主图标「使用第 1 个消耗品」的功能（用户：「取消第一个图标识使用第一个配置的消耗品的功能」） | 主图标现在是**帮手自己的特殊图标**（不代表任何物品），再让它「用第 1 个」既别扭又容易误点 → 物品使用**一律走横排各枚**（各自点用）；主图标**左右键都只负责开/关选择面板**（空选中时也能开——那正是最需要入口的时候）；主图标同时**不再显示 CD**（每件物品的 CD 只由它自己那一枚显示）；★旧规矩「左键不触发弹窗选择」是「左键=用物品」时代定的，随该功能一起取消 |
| **1.74.15** | ⏳ 消耗品 CD 改成**每件物品各自**的（用户：「消耗品助手 的CD 是每个物品自身的CD 而不是全局的CD」） | 旧实现是**一个全局** `cdUntil` → 用了一瓶药水就让**所有**消耗品一起变灰、显示同一个倒计时（错）；现在 `cd[物品名]` 各记各的：**只有用掉的那件进 CD**，别件照用；横排**每枚各按自己的物品**显示遮盖与倒计时（主图标按左键用的那件）；CD 判据也从全局改成按物品名（A 在 CD 时 B 仍能用）；判据组 193 |
| **1.74.14** | 🧩 新增**通用**模版组（12 组 34 条）+ ✂️ **推翻 1.74.12 的拆分使用** + 🎯 消耗品图标与数量 | ①**通用模版**：新增 `examples/general.lua` 一组 5 条**不依赖职业**的动作积木（脱战吃喝 / 危急喝药 / 一键下马 / 顶掉保护祝福 / 一键停手），首次把 `物品:名称` 与 `取消自身buff:名称` 做成现成模版；②**★推翻 1.74.12 的拆分使用**（用户：「先分析技能编辑 物品使用的逻辑.那边工作是正常的」）—— 实测**技能编辑那条路正常、消耗品助手吃整组**，对齐后（背包定位 + 单次 `UseContainerItem`，不做任何拆分/光标操作）**用户实测确认「使用个数功能正常了」**；③消耗品图标：主图标改成**特殊图标**（插件自带 `media/icons/chests`，不再是第 1 个物品的图标），选中项**全部**横排在右侧（选 1 个 = 1 枚，>1 才往右延伸）；④**用后延迟 0.5s 刷新数量**（客户端 `GetContainerItemInfo` 不是立刻更新 → 原来「数量和实际对不上」），刷完自动摘掉 OnUpdate |
| **1.74.13** | 🔔 任务通知频道**多选**（队伍>说>自己 · 每次进度仅播报一次） | 工具箱→任务通知的下拉改成多选：关闭/仅自己/说/队伍；多选时按优先级 队伍>说>仅自己 **只播一个频道**，每条进度**仅播报一次**；队伍不在队时在已勾选集合内顺延（说→仅自己），只勾队伍却不在队**如实跳过**；旧单值配置自动迁移为集合（关闭 = 清除） |
| **1.74.12** | ✂️ 消耗品**拆分使用**（修「一次用完一整组」）+ 🔍 探针命令 | 用户实测确认「使用了一组.一组没了」⇒ `UseContainerItem` 对堆叠物品**一次用一整组**；改成用之前 `SplitContainerItem` 拆 1 个 → `PutItemInBag`(别的背包，放回原包会合并) 放下 → 对那 1 个用；★拆不动/放不下就**如实拒绝**（绝不退回「用整组」）；★放不下时客户端不报错 → 靠**查光标**才发现（报错原因才准）；新增 `/eh go 消耗品探针 [物品名]`；判据组 191 + 变异 M622~M626 全捕获 |
| **1.74.11** | ⏱️ 喂食间隔放宽到 2s（GCD 1.5s 之上留余量）+ 🧹 背包候选 类型过滤 | ①喂食限频 1s → **2s**（公共 CD 1.5s 之上再留余量，不会在 GCD 没转好时就放行下一笔）；队列单元**取走即删**（`table.remove(HH.q, 1)`，不会重复执行同一笔）；②喂食助手/消耗品助手的候选**剔掉非可食用**：武器 / 护甲（装备）/ 任务道具 / 灰色品质（矿石草药杂物）；**拿不到类型不剔**（查不到 ≠ 不是食物，宁可留着）；判据组 186（改 2s）+ 组 187 + 变异 M607~M613 全捕获 |
| **1.74.10** | ✨ 取消自身buff 支持**多选 buff** + 🐛 修自身debuff 时长/类型重叠 + ✏️ 剩余→时长 | ①取消自身buff 的技能格改成**多选面板**：实时扫描自身 buff + 已记录 + 自定义输入，默认「不限」= 取消全部（存 `取消自身buff:名1,名2`，集合/文本往返单一来源 `EVAL_CANCELBUFF_FROM/TO_TEXT`）；②自身debuff 检查行「任意负面」与「剩余时长」不再重叠（dtBtn 渲染期挪到行内 484，队伍/团员行照旧 348）；③「剩余不限」→「时长不限」，「剩余」二字全部改成「时长」；判据组 185 + 变异 M603~M606 全捕获 |
| **1.74.9** | 🐛 修「角色技能下拉里出现一个空技能」（用户截图） | 动作条扫描里 Lua 的 `""` 是**真值**，旧判据 `if name` 会放过空名/纯空白名 → `wslots[""]` 成立、下拉第一项就是那个空行（1.74.8 起「全部非宏格子」全列出来才暴露）；现按「**查不到 ≠ 没有**」剔除，并**如实报出是哪个槽位**（用户排查那个没名字的格子时唯一线索）；判据组 184 + 变异 M599~M602 全捕获 |
| **1.74.8** | ✂️ 新增技能级行为「取消自身buff」 | 方案 → 技能里写 `取消自身buff`（取消**全部**可取消增益）或 `取消自身buff:光环名`（只取消那一个）· 与「取消施法 / 停止攻击 / 跟随」同族的**不占动作条**特殊技能（`skillNoSlotOk` 单一判据）· 恒就绪 · 图标借**该光环自己的纹理** · 走 `CancelPlayerBuff(内部索引)`（非 Protected；★内部索引 ≠ 槽位号）· **没有该光环 → 跳过并如实点名**，绝不瞎取消别的光环 · 技能选单里可直接选（含「指定名字…」弹输入框）· 判据组 183 + 变异 M590~M598 全捕获（★其中 M591 揭穿一条**自欺断言**：技能名里就含光环名，只查「日志有没有这个名字」在变异下照样绿 → 改成只查「跳过:」之后的消息体）|
| **1.74.7** | 🐎 工具箱新增「骑乘助手 · 一键下马」（`tools/DismountHelper.lua`） | 本客户端**没有** `Dismount`/`IsMounted`/`Mount` 任何 API ⇒ 按官方文档走 **`GetPlayerBuff(i,"HELPFUL\|CANCELABLE")` 过滤器 + tooltip 识别坐骑**（速度提高X%/增加移动速度/Mount/坐骑/骑乘），再 **`CancelPlayerBuff(内部索引)`** 取消 —— ★传的是**内部索引**而非槽位号（传错会取消错光环）· 悬浮图标**左键 = 一键下马**、**右键 = 下拉菜单**（下马 / 自动下马开关 / 位置复位）、可拖动并记忆位置 · 可选**自动下马**（被「你正在骑乘/无法在…」拒绝时顺手下马，默认关）· **查不到 ≠ 没有**：tooltip 读不出来时如实报 `UNREADABLE`，绝不瞎取消 · 载入期零副作用（开关打开才建帧）· 命令 `/eh go 下马` · `/eh go 下马 状态`（探针）· 判据组 182 + 变异 M578~M581 全捕获 |
| **1.74.6** | ☠️ **自身/目标 debuff 支持「负面类型」** | 与队伍/候选者 debuff **同一套**：解析 `名(类型)` · 求值按类型过滤（**名字对上但类型不符 = 没有**）· **名称留空 + 类型 = 「有任意该类型」** · 导出 token / 显示本地化名同一份；顺手修 `candDebuff` 漏掉的类型后缀 |
| **1.74.5** | 🎬 分享「场景描述」+ 🧰 两个工具助手（一键喂食 / 消耗品） | [导入]/[忽略] × 有/无目标 × 三语言 **40 条场景描述**（头衔按档位染色、整条只许一段色码）· 方案「作者/来源」四类 + 方案列表 tooltip · 「不弹窗」**8 条分叉全落日志** · 彩蛋反应**不再接线**（机制与文案保留）· 猎人「一键喂食」/「消耗品助手」共用 8×N 图标网格（未实测的标「待测试」黄感叹号） |
| **1.74.4** | 🐞 修「对方已入队却仍显示邀请队伍」：邀请队伍加「对方不在我队伍里」条件 | 1.74.1 的条件只看「我」的状态 → 你是队长、对方又已入队时「邀请队伍」和「踢出队伍」同屏出现（自相矛盾）；现改为「对方不在我队伍/团队里 且 （不在队伍 或 我是队长）」 |
| **1.74.3** | 📥 修「**队伍频道收到分享不弹窗**」（用户报）：接收帧补注册**队长版事件** | 【根因】1.12 里**队长/团长**发的队伍/团队消息走独立事件 `CHAT_MSG_PARTY_LEADER` / `CHAT_MSG_RAID_LEADER`，而接收帧原来只注册 `CHAT_MSG_PARTY`/`CHAT_MSG_RAID` ⇒ **发送者是队长时一片分片都进不来**：接收端静默、发送端照样报「已发送 N 片」；★★频道名函数**早就为这两个事件写了分支**、**只是注册漏了**（接线漏接不报错那一族）→ 补注册 + 新增取证命令 `/eh go 分享事件`（接收开关 / 七个事件逐个 `IsEventRegistered` / **最近真收到的事件名**）。判据 = 组 175（七事件逐个注册 + 逐个喂真分片都要弹窗并标对来源）+ 源码检查 `SHARE RECV EVENTS CHECK`；变异 M571/M572 全捕获 |
| **1.74.2** | 🖱 战斗信息UI 技能格：**左键 = 快速切换启用/停用 · 右键 = 技能配置弹窗**（用户定） | 原来任何点击都开编辑窗 → 左键就地切 `enabled` **配置真值**（如实播报 + 双刷新：`EVAL_WAR_TAB_REFRESH` 让配置窗勾选同步、`EVAL_HELP_UI_TICK` 格子立刻重画「停」/亮起），右键才开**被点那一格**的配置弹窗（不顺手切换）；tooltip 操作提示三语言同步。判据组 174（全走真实 OnClick：左键写真值可反复切 · 右键开对技能且不动状态）+ 源码检查 `UI CELL MOUSE CHECK`（注册右键 / 分派 / 写真值 / 双刷新）；变异 M568~M570 全捕获 |
| **1.73.41**（分支 `probe/share-hide-link`） | 🧪 调研探针：**自定义链接能不能过服务器** + **聊天消息真实长度上限** | 用户：「新开一个分支测试以上探针」——只做**取证**、**不动分享协议**：① **链接转发探针**：一次发 4 种形态（自定义链接 `\|HEHPF:…\|h[方案:探针 …]\|h` / 假物品链接 / 假玩家链接 / 现状明文），收到后**逐字节比对**，如实判「一致 / 被改（打印首个不同处 + `\|c`·`\|H` 还在不在）/ 未收到」；② **长度阶梯探针**：200/230/250/270 字节四条，每条尾带哨兵 `E<n>E`，报告「目标 → 收到多少字节 + 哨兵在不在 + 完整/截断」；③ ★探针消息**只进探针、绝不进正常接收缓冲**（不污染导入流程）；命令 `/eh go 链接探针`、`/eh go 长度探针`、`/eh go 探针结果`、`/eh go 探针关`。判据 = 组 141（完美回声全「一致」/ 被剥标记必判「被改」/ 截断那档必判「截断」/ 不污染缓冲与完成表 / 未收到如实报）；变异 M312~M317 全捕获；★命令形如 `/eh go 链接探针` / `go 探针全跑`（接在 `go` 组上）—— 接线由组 141⑩ 走**真实命令入口**守（真事故：前缀写错 → 敲了**静默无反应**）。★★**实测结果（2026-09-19 落盘）**：① 自定义链接 **逐字节一致（88B 进、88B 出，`\|c`/`\|H` 都在）** → 隐藏载荷**可行**；② 长度：**250B 通过、270B 整条丢失**（不是截断）→ 总长按 **≤ 250B** 设计；③ v2 换算：hex **180 字符/片** 单条 ≈ 236B，每行只显示 `[方案:名 i/n]`（**视觉 −92%**）。 |
| **1.73.40** | 🟦 右键菜单**圆角** + 内边距/透明底 + 悬停**文字**变白 · 🐞 修「**关闭按键不在框里**」（用户截图）· 🧩 新案例模版 **猎人收宠** · 📖 [使用引导] 打开**载入引导窗** | ① **圆角**：改用客户端**原生圆角边贴图**（`SetBackdrop` + `edgeFile = Interface\Tooltips\UI-Tooltip-Border`，`edgeSize 16`（**整数**）、`insets 4` 让底缩进圆角内侧）、`SetBackdropBorderColor(1,1,1,0.75)` 保持**白色高亮**；★**读回确认**（`GetBackdrop()` 的边框段）+ **挂不上就退回四条平边**（两条路都有断言）；② **悬停变色**（用户「鼠标获取焦点 增加变色美化」）：每条按钮挂**真实 `OnEnter`/`OnLeave`**（本项目禁用 `SetHighlightTexture`），**只把文字加白**（金 → 纯白）、**背景一点都不动**，离开**逐值复原**；③ **「关闭」不在框里**的根因：高度公式只算 `(rows-1)` 个**行距**、**末行自身高度没算** → 末行吊在窗口底 **9px 之外**（关闭正好在末行）→ 改成 `rows * 行高`，并补**独立于公式**的**包含性**判据（每条按钮矩形必须落在窗口矩形内）；④ **案例模版**：用户要求「将当前方案添加到案例模版：猎人分组 → 停止攻击&收回宠物」→ 猎人组补 **猎人收宠**（4 条**无条件**行：停止攻击 / 取消施法 / 宠物:跟随 / 宠物:被动姿态），⑤ **内边距 + 透明底 + 外框加深**（用户「框增加 padding…背景透明，外框背景加深」）：左右/上/下**都有 padding**（宽高/标题/条目位置全由 `TB_MENU_PAD` 算）· 条目按钮底**完全透明**（alpha 0）· 面板底**加深**到 0.88；⑥ **[使用引导] = 打开载入时那个引导窗**（用户：「使用引导显示弹窗的插件载入的那个引导窗」）：配置窗按钮 / `/eh guide` / 载入提示**三条路同一个窗**（`EVAL_HELP_LOADPOP`，同一份正文、同一个 `cfg.loadMsgSeen`），且手动重看**无视该开关**（点过「知道了」仍看得到）；弹窗不可用才退回聊天打印（降级也说出来）；模版总数 → **29 条**（★顺手修掉一处**长期少写**：1.73.25 加猫德那条时没同步计数，文档一直写 27 而实际已 28，本轮新增后为 29；toc Notes / 三语言 README 已全部同步，并落成源码检查 TEMPLATE COUNT CHECK） |
| **1.73.39** | 🖼 右键菜单：**白色高亮边框** + **分享方案 ↔ 邀请 对调位置**（用户截图指出） | ① 四条 1px **白边**（`tbSolid(1,1,1,0.75)`，BORDER 层，横竖都跨满整个窗口）——半透明白，抬亮外框但不抢文字；② 条目顺序按用户要求对调：第 2 条 = **分享方案**、第 5 条 = **邀请**（其余顺序不变：悄悄话 / 分享方案 / 目标 / 复制名字 / 邀请 / 交易 / 查询 / 踢出队伍 / 邀请公会 / 踢出公会 / 关闭）。判据组 130⑥b（读真实纹理的**顶点色**与尺寸）+ 变异 M271（退回金色）/ M272（没对调）/ M273（缺一条边）全捕获 |
| **1.73.38** | 💬 分享方案的**私聊接收**核对 + 🎨 右键开关 tooltip 列全功能并美化 | ① 「接收方要支持私聊」：核对结论 = **本来就支持**（分享事件帧早注册了 `CHAT_MSG_WHISPER`、接收侧不看来源事件），本轮把它**锁进判据**（注册检查 + 用**真实接收路径** `EVAL_SHARE_ONMSG` 喂一个密语分片 → 断言弹窗点名发送者、来源标「密语」、方案内容解开）；★提醒：对方要**开着「接收方案」**才收（默认关，工具箱里勾）；② 右键菜单开关的 tooltip 重写（三语）：金色标题 + **逐条列出全部 10 个功能**（悄悄话/邀请/目标/复制名字/分享方案/交易/查询/踢出队伍/邀请公会/踢出公会）+ 灰色注意行（复制名字不代发、左键与其它链接不受影响），不再是旧的五条一行糊。判据组 140 |
| **1.73.37** | 🐞 修「右键菜单某些按键点了没反应」（用户报：关闭/悄悄话/目标/查询/邀请/复制名字） | 【根因两个】**① 名字被建菜单时捕获成 nil**：`EVAL_TB_MENU_SHOW` 原来先 `tbMenuBuild()` 再写 `TB_NAME_MENU.name`，而条目闭包在**建菜单那一刻**就把名字快照下来了（首次右键时名字还是 nil）→ 所有动作拿到 nil、静默 `return false`（点了像没反应）；**②「关闭」传的是空函数** → 点了不关。修法：名字**先写再建** + 闭包改成**点击时**才读 `TB_NAME_MENU.name` + 关闭真的调 `EVAL_TB_MENU_HIDE`。★判据组 139 全部走**真实 OnClick**（按标签点按钮），不再直接调动作函数 —— 上一轮就是这么漏掉的（直接调函数全绿、按钮接线断了也不知道）；★★变异 M267/M269 **单拆都 SURVIVED（等价）**，**整组拆 M270 才 CAPTURED**（第二次印证「冗余守卫必须整组拆」）|
| **1.73.36** | 🖱 右键菜单锚点修正：**相对鼠标点击位置的右上角**（用户澄清） | 1.73.35 我把它固定在「屏幕右上角」，用户要的是**跟着点击的位置**——现在弹窗**左下角贴光标**、向上向右展开；拿不到光标退回屏幕右下角，贴到屏幕边时**夹取**（不跑出屏幕）；坐标按 UIParent 缩放折算（`GetCursorPosition` 给的是物理像素，高缩放不折算会飘）。判据组 130⑥ 改成读**真实光标位置**（桩补 `GetCursorPosition`），组 138 全绿 |
| **1.73.35** | 🖱 右键菜单扩展（两列 · 宽高自适应 · 锚点右上）+ 📋 载入信息独立弹窗 | ① **聊天名字右键开关**（工具箱 → 队伍/社交 →「聊天名字右键菜单」，1.73.24 起就在）；② 菜单新增 **分享方案（默认当前激活方案，密语给对方）· 交易 · 踢出公会（CanGuildRemove() 才显示）· 邀请公会（CanGuildInvite() 才显示）· 查询（玩家信息）· 踢出队伍（在队伍里才显示）**；**操作分两列**、**宽高由条目数算出来**（单一来源 EVAL_TB_MENU_LAYOUT）、**锚点固定在屏幕右上角**；★名字→unit 解析只扫「本机看得到」的单位，解析不到就**如实退回**；★「查询」走**限频 who 队列**（源码检查当场抓到直调 SendWho 是热路径）；③ **插件载入信息移到独立弹窗**（原来往聊天打 7 行）：[知道了]（tooltip 说明，写 cfg.loadMsgSeen，下次不弹）/ [关闭]（每次都弹），与帮助/引导**共用同一个开关**；弹窗可拖（Button 柄 + 全套注册）、层级 230（高于输入弹窗 220、低于下拉 250）。判据组 138 |
| **1.73.34** | 🖱 审计自动购买弹窗的**滚动条机制**（用户要求） | 对照项目规范查出 **6 处不符**并全修：① **完全没有滚轮**（配置窗内其余 6 处列表都有）→ 补 `EnableMouseWheel` + `OnMouseWheel`（方向走单一来源 `EVAL_WHEEL_DIR`，上滚 = 回到前面，`dir == 0` **链式交还原脚本**，`off` 双侧夹取）；② ▲▼ 箭头画在 `x=W-20`，**压在「删除」按钮上**（删除列 316..348 vs 箭头 340..354）→ 窗口加宽到 384、右侧留 **30px 专用滚动槽**（删除列右边缘 = W-30），箭头进槽；③ 页码指示锚在 `y=-46`，**压在第一行数据上** → 挪到底栏；④ 页码是**浮点除法**（`off / ROWS + 1`）→ 改 `math.floor` 并**到底取最后一页**；⑤ 箭头按「行」滚、指示按「页」算（二者对不上）→ 箭头改为**按页**滚；⑥ 无边界反馈 → 到顶隐藏 ▲、到底隐藏 ▼。判据组 137 + 变异 M261~M266 全捕获（★**M263「浮点除法」为等价变异**：真机 Lua 5.1 的 `%d` 会把 1.25 截断成 1，只有 fengari 报错 → 保留 floor 作防御性修复，可判的是 M263b「到底 = 最后一页」；★M265 先存活：判据只跟第一行比 y，改成跟**末行**比才抓住「指示压在第 2~8 行」） |
| **1.73.33** | 🎯 自动购买弹窗三处修复（用户：「还是不能被拖拽；标题右侧的说明移到安全说明下面对齐；红线位置布局异常」） | ① **列头布局异常**（红线那两处）——【根因】列头把「距左边」的数值当「距右边」的偏移用（`head(244, …, true)` → 右边缘跑到 x≈116），两个列头**叠在一起**（截图里那串「启用每次称 总数」就是叠着画出来的）、与数据列也对不上 → 引入**列几何单一来源** `TB_BUY_COLS`（全用「距左边」表示，右对齐列写右边缘），列头与单元格都从它取，且**列头宽度 = 本列宽度**（盒子不重叠、文字不串列）；② **不能拖拽**——【根因】这个弹窗**根本没有拖动柄**（1.73.31 只补了层级/鼠标，柄是 1.73.32 给「通用输入弹窗」加的，它漏了）→ 照规程补：Button 柄 + `SetMovable` + `RegisterForClicks(LeftButtonUp)` + `RegisterForDrag(LeftButton)` + level 201（窗口 200）+ warm-up 两步；③ 标题右侧的说明行 → 挪到**安全说明正下方**并与之**左对齐**（都贴 12；两条都限宽 W-24）。判据组 136 + 变异 M256~M260 全捕获 |
| **1.73.32** | 🔍 通用输入弹窗（`EVAL_TN`）**规程审计 + 修复**（用户：「审计下…是否符合项目弹窗规程；要能拖拽（现在无法拖拽）·加宽尺寸 · 内部布局乱 · 添加物品弹窗被遮盖」） | 【审计结论 4 条】① **层级 130 < 工具箱弹窗 200** → 从「自动购买设置」打开它就被**父弹窗压住**，点击与拖动全被吃掉（= 用户看到的「被遮盖 / 无法拖拽」，一个根因两个症状）→ 改成 **220**（高于工具箱各弹窗、低于全局下拉 250）；② 宽度固定 300、输入框固定 220 → 窗口 **400**、输入框跟着宽度走；③ 两个按钮 x **写死 70/160** → 加宽后不居中，改成**按窗口宽度居中**（这才是用户说的「内部布局乱」）；④ 输入框上方标签**写死「目标名称：」**（那是「指定目标」用法的文案，物品名/数量/每次都跟着错）→ 改为 `EVAL_TN_OPEN(title, cur, cb, label)` 由调用方给，新增 `TN_LABEL / TN_LABEL_NAME / TN_LABEL_N / TN_LABEL_PER` 三语键。判据组 135（层级 200~250 · 柄 level > 窗口 · SetMovable + RegisterForDrag(LeftButton) + RegisterForClicks(LeftButtonUp) · 宽 ≥380 且输入框跟随 · 按钮组**居中**且不出界 · 标签随调用方）+ 变异 M251~M255 全捕获（★M252 锚点说明：同一行文本全文件 12 处 → 改用**位置锚点**（先找独有的 `tnUI.titleBar`，往前找最近那行）） |
| **1.73.31** | 🖱 修「自动购买弹窗**点击不到**」（用户报） | 【根因】那个弹窗只设了 strata（DIALOG）、**没设 frame level** —— 同一个 DIALOG 层里按 frame level 排先后，工具箱的行按钮是配置窗的子帧（level 更高）→ 它们**画在弹窗之上**，鼠标也被它们吃掉（用户截图里能看到行文字/按钮压在弹窗上）。【修法】① 明确抬到 **200**（高于配置窗各弹窗 100~140、**低于**全局下拉 250）；② **EnableMouse(true)**：落在弹窗体内的点击由弹窗吃掉，不再穿透到底下的复选框。★桩保真同步：真客户端**子帧 level = 父 + 1**，桩补上这条继承语义（否则弹窗 level 恒 0，这个 bug 在测试里根本不存在——变异 M248 实测：不设层级时它的自然层级只有 2）。判据组 134 + 变异 M248~M250 全捕获 |
| **1.73.30** | 📏 工具箱组标题栏**宽度自适应本列**（用户：「工具箱 内项目标题栏宽度自适应」） | 组标题原来是语言包里写死的 `—— 任务·通知 ——`（破折号字数固定 → 列宽变了它不变，右端总差一截）→ 现在由**纯函数** `EVAL_TB_HDR_TEXT(标题, 列宽)` 拼：**剥掉**语言包里的装饰 → 按**本列宽**补足两侧破折号（宽度用既有的「字符数 × 11px」估算，本机没有 GetStringWidth）→ 渲染与断言**同源**（读值口读的是真控件文本）；★判据：标题只出现一次 · **填满本列但不溢出**（估算 ≤ 列宽且 ≥85%）· 列宽变大分隔条更长 · **带装饰的标签与纯标题算出同一条**（专门钉「剥装饰」承重）· 渲染 == 纯函数输出；变异 M244~M247 全捕获 |
| **1.73.29** | ✍️ 「复制名字」改成**只填输入框、不发送**（用户澄清口径） | 用户原话：「点复制名字 → 是在 /say 频道**输入名字**，但是**不要打印出去**，只是打开输入框输入名字」→ 行为从「用 /s 说出来」改成**只预填 `/s 名字` 到聊天输入框**（★**绝不替用户按回车**：不走 `SendChatMessage` / `RunScript`），并如实提示「★还没发出去，按回车才发出」；★与「悄悄话」共用同一条「预填输入框」实现（`ChatFrame_OpenChat` 优先 → 退回 `ChatEdit_ActivateChat` + 编辑框 →两条都没有就**如实提示手动输入**，绝不偷偷发送）；判据组 130⑤/⑤a + 变异 M240~M243 全捕获（**M240 = 除了填框还发出去**，被「一个字都没发出去」当场抓住） |
| **1.73.28** | 🖱 右键菜单定稿（原始功能三条 + 复制名字 · 自适应高度）+ 🔒 **语法闸门补漏** | 按用户逐条要求：①**开关在工具箱**（工具箱 → 队伍/社交 → 「聊天名字右键菜单」，默认开）；②把**原始功能**「悄悄话 / 邀请 / 目标」直接做进菜单（悄悄话 = 打开密语输入框并预填 `/w 名字 `；邀请 = `InviteToParty`；目标 = `TargetByName`，只认附近单位、够不着就如实说明）；③**取消「官方菜单」条目**（不再转发客户端菜单，改为自己做这三条）；④「`/s` 说出」**改名「复制名字」**，动作 = 发一句 `/s 名字`（本客户端没有剪贴板接口）；⑤**弹窗高度自适应条目数**（单一来源 `EVAL_TB_MENU_HEIGHT(条数)`，且**没有「打开聊天框」接口时就不摆「悄悄话」**→ 条目少了高度跟着矮）；服务器写动作（邀请 / `/s`）一律 **0.5 秒去抖**并如实播报。★**闸门补漏**：`luacheck.js` 原先**只解析一个文件**（EvalHelp.lua）→其余 9 个模块的语法错误会**整片漏过**（本轮实测：手写转义把 Toolbox.lua 写坏，luacheck 照样报 SYNTAX OK，直到运行时 load 才炸）→现在**逐个文件**检查（27 个 .lua），已实测能抓到 Engine.lua 里的错误。判据组 130（六条/顺序/字面/半透明/宽度/自适应高度/五个动作/接口探针）+变异 M232~M239 全捕获（★M239 先存活：判据拿 `EVAL_L` 当期望值 → 期望跟着语言包一起变 = **测自己** → 改正为**字面**钉用户点名的词） |
| **1.73.27** | 🔋 资源条件**名称实时化**（用户问：「能量对法系职业是否也是魔法值？名称完善下」） | 【答】是 —— 1.12 的资源约定 `UnitPowerType`：**0=法力 / 1=怒气 / 2=集中值 / 3=能量**，法系的「能量」就是**法力（魔法值）**；而且**德鲁伊会随形态变**（豹=能量、熊=怒气、人=法力）⇒ 资源名**必须显示时现算**，建表时定死一定会在变形后显示错（旧代码就是这么错的）。改了三处，全部同源：① 条件类型下拉与类型按钮 → 实时名（法系显示「法力 / 法力%」）；② 方案行/条件回显/导出文本（`EVAL_COND_STR`）→ 同一个实时名（原来这里对 `powerPct` 还是写死的「能量%」）；③ **解析侧补齐全套写法**（法力/魔法/魔法值/蓝/集中值/集中/精力 + 各自的 % 形态，并顺势补上原来缺的「怒气%」）—— 否则法系玩家**导出再导入就会丢条件**。判据组 132 + 变异 M227~M231 全捕获 |
| **1.73.26** | 🎨 右键名字菜单**美化 + 保留官方右键**（用户拿官方菜单截图对比） | ① **透明度/宽度按用户要求**：背景改**半透明黑**（alpha 0.62，能透出背景）、**宽度压到 62px（≤4 个汉字宽）**、条目文字也压到 ≤4 字（`/s 说出`），行内按钮改半透明底 + 左对齐；② **保留原来的右键功能**：菜单新增一条「官方菜单」→ 把这次点击**原样转发**给客户端的 `SetItemRef`（官方有菜单就弹官方的，没有就如实说明），**绝不因为我们的菜单丢掉既有行为**；③ **官方风格的另一条路（待取证）**：新增 `/eh go 聊天 右键` 探针（逐个报 `UnitPopupMenus` / `UnitPopupButtons` / `UIDropDownMenu_*` / `ToggleDropDownMenu` … 是否存在 + `UnitPopupMenus[PLAYER]` 内容）——**有官方菜单表就能把我们的条目注入官方菜单**（真·官方风格）；④ 桩保真：补 `SetVertexColor` / `GetVertexColor`（原来桩里没有 → 「半透明」这类判据根本读不到值）。判据组 130⑧⑨ + 变异 M221~M226 全捕获 |
| **1.73.25** | 🧰 四项用户要求：**按键与名称对齐** · **删方案清绑定** · **「方案」标签调高** · **猫德一键宏模版** | ① **按键对齐**：行内「添加/清空」（及值按钮）原来按行 y 定位、而标签自己下偏 3px → 按钮比名称**高 3px**；改成两者共用**同一个下偏常量** TB_ROW_TXT_DY（横向一像素不动），判据读**真实控件顶边**（容差 1px）；② **删方案连带清绑定**（用户：「方案删除 要删除对应绑定的按键信息」）：绑定按**方案序号**存 → 删除时 ①解绑它自己的键 ②后面的绑定**整体左移一格**（不左移 = 键会「粘」到错方案上）③SaveBindings 落盘 + 如实播报；③ **「方案」标签调高**：方案按钮行在 -y，标签却被额外压 3z px（看着比按钮低半行）→ 改成与第一行按钮**共中线**；④ **新增「猫德一键宏」模版**：逐行照抄用户截图里的猫德循环（精灵之火（野性）/ 猛虎之怒 / 扫击 / 爪击 / 撕扯 + 未免疫:/ 无debuff:/ 连击>=4 的写法）。判据组 120③b · 93 · 131 · 60 + 变异 M214~M220 全捕获（★M214 直接报出 6 个控件各差 3px —— 证明判据真的量到了控件） |
| **1.73.24** | 🖱 右键聊天名字 → **公会邀请 / 复制名字 / `/s` 说出名字**（用户要求；含 API 排查） | 先按要求**查 API 全表 + 参考插件**：① 本机**有** `GuildInviteByName`（类别 Guild）与 `CanGuildInvite`，**没有** `SetClipboardText`/`GetClipboardText`（→「复制名字」**写不进系统剪贴板**）、**没有** `UIDropDownMenu`/ToggleDropDownMenu（→菜单自己画）；② 参考 `tmp/ChatMOD.lua`（包 `SetItemRef` + EasyCopy 弹窗）→ 照抄「包 `SetItemRef`」这条通道，但**不塞自定义链接**（我们拼的行本来就带 `\|Hplayer:名字\|h`）⇒ 认 **player: 链接的右键**弹菜单，**左键与 item:/quest: 等一律原样放行**（不动客户端既有行为），挂载后**读回确认**+幂等；③ 三个动作：**公会邀请**（先问 `CanGuildInvite`，没权限**就不发**并如实说明；直调不可用走 `RunScript`）· **复制名字**（无剪贴板 → 弹窗 + 名字**全选**，Ctrl+C 即可，并**如实说明**没有剪贴板接口，绝不假称已复制）· **`/s` 说出名字**（`RunScript` + **0.5 秒去抖**）；④ 工具箱新增开关（默认开；关掉 = 一律不接管）；判据组 130 + 变异 M205~M213 全捕获 |
| **1.73.23** | 🟩 公会前缀恢复绿色：类型色补「内置兜底」（颜色是从你截图里**取色**取出来的） | 用户反馈「`[公会] [名字]: 内容` —— 前缀**白色**、名字职业色」→ 根因：类型色只读了客户端的 `ChatTypeInfo`，而这个客户端**取不到**（所以前缀没色）。修：类型色改**三来源** ① 客户端 `colorStr`（**永远优先**，尊重玩家自己的配色设置）→ ② 客户端只给 `r/g/b` 就自己合成 8 位色码（四舍五入）→ ③ 都没有用**内置兜底表**；★兜底色**不是猜的**：把你截图里**客户端自己画的**「[公会]」逐像素取色 = `40FF40`（29 次命中）→ 兜底表**只放有证据的项**，**没证据的类型宁可不加色**（队伍/团队/官员等以后从截图取到色再补）；聊天诊断新增「类型色来源」一行（逐类型列出 `色（client / client-rgb / builtin / none）`）。判据组 129e + 变异 M200~M204 全捕获 |
| **1.73.22** | 🎨 聊天名字染色**终于成了**：吞掉客户端那行 + 自己拼整行（用户选定**方案A**） | 定案「名字槽不吃富文本」之后，唯一能染色的路 = 不让客户端打那行、**我们自己拼**。做法：① 格式串**抄客户端的**（`CHAT_SAY_GET` / `CHAT_YELL_GET` / `CHAT_GUILD_GET` … 取证屏已确认都在）—— 文案/三语言/客户端改动都不用我们跟；② 类型色取 `ChatTypeInfo[类型].colorStr`，**只加在前缀上**（前缀后立刻 `\|r` 复位 —— 与客户端一致，公会前缀照样是绿的）；③ 名字 = `\|c职业色\|Hplayer:名字\|h[名字]\|h\|r`，**自己补链接** → 点名字照样能密语；④ **拿不准就不吞**：事件不在支持表 / 客户端没那个格式串 / 模板里不止一个 `%s` / 名字不在缓存 → 一个字都不改、原样交给客户端（**宁可没色，绝不丢消息或串格式**）；⑤ 支持范围 = 说/喊/公会/官员/队伍/团队，**频道/密语暂不支持**（诊断里写明）。判据组 129d + 变异 M193~M199 全捕获 |
| **1.73.21** | 🧯 立刻停掉聊天里的**颜色码原文**（暂不回写 arg2） | 1.73.20 定案「名字槽不吃富文本」之后，**当下就在产生可见破坏**（缓存过的说话人，名字会显示成 `\|cfff58cbaIonol\|r`）→ 立刻给回写挂闸门（`TB_NAMECOLOR_WRITE=false` **默认关**）：**宁可不染色，也绝不在玩家聊天里显示色码原文**；认得出来照旧计数、并如实记「**暂不回写**」的次数（诊断里红字写明原因，**不是静默**）；判据组 129b 改成两条都钉（闸门关 → arg2 一个字不改；闸门开 → 真的包成 8 位职业色 —— 回写那段不留死代码），变异 M189~M192 全捕获 |
| **1.73.20** | 🧪 名字槽渲染试验（取证口）+ 逐字放大定案「名字槽**不吃富文本**」 | 把用户截图**逐字放大**后确认：名字槽里**8 位色码也原样显示**（`[1. 综合] [\|cfff58cbaIonol\|r]: 1`），而同一屏里经 AddMessage 打印的 8 位色码**是有色的** ⇒ 问题不在长度，而在**发送者名字槽不做富文本解析**。新增 `/eh go 聊天 色测`：让客户端**自己渲染** 5 种名字写法（8 位码 / `\|Hplayer:` 链接 / 链接+内层色码 / 纯名字 / `A\|\|B` 转义测试），并打印客户端聊天格式串 + 展示「吞掉客户端那行、自己拼整行」的样子与**代价**（窗口分流 / 说喊气泡 / 音效）——**先看效果再定方案**；判据组 129c（5 种写法逐条渲染 + 示例三形态逐条钉住 + 代价必须写出），变异 M185~M188 全捕获（★M186 先 SURVIVED：只断言「含链接」会被**另两行示例**顶住 → 改成**三形态逐条**断言） |
| **1.73.19** | 🎨 修「名字染色变成**显示颜色码原文**」——颜色码必须 8 位 | 用户真机截图：聊天里出现「[\|cf58cbaIonol\|r]说: 1」——**颜色码被当普通文字画了出来**。根因：职业色表存的是 **8 位 aarrggbb**（`fff58cba`），写进聊天文本时却用 `string.sub(hex, 3)` **把 alpha 两位切掉** → 6 位 `\|cRRGGBB`，而本客户端**不解析**这种码（1.12 约定 = `\|cAARRGGBB`）。修：四处构造 `\|c` 全部改用**完整 8 位**（arg2 名字 / 正文包链接 / 正文改客户端色码 / 开关示例）；新增源码检查 **`COLOR CODE LEN CHECK`**（禁止 `"\|c" .. string.sub(` 这种切码写法 + 职业色表 9 项逐项必须 8 位）+ 行为判据「颜色码 = `\|c` + **8 位** hex」；变异 M181~M184 全捕获（★M184「表里一项写成 6 位」**只有源码检查抓得到** —— 那项没有被任何行为断言钉住） |
| **1.73.18** | 💬 聊天窗名字染色**真机定案**：名字在 arg2 | 用户「角色迷你还是没染色」两轮后靠**取证探针**定案——**arg1 只有正文**（`1` / `你有队`）、**arg2 才是发送者名**（`Ionol` / `猎狂`）：客户端自己把名字拼成「[名字] 说: 正文」，所以改正文**永远染不上色**；着色改到 **arg2**（回写全局 + 主动查询也按发送者名触发，正文里根本没名字可找）；★姿势改成「**剥颜色码 → 查纯名字 → 用纯名字重建**」：客户端先上的**默认灰**照样改成职业色，已经是我们的色则重建逐字节相同 → **幂等不重复包裹**（旧写法「看到 \|c 就跳过」是**死代码**——查缓存用的是带码的串、永远查不到人 → 变异 M176 先存活）；新增「灰名改职业色 / 幂等时两个计数都为 0」判据，变异 M173b+M174~M180 全捕获（M173 单拆被兜底路径等价抵消，如实记录） |
| **1.73.17** | 🧭 聊天入口**取参兼容全部调用形态** + 调用形态取证 | 真机实测 ChatFrame_OnEvent 被调用 **1513 次**却零样本、连事件计数都是空的 → 两个病根：① **计数被写在「取到文本」之后**（取不到就什么都不记，诊断把证据自己藏了）；② **取参姿势不对**（客户端未必是 (event, 文本)）。修：兼容 (event)+全局 / (event,文本,发送者) / (self,event,文本,发送者) 三种形态（事件名/文本/发送者都按候选挑），**取不到文本也计数**（noMsg）并记下**前 6 次调用的原始参数形状** —— 下一次运行就能把真机姿势钉死 |
| **1.73.16** | 🔎 聊天取证探针修两处 | ① 取证样本改成**只留玩家聊天**（说/喊/队伍/团队/公会/官员/频道/密语/表情）——上一版把全部 CHAT_MSG_* 都塞进 12 格缓冲，战斗/法术刷屏（实测 12 条全是 SPELL_*）把真实聊天行挤出去，关键证据永远看不到；现在同时记 **arg1 + arg2** 并打出**事件计数**；② 官方消息组判读**分清三态**：还在 / 是我们摘的 / **本客户端根本没有这个组**（实测窗口1 只有 SYSTEM、摘掉 0 个，上一版却假报「已由官方接口屏蔽」） |
| **1.73.15** | ⚙️ 分享弹窗加「接收方案」开关 + 底部三段居中 | 收到别人分享时**当场就能关掉接收**（原来要：关弹窗 → 开配置窗 → 找到那个勾，三步）；新勾选框与配置窗那项是**同一个开关**（同一份真值，弹出时自动同步）；底部一行改成「勾选框 + [导入] + [忽略]」**三段整组以窗口中线对齐**，并补上「三段互不重叠 / 生产数字真的落到控件上」的几何断言 |
| **1.73.14** | 🖱 修「开启战斗UI 后物品 tooltip 几秒自动消失」 | 根因：读数口把**真实 GameTooltip 当自己的隐形 tooltip 用**（`local WTT = GameTooltip`）——每次读光环/技能都要 SetOwner → ClearLines → 填内容 → Hide，于是战斗UI/状态UI 的**定时刷新**把玩家正看的物品 tooltip 清空并关掉；改成照 Heart/C 范式**自建隐形 tooltip**（只读它、读它自己的 TextLeft1），并加「真实 tooltip 正显示就绝不读」的退化守卫；`/eh go wtt` 可查（碰过真实 tooltip 的次数应为 0） |
| **1.73.13** | 🏷 debuff 类型显示本地化 | 编辑窗底部预览原来吐的是**导出用的英文 token**（`(Magic/Curse/Poison)`），与格子里显示的「魔法/诅咒/毒」不一致 → 改成**同一份格式化 + disp 开关**：界面（预览 / 方案行 / 行内）走本地化名，**导出/往返仍走 token（存量逐字不变）**；本地化显示形态照旧能解析回同一集合 |
| **1.73.12** | 💬 聊天窗名字着色 | 聊天框里的角色名按**职业色**显示（缓存与窗口着色共用一份：公会/查询/好友窗口上色时**顺手白拿** + 队伍/团队/自己/目标**只读**采集）；判据**拿不准就不碰**（未知名/单字名/已上色/物品链接里的同名一律原样），且**不做自动 /who**（不向服务器发查询）；一个包装体同时管「频道屏蔽」与「名字着色」并覆盖 ChatFrame1~7；**兼容客户端自带的「玩家链接 + 客户端颜色」（改色不叠色，链接照样可点）**；「/eh go 聊天」打印最近 12 条原文与判决、入口在位检查、三种「没染色」原因的如实判读，当**格式校准**入口；**未缓存角色主动查询 /who 进缓存**（默认开、工具箱可关；**四道闸门**：5 秒/发 + 单飞 + 查不到记负缓存半小时不重复查 + 队列上限，日志只进调试日志）；★**换第二入口 ChatFrame_OnEvent**——真机实测 frame.AddMessage 那层「写成功但不生效」（自报挂上 8 个、读回来 0 个），改挂全局函数入口并**读回确认**才作数；★深入 API + 参考插件后补两条新路：**官方消息组**（Get/Remove/AddChatWindowMessages，类别 ChatWindow —— 与走不走 Lua **无关**地屏蔽频道通知，且能按组完整还原）+ **this 帧懒挂载**（ChatMOD 的做法），诊断逐条分开计数 |
| **1.73.11** | 🧰 工具箱改两列 | 16 项开关/设置按**组边界**切成左右两列（切点 = 两列行数差最小 + 组不拆列），列宽由窗口宽算出、窗口太窄**自动退回单列**；每页 = 列数 × 行数（16 条一页装下 → 计数行与翻页按钮自动隐藏）；行内控件全部**显式限宽**，列不相交由几何断言守着 |
| **1.73.10** | 🎨 工具箱新增「角色名职业着色」 | 公会 / 查询 / 好友三个窗口：角色名按**职业色**、等级按难度色、与自己同地图绿字、离线成员整体减半亮度；参考 `tmp/XGuild` 的需求**轻量化重写**（一张描述表 + 纯函数判据 + 幂等包装 + 缺窗口如实记账），默认开，`/eh go 着色` 看诊断 |
| **1.73.9** | 🧰 工具箱滚动按钮挪到底部行 | 撤掉右上 ▲ 与列表下方 ▼+计数，改成图标库同款：`[上翻][下翻]` 文字按钮**右对齐停在 [关闭] 左边、与它中线对齐**，计数文字同排左侧；几何同样取自生产读值口 `EVAL_HELP_CFG_BOTTOM` |
| **1.73.8** | 📄 图标库分页信息挪到窗口底部 | 「共 N 枚 · 第 x/y 页」+ [上页][下页][重扫] 从顶部挪到**与 [关闭] 同一行**：中线对齐、整组停在关闭左边、状态文字与按钮组不重叠；几何取自生产读值口 `EVAL_HELP_CFG_BOTTOM`（不写死坐标） |
| **1.73.7** | 🗂 案例模版：类别独立一行 + 方案同行多个 | 每组标题**独占一行**（本列行首），本组方案从下一行起横向排、放不下自动换行（原来标题与按钮挤在同一行，续行没有标题 → 长组看着像两个类别）；窗口高度由内容自动长到 660×340 |
| **1.73.6** | 🔍 抓宠详情页改成真·放大镜图标 | 右侧按钮原来贴的是 1.12 老路径 + 客户端里不存在的 `_02`（真机只剩「?」缺图占位，用户看到的一直是露在外面的保底文字「查」）；改用客户端真实 `INV_Misc_Spyglass_01_TEX` 并显式藏掉保底文字；`PET ICON CHECK` 覆盖面从「只扫 PetData」扩到 PetHelper（这行此前从未被检查过） |
| **1.73.5** | 🔤 图标语义名 + 多标签 + 关键字过滤 | 1018 枚客户端图标逐枚给语义名（823 枚中文名）并带多个标签（如「爪/爪子/爪击」）；图标库新增输入过滤，按语义名/标签/原始名/完整路径筛选（与分组叠加） |
| **1.73.4** | 🐾 抓宠图标全部换成真实路径 | 客户端纹理是 `/Game/Interface/Icons/<名>_TEX`（不是 1.12 的 `Interface\Icons\`——那种写法只显示成引擎的「?」）；21 技能 + 16 家族全部语义匹配并逐条核对存在 |
| **1.73.3** | 🖱 滚轮方向统一 + 图标路径采集 | 图标库滚动方向反了（且旧断言把 -1 当「向上」一起验过）；方向收成单一来源 + 源码检查；新增 /eh go icons 采集客户端图标路径 |
| **1.73.2** | ☠️ debuff 类型支持多选 | 可驱散类型（魔法/诅咒/毒/疾病）可多选，命中任一即算命中；单选与老配置导出逐字不变 |
| **1.73.1** | 🩹 抓宠详情页修复 + 布局审查 | 修「详情页搜索行叠印」与「宠物名/元信息永远不显示」两处真 bug；列不相交/行距/越界改由几何断言守着 |
| **1.73.0** | 🐾 抓宠帮手 | 第 6 个 Tab：检索宠物技能（图标+各等级）→ 详情页列驯服来源 → 放大镜跳数据检索 |
| **1.72.2** | 🔍 分享方案光环修复 + 引导每次都在 | 别人角色没学过的 debuff 改成按名字扫描兜底并自愈；引导改成每次加载都打；修具名帧顶掉同名全局函数导致的静默失效 |
| **1.72.1** | 🧭 新手上路友好化 | 加载成功提示 + 首次进入自动播报四步新手指引；配置窗全局页加 [使用引导]、新增 /eh guide；三语言 README 同步（含赞助板块） |
| **1.72.0** | 🎉 方案快捷键攻坚 + 一批界面精修（跨越 1.71.3~1.71.24） | 22 个版本一次发布：**方案快捷键从「永远不触发」到真正生效**（四次试错定案 → 接管 ActionButtonDown/Up，零成本/不占宏位/不抢键位/运行中立即生效）；**右键功能合并**成一个「方案管理」窗；案例模版窗三次改版；图标库 Tab + 右键设为小地图图标；战斗信息UI 标题改玩家名 |
| **1.71.24** | 🗂 右键功能合并成一个「方案管理」弹窗 | 用户：「将这两个功能合并成一个弹窗管理.都是右键触发.」—— 把此前**两个独立的右键弹窗**（配置窗方案按钮右键=重命名 / 战斗信息UI 方案按钮右键=快捷键绑定）合并成**一个方案管理窗**：窗内两段 ① 方案名称 ② 快捷键，**[保存] 一次提交两段**（改名 + 绑键，互不牵连）；**两处右键都开同一个窗**（左键语义不变，仍是激活方案）；★关键决定 = **不为旧入口留别名**（留别名会让「调用点改回旧名字」的回归悄悄通过——实测变异 M1/M2 正是这样 SURVIVED 的）；组 102 + 变异 **8/8** 全捕获 |
| **1.71.23** | 🧹 清理四次试错留下的死探针代码 | 快捷派发定案后回头清理：删掉已判死的 go bind2/go bind3/go actbar 三个写入型探针（它们验的是 CLICK / 裸命令名 / Bindings.xml 三条已被证伪的路线，**留着只会误导后人**），go bind 从「写入试验」改为「现状检查」（只读：派发前提 / 接管状态 / 逐方案绑定与占用格）；文件净减 **205 行**；/eh go diag 保留为唯一诊断入口 |
| **1.71.22** | 🎯 方案快捷键真正生效：接管 ActionButtonUp 派发 | 四次试错后的最终定案 —— ①CLICK 劫持鼠标、②裸命令名不派发、③**Bindings.xml 本客户端根本不读**（把 ArchiTotem 启用后完整重启，它的 CAST_EARTH_TOTEM 依然不在命令表里；unrealUI 源码注释在 2026-08-19 也记录过同一结论「60 commands were absent from a 225-entry binding table」）→ ④**可行路线 = 命令名用客户端自带的 ACTIONBUTTON<n> + 插件接管全局 ActionButtonDown/Up**（/eh go diag 实测四者 	ype=function、SetOverrideBindingClick=nil）。★零成本：不占宏名额、不占动作格、不抢已有键位、**游戏运行中立即生效**；补回 unrealUI 警告的组合键守卫（按住 Alt/Ctrl/Shift 且该组合另有归属时不吞）；格号分配两轮挑（优先「格空 + 命令没人绑键」的完全无主格）；清理解绑时**把格交还客户端**。变异 6/6 全捕获 |
| **1.71.21** | 📦 Bindings.xml 随插件自带（用户零配置）+ 登录生效自检 | 用户问：「Bindings.xml 是插件帮忙完成的吗?用户零操作配置文件吗?重启就可以?」——**都是**：文件随插件发（用户零配置）、绑定全在弹窗里完成（零手动）、用户只需**重启客户端一次**。文档深挖佐证：`RunBinding` 派发靠「命令的 press/release 处理器」且插件无法注册（`SetConsoleKey` 是写明「什么都不做」的兼容桩）→ 裸名永不触发、注册只走启动命令表；新增 `EVAL_BIND_XML_STATUS()` 登录自检：命令表没有 `EVAL_GO_PROF_*` 就如实提醒「重启一次即生效」，有则静默；★断言精确前缀计数（`EVAL_GO_PROF_1X` 不算），变异 2/2 全捕获 |
| **1.71.20** | 🖨 左键点「方案」= 聊天框打印当前绑定情况 | 用户：「左键点击<标题方案>.日志打印方案绑定情况.」——「方案」标签左键新增：有绑定则逐行打印「序号. 方案名 = 按键」（只打印、**不动任何绑定**），没有就如实说「当前没有自定义绑定」（不是静默）；tooltip 同步补第 4 行说明（三语言）；顺带：`/eh go bind3` 无尾 CLICK 实弹探针（验证「不带 :LeftButton 尾巴的 CLICK」能否既触发又不碰鼠标）；★断言走真实左键/真实悬停，变异 4/4 全捕获 |
| **1.71.19** | 🏷 「方案」标签加 tooltip 三行说明 + 右键点它 = 清除全部自定义绑定 | 用户：「方案文字 添加 tooltip 提示：右键取消所有自定绑定，左键点击具体的方案激活方案，右键绑定方案按键，美化说明下」——「方案」二字做成可点标签：悬停出**三行美化说明**（左键点方案=激活 / 右键点方案=绑定或改键 / 右键点「方案」=全部清除，三语言）；右键点它真的**逐键解绑 + 清表 + SaveBindings**（返回清除个数，幂等不炸）；左键点它不动绑定；★断言走真实右键/真实悬停 tooltip，变异 4/4 全捕获 |
| **1.71.18** | 🔧 绑定派发弃用 CLICK 改裸命令名（修「E 没触发 + 鼠标左右键被劫持」） | 用户实测：CLICK 隐藏按钮派发在本客户端解析岔了——E 没触发、**鼠标左右键的转屏被顶掉**（但会触发插件，证明按键 → 插件链路是通的）→ 派发改**裸命令名 `EVAL_GO<i>`**（T0 里客户端内建命令就是 MoveForward/Jump 这种驼峰全局函数名；`EVAL_GO1~12` 本就存在，零新增函数；T2 也证明绑定表接受该名）；★断言补反向哨兵「每个派发命令名都必须解析到真实全局函数」，变异 3/3 全捕获 |
| **1.71.17** | 🖼 初始默认图标改「力量祝福」（用户定稿） | 用户在图标库亲自挑了 `Spell_Holy_BlessingOfStrength`（金色拳头）当小地图按钮图标后，要求**初始默认也用同一枚**——自动挑选候选把它排最前（压过扳手/工程学/齿轮）；亲自右键挑的仍然永远优先于一切默认；★纯函数断言「力量祝福压过扳手」+ 本客户端 `/Game/..._TEX` 形态，变异 2/2 全捕获 |
| **1.71.16** | ⌨ 方案快捷键绑定（战斗信息UI 方案行右键 → 美化弹窗，下拉按键分键盘/鼠标/组合键） | 先验证后实装：`/eh go bind` 实测 SetBinding 接受自定义命令名（T1/T2 全过）→ 战斗信息UI **右键任意方案**开绑定弹窗：金边深底 + 标题栏拖动 + **分类下拉选键**（功能键/数字/字母/鼠标/组合键，分类标题不可点）+ **冲突/空闲提示**；派发走 **CLICK 隐藏按钮**（vanilla 经典招，按键 = 客户端点 `EVAL_GO_KEY_i` → 执行并激活方案）；换键自动解旧键、绑定即 `SaveBindings` 持久化；附实弹探针 `/eh go bind2` 复核 CLICK/裸命令名两种派发；★断言走真实右键 OnClick，变异 6/6 全捕获 |
| **1.71.15** | 🖱 图标库右键 = 设为小地图按钮图标（修「图标调整的不对」） | 1.71.13 自动挑的小地图按钮图标用户不满意：「图标调整的不对」——她的图标让她自己挑：图标库里**右键**任意一枚即设为小地图按钮图标（存 `cfg.mbIcon`、重登不丢、tooltip 有提示），**亲自挑的永远优先**于自动挑；左键仍是原来的打印路径；`/eh go mbicon [reset]` 查看/清掉自定义；★断言走真实右键 OnClick（写配置 + 按钮当场换 + 左键不动配置），变异 3/3 全捕获 |
| **1.71.14** | 🧹 全局页「一键宏」组底部对齐（修与「状态信息UI」重叠） | 1.71.12 给「界面」组加两行子开关后，「一键宏」组头与「状态信息UI」直接叠上（用户截图红圈）→ 整组改**从底部按钮行向上锚**（底部对齐）：去抖行下缘与导航/关闭按钮行上缘留 6px，组内行距不变；★y 从 NAV_ROW_Y 推导（单一来源）；断言读**真实控件**位置（不重叠 / 底部对齐 / 行距 / 不出窗口），变异 3/3 全捕获 |
| **1.71.13** | 🖼 小地图配置按钮改图标钮（无边框·与图标同尺寸）+ 💬 tooltip 文案美化 | 用户要求：「配置按钮 换图标，尺寸和图标尺寸保持一致，不用设置外边框；tooltip：全职业一键宏，常用工具箱，世界数据库检索，描述美化下」——按钮 = 一枚宏图标（从宏图标表**按名字优先级**挑：扳手 > 工程学 > 齿轮…，运行时查表不写死路径），**26×26 与图标库单格同边长、图标占满整钮**；挑不到图标自动退回旧「金框 + EH」样式（不留空白按钮）；tooltip 改三行功能描述 + 一行操作提示（三语言）；顺带：新探针 `/eh go bind`（快捷键可行性：T0 枚举命令表 / T1 绑 JUMP 验证 API / T2 试绑 EVAL_GO2，不存档不动现有绑定） |
| **1.71.12** | 🎛 战斗信息UI：标题改用玩家名 + 新增「战斗 / 方案」两个子开关 | 用户要求两件事：「战斗信息 标题换成 用户名字」「再加2个子选项，战斗，方案，分开控制显示和隐藏」——标题取 `UnitName("player")`（取不到就退回语言包文案，绝不把标题栏留空）；「**战斗区**」（血/能量/连击/目标条 + 读条 + 挥击条 + 状态行）与「**方案区**」（方案切换行 + 技能图标带）各自独立开关，★关掉是**整块不画**（布局随之前移、`SetHeight` 跟着变小），不是画完再 Hide；老配置没有这两个键 → **nil = 开**，行为与旧版逐字一致 |
| **1.71.11** | 📐 战斗信息UI 方案行：给「拉伸」设上限（修「空的太多了」） | 上一版把每行剩余**全部**均摊 → 一行里只有一个短名时被拉成**大空框**（用户截图红圈）→ 现在每格**最多比名字宽 +24px**，超出的部分**留在行尾**；★判据补**反向**一条「不许拉成空框」，且**故意不读生产的上限值**（读了它，把上限调大的变异会连判据一起改 —— 实测 M6 就这样 SURVIVED） |
| **1.71.10** | 📐 战斗信息UI 方案切换行：按名字实测宽 + 自动换行 | 用户实测「有些方案 会溢出宽度」——**根因是按钮宽度按行均分**：长名字（「一键团队驱散」）撑破自己的格子、压在邻居上。现改为**按名字实测宽度**分配 + **贪心换行** + 每行剩余均摊填满 + 文字格限宽；★量宽走 ruler FontString（无 `GetStringWidth` 时退化为「中文字一字约 9px」）；★★**改名/增删后由 `EVAL_WAR_TAB_REFRESH` 比对签名整体重建**（行数变了帧高与下方技能带一起挪），每 0.15s 的 tick 里**一次都不量宽**；变异 **5/5 全捕获** |
| **1.71.9** | ▲ 调序按钮改真三角 + 🖼️ 图标库一页 6→9 行 | 技能列表的调序/滚动按钮把 `^`/`v` 换成**真三角 ▲/▼**（客户端字体里 `^` 看着像横线、`v` 就是字母）；图标库一页行数 **6 → 9**（图标区 +102px，一页 **162 枚**，原 108）——★行数是**先算清可用空间**定出来的（照字面「+250px」会把末行画到窗口外，而越界在本客户端是**静默的**）；变异 4/4 |
| **1.71.8** | 🧩 案例模版窗改「分组分两列 + 组内同行自动换行」 | 用户要求「案例模版 分组 分两列,分组内的方案 同行 自动换行.」——版式从「单栏一条流」改成：**窗口按列宽一分为二**（列宽 = (窗宽 − 2×边距 − 列间距) / 2），**组是不拆的最小单位**（按顺序切成左右两段，一个组绝不跨列——拆开看着像少了一组），**组内流式**（标题后跟本组按钮，同一行依次往右排、放不下就换行回**本列**左边界），每个组从新的一行开始；★切点 = **两列行数差最小**（遍历所有切点；不用「塞到过半就停」那种贪心——1.71.5 的教训）；实测中文窗 660：列宽 310、11 组排成 **左 8 行 / 右 7 行**、窗口 **660×240**（原单栏 660×180）；★★版式仍是**纯函数** `tplTwoColPlan(...)`（位置单一来源，渲染只读这张 plan），并用**合成数据**逼出「组内换行 + 分两列」这两条在真实数据下可能等价的路（8 组 × 9 个宽按钮 → 12+12 行、两列各 36 个）；断言改成**分列不变量**：按钮在**本列**列宽内、同行相邻留足 GAP、**组不跨列**、两列都有内容、两列行数差 ≤2、标题在**本列行首**且所在行有本组按钮；变异 **6/6 全捕获** |
| **1.71.7** | 🧑‍🤝‍🧑 队伍/团队案例模版审计：一律「指定技能 + 队伍/团员条件」 | 用户要求「审计下之前添加的队伍团队相关的方案技能配置优先采用: 指定技能搭配 队伍/团员条件类型完成需求」——把 **17 条**队伍/团队模版从「选取目标:队伍成员/团队成员 + 候选者\* 条件」（两行配合）改成**指定技能 + 队伍/团员条件**（一行自足）：`- 快速治疗 \| 队伍血<70 & 能量%>15`、`- 奥术智慧 \| 无队伍buff:奥术智慧[职业:…]`、`- 驱散魔法 \| 有队伍debuff:魔法 & 能量%>10`；★依据是引擎原生语义（1.70.47）：**这些条件自己就会扫人 + 切目标**（命中即记 st.allyUnit 并切过去），后续技能天然打在他身上——比「先切目标、再施法」少一行，也不会出现「切了目标却没施法」的空切；★★★**成员类条件必须写在一行的最前面**（它负责切目标，写在后面就是拿「切之前的目标」求值 —— 静默出错）；★顺带把萨满「治疗波」的门槛写成 desc 里说的血线（队伍血<40），原来那条 `<70` 会让大治疗砸在 65% 的人身上；★★★**审计落成 4 道自动闸门**（不然下次又会被改回去）：① 内置模版一律不许出现「选取目标:队伍成员/团队成员」② 成员条件必须在行的最前面 ③ 文本里写了几条成员条件、解析后就必须有几条（**静默丢弃** = 那一行变成**无条件施法**）④ 成员施法模版的**每一行**都得自己带成员条件；另加**端到端**：拿模版原文真跑一遍（真扫人、真切目标、技能真打在乙身上）；变异 **11/11 全捕获** |
| **1.71.6** | 🧩 案例模版窗改「流式（自动换行）」布局 | 用户要求「模版不用一列，可以同行，自动换行布局」——版式从「一列 / 两列」改成**流式**：组标题与方案按钮**在同一行里依次往右排，放不下就换行回到左边界**；★每个按钮宽度 = `FontString:GetStringWidth()` **实测** + 内边距（拿不到就退化为「字符数 × 9px」近似），窗口高度**由内容算出**（中文 660×180，11 组 27 条排成 5 行）；★★**组标题不许「孤零零留在行尾」**——要求「标题 + 该组第一个按钮」能一起放下，否则先把标题换到下一行；版式抽成**纯函数** `tplFlowPlan(W, groups, measure, margin, gap, rowH)`（位置**单一来源**，渲染只读这张 plan 表，不会两处各算一遍而漂移）；★★★**真实数据下每组的按钮串刚好放得下 → 「不换行 / 一行一个」的实现在真实数据上照样全绿**（本项目老家族：只在当前数据上看着对 = 迟早退化）→ 新增**合成数据**断言入口 `EVAL_TEST_TPL_PLAN_FAKE(w, n, chars)`，直接喂 60 个宽按钮**逼出换行路径**（实测排成 9 行、每行最多 7 个），并逐行钉「无右越界 / 同行相邻留足 GAP / 标题不孤悬」；另加真实渲染的标题几何断言（`EVAL_TEST_TPL_HEADERS`）「每个标题所在行至少有本组一个按钮」；变异 **7/7 全捕获**（取消换行 / 真·一行一个 / 每个元素前都换行 / 去掉 GAP / 量宽恒为 1 / 去掉标题孤悬守卫） |
| **1.71.5** | 🧹 案例模版补「一键队伍驱散 / 一键团队驱散」+ 分列改「最优切点」 | 队伍/团队功能组补两条驱散模版（覆盖 **魔法 + 疾病** 两条链：牧师 = 驱散魔法 + 祛病术；骑士把两个法术名都换成「净化」；德鲁伊/法师/萨满改成 毒/诅咒 + 自己的解咒法术）；★两列分列从「塞到过半就停」的贪心改成**遍历所有切点取两列行数差最小**（贪心在加两条模版后偏成 22/16 → 现在 18/20，且以后增删模版会自动重新平衡），断言同时钉「选的确实是最优切点」；模版总数 25 → **27 条 / 11 组**；变异 **3/3 全捕获**（删一条驱散模版 / 类型写错字 / 退回贪心分列） |
| **1.71.4** | 🧩 案例模版改版（两列·更大·标题提示）+ 📚 模版扩充到 11 组 25 条 | 案例模版窗**加宽到与配置窗同源 + 分两列**：★**按「职业组」分列**（一个组绝不被拆到两列——拆开看着像少了一组）、两列行数自动对半；标题上加**金色文字感叹号**，悬停提示「如果您有好的配置方案，欢迎在插件评论里分享给大家！」（★不用那两枚有固定含义的 mark 图标，避免语义叠加）；★模版 **6 组 → 11 组 25 条**：新增 **牧师 / 德鲁伊 / 术士 / 萨满** 四组常用一键宏、**队伍/团队**功能组（一键队伍治疗 · 一键团队治疗 · 一键队伍buff · 一键团队buff），**通用法系**补「队伍/团队刷 buff（智力/耐力）」，**骑士**加按职业区分的「力量祝福（物理职业）/ 智慧祝福（法系职业）」；★「职业过滤」同时支持**候选者条件**（`候选者缺buff:奥术智慧[职业:法师/…]` —— 「只给法师补智力」这类模版就靠它），解析 / 导出 / 编辑器三处一起打通；★新增内容质量断言（模版名非空 / 同组不重名 / 首行「# 方案: X」== 模版名 / 至少 1 条技能行）；变异 **10/10 全捕获**（写死宽度 / 退化单列 / 分列失衡 / 去感叹号 / 文案不走语言包 / 候选者导出丢过滤 / 候选者不许带过滤 / 候选者行不显示过滤格 / 模版名与首行不一致 / 同组重名） |
| **1.71.3** | 🛑 停止攻击（含真停读条）+ 📤 分享频道精简 + 📥 接收规则 + 🔇 频道进出屏蔽 + 🛍 自动购买重做 + 💬 分享来源玩梗 + 🔁 自己方案回声忽略 + 🎨 图标美化（类别下拉/异常标记/弹窗）+ 🖼 图标库 Tab + 🎯 选取目标图标/如实日志 + 🔇 频道屏蔽修好 + 🧑🤝🧑 候选者条件与按比较符选人 | 「角色行为」新增 **停止攻击**：一次点击停掉【读条 + 自动射击 + 魔杖自动重复 + 近战普攻】（≈按 ESC 的停手），与 取消施法 同族的特殊行为、不占动作条；★停施法/自动重复走 RunScript（文档 **Protected: yes**）；★停近战普攻必须先确认「当前真在挥击」才敢按（文档原文 **Toggles**=切换，否则会把没在打的人打起来）；日志如实报账到底停了哪几类；★**分享的「团队」频道下线**（用户实测「插件分享. 好像团队模式无法检测到.」→ 追问后定案「不要团队模式发送了.」）——1.12 系客户端里**队长**发的团队/队伍聊天走 CHAT_MSG_*_LEADER 独立事件，而接收端只注册了 CHAT_MSG_RAID/PARTY，**队长一分享接收端永远收不到**（且失败是静默的：发送方照样提示「已发送 N 片」），故发送侧只留 公会/队伍/说（接收侧注册保持原样，老版本往团队发的照样收）；并加**频道白名单**——直接调 `EVAL_SHARE_SEND("RAID")` 也会被拒，禁止「界面上没有、代码里还能发」；★**接收端数据规则重做**（用户要求「在多个分享同时出现的时候只显示最新的数据，防止有些人滥发，或者多人发送接收异常」）：① **只保留最新一份**（新收齐的直接替换待导入的那份，被替换掉的一行如实说明谁取代了谁）；② **同一发送者开新的一笔 → 他旧的未收齐缓冲立刻作废**（连点两次分享不再互相盖；★只按发送者作废、**不跨发送者**——不同人同时发时分片交错，互相作废会两边都收不齐）；③ 四道上限：在途 4 笔 / 单笔 40 片 / 单笔解码 8192 字节 / 去重表 64 条，超限**整笔拒收并如实提示**；④ 接收侧提示统一**限频 5s**（窗口内只报一条并告知「另有 N 条已省略」——防止提示本身被滥发者刷屏；限频的是「说不说」、数据侧的拒收照常执行）；⑤ 弹窗详情行改为**先清空文本再 Hide**（本客户端 Hide 后仍可能被绘出，只 Hide 会让上一份分享的残留行挂在屏幕上）；★**「停止施法」功能保留、移动脉冲撤回**（用户实测两轮：「查看 API LUA 停止施法有方式…现在停止施法功能无法停止，虽然看着施法进度条没了，其实还在释放法术」→ 接着「确实没效果」）——查本机 API 全表（1370 条）确认停读条接口**只有 `SpellStopCasting` 一个**（文档 Protected: yes｜addons cannot call this），插件调它在本客户端**只清本地进度条**、服务端读条照旧；用户判断「**可能是现在客户端的 bug，功能先留着**」→ **保留原始的 SpellStopCasting 方式**（走 RunScript 排队），`取消施法` 动作**照旧保留、不隐藏**；本轮试过的「移动脉冲」真中断通道**实测同样无效**（没效果还把角色往前挪）→ **已删除**，并留反向哨兵断言「绝不移动玩家角色」；`取消施法`/`停止攻击` 收敂成同一个通道函数 `EVAL_STOP_CAST()`（原先两处各写一份、重复调用）；新增 `/eh go 停施法 1|2|3` 取证命令便于日后复测 |；★**新增「屏蔽频道进出信息」**（用户截图：`[4. 世界防务] 离开频道。` / `[5. 寻求组队] 离开频道。`，要求放在 工具箱 → 队伍/社交、**默认开**）——查本机 API 全表确认**没有任何聊天过滤器 API**（`ChatFrame_AddMessageEventFilter` 之类不存在），故实现为**挂 `DEFAULT_CHAT_FRAME:AddMessage`** 这一层（有文档的控件方法）：判据 = 「频道词」**且**「进出词」同时出现（`频道/channel/канал` × `加入/离开/joined/left/присоединил/покинул`）——多要一个频道词是为了**不误伤真人消息**（只匹配“加入/离开”会把玩家聊天里一句 has left 也吞掉）；开关**在调用时读**（改开关立即生效、不用 /reload）、挂载**幂等**（绝不重复包装）、只吞通知不动频道聊天本身；挂不上（或客户端不走 Lua 打印）则什么都不做并如实报告；新增 `/eh go 频道` 诊断（开关/入口/已过滤条数/样本/聊天窗口消息组）与 `/eh go 频道 试 <文本>` 判据自测 |；★**「自动购买指定物品」重做**（用户要求：弹窗 UI 美化、功能增强、购买频率限制防瞬间购买、支持多个数量、列表显示 启用|名称|数量|每次购买数量(默认1)、安全界限与异常终止（如背包满）、防止重复购买）——① **独立设置窗**（列表四列可编辑：勾选框=启用、名称、总数、每次；点格子弹输入框；单条[删]/[清空]；隔行底色 + 标题栏 + 1px 金边 + 底部安全提示行）；② **分批购买**：一拍只下一笔（TB.buyInflight），全部走既有限频队列（0.3s/笔）→ 绝不瞬间买光；每次数量受 **每次 / 商人一次上限(GetMerchantItemMaxStack) / 库存 / 还差多少** 四重夹取；③ **逐笔核对**：下一批前数背包，上一笔没涨即记失败，连失败 3 次自动停用该条目；④ **安全闸门**：背包满 / 商人窗口关闭 / 库存卖光 / 本轮总量上限 200 件 / 连续失败 —— 任一命中即停并如实提示；⑤ 会话状态放 TB.buySess（不落盘），配置只存「想买什么」；⑥ 判据集中在纯函数 EVAL_TB_BUY_DECIDE（可脱离游戏直接测）；旧的一次买够路径整块删除（一处真值） |；★**修正（用户实测报回）：「购买数量」是绝对值目标** —— 对着**背包里现有数量**算（够了就一件都不买），否则每次跟商人对话都会重复购买；我第一版把 n 当成「本次再买 n 件」（以会话开始时的数量为基准），是错的，已改并补 3 条断言 |；★**分享来源频道标注**（用户问「方案分享来源能否辨别是否是公会来源」）——收到分享时弹窗标题带上来源频道 [公会]/[队伍]/[团队]/[说]/[密语]（判据是**触发的事件名**，按频道分开注册的；队长/团长的 *_LEADER 事件一并归类；认不出来就**不标**，不瞎猜）；SH.pending.chan 留给后续判断；三态事件分派抽成 EVAL_SHARE_DISPATCH 以便断言直调真实逻辑；★**分享来源玩梗 + 自己方案回声忽略**（用户要求）——① 自己往公会/队伍/团队发分享时，客户端会把分片**回声给自己**：判据 = 「**知道来源**（事件名非空）**且**来源**不是「说」**且内容就是**我自己的方案**」→ 直接忽略（同名 **且** 序列化后逐字相同才算我的——只比名字会误伤同名的人）；★**例外必须留着「说」**：用户自测分享的唯一方式就是发到「说」（只有自己看得到），顺手吞掉等于让功能没法自测；★**认不出来（事件名缺失）时照旧弹窗**——宁可多弹一次，也不误吞别人的真分享。② 公会来源点 [忽略]/[导入] → 在**公会频道**各玩一句梗（文案在语言包 SH_FUN_IGNORE / SH_FUN_IMPORT，按 发送者 / 方案名 格式化）；三道闸门：**只认公会来源** / **必须真的在公会里**（IsInGuild，不在就一句不发、不假装）/ **走 RunScript 排队**（公会聊天 Protected，同分享发送那条已验证通道）；玩梗**不替代导入**（导入照旧真的写进方案列表）。变异 11/11 全捕获；★**图标美化（用户要求）**：① 「角色行为/角色技能/宠物行为/目标选取/物品使用」五个类别下拉**带图标**（★图标元数据写在 `EVAL_GO_SKILL_CATEGORIES` 里 = **单一来源**，UI 层不另写一份映射；用**下标赋值**而非 `table.insert`——insert 遇 nil 会不插入、整表错位）；② **「取消施法」行右端挂问号标记 + 悬停说明**（原因如实写：官方 API 里停止读条只有 `SpellStopCasting` 且 Protected，插件只能清本地读条条、服务端读条照旧）——★标记与技能图标是**两列**（混用会让「图标画了但标记没出来」这类 bug 永远测不出来），且**每次重绘都重置**（下拉行池是复用的，不清就会把上个菜单的标记漏到下一个）；③ **分享接收弹窗美化**：标题栏 + 左侧图标 + 详情区下沉底板 + 按钮上方分隔线，两个按钮**整组以窗口中线对齐**（93/197；宽与间距单一来源）；④ 图标**自包含**：从 UnrealQuest 拷入 `media\icons\` 28 个文件（约 160 KB，MIT；EQL3 羊皮纸那批有单独版权、没拷）；新增第 14 条源码检查 **UI ICON CHECK**（纹理路径写错时客户端**什么都不画** → 逐个核对文件真的在磁盘上 + 类别图标不许重复）。变异 **15/15 全捕获**；★**新增第 5 个 Tab「图标库」**（用户要求：「增加个 tab 独立脚本…分组归类显示图标用途，tooltip 图标名称，支持滚动上下页，方便我调用哪些图标」）——独立文件 `IconBrowser.lua`（照 Toolbox/DataSearch 的 `EVAL_XX_BUILD(root, page, refreshes)` 约定接线）；数据源 = **客户端内置宏图标表** `GetNumMacroIcons`/`GetMacroIconInfo`（就是宏编辑器「选图标」那一屏，本机 api 索引 Macro 分类 8 个函数里那两个）；★它给的是**路径**不是文件（整套图打包在 `Content\Paks`，磁盘上取不到），但路径正是 UI 要的东西，而且客户端内置、无版权、零依赖；按**文件名前缀**分组（火焰/冰霜/暗影/神圣/自然/奥术/其他法术/技能/物品/商业/种族/临时/其他）+ 一组「本插件在用」（**从生产代码读** = 类别图标 + 警示图标，不另抄名单）；图标池按「真实窗口宽度算出的列×行」建、**滚轮或按钮翻页**（滚轮**链式接管**：非本 Tab 时转发给别的处理器）、悬停 tooltip 显示 名称/分组/序号/完整路径、点击把路径打到聊天框；★取不到就**如实说**：noapi（没有这个接口）/ empty（接口在但一枚都没拿到），两种失败都只列「本插件在用」那组，取失败的枚数也如实写进状态行；★顺带新增读值口 `EVAL_HELP_CFG_TAB` / `EVAL_HELP_SE_WARN_ICON` / `EVAL_CFG_WHEEL_SCRIPT` / `EVAL_TEST_CFG_TAB_NAMES`。变异 **16/16 全捕获**；★**「选取目标:」族图标 + 队伍成员选取的如实日志**（用户实测报回两件事）——① 用户原话「目标选取子菜单,换一些好看点的图标」：原来整族都回退成红问号（`wicon` 对 `targetSelOf` 一律返回 `INV_Misc_QuestionMark`）→ 新增 `TARGET_SEL_ICON` 表（**单一来源**，11 个 id 各一张，全部取自自包含的 `media\icons\`：boss-mobs / spirithealer / meetingstone / elite-mobs / rare-mobs / flight / quest-marker / database / repair / battlemaster / raid-entrance），表里没有的 id 仍回退问号（不瞎指一张图）；② 用户原话「角色行为->选取目标:队伍成员 日志明确输入匹配到的目标名称,达成的条件,比如是血量筛选,的显示血量N% 符合,现在无法正常工作」：旧版 `wuse` 的选取目标分支**无论选没选到人都写「→ … | 当前目标:X」并返回 true** —— 用户看到日志像成功、实际目标没换，后面的技能打在旧目标上，「功能不工作」却查不出原因 → 现在**如实报账**：命中写「命中:乙 party2 血量 10% 法力 8% · 条件: 目标血<40√」（条件明细 = `groupsOK` 的第三个返回值 trace，旧版被丢掉），一个都没选到则写「未选中成员：甲 50% / 乙 10% 都不满足（提示：本行条件是「自身*」——它只描述你自己，筛不出队友；筛队友请用「目标血%／目标buff／目标debuff」）」并 **return false**（如实：这次没出手）；百分比统一**取整**（`tostring(10.0)` 在 Lua 5.1 与 fengari 下不一致）；`UI ICON CHECK` 扩到 17 个图标（5 类别 + 1 弹窗 + 11 选取目标）并改成**按组查撞图**。变异 **10/10 全捕获**；★**修「屏蔽频道进出信息」实测不生效**（用户报回「屏蔽频道进出信息未能正确工作」+ 截图里「进入频道/离开频道」照旧刷屏）——**两个独立根因**：① **判据漏词**：客户端真实文案是「**进入**频道。」，而旧表只写了「加入」→ **进入那一半根本没被匹配**（「离开」那半是命中的，所以看着像「有时有效」）；已补 进入/退出 + 繁中 進入/離開/頻道 + enUS entered（★教训：判据表要照着**客户端真实文案**抄，差一个字就是半个功能失效）；② **静默失效（主因）**：`Toolbox.lua` 载入时 `DEFAULT_CHAT_FRAME` 往往**还没建好**（FrameXML 聊天框晚于插件载入）→ 旧版「载入即挂」失败后**再也没有人重试**：开关看着是开的、实际一层都没挂上；现在 ① 事件驱动（VARIABLES_LOADED / PLAYER_ENTERING_WORLD）+ ② 每帧队列帧兜底 + ③ `/eh go 频道 装` 手动立刻重试，限频 1s / 最多 60 次、成功后短路；③ 顺带去掉 `type(f) ~= "table"` 这道闸（本客户端帧未必是 table——那道闸会让某些环境**一次都挂不上**），写入口用 pcall 包住、挂不上**如实** return false；④ **诊断升级**：显示「我们的包装在位吗 / 经过入口的消息数 / 被吞数 / 尝试次数」并**当场判读**两种失败（包装不在位 = 挂载没成功；在位但零消息 = 客户端不走 Lua 打印）——现象一样、修法完全不同；新增第 15 条源码检查 **CHAN RETRY WIRING CHECK**（`tbChanRetry()` 必须 ≥3 处接线 + 诊断手动入口），因为行为断言照不到「生产里到底有没有接线」。变异 **11/11 全捕获**；★★★**成员选取器：候选者条件 + 按比较符选人**（用户三轮定案，含截图）——① **新增 4 类独立条件**「候选者血% / 候选者能量% / 候选者缺buff / 候选者debuff」；原 8 项「队友/团员…」**一字不动**保留（名字/行为/文本/范围前缀全在），两套互不干扰；② **条件类型下拉按技能行过滤**：技能行是 `选取目标:队伍成员/团队成员` 时**只提供这 4 项** + 顶部提示「本行条件用于筛选候选者（队伍/团队，含自己）」，其它行照旧全 5 组——每次打开**现算**（换技能立刻变），已配旧行照旧显示可跑；③ **选人规则由行内第一个比较型条件决定**：`<`/`<=` 取**最小**、`>`/`>=` 取**最大**、`=`/`==` 取**最接近 N**（该条件不再要求精确相等）、`~=`/无比较条件回退「血量最低」；★老数值型（队伍血/蓝、团员血/蓝）**也统一到同一规则**（用户定案：界面里只有一套规则）；④ **候选上下文**：选取器行上的「自身血%/能量%」（用户截图那种旧写法）按**候选者**读 → 老配置不用改也能筛人，同时纠正上一版「自身* 筛不出队友」的提示（只有整行没有候选可比条件时才提示「会退化为血量最低者」）；⑤ 日志补「**依据**: 取最小/取最大/取最接近(条件原文)」；⑥ 文本层新增独立拼写（`候选者血<60` / `候选者能量=50` / `候选者缺buff:回春术` / `候选者debuff:痛苦诅咒(魔法)`，候选者血/能量额外支持 `=`），老拼写照旧只对应老 8 项 → 双向可读。变异 **16/16 全捕获**；★**「指定名称…」那一行没有图标**（用户截图红线圈出）——分类下拉里那一项的文案是 L("SE_PICK_TGT")=「指定名称…」（**带省略号**），而 `targetSelOf` 直接拿它查 TARGET_SEL 表 → 查不到 → `wicon` 一路落空 → **整行空白**；现在查表前先剥掉结尾省略号（`…` / `...`），让**同一份 TARGET_SEL 表既管文案又管图标**（单一真值）；★**取消施法**：左图标改从**客户端自带的宏图标表**（`GetNumMacroIcons`/`GetMacroIconInfo`，与「图标库」Tab 同一数据源）里按关键字挑一张（Kick/Interrupt/Counterspell/Silence/Stop），**找到才用、找不到如实退回问号**——因为本客户端**纹理路径写错时什么都不画**（不报错、不崩），而内置图标在磁盘上取不到（整套打包在 `Content\Paks` 里，本机散图只有 AddOns 下那 163 个），写死路径等于赌一个看不见的字符串；右端的异常标记从暴雪那个红问号换成**插件自带的白色感叹号**（`media\icons\mark-unavail`，由现成的任务标记转成**正方形** 32x32——原图 27x64/19x32 细长条，塞进 12x12 方格会被**横向压扁**）；★**[+添加条件] 的初始类型 = 这一行下拉里的第一个可选条目**（用户要求「添加初始值从过滤条件内选」）：成员选取器行 → 候选者血%；**其它行仍是 SE_TYPES 第 1 项（原行为一字不动）**；原先恒用第 1 项 → 在选取器行上会加出一条**下拉里根本没有**的条件；两处判据收敂成同一个 `seIsPickerRow`；★**[启用此技能] 右侧新增两个图例**（白感叹号=不可用 / 黄感叹号=待测试），**意义只在悬停里**（文案走语言包 4 键 ×3，并且写成**字面量键**让静态 LANG KEY CHECK 扫得到，262→266）；位置按「启用」标签的 **GetStringWidth 实测宽度**推（不硬编码 x——英/俄窗口 800 宽、文案长得多）；★新增第 88 组回归 + UI ICON CHECK 扩到 **19 张自包含图标** + 图标库「本插件在用」列出这两枚标记；变异 **12/12 全捕获**（省略号归一 / 宏图标不按关键字挑 / 没命中关键字也硬用第一枚 / 标记换回问号 / 初始类型恒为第 1 项 / 恒为候选者血% / 两枚图例重叠·同图·说明串台·越出窗口 / 标记图文件名写错 / 图标库不列这两枚）；★其中 3 条第一次分别报 **ANCHOR-MISS 与「变异体自己语法错」**（Lua 里 `x = 1 return y` 非法、把 `return 1` 整行删掉直接崩）——查锚点时反而揪出生产代码里一个真 bug：**Lua 模式里 `%.` 被我误写成 `%%`**（`%%` = 字面百分号）→「三个点」那条归一分支其实是**死的**，已修并补断言钉住；★**「待测试」标记改版**（用户实测后要求，原话：「第二张的八个条件名称前面的感叹号去除. 在右侧添加黄色感叹号. tooltip 提示待测试, 第一张也是相同操作.」）——上一版（1.71.2）把「!」加在**名字前面**，但它会**跟着条件名到处跑**（条件行 / 菜单 / 日志里都是「!队友血量%」），看着像乱码；现在**名字撤掉前缀**，改成条件类型下拉里**行右端一枚黄感叹号**（黄=待测试、白=不可用，复用上一轮那两枚自包含标记图），悬停给出「待测试 + 说明」；覆盖「队友/团员」8 项 + 「候选者」4 项；★「哪些算待测试」提为**单一真值** `SE_TODO_KINDS`（下拉标记与悬停说明同一份）；变异 **6/6 全捕获**（不挂标记 / 挂成白色那枚 / 悬停丢掉待测试两行 / 给所有类型都挂 / 名字又加回「!」/ 候选者 4 项漏算）；★**「角色行为」新增 `跟随`**（用户要求：「角色行为 添加: 跟随:名字输入, 查看API是否跟随.跟随目标.验证可行性」）——两种写法：`跟随`（跟当前目标）/ `跟随:名字`（按名字跟；编辑器里有「跟随:指定名字…」入口，点了弹名字输入框、空名不收）；★★★**API 取证（这就是用户要的「验证可行性」）**：本机 `api_*.html` 的全局函数索引里，Movement 分类**只有两个函数不是 Protected**：`FollowByName` 与 `FollowUnit`（其余 `MoveForwardStart/Stop`、`TurnLeftStart` 那一批**全是 Protected**——★这正好**回顾性解释了**上一轮「移动脉冲」为什么实测无效：插件根本调不动它们，那条路已删）；`FollowByName(name)` 原文「Starts autofollow on a nearby player by name, or on the current target if name is omitted or empty」＋「Name matching is case-insensitive and searches known player objects only」＋找不到时**客户端自己显示错误并返回**（不抛异常）、**无返回值** → 故**直调**（非 Protected，不走 RunScript）、日志如实写明「能否跟上由客户端判定」；本地先挡掉文档明列的两条硬规则（**不能跟随自己** / **没给名字且当前没目标**）；★接线照旧 7 处，源码检查扩成「`stopAllOf` **且** `followOf`」两条判据；★★★**顺带修掉一个老 bug（本轮踩到才发现的）**：中文输入法默认打出**全角冒号**「：」，而 **Lua 的字符集 `[...]` 是按字节匹配的** → `[:：]` 只吃掉「：」的第一个字节，捕获到的名字前面多出两个乱码字节 → 下游查表全查不到 → **选取目标/物品/宠物/姿态/条件解析全线静默失效**；现在 `colonNorm()` 在**解析入口**统一归一（不动那 30 处既有模式 → 零回归面）；★新增第 89 组回归；变异 **13/14 捕获 + 1 等价**（等价那条：输入弹窗的 `apply()` 已先 trim 并拒空，回调里的空名守卫是冗余的第二道）；★另记一条测试基建教训：**源码检查失败走 `process.exitCode = 1`、脚本照跑到底** → 只 grep「ALL TESTS PASS」会**漏报**，判据必须同时看退出码与 `: FAIL` 输出；★**队伍/团员条件加两个「成员过滤」**（用户要求：「队伍团员状态 8个条件类型.参数支持 目标职业多选,默认空不过滤职业.也就是全部」＋「团员级别的条件选额外增加 队伍多选,默认全部队伍, 先验证可行性」）——8 项队伍/团员条件都多一个**职业多选**格，其中 4 项**团员**条件再多一个**小队多选**格；★★**空集 = 不过滤**（不选任何职业 = 全部职业；不选任何小队 = 全部小队），与「目标职业」那条「空 = 永不满足」**正好相反**（两处各有专属断言钉住）；★★★**可行性取证（用户要的「先验证可行性」）**：本机 `api_*.html` 索引 + emberveil wiki 的 **Raid 页原文**现场核对 —— `GetRaidRosterInfo(index)` 返回**九个值**：name, rank, **subgroup(1–8；未知按 1)**, level, classLoc, **classFile(WARRIOR/MAGE/…)**, zone, online, isDead；★**非团队或下标越界时只返回一个 nil** → 小队号**拿得到**，**可行性 = 可行**（队伍成员走 `UnitClass` 的第二返回值取职业，队伍里没有小队号概念、恒记 1）；实现：扫描记录加 `cls`/`grp` → 单条条件求值时按**它自己的**过滤收窄成员集合；`选取目标:队伍成员` 那一行则按**行内条件的并集**收窄**候选集合**（战士血更低也不选、选到法师）；文本框往返新增两个后缀 `[职业:战士/法师]` / `[队伍:1/3]`（空集不写 → 老配置导出**逐字不变**；非队伍/团员类型带它 = 写法错误 → 如实丢弃）；编辑器里两格放在 484/544，占的正是**行内预览**的位置 → 那 8 行不显示行内预览（窗口底部的整串预览照旧）；★变异 **13/14 捕获 + 1 等价**（过滤失效 / 空集当成谁都不匹配 / 去掉小队过滤 / 解析不挂到条件上 / 非队伍类型也接受后缀 / 不写职业后缀 / 编辑器不显示职业格 / 队伍行也显示小队格 / 不让位 / 退化成单选 / 扫描不记小队号 / 扫描不记职业 / 留下空集；等价那条：`[职业:]` 的写入分支在「空集不存在」之后已是死代码）；★顺带修掉两个自己踩出来的坑：后缀解析用 `[:：]` 字节集 + 贪心捕获会把**后面那个方括号组一起吞掉**；以及解析出空集会让导出写出 `[职业:]` |
| **1.71.2** | 🧙 施法条件改名+归组 + 🗑 删重复按钮 + 📤 分享不弹窗 + 💬 分享指引提示 + ❗ 队友条件标记 + 🔢 日志小数 1 位 + 🩹 生命条阴影 + 📂 模版拆到 examples/ + 🔀 任务分开关 + 🕳 黑块修复 + 🔎 配置期技能扫描 + ⏸ 按键暂停 + 🛑 交接死循环修复 + 📍 EH 按钮归位 + 📐 导入导出窗按钮同行 + 🔄 分享导入即时刷新 + 📄 分享弹窗显示方案详情 + 📚 开关组横排并入接收 + ⌨️ buff 下拉改用行内输入框 + 📦 三按钮移出 IO 窗 + ⬇️ 开关组下移 40px | 工具箱「任务自动交接」拆成【自动接取】+【自动交付任务】两个独立开关（旧配置自动搬迁）；★修「光环下拉加了输入框后，所有弹窗上部都有一条看不见的黑块」——EditBox 被 Hide() 后仍会画出底条，改为「挪出可视区 + 压到 BACKGROUND 层 + 面板补不透明背板」；**每次打开配置窗静默重扫动作条**并写调试日志（不刷聊天框），日志逐条记「技能名 → 格子号」+ 触发原因，便于核对「拖上去了却说未找到」；**工具箱→自动交接任务支持「按住按键临时停止」**（默认 Shift，可换 Ctrl/Alt 或关闭），按住期间不接取/不交付并撤回已入队的待执行交接，松开立即恢复；★修「自动领奖无限刷屏」——`GetQuestReward` 会再次触发 `QUEST_COMPLETE` 形成自触发死循环，现加**任务名去重 + 领取窗口闸门 + 全局频率上限**三层防护；★修「EH 小地图按钮压在圆形小地图左下角」——默认锚点原来贴的是小地图**左边缘**（圆内会被吃掉），改为落在圆外侧；压缩在小地图上的记忆位置一律丢弃；★修「导入导出窗底部按钮被排成两排、还被路径提示压住」——窗口加宽到 560，6 个按钮统一等宽同行（间距/宽度单一来源），标签显式限宽并禁止折行（新增 IO BTN LABEL CHECK 守住）；★修「分享触发导入后，方案列表未能及时显示」——刷新原来交给每个调用点自己记得调，分享弹窗漏了，现改为在导入写入点统一刷新；**分享接收弹窗直接列出方案详情**（逐行显示技能与条件，最多 6 行、超出折叠提示），不用先导入就能看清内容；**开关分组改为横排一行，并把「接收方案」开关并入该组**（共 4 项：启用一键宏 / 自动攻击 / 调试日志 / 接收方案）；**buff/debuff 条件的过滤输入框改到条件行自身**（直接在那格打字即过滤，回车即作为光环名），下拉面板不再内置输入框——彻底避开那条链路的历史问题；**案例模版 / 分享 / 接收三个按钮从导入导出窗移出，改放到配置窗左侧【激活方案】列表正下方横排一行**（IO 窗从此只保留 导入 / 导出 / 关闭）；**开关分组按用户要求再下移 40 像素**（-349 → -389），为此窗口高度同步 420 → 460——原高度下移后开关行底边会落到 -425、**超出窗口 5px 根本看不见**；**「方案技能日志」开关从「开关」组移到「全局 → 日志」分组**（排查确认它与「记录调试日志」**不是同一功能**：后者是**总闸门**——关了`EVAL_LOGLINE`一个字都不记；前者只把决策原因**同步刷到聊天框**、日志照写，故**不合并**而是移位；同时改名以消除两者同名的歧义）；★修「自动接取任务有时输出一条空白日志」（我方播报的「任务接取： 」名字为空）——新接任务刚进日志时标题尚未填充，`GetQuestLogTitle` 返回**空字符串而非 nil**，而 **Lua 里空串是真值** → 被当成一个「名叫空的新任务」立刻播报；现在**入库与播报两层各挡一道**（已补回归用例：拆掉两道防护即复现空白行）；**【案例模版 / 分享 / 接收】三个按钮改为在右侧与[关闭]**同一行**（整组靠右、中线对齐），**开关组同步下移并与按钮行对齐**（同一条中线，左右分列不再拥挤）；★修「buff 检查类型的技能标题无法输入」——该格**上一轮已改成输入框**，但有**两个独立根因**使它看不见也用不了：① 刷新只写了回声 FontString、**从未把值同步进 EditBox**（控件建了但没被喂数据）；② 点击热区（Button）**后建而压在 EditBox 上面**，点一下命中的是开下拉、输入框永远拿不到焦点→打不了字；现已修（刷新同步值 + 点击同时聚焦与弹下拉，打字时刷新不冲掉输入）；★按用户要求**还原**输入框位置——光环条件的输入框**回到下拉面板内**（打字即过滤 + 自定义输入可提交），条件行那格回到纯文本显示；隔一轮的两个修复（面板不透明背板、搜索框挪出可视区）**仍然全在**，故重启不是把老 bug 请回来；★修「光环条件下拉里为什么有『清凉的泉水』这类物品」——第三组「全部技能」列的是**动作条上的全部非宏格子**，而动作条上可放物品（饮料/药水）→裸名字混进来；原有的 `not itemOf(n)` 只认**带前缀**的「物品:名称」写法，裸名字漏网；现扫描时用**背包名集合**给物品格打标，下拉里物品条目**保留但显示为灰色 `[物]`**区分（取值仍是裸名字，不影响规则）；★★★修「骑士光环判定 buff 状态失败」——根因是**光环名解析不出纹理时被静默当成「0 层」**，于是「否/无」方向永远成立 → 规则无限重放（日志刷屏）；现在**解析不出就如实失败并说明原因**（自身/目标的 buff、debuff 四类全部加闸）；另新增 **`/eh go 光环 [名字]`** 定向探查（列出光环到底在不在增益条上、纹理是否对得上）——官方文档：`GetPlayerBuff`/`UnitBuff` **只枚举增益条上的光环**，且 hidden/tracking 会被跳过，本客户端无 aura 专用函数；★★**光环判定改为「图标 + 名字」双软识别**（用户指出：有些法术图标不同）——实测虔诚光环动作条图标是 `Spell_Nature_WispSplode`、增益条图标是 `Spell_Holy_DevotionAura`，只靠图标必然漏；现在**图标没命中时按名字再确认**（限频 0.5s + 顺带自愈学习正确图标），同时 `auraTexOf` 改为**学习表优先于动作条**；★★★修「条件类型下拉最后一组整组消失 + 面板右侧多出一条空黑区」**同一个根因**：下拉行池硬编码 **48 行**，而条件类型菜单已需 **54 行**（五组 49 项 + 5 个组标题）——最后 6 行（`冷却就绪/技能可用/未排队/施法范围内/施法中`）被**静默丢弃**，而面板已按 5 列算宽 → 第 5 列空着，就是那条黑区；现行池提到 **96**，且**超出时如实提示「还有 N 项」而不再静默截断** ★按用户要求调整「施法相关」的条件名并归组：①【施法中】从最右「技能状态」组**移入**「自身状态」组（用户选定方案 A）——自身侧现为 施法中/施法时间/施法剩余时间，目标侧为 目标施法中/目标施法时间/目标施法剩余时间，两侧完全对称；②四条时间条件改名（原 自身读条进行 / 自身读条剩余 / 读条已进行 / 读条剩余；引擎 id 不变，存量方案与历史导出文本照旧可用）；③修「规则摘要里显示裸 id `castEl`」；④★顺带定案并修掉一个**静默丢弃**——「施法中」的导出串在未指定技能名时写成「施法中:nil」「施法中:」，而解析侧不认 → 导入即丢条件；现已与目标侧写法对齐（空名不拼冒号），历史串仍能读回；⑤★英文/俄文里「目标施法时间」与「施法时间」原本**同串**（菜单里两条一模一样、无从分辨），已区分 ★按用户要求把「自身施法族」（施法中 / 施法时间 / 施法剩余时间）与「目标施法族」（目标施法中 / 目标施法时间 / 目标施法剩余时间）**一起并入「技能状态」组**并重新排序——顺序为**技能本身的状态 → 我的施法 → 目标的施法**：冷却就绪 / 技能可用 / 未排队 / 施法范围内 ｜ 施法中 / 施法时间 / 施法剩余时间 ｜ 目标施法中 / 目标施法时间 / 目标施法剩余时间（自身与目标两段名字互为镜像）；原「自身状态」「目标状态」两组不再重复出现这些条目（是**移动**不是复制），全表总行数不变（仍 54 行） ★按用户要求清理配置窗底部：①**删除「分享」右边的 [接收] 按钮**——它与开关组的「接收方案」读写**同一个开关**（两者都调 `EVAL_SHARE_RECV_TOGGLE`，动手前核实过），删掉不丢功能；②**隐藏左侧「开关」分组标题**（用「不画」而不是「画完再 Hide」——本客户端 Hide 后控件仍可能被绘出）；③★顺带挖出并修掉一个**藏了好几轮的静默 bug**：一段描述旧状态的残留代码仍在执行 `cfgWin.nav = nil`（把导航按钮登记当场清空），而唯一读它的测试钩子 `EVAL_TEST_CFG_NAV()` **当时没有任何断言调用** → 「底部有哪几个按钮」在测试里长期为空；本轮补断言时当场暴露（`got=0 want=2`），已删残留块并给钩子接上断言；④关闭按钮改用**单一来源**的几何常量（原先把宽高/锚点又硬编了一遍，与导航组对齐用的常量是两份）；⑤删掉因此变成孤儿的 `EVAL_SHARE_RECV_LABEL` ★按用户要求：**配置窗的 [分享] 按钮不再顺带弹出「导入导出」窗口**——核实后确认分享流程根本不需要它（`EVAL_SHARE_SEND` 走 `EVAL_PROFILE_TO_TEXT` → hex 分片 → `RunScript(SendChatMessage)`，**全程不碰 IO 窗、也不碰它的输入框**；原注释「分享流程需要 IO 窗承载 hex 文本」是旧设计的残留）；而且那次调用是 **Toggle**——IO 窗本来就开着时点分享反而会把它**关掉**。现在点 [分享] 只弹频道下拉 ★按用户要求：**技能日志的小数值统一保留 1 位小数**——修掉聊天框里的浮点尾巴（用户截图原文 `自身buff剩余时间不符(剩余295.24997172132s)`，现为 `剩余295.2s`，格式与既有的「冷却剩 %.1fs」对齐）；同一条「无动作」状态行里的 `目标血%` 也从 0 位改为 1 位（`%.0f` 会把 0.4% 的残血抹成「0%」，而残血正是调阈值时最要看的那一档） ★按用户要求给配置窗底部的 [分享] 按钮加了**指引 tooltip**（两段式）：**【怎么分享】**= 点按钮 → 选频道（公会/队伍/团队/说）→ 当前方案自动分片发出；**【对方要怎么才能收到】**= 对方也要装 EvalHelp + **必须开着「接收方案」开关** + 在同一频道 + 分片收齐后自动弹接收面板点【导入】。其中「必须开着接收开关」一条**用绿色高亮**——它是唯一会**静默失效**的条件（对方开关关着时 `Share.lua` 的 `shOnMsg` 第一行就直接 return，双方都没有任何提示）；[案例模版] 按钮也换成自己的说明文案（原来两个按钮共用一句「方案级操作：案例模版 / 分享当前方案」，对哪个按钮都不准）；实现上 `navBtn` 的提示参数现在同时支持**一行字符串**与**多行富文本**（`{文本,r,g,b}` 表，带空行分段与分色） ★修「战斗信息 UI 生命条看着有阴影」（用户实测）——根因是状态条把彩色填充层**内缩 1px**（锚点 +1,-1 / 高度 h-2 / 可用宽度 w-2），满血时四周露出一圈近黑的底（0.08,0.08,0.10@0.9），看着就像描边/阴影；现改为填充层**铺满整条**（三处必须同时改，只改前两处的话满血时右侧仍留 2px 缝），深色底仍保留作为「未填充部分」，掉血后的空槽照旧可见；★顺带修掉测试桩的一个大坑——`newMock()` 漏了 `local x, y`，导致所有帧/纹理**共享最后一次写入的位置**（几何断言其实一直在读同一个值，IO 窗「按钮在同一行」那条此前恒真），修完后原有几何断言才第一次真正生效 ★按用户要求：**「队友/团员」那批条件（8 项）在条件类型里统一加感叹号前缀**（如 `!队友血量%`）——该批条件（1.71.1 新增的队伍/团队扫描）**尚未实机验证**，用「!」把它与其它条件区分开，免得被当成同样可靠；标记加在**语言包的值**上（三语言同步），所以**条件行、规则列表里也一并带着**这个标记，而不只在挑选的那一刻提示 ★按用户要求把**案例模版库搬出脚本**：`EVAL_IO_TEMPLATES` 的数据拆到 **`examples/` 目录、按职业分文件**（warrior / mage / caster / rogue / hunter / paladin），由 `EvalHelp.toc` 按选单顺序载入——EvalHelp.lua 因此**少 68 行**，只留一行兜底初始化。★为什么是「.lua 文件 + 列进 .toc」而不是「运行时读 .md」：**本客户端没有文件读取 API**（Lua 沙箱里没有 io/os），插件读不了自己的目录，唯一受支持的「从文件载入」就是让客户端把它当 Lua 模块加载。★新增 **EXAMPLES TOC CHECK**（四个方向各一条）：磁盘 ↔ toc 集合**双向**一致（漏列 / 幽灵行都能抓）、toc 顺序 = 选单职业顺序、**模版数据不许再搬回生产文件**、examples 必须列在 EvalHelp.lua 之后 |
| **1.71.1** | 🩺 队友/团员扫描 | 一键扫描队伍/团队按条件挑人（血最少/蓝最少/中魔法/缺buff）→ 切目标 → 技能落在他身上；debuff 检测带 魔法/诅咒/毒/疾病 下拉 |
| **1.71.0** | 📡 方案分享 | 一键把方案发到聊天频道，队友收到弹窗点「导入」即用 |
| **1.70.44** | 🎯 定位圆点空白根因 | ★实测定案：`dsSetOverlay` 定义在 `local dsAnnOverlay` 之前→写全局，`dsAnnDraw` 在其后→读局部，一个变量被两批函数绑到两个位置→定位圆点永远画不出。状态块整体上移；新增 DECL ORDER CHECK 自动守住（顺带查出 DS_MAX_PINS/dsRndOn 两处同类） |
| **1.70.43** | 🏹 猎人案例模版 | 新增【猎人】案例模版（选敌/印记+钉刺/宠物攻击/猛禽/奥射）；★修导入导出往返缺口——11 个取反条件（不可攻击/非友善/未就绪…）导出后导入被静默丢弃；★修无条件技能行（如「- 宠物:攻击」）被静默丢行 |
| **1.70.42** | 🔍 定位后空白排查 | 定位全链路日志（不受 trace 门控）+ 覆盖层/失败数上浮层；补回「先校验再修复」自愈（打算画>0 而可见=0 才重画，2s 节流）；修 1.70.40 补丁残留的重复绘制 |
| **1.70.41** | 🩹 过滤钮消失 + 彩色标注 | 「类型」过滤钮因局部变量声明顺序错误（第 10 次）整颗不显示→布局常量上移；检索 NPC 标注改为按实体 id 派生的稳定彩色圆点 |
| **1.70.40** | 🗺️ 数据检索 + 地图标注层 | 新增 Tab4 数据检索（任务/物品/怪物/NPC 检索详情+地图定位）；世界地图标注层 16 类别随地图切换实时重绘；★根治切图不更新——tick 帧改挂 WorldFrame（全屏地图会隐藏 UIParent 致 OnUpdate 停摆），并删除三个失效兜底 |
| **1.69.2** | ⏱️ 任务通知滞后修复 | 事件先于数据落地→延迟 0.3s 扫描，进度/接取通知不再滞后一个状态 |
| **1.69.1** | 🧾 售卖明细日志 | 自动售卖逐笔明细：物品名称/数量/物品种类（本地缓存缺失兜底 ?） |
| **1.69.0** | 📢 任务通知+交接限频 | 任务交接 API 进限频队列+交接日志；任务进度/接取/完成通知到可选频道（关/自己/说/队伍）；工具箱任务组拆 自动交接/通知 |
| **1.68.2** | 🛡️ 商人API限频 | 出售/购买/丢弃全部队列化 0.3s 滴出+逐笔成功验证，防反滥用踢线 |
| **1.68.1** | 🩹 卖灰统计修复 | MERCHANT_SHOW 本客户端连发（实测提示×2）→ 1.5s 去重窗，数量不再重复计 |
| **1.68.0** | 🧰 工具箱Tab | 配置窗第三页：商人/社交/任务 自动化小工具，独立载入+滚动归类 |
| **1.67.6** | 💬 模版描述入tooltip | 案例模版行内只留名字，悬停显示 描述+方案技能预览（模版扩容不溢出） |
| **1.67.5** | 🔮 通用法系分组 | 「周围补buff」从法师组拆出，独立通用法系分组（牧补韧/德拍爪同模式） |
| **1.67.4** | 🗡️ 模版阈值微调 | 盗贼模版斩杀终结 连击>4 → 连击>=4（真·4 星即收） |
| **1.67.3** | 🗡️ 盗贼模版更新 | 模版加选敌起手（选取目标:最近敌人）；终结条件 自身血<30 → 目标血<30 |
| **1.67.2** | 🗡️ 盗贼模版 | 模版库新增盗贼分组：邪恶攻击攒星/剔骨终结（用户实配收录） |
| **1.67.1** | 🏷️ 标题显版本 | 配置窗标题栏显示版本号（启动信息本就有版本） |
| **1.67.0** | ⭐ 连击点条 | 战斗UI 能量条下方新增连击点段（贼/德专属）：5 等宽小块，激活高亮金、未激活浅灰 |
| **1.66.0** | 🖼️ 目标头像图标 | 战斗UI 选取目标类技能图标实时显示目标头像（SetPortraitTexture），切目标即变 |
| **1.65.0** | ↩️ 接管时序还原 | 撤销 1.62.0 后移（当时是误诊）——接管动作回到规则评估之前，每次按键先补自动攻击 |
| **1.64.1** | 🩹 难度色垫片 | FrameXML/TargetFrame 调用 GetDifficultyColor 但客户端缺失→全局垫片（vanilla 五档难度色），目标框报错消除 |
| **1.64.0** | 🐛 多返回截断根治 | ★s and pcall 只取第一返回——可用性恒 false 的真凶（1.62.0 结论修正）；可用性只信 noMana 资源信号 |
| **1.63.0** | 🩸 可流血免疫驱动 | 可流血默认 true；免疫体系命中流血技能（撕裂/割裂/绞袭/斜掠/撕扯）→ false；生物类型硬排除删除 |
| **1.62.0** | 🎯 冲锋疑案真凶 | 接管动作后移到规则评估之后——AttackTarget 先开弓进战，冲锋类脱战限定技能可用性瞬间翻 false |
| **1.61.4** | 🔬 可用性结案 | 探针实证 IsUsableAction 正常（noMana=true=资源不足）——跳过原因细分：不可用:资源不足 |
| **1.61.3** | 🚑 关系列漏改 | 条件关系列同换全角竖线（1.61.2 只改了 uiEsc 调用点，这处内联写死的漏网） |
| **1.61.2** | 🚑 竖线显示修复² | || 转义在本客户端渲染异常——改用全角竖线（U+FF5C，中文字体自带，视觉一致） |
| **1.61.1** | 🚑 竖线显示修复 | 条件关系 | 在 WoW 是颜色转义符被吞——显示层全面 || 转义（关系列/条件列/预览/聊天/IO预览） |
| **1.61.0** | ↕️ 下移按钮 | 技能列表每行新增 v 下移（与已有 ^ 上移成对），调序按钮文本改为箭头 |
| **1.60.1** | 🔬 可用性探针 | /eh go probe usable [技能]——dump 格子原始可用性（IsUsableAction 本客户端走缓存态） |
| **1.60.0** | 📏 列表高度衍生 | 配置窗技能列表行数按窗口高度动态算（8→12 行），延伸到底部关闭按钮上方 |
| **1.59.0** | 🎭 姿态下拉自适应+去GCD | 条件姿态下拉按角色实际姿态栏生成（带名称）；符合的规则全部顺序执行，能不能放交给游戏内置 |
| **1.58.1** | 🚑 布局双修复 | 方案栏删按钮间距 21→19（逐行下沉错位修复）；战斗UI 方案第二排不再异常拉伸 |
| **1.58.0** | 🔢 时间型步进 | 时间类条件（距攻击/读条系）步进 0.1、区间 0.0-10.0、初始 0.1，一位小数显示 |
| **1.57.0** | ⏲️ 距攻击条件 | 新条件「距下次攻击」数值比较（距攻击<0.5 卡挥击节奏出手），复用挥击计时 |
| **1.56.1** | 🔮 法师案例×2 | 模版库新增 法师一键输出/周围补buff；修导入技能名 24 字节上限丢行（带前缀名最长 39B） |
| **1.56.0** | 📐 方案行自适应 | 战斗UI 方案切换行与状态条同宽对齐；方案多时自动换行，每行按钮拉伸填满 |
| **1.55.0** | ⏲️ 挥击计时 | 施法学习同机制：平砍事件锚点+UnitAttackSpeed → 战斗UI/状态UI 显示距下次攻击时间 |
| **1.54.4** | 🔢 连击初始1 | 条件「连击点数」默认参数 3→1（域 1-5） |
| **1.54.3** | 🗡️ 连击域修正 | 条件「连击点数」参数步进限定 1-5（GetComboPoints 上限 5），不再能步进到 8/300 |
| **1.54.2** | 🎚️ 去抖可配+追踪 | 去抖窗口入全局配置（默认 0.30s，±0.05 步进，0=关闭）；/eh go trace 看最近12次执行时间戳 |
| **1.54.1** | ⏱️ 执行去抖 | 连按不再堆积——0.2s 内重复调用直接丢弃（宏走 RunScript 队列，松手后陈旧调用全失效） |
| **1.54.0** | ✨ 光环检查四型 | 自身buff/目标debuff 合并为是/否检查；新增 目标buff/自身debuff 检查（实时下拉 ◆○●▲ 全配齐） |
| **1.53.0** | ⚡ 并行执行模型 | 仅角色技能占公共CD终止按键；角色行为/宠物/选目标/物品全部可并行——一键=切目标+宠攻+喝药+法术 |
| **1.52.1** | ⛓️ 选目标续行 | 选取目标切换成功后不再吃掉按键——刷新状态继续评估后续规则（切完目标当场放技能） |
| **1.52.0** | 📏 自动攻击距离检测 | 自动射击/魔杖超程（IsActionInRange=0）自动降档近战普攻，贴脸猎人不再空挥弓 |

## 🔍 v1.75.13 — 地图标注：右侧放大镜入口 + 分类标注下拉 + 弹出层「点外面即关」

**① 地图右侧放大镜入口 + 分类标注下拉**

- 用户原话：「**在地图右上角位置使用放大镜图标(只在任务插件可用的情况下,采用任务插件那边的放大镜图标.).左键点击 弹出地图标注的下拉选项.点这个下拉窗之外的任意位置都可以关闭下拉窗**」+「**放置在地图右侧参考图片.对应位置.添加一些描述信息**」。
- 图标素材**取任务插件自己的那一张**：`Interface\AddOns\unrealQuest\media\search-icon`（它的路径常量也照抄对方，不自己拼）—— `GetTexture()` **读回自证**贴没贴上，取不到就退回**文字兜底**（绝不静默留一个空图标）。
- 位置 = **右对齐地图窗**：`SetPoint("TOPRIGHT", 图标条, "TOPRIGHT", WorldMapFrame, DS_MAPICON_DX, DS_MAPICON_DY)`，两个偏移是**独立常量**（照参考图微调不用动别处；本轮按用户口令从「固定坐标」改成右对齐 + 两轮微调）。
- 左键 = 弹「分类标注」**多选**下拉（走 `EVAL_DD_OPEN`）；条上常显**当前类别数**（`DS_MAPICON_LABEL (N)`）。
- ★**层位**（老坑复用）：图标先扫地图的具名子帧取最大层，再 `max(500, top+40)` —— 本客户端的**全屏点击画布 `WorldMapButton`** 会成为后来者的"隐形天花板"，症状就是「看得见、点不动」。
- ★**反馈上屏**：开地图时**聊天框不可见** ⇒ 点击回执（3 秒）写在图标条自己身上，而不是只往聊天框说一句没人看得见的话。

**② 地图标注也画到小地图上**

- 用户原话：「**查看任务插件,地图标注的内容如何在小地图上也显示**」。
- 做法 = **照抄任务插件自己的小地图配方**（`Map/NpcPins.lua` 的 `DrawMinimap` + `Client.CreateMinimapPin`），全部走它的公开 API、**不自己发明常数**（本客户端的小地图尺度是实测出来的，写错的表现就是"图钉跟着人跑"）。
- 图钉是 **Minimap 的子件** ⇒ 开大地图时随小地图一起藏；层关掉 / 地图不在时一并收（补 `dsMmHide` 前向声明）。

**③ 弹出层「点外面即关」（用户：「地图设置.需要点击地图以外也能触发关闭.」）**

- `EVAL_DD_OPEN` 新增 `opts.strata` / `opts.level`（默认 DIALOG / 250），并**把行与搜索框逐个 `SetFrameLevel`**。★**父帧抬高不带动子件** —— 行都是绝对层帧：不抬 ⇒ 背景压在行上（用户截图里的"**黑框**"）；行被埋 ⇒ **点不动**。这一条本项目已踩过两次，这次写进记忆体。
- 新增**全屏捕获层** `EVAL_HELP_DD_CATCH`（挂 UIParent、层级 = 下拉 level − 5、`OnClick` → `EVAL_DD_HIDE`）：点在下拉窗**之外的任何位置**即关；`EVAL_DD_HIDE` 是**唯一**隐藏出口（捕获层与窗口一起收，绝不会留下一个吃点击的空层）。
- 工具箱 →「缩放大地图」的地图设置面板同款：`EH_SM_CFG_CATCH`（锚 UIParent、覆盖整屏、层级 = 面板 − 5），显隐统一收口到 `smCfgShow(show)`，面板 `OnHide` 兜底、`featTeardown` 一并收掉（**关功能不留残渣**）。

**④ 黑幕抗闪收口**

- 用户三连：「**地图每次打开.会闪烁一次背景黑幕?这个如何避免?**」→「**能否直接设置黑幕透明度是0**」→「**然后关闭地图不要恢复这个背景的透明度**」。
- `featHideBlackout` = 遮蔽**唯一出口**（`featApply` / `featKeep` / tick 瞬时窗口三处都走它）：① `SetAlpha(0)` = **硬抗闪**（客户端每次开图都会自己 `Show` 它、而且在**我们之后** ⇒ 只 `Hide` 的话最多空 0.2s = 那一下黑闪）；② `Hide()` **不能省** —— **显示着但透明的全屏帧照样吃点击/悬停**，省了会让地图点不动（比黑闪更糟）。
- 开图那一拍起 `SM_BLACKOUT_SEC`（**0.6s**）内**逐帧**重申（有界窗口，与"开图后逐拍重申居中"同一手法）；**关图只失效窗口**，不写透明度/缩放/位置。
- 只有**关功能**才还原：`featBlackoutRestore` 唯一调用点 = `featTeardown`；★**改属性前先抓原 alpha**（读不到就记 `false`、如实说"无法还原"，**绝不拿 0 冒充原值**）+ 归零后**读回自证**。

**⑤ 删掉测试框架（用户定案）**

- 用户原话：「**删除tests/下的文件.也不需要测试**」⇒ `tests/`（7 个源码检查 + 3 个断言组）· `test_assert.lua` · `test_engine.js` · `check.js` · `mutate.js` · `tmp/runcheck.js` 全部删除；**唯一闸门 = `node luacheck.js`**（fengari 逐文件真解析，必须 `SYNTAX OK`）。
- ★**代价如实写进记忆体铁律 1**：接线 / 渲染 / 存档 / 清单结构这类**静默失效不再有自动判据兜底**，只能靠「改完自查调用点 + 真机实测」。保留 `luacheck.js`（语法）与 `test_stub.lua`（WoW API 桩，`quest/probe_*.js` 离线探针用）。
- 记忆体/文档同步：`CLAUDE.md`（铁律 1 重写 · §4.1 三件套 → 只留读值口 · §5.3 整节标作废）· `CLAUDE_REFERENCE.md`（出包改**人工核对** · 新增模块降到两处 · 自查清单）· `DEVELOPMENT.md`（提交前只跑 luacheck）。

**质量声明**：`node luacheck.js` ⇒ `SYNTAX OK: 39 files checked`（测试删除后文件数 43 → 39）。★**本版三处 UI 均未在真机实测**（放大镜入口 · 小地图图钉 · 两个捕获层）—— 改盘后需 `/reload` 才生效；发布包条目数按 `.toc` 现算**人工核对 = 75 条**（本版未增删模块）。


## 🗺 v1.75.5 — 探索层原值只在会话内存 · 关图即清空 · 每次开图重新读并重新缩

**① 原值只在会话内存，存档里一条都不留（用户提问定案）**

- 用户原话：「这个探索层每个地区的定位是从缓存读取的吗?理论上每次打开地图都要重新计算的，不需要缓存区记录这个定位信息?」
- 结论：**不缓存**。客户端每次开图都会自己重摆这批叠加层（同一纹理名在不同地图上是**完全不同的矩形**），存盘只会带来脏值 —— 真机存档里 8 条原值有 **6 条完全相同**（把「还没布局 / 本图不用」的残留几何记了下来）⇒ 用户报的「坐标丢失 + 全挤在左下」。
- 落地：① 记录只活在 `SMFIT.recMap[地图身份]`（会话内存，切走存回 / 切回取出 —— 不记的话切回时会把**折后值当原值再折一遍**）；② 老存档键 `mapFitOrig` / `mapFitVer` **一次性清掉**、**不再写版本戳**（判据 = 「老键还在就丢」，天然幂等）；③ `smFitApply` 只读记录、**绝不记原值**；`smFitRestore` **绝不清记录**。

**② 关图即清空 · 开图重新读原值并重新缩放（用户要求）**

- 用户原话：「**每次地图关闭都把原值数据清理,不要保存这个值.每次打开地图.都重启获取原值.重新缩放大地图**」（前一句是「每次打开地图都当做是第一次打开」「每次 reload 插件载入也都当做第一次」）。
- 落地：清空动作放在**关图那一刻**（`smFitDropOnClose`）—— 地图不可见、不闪，也不跟客户端的版式计算抢；开图时记录天然是空的 ⇒ 直接走既有的「本图还没有原值 ⇒ **先不缩放**、在自然档读一次 → 落闩 → 套缩放」= 重新获取 + 重新缩放。
- **顺序铁律**：先**把折过的几何还回自然档**、再清空；而且还回要**按本档口径严格自验**（`|读回 − 原值| ≤ 0.75`）—— 两种读回口径在数值上长得一样（读回 70 既可能是「级联客户端还回成功」，也可能是「不级联客户端还回失败」），只信 `smFitRestore` 的宽松判定会把后者放过 ⇒ **验不过就绝不清空**（保留记录 + 如实播报，宁可退化成老行为，也绝不把折后的值当原值 = 折两遍）。
- 门控与回退：总开关 ∧ 叠加层适配 ∧ **折算档**；真值 `SM_CFG.mapFitFresh`（**nil = 开**），回退开关 `/ehm mapfit fresh on|off`。★`/reload` 本来就等于「第一次」（原值只在会话内存）⇒ 载入后首次开图必然重读。

**③ 修「原值为 0 也定义为原值抓取完成」（用户报障）**

- 用户原话：「**/reload 插件载入的时候为什么原值为 0 也定义为原值抓取完成?这个流程本身就不对**」。
- 根因：`todo`（待抓清单）为空有**两种完全不同的来法**，旧代码一视同仁地落闩 —— 「目标都已有记录」该落闩 ✓；但「**候选一个都不在用**（客户端还没摆版式 / 还没贴图）」是**一条都没读到**，落闩就等于**以后再也不会抓** ⇒ 地图缩了、探索层一个几何都没折（用户看到的错位）。
- 修法：落闩以「**确有记录**」为前提；没有记录就**绝不落闩**，交给 **1 秒一次的有界重试**等客户端把叠加层摆出来（真机实证：随后自动补抓到 8 条、落闩）。窗口收口的措辞也**必须与实际条数挂钩**（0 条时改说「还没读到」，不再报「已抓到原值」）。

**④ 抓原值不再每帧重跑 · 地图缩放不再抖（1.75.4 事故收口）**

- 加严候选筛选（**先筛候选、再动缩放**）· 空候选**当场落闩** · 有界重试（`SMFIT_CAP_GAP = 1s`，绝不每帧）· 瞬态 `es=1.000` **不折算**（同时主动把缩放掰回去）· 折算读回**两种口径都认**（写过一次之后永远判「已对齐」⇒ 不再每拍重写、不再刷屏）。
- 新增**取原值窗口**：本图还没有原值 ⇒ **先不缩放**、在自然档读一次（有界 `SMFIT_HOLD_SEC = 2s`）；先不缩放就不必翻转外框、也不必押注 ÷es 口径。★**不折算的档不许开这个窗口**（否则 nofold 的地图会白满尺寸 2 秒）。

**⑤ 修「探索层贴图跑到游戏画面上」（用户截图）**

- 根因：写锚点时把记录里的相对帧**名字（字符串）或 nil** 直接丢给 `SetPoint` —— 本客户端对「字符串 / nil 相对帧」的处理是**锚到屏幕（UIParent）** ⇒ 这些纹理逃出地图窗口、画在游戏世界上（而 `smFitRestore` 那条路一直是解析成对象的，所以「关功能」能救回来 —— 两边行为不一致的真因）。
- 修法：写锚点**必须先解析成帧对象**（记录里的活对象 `relObj` → `_G[名字]` → 兜底父帧），并把活对象一起记进记录。

**⑥ 新增手动适配命令 `/ehm fitnow`（别名 适配 / 修正）+ 播报分两档**

- `/ehm fitnow`：按用户指定的过程**逐步打印** —— 开图（`ShowUIPanel`，本项目纪律：不用 Toggle）→ 外框置回**自然档 1.00** → **等满 2 秒**才读原值 → **逐条打印原值** → 套缩放并打印「设置值 / 自身 `GetScale` / 父链连乘」→ 折算并**逐条打印「原值 → 写入」**；期间 `smFixActive` 让自动逻辑**整段让位**，跑完（**出错也**）必放掉。
- 播报 = **安静档（默认）+ 详细档**：安静档每次开图只留 2 行（「已读到 N 条 ⇒ 落闩」+ 本图首次「折算已对齐」）、关图 1 行（「已清空本图原值 N 条、M 个几何已还回自然档」）；逐条原值 / 第 N 次尝试 / 每次重试与折算细节 = `/ehm mapfit verbose on`（**不跨会话**，`/reload` 自动回默认关）。★真出问题（读不到 / 还回失败 / 窗口结束仍没读到）**必须出声**且各只一次；两档都照旧写进取证环 `mapFitTrace`（安静档只是不上屏）。
- 同时修：地图设置面板的值 ↔ 滚轮**双向同步**（统一走 `smPanelSync`）；`/ehm mapfit diag` 的 `bad argument #8 to 'format'` 崩溃（`ready` 在 `local` 之前被引用 ⇒ 走 `nWaitDiag`）。

**⑦ 判据与验证**

- 新增断言 **组 255**（手动适配五步：1.0s 不读原值 · 逐条原值 · 三个缩放读数 · 逐条「原值→写入」· 让位期间主 tick 零动作 · 跑完必放掉让位 · 自动路径的抓原值**进聊天框**）、**组 254**（一次抓齐即落闩 · 30 拍零 SetScale · 瞬态不折算 · 两种读回口径 · 有界重试 · **取原值窗口**）、**组 256**（**关图即清空**：先还回再清空 · 开图重读仍折出 70 而非 49 · 默认开/可关 · nofold 不动 · 还回失败安全阀 · **0 条不算抓到 + 自动补抓**）；`SM MAP KEY CHECK` 扩到 ⑯⑰⑱；`SM GROUP ROSTER CHECK` 组号清单加 255/256。

**质量声明**：`node luacheck.js` = `SYNTAX OK`（43 个文件逐个解析）· ★按用户要求（「不要跑测试，最高记忆记住」），本轮 **`test_engine.js` / `check.js` 一律未跑**，所以涉及清单/结构的判据（组 253~256、`SM MAP KEY CHECK`、`README TABLE` / `MILESTONE` / `PACK LIST`）**是按推理写的、未跑验证**；发布包条目数按 `.toc` 现算**人工核对 = 75 条**（与 1.75.1 基准一致 —— 本版未增删模块）。


## 🎯 v1.75.4 — 稀有转播=通知（不受总闸门）+ 探索层原值带地图身份

**① 稀有提醒转播 = 通知，不是调试日志（用户报障「无法检测到稀有通知」的定案）**

- **真因**（读存档实证）：转播自己的开关是开的（`rareWatch = true`），而配置项「**调试日志**」= **整个插件往聊天框说话的总闸门**（`cfg.log.on = false`）把 `say` 与 `logLine` **一起静音** ⇒ 转播一行都不出、**日志环里也没有任何 `[稀有转播]` 记录**（两个证据同时消失 ⇒ 看着像「插件失效」）。而任务插件那边机制没变（`RareAlert:Show` 签名未变）。
- **修法**：转播改走 **`EVAL_SAY_FORCE`**（Core 的**不门控**出口，与「日志开关自己的确认行 / 引擎未加载兜底」同一族），并**只受自己的开关** `cfg.rareWatch` 管。★**总闸门语义一个字不改**：其余所有 `say` 照旧被静音（断言组 252① 有反向哨兵钉死）。
- **配套三件**：① **专属取证环** `cfg.rareProbe`（有界 30 行、**不门控**，随存档落盘）—— AI 侧读存档即可判「有没有转 / 走的哪个出口（强制 or 受门控）/ 回调有没有抛错」；② **接线自检**（状态命令里比对 `EVAL_RW.showRef`：我们的包装被人重载/被别人再包一层 ⇒ **当场重装**并如实播报）；③ 回调**异常如实计数 + 落盘**（原来 `pcall` 把异常静默吞掉 = 排查盲区）。
- **兼容对方的测试入口改名**：`Test` 优先、旧名 `TestNearest` 也认（原来只认旧名 ⇒ `/eh go 稀有 试` 这条自证命令**静默失效**，排障时还会误导用户「测试没触发」）。

**② 缩放大地图·探索层：原值必须带「地图身份」（用户报障「坐标丢失 / 全部图层挤在左下重叠」）**

- **根因**（真机存档 + 客户端同源实现双证据）：`WorldMapOverlay1..N` 是客户端**按序号逐图复用**的纹理 —— 每次开图按当前地图的叠加层列表**重新分配**，**本图用不到的会被 `:Hide()` 且不清几何**（留着上一张图的矩形）⇒ **同一个纹理名在不同地图上代表完全不同的矩形**。而旧实现把「原值」**只按纹理名**记一份、永不失效（存档里 8 条有 **6 条完全相同** = 把「还没布局 / 本图不用」的残留几何记了下来），再**每次开图无条件写回** ⇒ 客户端刚摆好的坐标 0.3 秒内被顶掉、且本图不用的层全停在同一格。
- **修法五条 + 存档自愈**：① 原值**按地图身份分桶**（`GetMapInfo` 的**文件名 + 纹理尺寸**）· **换图当场作废**内存记录并重抓；② 只碰**本图在用**的层（`IsShown()==false` / `GetTexture()==nil` 一律跳过；★**读不到就放行** —— 判不出不拦）；③ 抓原值**等版式稳定**（`SMFIT_SETTLE = 0.4s`；开图那一瞬抓到的就是残留几何）；④ 客户端自报 `GetNumMapOverlays()==0` ⇒ 一个几何都不碰；⑤ 折算**逐层记账**（`wroteKeys[名] = 地图身份`）⇒ 还原**只还本图写过的层**。★存档版本戳 `mapFitVer` **2 → 3**：旧污染**整块丢弃、自动按图重抓**（用户不用手动清存档）。
- **新增只读取证清单 `/ehm mapfit dump`**：地图身份 / 客户端 `GetMapInfo`·`GetNumMapOverlays`·`GetMapOverlayInfo` 真值 / 每层「显示 · 贴图 · 本图在用 · 记录 · 本图已写」——「谁写的、该是多少」一眼可判。

**③ 判据与验证**

- 新增断言 **组 253**（换图重抓 · 折算用本图矩形 · 隐藏/没贴图层零几何 · **种一条脏记录**后仍零几何 · 零叠加层零动作 · 还原只还写过的层 · 版本 2 自愈 · 只读清单）+ 新增源码检查 **`SM MAP KEY CHECK`**（9 条）；`SM GROUP ROSTER CHECK` 组号清单加 253。
- **变异验证 4/4 全部被抓**，其中两条是**当场漏网 → 补判据**：① 去掉 apply 的「本图在用」闸门时，只查「调了 `smFitInUse`」不够（判据必须**真的闸住读写块**）+ 必须**先种一条脏记录**（= 用户那台机器的真实状态）再验零调用；② `smFitNewMap` 不清记录时第一版测不出来（换图前 `SMFIT.rec` 还是空的，它由 apply 采纳时才填）⇒ 补「**先在本图折一轮**再换图」。
- 稀有转播侧：**组 252** + `RARE WATCH WIRING CHECK`（通知走强制出口 · 常规出口仍受控 · 认新入口 `Test` · 回调异常落盘 · 接线自检自愈）。

**质量声明**：`node luacheck.js` = `SYNTAX OK`（43 个文件逐个解析）· `node test_engine.js` = **exit 0 / ALL TESTS PASS**（含全部源码检查）· `node check.js --full`（任务线生成物 + 数据审计闸门）通过。

## 🎯 v1.75.3 — 选取目标一族 + 修「自动攻击抢目标」+ 围攻人数条件 + 任务线数据补完

**① 选取目标一族：`玩家的目标` · 条件「目标玩家:X」· 条件「目标死亡」**

- 「选取目标 → **玩家的目标**」= `AssistByName(名字)`：与「指定名称」**同族**（同一份 `TARGET_SEL` 表 · 同一个名字下拉 · 同一个自定义输入弹窗，**只差客户端函数**）。官方明文「Assists a **nearby** player: sets the target to that player's target」，**无 Protected 行** ⇒ 插件可直调（先核文档再动手）。
- **失败必须如实**：官方只承诺「附近」，名字不在附近/不存在时**是否清目标文档没写** ⇒ 用 `UnitExists("target")` 兜底 —— 调用后没目标 ⇒ 条件级 `return false, "…未生效"`、技能级 `return false` + 如实日志（不兜底 = 「没协助到也放行、后面那手打在旧目标上，日志却写着成功」）。
- 新增条件「**目标玩家:X**」（名称支持自定义输入 · 是/否；**未填名 / 无目标 / 不是玩家一律如实失败**，连「否」方向都不放行 —— 否则「查不到」被当成「不是他」）与「**目标死亡**」（布尔类；★**没有目标时「否」成立**，口径与「目标存在/可攻击/可流血」一致）。
- ★英文 id 形态（`byName=` / `playerTarget=` / `assistbyname=` / 裸 id）改在**解析入口**归一 —— 原先那两条写在 `if tg then` 里面 = **死代码**（`byName=嗜血者` 连门都进不来）。

**② 修「没按键目标也在尸体与活怪之间来回跳」（用户真机复现 → 插桩取证 → 定案）**

- **根因**（用户最终确认是「自动攻击」那一支）：`EVAL_GO` 里「补自动攻击」① **不看目标能不能打**（只看「攻击格当前没在自动攻击」+ 2 秒节流，目标是尸体也照按）、② 位置在**规则评估之前**；而本客户端的「攻击」= `AttackTarget()` **会顺带切目标**（官方文档只写 Toggles，**实测会切**）⇒ 每一发的顺序成了「先被它把目标切走 → 再被 选取目标:最近敌人 切回来」。
- **修法**：**目标守卫**（实时 `UnitExists` + 非 `UnitIsDeadOrGhost` + `UnitCanAttack` ⇒ 尸体/无目标/不可攻击 **一次都不按**；★不用 `st.canAttack`：规则可能刚切过目标，那是这一轮开头采的**过期值**）+ **挪到规则之后**（`if acted then return end` 之前，出手了也照样补）；守卫不过时**不消耗** 2 秒节流（目标一变可打就立刻补）。
- **配套调用点取证**（新）：唯一调用口 `tselInvoke`（技能行 / 条件 / 队伍命中 / 队伍候选 / 队伍·名字枚举还原 **全走它**；源码检查 `SEL TRACE WIRING CHECK` 禁掉裸 `pcall(fn…)`）· 读值口 `EVAL_TSEL_PROBE()` · 命令 **`/eh go tsel log [行数]`｜`clear`** · 落盘 `cfg.selProbe`（有界 40）。每条 = 「**谁调的** + 前 ⇒ 后 目标快照（名字/血量%/是否尸体）+ 第几发按键轮 `p<N>`」，另记「接受发 / 被去抖丢弃」两条腿。
- **顺带查明的机制**（已写进记忆）：**「没按键却在跳」只有一种可能 = 调用在源源不断地来** —— `/run` 宏走 **RunScript pending 队列**，松手后旧调用仍按客户端自己的节奏冲刷（真机实测 13.6 秒 51 发 ≈ 3.75 次/秒），而去抖窗只是「两次被接受调用的最小间隔」：**冲刷节奏大于窗口 ⇒ 每一发都放行**。

**③ 新条件：`围攻自身数量` · `10s 交战人数`（配近战探针）**

- 数据源 = 聊天正文里的「X 击中你」/「你击中 X」（**官方索引里根本没有 Events 页** ⇒ 事件名只能真机实测）；**只统计战斗状态**（非战斗恒 0）。「围攻」按怪名分组 + 攻速档位（1.5 / 2.0 / 2.5）估算，与名字数**取最大**、封顶 8 —— 计算只在 `EVAL_MW_ACTIVE_INFO` **一处**，`UPDATE_STATE` 每拍写 `st`，条件与两个 UI **只读**。
- 取证命令：`/eh go melee 开始|停|报告|周围|通道|截获|全事件`；战斗信息 UI 与状态信息 UI 各有一行「围攻 / 交战」。

**④ 任务线 & 装备：全量数据 + 过滤 · 占位 · 请求泵**

- 新增 `quest/QuestBulk.lua`（**全量任务/装备数据**，生成物）；任务线列表条数上限改 **0 = 不限**（等级升序下不再静默砍掉高等级线）。
- 装备「类型」改**多选 + 武器子类型**；任务线视图加**等级过滤**（两视图共用一份档位）与**种类筛选**（与装备视图同源）；缺图**画占位（宏 961）并置灰 + 点击优先刷新**；未缓存物品的「向服务器要」泵**常驻开启**（★帧一 Hide，OnUpdate 就不触发 ⇒ 显隐管线漏一处 = 泵永远停摆）。
- 顺带修：任务线详情**标题压住副标题**（标题锚在 36 高大图标上 = 垂直居中）、底部按钮顺序改 **[上页] [下页] [← 返回]**（整体左对齐）、图片内输入框照主搜索框配方逐条补齐。

**⑤ 其他**

- 工具箱两行改名：**「图层拖拽柄」→「图层拖拽」**、**「图层特殊处理」→「图层隐藏」**（只动语言包的**显示值**与模块内播报前缀；键名 `TB_DFFIX`/`TB_DBGDRAG`、模块名 `LayerFix`、存档键 `layerFix.fix` **一个都不动**）。
- 追踪状态真机取证（本客户端只有三条 API、**没有枚举接口**）与**追踪类型条件**（13 条实证候选表 · 纹理全等比对）；宏图标号→图标取证命令、宠物家族探针、小地图按钮图标默认**头龙** + 贴图接线自愈。
- 测试基建：新增/扩写断言 **组 240–251** 与多道源码检查（`SEL TRACE WIRING` · `TRACK ICON` · `PET FAMILY` · `MINIMAP BTN` · `CHAT GATE` · `SM POS CENTER` …）；记忆体 `CLAUDE.md` / `CLAUDE_REFERENCE.md` 压缩重排（**锚点一个不丢**，完整性自检脚本守着）。

**质量声明**：`node luacheck.js` = `SYNTAX OK`（43 个文件逐个解析）· `node test_engine.js` = **exit 0 / ALL TESTS PASS**（含全部源码检查）· `node check.js`（任务线生成物闸门）通过。

## 🎯 v1.75.2 — 追踪条件 + 聊天输出总闸门 + 缩放大地图探索层折算收口

**① 采集/生存类条件：`自身状态 → 追踪类型`（下拉单选）**

- 本客户端**只有三条追踪 API**（`GetTrackingTexture` 读状态 · `GameTooltip:SetTrackingSpell` 读本地化名字 · `CancelTrackingBuff` 取消），**没有枚举接口**、小地图圆点读不回来、事件名官方未公开 ⇒ 判据 = **纹理归一后与 13 条实证表全等比对**（`/Game/Interface/Icons/<Base>_TEX.<Base>_TEX`）+ 名字备用路互证。
- 落地：`condOne` 的 `tracking` 分支（导出 `追踪:beast` / `未追踪:beast`，界面显示本地化标签）；候选表由 `TRACK ICON CHECK` 逐条钉在本机图标清单上，`TRK_*` 三语 17 键齐全。
- 取证：`/eh go 追踪探针 [监听|表|存档|清空]`。

**② 聊天输出总闸门：配置项「调试日志」（原名「记录调试日志」）**

- `say`/`EVAL_SAY` = 插件往聊天框说话的**唯一出口**；关掉 ⇒ 模块播报 / 命令回复 / 探针输出**一个字都不刷**，同一判据也管日志环。
- 例外只两类，走常开的 `EVAL_SAY_FORCE`：**日志开关自己的确认行**与「引擎未加载完整」兜底 —— 否则关掉后再也看不到「怎么开回来」。
- 模块自建打印口（`tools/SimpleMap.lua` 的 `P`）读 `EVAL_CHAT_ON()`；配置项与「方案技能日志」不再撞名。

**③ 缩放大地图：探索层（`WorldMapDetailFrame` 12 号之后那批纹理）折算收口**

- 真机实证：本客户端几何**读回含父帧缩放**，而 `GetEffectiveScale` **会滞后**（第一次开启读 1.00、关开一次才读 0.70）⇒ 折算系数改走**父链连乘**。
- 四条防线：**关掉零动作**（闸门在任何读/写之前）· **折算前先探测**（`smFitDetect`；策略 `fold`（默认）/`auto`/`nofold`）· **原值只在自然档抓一次**、记过不重记、**关→开不连乘** · **关闭收钩子 + 开启再武装**（`featTeardown`/`featRearm`：拖拽柄 / 坐标行 / ESC 列表项）。
- 取证：折算取证环 `simpleMapCfg.mapFitTrace`（60 行小环，**不受「调试日志」开关管**）＋ `/ehm mapfit [on|off|restore|diag|trace|mode …]`。
- 顺带修：模块在**文件执行期**缓存 `EVAL_HELP_CONFIG.simpleMapCfg` = 孤儿表 ⇒ 设置与「原值」**一个都没落盘**（真机证据：存档里 `tb.simpleMap=true` 在、`simpleMapCfg` 整块不存在）⇒ 改**懒代理**。

**判据与质量声明**：新增断言 **组 229 / 231 / 232** ＋ 源码检查 `TRACK ICON CHECK` / `CHAT GATE CHECK` / `SM FIT CASCADE CHECK`；`node luacheck.js` 与 `node test_engine.js` 均 exit 0（关键判据做过变异验证）。

---
## ☠️ v1.74.6 — 自身/目标 debuff 支持「负面类型」

**用户原话**：「查看自身/目标debuff 条件类型配置项. 需要参考队伍debuff 配置支持 debuff 负面类型功能」。

**改前**：「负面类型」（魔法/诅咒/毒/疾病 + 任意）只有**队伍 / 团员 / 候选者 debuff** 有；**自身（`pDebuff`）/ 目标（`hasDebuff`）**
的解析不认 `名(类型)`、求值也完全不看类型，编辑窗那一格也不出现。

**改后（四族同一套写法与语义）**

| 环节 | 做法 |
| :-- | :-- |
| 状态层 | 层数表**保持数字**（`st.playerDebuffs/targetDebuffs`，既有读侧一字不改）；类型另存**平行表** `st.playerDebuffType/targetDebuffType`（`UnitDebuff` 第 3 返回） |
| 解析 | `auraStackSplit()`：**先剥层数后缀再拆名字/类型** ⇒ `断筋(魔法)` · `断筋(魔法) >=2` · `魔法` · `魔法 >=2` 全认；**没写括号时与旧行为逐字一致**（存量文本一个字节不变） |
| 求值 | `auraCountOf(..., 类型表)`：**名字对上但类型不符 = 没有**；**名称留空 + 类型 = 「有任意该类型」**（层数取命中项最大值 ⇒ `>=1` 是存在性、`>=N` 是「有一条叠到 N 层」） |
| 导出/显示 | **同一份** `dtSuffix()`：导出英文 token（`(Magic/Poison)`）、界面显示本地化名（`(魔法)`）；顺手补上 `candDebuff` 原先**漏掉的类型后缀**（它以前存不住类型） |
| 编辑窗 | 类型格对 `pDebuff`/`hasDebuff` 也 Show；**buff 行不许有** |

**判据**：组 181（解析 / 存量导出逐字不变 / 类型过滤 / 名称留空=任意该类型 / 往返稳定 / 编辑窗三态）；
**变异 M181**（拿掉「类型不符=没有」那一行）→ 当场 `ASSERT FAIL [③★★★名字对上了但类型不符 → 不命中]`。

**已知边界**：编辑窗光环名下拉**没有「清空回任意」这一项**（自由文本空串被忽略）⇒「名称留空 + 类型」目前靠默认空名或文本导入来配。

## 🎬 v1.74.5 — 分享「场景描述」+ 🧰 两个工具助手（猎人一键喂食 / 消耗品）

**① 分享「场景描述」**（用户：「在接收方案完成之后再加一句模版：<头衔> 根据当前所在<地点>、<有无目标> 做场景描述（背景=魔兽世界）」）

- 分派 = `[导入]` / `[忽略]` × `UnitExists("target")` ⇒ **4 张表各 10 条 × 三语言**（地点/目标永不写死，只用魔兽世界通用意象）；
- 占位符优先级：① 头衔（句首，**按档位染色**）② 地点（`GetRealZoneText`，拿不到 → 「艾泽拉斯」）③ 目标（只在「有目标」两张表里，目标消失 ⇒ **整条不发**）；
- ★★**形态铁律**（用户实测「信息没生效」后定案）：客户端**不画「有色码但没有链接」的消息** ⇒ 头衔必须包成与封皮 A 行同款的 `|HEHPF:<id> 0/1:0|h[<头衔>]|h|r` 链接；整条**只许一段色码**；
- 防刷屏：进同一个限频队列（≥1s 滴一条），绝不与任何东西同帧倾泻。

**② 方案「作者 / 来源」四类**（用户：「增加一个作者类型…在方案列表 tooltip 显示方案基本信息、评级、作者」）

自创（**只显示「自创」**，实际值仍是你自己的名字）· 案例模版（`技能学院`）· 别人分享（发送者名）· 导入（文本粘贴）；
三处导入点各自盖章（手建 / 模版选单 / 分享弹窗的 [导入] 按钮）；方案列表 tooltip = 方案名 + **评级**（品阶名 + 分数）+ **来源** + 技能数。

**③ 分享「不弹窗」的 8 条分叉全部落日志**（用户：「现在为弹窗原因在日志内显示」）

统一前缀 `[分享] 不弹窗：` —— 接收开关关着 · 载荷不完整 · 分片超上限 · hex 还原失败 · 重复收齐 · 暂未收齐 · 超时丢弃 · 在途超上限。
（此前只有「弹窗 / 方案过大 / 自己的回声」三条落盘，而**最容易踩的「接收开关关着」反而完全静默**。）

**④ 彩蛋「角色扮演反应」不再接线**（用户：「取消彩蛋」→ 随后澄清「不是把彩蛋功能删除」）

[导入] / [忽略] 不再发反应句（改发场景描述）；**函数 / 频道表 / 抽奖 / 名链接 / 三语言 50 格文案全部保留**，
`SH FUN CHECK` 现在钉「机制都在 **且** 调用点未接」，组 82④⑤ 是**反向哨兵**（点了不许发出任何 `SH_FUN_*` 句子）。

**⑤ 工具箱两个助手**（用户：「参照以上宠物喂食助手. 添加个消耗品助手」）

**猎人助手 → 一键喂食**（`tools/HunterHelper.lua`）与**消耗品助手**（`tools/ConsumableHelper.lua`）共用 8×N 图标网格件
`tools/IconGrid.lua`（分页 · 每格 tooltip · 单选/多选两种模式 · 空态如实提示）；消耗品助手 = 主图标（第 1 个）**左键用**、右键开面板，
选中的其余物品在旁**横排小图标**（左键用、右键取消选中），**用光变灰**；未实测的功能在列表右侧标**黄感叹号「待测试」**。

**⑥ 修「回声忽略」误判**（用户报「盗贼方案分享无弹窗，其他正常」）

旧判据 = 「接收方库里有没有这份方案」⇒ 只要你有同款，就把**别人发的**也当自己的回声吞掉；
改为「**发送者是不是本机玩家**」，并加取证探针 `/eh go 分享事件` ⑤（EHPF 原文 + 走到哪一步）。

**⑦ 案例模版**：武器战模版按用户截图重写为 10 行（姿态 / 选取目标 / 冲锋 / 压制 / 战斗怒吼 / 断筋 / 撕裂 / 血性狂暴 / 猛击 / 英勇打击）。

## 🐞 v1.74.4 — 修「对方已入队却仍显示邀请队伍」

**用户截图**：右键一个**已经在我队伍里**的人，菜单里「邀请队伍」和「踢出队伍」**同屏出现**（自相矛盾）——
理论上已入队的人不该再给「邀请队伍」。

**根因**：1.74.1 给「邀请队伍」写的条件是「**不在队伍 或 我是队长**」——它只看「**我**」的状态，
**没看被右键的人是不是已经在我队伍/团队里**。你是队长、对方又已入队时，这个条件照样成立 → 邀请队伍冒出来。

**修法**：条件补上「**对方不在我队伍/团队里**」⇒ 现在是「**对方不在我队伍/团队里 且 （不在队伍 或 我是队长）**」。
已入队的 → 邀请队伍藏掉（踢出队伍/离开队伍照常）；没入队的 → 单人可邀、队长可再邀、非队长不可邀，照旧。

**判据**：组 130 ⑥e2 反转（队长 + 右键**队友** → 邀请队伍**藏掉**；队友已入队不该再邀）；变异 M568 捕获。
## 📥 v1.74.3 — 修「队伍频道收到分享不弹窗」：接收侧补注册**队长版事件**

**用户报**：方案分享的接收方，在**队伍频道**收完方案后**不弹分享窗**。

- **【根因】**1.12 里**队长 / 团长**发的队伍 / 团队消息走**独立事件** `CHAT_MSG_PARTY_LEADER` / `CHAT_MSG_RAID_LEADER`，而接收帧原来只注册了 `CHAT_MSG_PARTY` / `CHAT_MSG_RAID` ⇒ **发送者是队长时一片分片都进不来**：接收端静默、发送端照样报「已发送 N 片」。
- ★★**频道名函数早就为这两个事件写了分支**（组 140 还在钉它「队长发言也算队伍」），**只是注册漏了** —— 正是本项目「接线漏接不报错」那一族 ⇒ 补注册之后必须**用源码检查守**。
- **补注册**：`CHAT_MSG_PARTY_LEADER` / `CHAT_MSG_RAID_LEADER`（走 `pcall`：万一客户端不发这个事件，也只是少收一类消息，绝不影响载入）。
- **新增取证命令 `/eh go 分享事件`**（`EVAL_SHARE_RECV_PROBE`）：① 接收方案开关（**关着时任何频道都不弹窗**，最容易踩的那条）② 七个事件逐个 `IsEventRegistered` ③ **最近真收到的事件名**（真机发的是 PARTY 还是 PARTY_LEADER，10 秒定案、不靠猜）④ 待导入 + 接收中缓冲笔数。
- **判据**：行为组 175（① 七个事件**真的注册上了**（问帧 `IsEventRegistered`）② 七个频道**逐个喂真分片**都要弹窗、点名发送者、标对来源、解出内容 ③ 诊断命令真的接上并列出队长版事件与接收开关）+ 源码检查 **`SHARE RECV EVENTS CHECK`**（七事件 + `SH_EV_ALL` 单一来源 + 命令接线 + 最近事件名记账）。
- **变异**：M571（删掉队长版注册）/ M572（诊断命令断线）全捕获。
- ★★★**过程中抓到我自己写的一条弱判据**（写下来省得再犯）：第一版 `SHARE RECV EVENTS CHECK` 是「在注册段里查有没有这个事件名字符串」——而**同一段里就有单一来源表 `SH_EV_ALL`，它含全部七个事件名** ⇒ 删掉注册行照样绿（**M571 实测 SURVIVED**）。改成**逐条在 `RegisterEvent` 那一行里找**才捕获。这正是本项目记过的「**含 X** 的断言会被同段其它示例顶住」那一族 —— 判据必须钉**调用本身**，不能钉「整段有没有这个词」。

## 🖱 v1.74.2 — 战斗信息UI 技能格：左键 = 切换启用/停用 · 右键 = 技能配置弹窗

**头条**：战斗信息UI（/eh ui）方案技能行的技能小图标，原来**任何点击都开技能编辑窗**；按用户要求改成 **左键 = 快速切换启用/停用**（就地切，不用进配置窗），**右键 = 打开该技能的配置弹窗**。

- **左键写配置真值** `r.enabled`（不是只改显示），并如实播报「技能 X = 已启用/已停用」（三语言 `TIP_ON`/`TIP_OFF`）；切换后**双刷新**：`EVAL_WAR_TAB_REFRESH()`（配置窗技能列表的勾选走同一份真值）+ `EVAL_HELP_UI_TICK()`（格子立刻重画「停」/恢复亮起，不等 0.15s 心跳）。
- **右键**走 `EVAL_HELP_SE_OPEN(prof, ci)` 开**被点那一格**的技能，且**不顺手切换状态**；格子补注册 `RightButtonUp`（光注册左键 = 右键永远到不了分派代码）。
- **tooltip 操作提示**三语言同步改（`TIP_CLICK`：左键 = 启用/停用 · 右键 = 打开编辑窗）。

**判据**：行为组 174 —— 全走**真实 OnClick**（左/右键分派就写在那段闭包里，直接调动作函数测不出接线断了）：① 左键停/启写配置真值可反复 ② 停用格左键恢复 ③ 右键开弹窗且开的是被点那格（读编辑窗当前技能名）④ 右键不改状态；源码检查 **`UI CELL MOUSE CHECK`**（① 注册 RightButtonUp ② 右键→SE_OPEN ③ 左键写 r.enabled ④ 双刷新）。
**变异**：M568（删掉右键注册）/ M569（分派写死 if false）/ M570（删掉 r.enabled 写值）全捕获。

## 🖱 v1.74.1 — 队伍菜单条件修正 + 新增「离开队伍」（顺带同步界面预览图）

**头条**：把聊天里角色名右键菜单的**队伍三条**按官方 API 语义改正 + 补齐 ——
「邀请队伍」从「只在不在队伍时显示」改成 **不在队伍 或 我是队长**（修掉「队长在队伍里反而邀不了人」）；
「踢出队伍」从「是队友就显示（点了才说不是队长）」改成 **我是队长 且 被右键的是队友**（非队长根本不出现这条）；
新增「**离开队伍**」（只在队伍内显示，退队不需要权限）。

**API 核对**（本项目铁律：返工涉及 API 先核官方调用对不对）—— `api_unit.html` / 索引 1370 条实核：
判队长 `IsPartyLeader()` / `UnitIsPartyLeader(unit)` / `GetPartyLeaderIndex()`；枚举成员 `GetNumPartyMembers()`（不含自己、团队里回 raid-1）+ `party1..party4` + `UnitInParty(unit)`；离开 `LeaveParty()`；踢人 `UninviteByName(name)`（**没有** `UninviteUnit`/`KickFromParty`/`IsRaidAssistant`，助理踢人无法判定 ⇒ 如实接受边界）。

**顺带**：`preview/` 界面预览图同步（删两张过时截图、新增「抓宠帮手」一节两张图），三语言 README 跟着改；`PACK LIST CHECK` 指向参考卷。

**质量声明**：`node luacheck.js` → SYNTAX OK（31 个 .lua）；`node test_engine.js` → ALL TESTS PASS（组 130 ⑥d/⑥e/⑥e2/⑥e3 + 组 173 + 「离开队伍」动作断言全过）。
## 🎉 v1.74.0 — 分享系统重做：封皮 · 品阶 · 头衔 · 彩蛋 · 角色扮演反应（跨越 v1.73.35 ~ v1.74.0）

**头条**：把「分享方案」从「能发出去」做成了**一整套有品阶、有身份、有彩蛋的社交系统** ——
分享出去的方案带上**品阶封皮**（`[品阶秘籍·名]` 可点链接，点开弹窗确认再导入，不再自动导入）；
方案按**评分制品阶**（≤6 普通 / 7-12 稀有 / 13-18 珍稀 / 19-24 绝版 / **25+ 神级**，五色含深红）染色；
你还有一个**抽卡头衔**（5 档 × 15 张、按方案库稀有度晋升、只升不降、可自定义名号+名号色）；
达成条件的玩家触发**彩蛋「创世者亲临」**（一次落笔、永不可改）；
收到方案的人点 [导入]/[忽略] 时，按「**你的头衔档 × 秘籍档**」**抽奖式**回一句角色扮演反应（公会/说/队伍三频道），
反应句里的方案名还是**可点的品阶色链接**（点开接收页）。

**里程碑（1.73.35 → 1.74.0）**

1. **📤 分享封皮 + 接收弹窗**（1.73.41 → 1.73.67）—— 分片照旧限频滴出，封皮行**最后一条**发（`[品阶秘籍·名]` 可点链接）；
   点击弹窗确认（不再自动导入）；接收弹窗带品阶图标 + 评分；摘要行带上品阶色方案名（并删掉与之重复的品阶文字行）。
2. **🏷 品阶评分制**（1.73.35 → 1.74.0）—— 方案分 = 技能数 + 条件数；五档阈值（**25 分以上才算神级**）；
   五色（白/绿/紫/橙/**深红**）；品阶图标 + 符号；HUD / 配置窗 / 案例模版 / 分享封皮**同一套判定**。
3. **👑 头衔抽卡**（1.73.41 → 1.73.60）—— 5 档 × 15 张、按方案库稀有度晋升、每档只抽一次、只升不降；
   彩蛋名号自定义（一次机会、落笔即定）+ 名号色候选（帝王金/烈焰赤等）。
4. **🎭 彩蛋 + 角色扮演反应**（1.73.62 → 1.74.0）—— 彩蛋「创世者亲临」三条件闸门（手动方案≥3 + 其中一个神级 + 头衔满档；
   导入别人的不算手动；一次机会）；点 [导入]/[忽略] 按（头衔档×秘籍档）从 **50 格 × 10 条**里抽奖式挑一句发到来源频道，
   方案名是**可点的品阶色链接**（点它重开接收页）。
5. **🖥 标题栏徽标**（1.73.55 → 1.74.0）—— 战斗信息UI / 状态信息UI 标题栏同一套：玩家名 → 头衔 → 品阶徽标 → 当前方案名；
   方案行按品阶染色（HUD + 配置窗 + 案例模版同一套品阶判定）。
6. **🧪 取证体系 + 色码定案**（1.73.35 → 1.73.60）—— 分支探针（自定义链接过服务器 / 消息真实长度上限 250B / 悬停机制 / 落盘账本）；
   真机反复定案出「**色码发送规则**」：一段色码打头 + **必带链接**（无色码的链接打头不画）、限频 **1 秒/条**、只认 `[EHPF#]` 标识。
7. **🖱 右键菜单 + 引导**（1.73.35 → 1.73.60）—— 右键名字：悄悄话 / 邀请队伍 / 添加·删除好友 / 踢出 / 复制名字
   （邀请按**名字** `InviteByName`、先查队长不假成功）；载入引导窗三条路同一个窗；菜单两列/圆角/悬停美化。

**质量声明**：`node luacheck.js` → **SYNTAX OK（31 个 .lua 逐文件解析）**；`node test_engine.js` → **ALL TESTS PASS**
（18+ 道源码检查 + 行为断言组 1~170 + 每轮变异测试；变异 M1~M563 全捕获或如实记录等价变异）。

**安装**：把 `EvalHelp` 文件夹放进 `Interface/AddOns/`（或解压随附的 `EvalHelp-v1.74.0.zip`）。
## 🧰 v1.73.34 — 工具箱弹窗规程 + 聊天名字右键菜单（跨越 v1.73.0 ~ v1.73.34）

**头条**：把工具箱里的弹窗按项目规程做齐（**层级阶梯 / 拖动柄 / 列几何单一来源 / 滚动条**），
并给聊天里的角色名加**右键菜单**（悄悄话 / 邀请 / 目标 / 公会邀请 / 复制名字 —— 复制名字 = **只把 `/s 名字` 填进输入框，绝不代发**）。

**里程碑（1.73.0 → 1.73.34）**

1. **💬 聊天名字：着色 + 右键菜单**（1.73.12 → 1.73.29）—— 查清「名字在 `arg2`、名字槽**不吃富文本**」后走**方案A**
   （吞掉客户端那行、自己拼整行）真正染上职业色；类型色三来源 + 内置兜底（公会绿 = 从截图取色）；
   右键名字：悄悄话 / 邀请 / 目标 / 公会邀请 / 复制名字（**只预填不发送**）；菜单半透明窄条 + 高度自适应。
2. **🧰 工具箱弹窗规程**（1.73.30 → 1.73.34）—— 弹窗层级阶梯（配置 10 · 编辑 100 · 输入 220 · 工具箱 200 · 下拉 250）、
   拖动柄必须是 Button、列几何单一来源、滚动条 6 项（滚轮方向单一来源 `EVAL_WHEEL_DIR` · 专用滚动槽不压数据列 ·
   整数页码 · 到底=最后一页 · 边界隐藏箭头 · 链式接管）。
3. **🔋 条件名与语法闸门**（1.73.27 → 1.73.28）—— 资源名按 `UnitPowerType` **显示时现算**（法系=法力 / 猎人=集中值 /
   德鲁伊随形态），解析侧同步认全套写法（**导出再导入不丢条件**）；`luacheck` 从只查一个文件改成**逐个文件**（27 个 `.lua`）。
4. **🧯 静默失败族清理**（1.73.14 → 1.73.24）—— 隐形 tooltip 不再借用真身（不再清掉玩家正在看的物品提示）、
   写入口必须**读回确认**、名字在 `arg2`、颜色码必须 **8 位**、`/eh go 聊天 色测` 等取证命令。
5. **🐾 抓宠与界面批次**（1.73.0 → 1.73.11）—— 抓宠帮手（第 6 个 Tab）、图标语义名 + 关键字过滤、案例模版分组分列、
   工具箱两列、debuff 类型多选、滚轮方向统一。

**质量声明**：`node luacheck.js` → **SYNTAX OK（27 个 .lua 逐文件解析）**；`node test_engine.js` → **ALL TESTS PASS**
（18+ 道源码检查 + 行为断言组 1~137 + 每轮变异测试；变异 M1~M266 全捕获或如实记录等价变异）。

**安装**：把 `EvalHelp` 文件夹放进 `Interface/AddOns/`（或解压随附的 `EvalHelp-v1.73.34.zip`）。
## 🐾 v1.73.4 — 抓宠图标全部换成客户端真实路径

- **需求**（用户原话）：「完善宠物助手tab 内所有图标替换.先根据语义从系统图标文件内自己匹配对应语义相似的图标.」
- **★事实更正（推翻 1.73.0 的假设）**：本客户端的纹理路径是 **Unreal 资产路径** `/Game/Interface/Icons/<名字>_TEX`，**不是** 1.12 的 `Interface\Icons\<名字>`；写成后者不会被"不画"，而是显示成**引擎的「?」缺图占位**——用户截图里那一屏问号就是这么来的。
- **采集**：`/eh go icons`（1.73.3 新增）把**宏图标表**（`GetNumMacroIcons`/`GetMacroIconInfo`，本客户端唯一能把整表合法路径吐出来的入口）采集进存档，取出 **1018 条**路径 → 落成 `doc/图标路径清单.txt`（含用途说明）。
- **语义匹配**（逐条都在清单里核对过）：撕咬=獠牙、爪击=爪、突进=疾跑、俯冲=羽毛、冲锋/雷霆践踏/战吼=对应战技、潜伏=潜行、蝎毒=毒刺、尖啸=尖啸、甲壳护盾=甲壳、畏缩/低吼=畏缩/嘲讽、耐力/护甲=耐力/自然护甲、五种抗性=对应抗性图腾与防护。
- **家族**：客户端自带一整套 `Ability_Hunter_Pet_*`（狼/猫/熊/野猪/蟹/龟/鳄/蝙蝠/秃鹫/枭/猩猩/风蛇/蝎/陆行鸟/蜘蛛），16 族**一族一枚、不撞图**；「其他」用 `Ability_Hunter_BeastCall`。
- **生成器同步**：`gen_petdata.js` 里改成 `SKILL_ICON` / `FAM_ICON` 两张表 + `iconPath()`（重生成仍逐字复现 `PetData.lua`）。
- **判据**：**`PET ICON CHECK` 升级为真存在性校验**——每个 icon 必须①以 `/Game/Interface/Icons/` 开头、`_TEX` 结尾，②**逐条精确存在于** `doc/图标路径清单.txt`，③无扩展名，④技能间/家族间不撞图，⑤数量 ≥ 20；断言组 112 同步改为资产路径 + 「不再是问号占位」+ 家族一族一枚。变异 M58（不存在的名字）/M59（退回 1.12 老路径）/M60（家族撞图）/M61（少 `_TEX`）**全捕获**。

## 🖱 v1.73.3 — 滚轮方向统一 + 图标路径采集

- **用户实测**：「图标库的滚动监测鼠标滚动方向和滚动效果相反了.排查其他有滚动条是否也是相同问题。」
- **审计结论**：6 处滚轮里 **5 处本来就对**（一键宏方案技能列表 / 工具箱 / 数据检索详情 / 抓宠列表 / 战斗UI缩放的取值），**只有图标库是反的**：它写成 `d > 0 → 下一页`，而本客户端约定是**上滚 = +1 = 回到前面**（`off - d` / 上一页）。
- **根因（比 bug 本身更值得记）**：当时那条断言**用 -1 当「滚轮向上」**，与反向代码互相印证 → 反向 bug 被「验证」通过了。断言自己写错方向 = 本项目最恨的「绿但真错」。已改成正向 +1，并补了**下滚（-1）必须往后**的反向哨兵。
- **修法（一次性根治）**：`Core.lua` 新增 `EVAL_WHEEL_DIR(a, b)` —— **方向单一来源**，同时解决三件事：① 参数在 a / b / 全局 `arg1`（1.12 老写法）三种都要认；② 非数字（可能是 self）要判类型；③ **幅度归一成 ±1**（有的客户端给 ±120，按原值位移会一次跳 120 行）。6 处滚轮全部改走它。
- **判据**：新增源码检查 **`WHEEL DIRECTION CHECK`**（6 处滚轮必须都走 `EVAL_WHEEL_DIR`，且禁止「位移与方向同号」与旧的反向写法）+ 断言组 115（助手三写法/归一、图标库双向、抓宠列表双向与到顶夹取）。变异 M52（图标库再反回去）/M53（助手取反）/M54（绕过助手）/M55（位移同号）**全捕获**。
- **新增 `/eh go icons`**：把客户端宏图标表（`GetNumMacroIcons`/`GetMacroIconInfo`，本机磁盘取不到内置图标的唯一官方出口）的**全表路径**写进 `EVAL_HELP_CONFIG.iconDump`，`/reload` 后落盘成文件，供挑选/替换插件里的占位图标。成功/失败都计数、**不打印整表**（上千行会刷屏 + 撞反刷屏限流）、如实报告并提示 `/reload`。判据 = 组 116；变异 M56（只写条数不写列表）/M57（取失败不计数）捕获。

## ☠️ v1.73.2 — debuff 类型多选（含团队/团员 debuff）

- **需求**（用户原话）：「debuff 类型检测 包括团队debuff 类型要支持多选」——类型格原来只能选一个（任意负面/魔法/诅咒/毒/疾病）。
- **数据模型**：`cd.dt` 允许三种形态，**读侧归一无迁移**：`nil/""/"any"` = 任意；字符串 = 单选（**存量数据的原样**）；集合表 = 多选（命中任一即算命中）。★**空集 = 任意**（与 nil 同义，绝不能反过来变成「永不命中」）。
- **判定**：`dispelMatch` 增加集合分支（递归逐项比对），所有 debuff 判定点（自身/目标/队友/团员/候选者 + 队伍选取器）**一处改动全部生效**。
- **文本往返**：导出 `(Magic/Poison)`、导入按斜杠（顿号/逗号也认）拆开；★**单选导出与存量逐字相同** `(Magic)`——老配置导出不变（有反向哨兵守着）。
- **编辑窗**：类型格改**多选下拉**（面板不会点一下就关，可连着勾）；「任意负面」与具体类型**互斥**，勾选状态每次由数据重算并整表重绘（`EVAL_DD_SYNC`），不留两处勾。窄格（62px）只列 2 项 + …，**完整清单走悬停说明**（不因为格子窄就藏信息）。
- **判据**：断言组 114（判定三态 + 文本往返含手写顿号 + 真实多选面板 + 互斥 + 数据侧真是集合 + 悬停全清单 + 单选老形态照常）；变异 M46（集合判定失效）/M47（导出只写第一项）/M48（下拉退回单选）/M49（去掉互斥重绘）/M50（解析不折叠）/M51（格子只显示第一项）**全捕获**。

## 🩹 v1.73.1 — 抓宠帮手：详情页两处真 bug + 布局审查

- **详情页里搜索行没被隐藏**（用户截图：标题被读成「宠物技能爪击 · 等级爪击」）：标签「宠物技能：」+ 输入框里的检索词 + 详情标题**三层叠印**在同一行，[清除] 还压在 [返回] 上 → 改成**按视图整行显隐**（详情隐藏搜索行、列表隐藏 [返回]）。
- **宠物行的名字与元信息永远不显示**（截图里每行只剩「?」和「查」）：刷新时六个控件一起 `Hide`，随后**只 `Show` 了图标与放大镜**，名字 / 等级·区域·类型再没出现过。★两处都是「文本全对、只是画不出来」→ 旧断言只读文本，**全绿**（这就是它们能出厂的原因）。
- **布局重排**：布局常量集中成单一来源；属性行 300 宽 + 需求行从 +310 起（原先 meta 宽 `W-260` 与 req 从 +300 起 → **重叠 100px**）；宠物行四列（图标 / 名字 / 元信息 / 放大镜）互不相交；交替底色 + 宠物标题分隔线；空结果显示「没有匹配…」；来源多于一屏**如实提示条数**（绝不静默截断）。
- **查询入口收成一个**：`EVAL_PH_SET_QUERY` 是唯一入口（输入框 `OnTextChanged` 也走它）；改词即退回列表视图；回写输入框**比对后再写**（本客户端 `SetText` 会再触发 `OnTextChanged` → 无条件回写就是递归）。
- **判据**：断言组 113（两视图可见性 + 读**真实矩形**的几何不变量：列不相交 / 行距一致 / 不越界 / 标题不压 [返回] / 空态与分页如实提示）；变异 M40（名字不 Show）/M41（元信息不 Show）/M42（详情不隐藏搜索行）/M43（属性行重叠）/M44（空态不提示）/M45b（元信息压上放大镜）**全捕获**；M45（仅收窄元信息宽度）经 660/800 两档实测为**等价变异**，如实记录。
- **图标仍是占位**（`INV_Misc_QuestionMark`）：内置图标打包在 `Content\Paks`，本机**无法验证路径是否存在**，写错就什么都不画（比问号更糟）→ 等真实路径确认后只改 `gen_petdata.js` 的 `ICONROOT` 重生成。

## 🐾 v1.73.0 — 抓宠帮手（配置窗第 6 个 Tab）

- **数据**：`PetData.lua` 独立载入（21 技能 = 13 主动 + 8 训练师被动，106 条「技能×等级」，280 条驯服来源），来自用户提供的宠物技能总表截图转录（`doc/pet_info.png` → `doc/宠物技能数据.md`）
- **图标**：暂用**客户端内置图标**占位（`Interface\Icons\INV_Misc_QuestionMark`，用户指示「先用内置随便选一个替代，下次再确认真实路径」）——真实路径确认后**只改 `gen_petdata.js` 的 `ICONROOT` 一行**再重生成；`PET ICON CHECK` 相应改为守「路径格式」：一律 `Interface\Icons` 下、不带扩展名、数量 ≥ 20（内置图标打包在客户端 `Content\Paks`，磁盘上取不到，无法做存在性校验——与宏图标表同理）
- **检索**：顶部输入框即时过滤（纯本地数据，无需去抖），列表逐条列出**各等级**（技能名 · 等级N（需宠物等级X）），支持按**技能名**或**野兽名/区域**两种搜法
- **详情页**：技能图标 + 名称 + 等级 / 技能简介 / 集中·施法·射程·冷却 / 需求宠物等级 / 拥有该技能的宠物列表（家族 + 名称 + 等级·区域）；训练师被动技能如实注明「无驯服来源」
- **放大镜跳转**：宠物行右侧按钮 → 填入宠物名 → 切到「数据检索」Tab 并立即检索，后续完全走数据检索自己的流程（新增 `EVAL_DS_SEARCH_NAME` 外部入口 + `EVAL_DS_LAST_QUERY` 读值口）
- 断言：新增组 106（数据/条目数/双检索/详情/被动技能/跳转 Tab 与检索词/家族标签）+ `PET ICON CHECK`；既有「Tab 数 = 5」断言同步改为 6

## 🔍 v1.72.2 — 分享方案光环修复 + 载入引导每次都在

**要点**
- **分享出去的方案在别人角色上放不出技能**：根因是六个光环分支写成「纹理解析不出就先 return」，
  按名字扫描的兜底**永远到不了**；而别人角色没学过该 debuff 的纹理（`debuffTex` 是**每角色** SavedVariables 表，
  目标身上的 debuff 也不是对方动作条上的技能）→ 条件恒假、技能永远不放。
- 改成**两级解析**：① 纹理快路径（学习表 > 动作条）② 名字慢路径（工具读名，0.5s 缓存，**命中即把名字→纹理学下来自愈**）；
  **两条都不可用才如实失败**（1.71.2 铁律：查不到 ≠ 没有）。
- **扫描可信度三态**：命中永远可信；「确实没有」只有在扫描**干净**（每个看到的光环槽都读到名字）时才敢下结论，
  否则一律如实失败——防止把「不可信」当「没有」而复活 1.71.2 的无限重放。
- **新手引导改成每次加载都打**：原来用 `cfg.guideSeen` 做成「只在人生第一局出一次」，
  新人没盯第一局的聊天框就**再也看不到**（该键已废弃、不再读写）。
- 顺带修：`EVAL_DS_HUD` 具名帧与同名全局函数冲突 → 帧把函数顶掉 → `/eh ds hud` **静默失效**（HUD 开得开、关不掉）。

**典型用法**
- `/eh go tex` 看光环「名字→纹理」学习表；`/eh go texdel 名字` 删一条（**下次判定会自动扫描并学回来**）；
  `/eh go texclear` 全清；`/eh go texscan` 立即扫四类光环并显示**本次扫描是否可信**。

**设计亮点**
- 命中即自愈：第一次多一次 16 槽工具扫描，之后**永久回快路径**。
- 新增源码检查 `FRAME NAME CLASH CHECK`（具名帧不得与全局函数同名）与断言组 103/104/105；
  变异测试 M1~M6 全捕获（含「把纹理未知直接当失败」与「把不可信当没有」两个方向的死法）。

## 🧭 v1.72.1 — 新手上路友好化（加载提示 + 四步引导）

- **加载成功提示**：进游戏/重载后聊天框明确播报「✅ EvalHelp vX.Y.Z 已加载成功」+ 三条入口提示（/eh cfg、/eh guide、/eh help）
- **首次进入自动引导**：老玩家不会被打扰——只在**首次**（`cfg.guideSeen` 未置位时）播报四步新手指引，之后可用 /eh guide 随时重看
- **绑键引导改为「首选右键、备选宏」**（用户要求：让用户少做复杂操作，点点点就能完成）：引导第 ③ 步主推「右键方案 → 方案管理 → 快捷键下拉 → [保存]」，**创建宏流程降级为备选一行**；配置窗全局页帮助与三语言 README 快速上手同步改为此顺序。
- **四步引导内容**：① 建议开启战斗信息UI（/eh ui）② 配置第一个宏（/eh cfg → 添加技能 / 案例模版）③ 绑定按键（新建宏 /run EVAL_GO() → 拖到按键）；再补 ④ **技能释放异常 → 开技能日志（战斗日志→方案）** 与 /eh logdump 查判定明细 ⑤ 小地图 EH 图标可点开配置台
- **配置窗全局页**：「帮助」标题右侧新增 **[使用引导]** 按钮；/eh guide（也认「新手」）等价
- **文档**：zh/en/ru 三语言 README 同步（版本表、里程碑范围 1.72.1、快速上手重写为「动作条→重扫→配条件→绑键→引导」）；en/ru 补齐**赞助板块**（含收款码，与 zh 一致）；zh 快速上手清掉旧的战士专用措辞

## 🎉 v1.72.0 — 方案快捷键攻坚 + 一批界面精修（跨越 v1.71.3 ~ v1.71.24）

> **发布说明**：上一次发布是 **v1.71.2**（已推送到两个远端）。此后 1.71.3 ~ 1.71.24 共 **22 个版本、
> 26 个提交**一直只在本地。本版把它们**合并成一个个 minor 版本一次发布**；各版本的逐条细节仍保留在
> 下方各自的小节里（CHANGELOG 不删历史）。

### 🌟 头条：方案快捷键从「永远不触发」到真正生效

这是本次跨度里耗时最长的一段（1.71.16 ~ 1.71.22，**四次试错**）。四条路逐一被实测淘汰：

| # | 方案 | 实测结果 |
| :-- | :-- | :-- |
| ① | `CLICK <隐藏按钮>:LeftButton`（vanilla 经典招） | ❌ 客户端把命令串解析岔了：**鼠标左右键的转屏被顶掉**（插件确实被触发 = 链路通、形态错） |
| ② | 裸命令名 `EVAL_GO<i>` | ❌ 绑定表接受、按键不触发（客户端命令表里没这个名字） |
| ③ | `Bindings.xml` 登记 | ❌ **本客户端根本不读插件的 Bindings.xml** |
| ④ | **`ACTIONBUTTON<n>` + 接管全局 `ActionButtonDown/Up`** | ✅ **本版实装** |

**③ 的判定证据（两个独立来源）**：
- **本机对照实验**：把装有 `Bindings.xml` 的 **ArchiTotem 启用 + 完整重启客户端**，它的
  `CAST_EARTH_TOTEM` **依然不在命令表里**（整表连一条 `CAST_*` 都没有），命令表恒为 **226 行**、
  重启前后一模一样 → 不是「名字写错」，是**文件根本没被读**。
- **同机同客户端的 unrealUI 插件**源码注释（`modules/actionbar.lua:991`，2026-08-19 实测）：
  「unrealUI's **60 commands were absent from a 225-entry binding table**」，它最后也放弃了这条路。

**④ 的可行依据（`/eh go diag` 实测）**：`ActionButtonDown/Up`、`MultiActionButtonDown/Up` 全部
`type=function`（插件可替换）；`SetOverrideBindingClick`/`ClearOverrideBindings` 均为 `nil`（该路确认堵死）。

**最终形态**：`SetBinding(键, "ACTIONBUTTON<n>")` + 接管 `ActionButtonUp` 把「格号 → 方案号」翻译成
`EVAL_GO(方案号)`。**零成本**：不占宏名额、不占动作格、**不抢用户已有键位**、**游戏运行中立即生效**。

**踩过的坑也一并记在这里**（防止后人重走）：
- **「绑定表接受」≠「派发会执行」** —— `SetBinding` 会照单全收任何字符串（`Keybinds.ini` 里真能看到那行），
  但客户端的命令表只含它自带的条目 → 按键时查不到执行体 → **静默无反应**。
- **组合键守卫要自己补**：本客户端按**物理键**触发 `ActionButtonUp`，替换原函数就丢掉了客户端原有的
  组合键保护（`unrealUI` 源码明确警告）→ 按住 Alt/Ctrl/Shift 且该组合另有归属时**不吞**。
- **格号两轮挑**：优先「格子空 **且** 该格命令没人绑键」的完全无主格，绝不悄悄抢走用户已有的键。

### 🗂 右键功能合并成一个「方案管理」弹窗（v1.71.24）

此前配置窗方案右键 = 重命名窗、战斗信息UI 方案右键 = 绑定窗（两个窗、两套 chrome）→ **合并成一个窗**：
窗内两段 ① 方案名称 ② 快捷键，**[保存] 一次提交两段**（互不牵连：空名字只拒绝改名、绑键照常）；
**两处右键都开同一个窗**、左键语义不变。

### 🎨 界面精修批次

| 版本 | 里程碑 |
| :-- | :-- |
| 1.71.3 | 停止攻击 / 跟随（Movement 非 Protected 路径）/ 方案分享与接收重做 / 成员过滤（职业·小队）/ 图标库 Tab / 全角冒号静默失效修复 |
| 1.71.4~1.71.8 | 案例模版窗**三次改版**（两列 → 流式换行 → 分组分两列 + 组内同行自动换行）+ 模版扩到 11 组 + 补队伍/团队驱散 |
| 1.71.9 | 技能调序按钮改真三角 ▲/▼ + 图标库一页 6→9 行 |
| 1.71.10~1.71.12 | 战斗信息UI 方案切换行按名字实测宽 + 自动换行；标题改**玩家名** + 「战斗 / 方案」子开关 |
| 1.71.13 | 小地图配置按钮改**图标钮**（无边框·与图标同尺寸）+ tooltip 美化 |
| 1.71.14 | 全局页「一键宏」组底部对齐（修与「状态信息UI」重叠） |
| 1.71.15 | 图标库**右键 = 设为小地图按钮图标**（亲自挑的永远优先于自动挑） |
| 1.71.17 | 初始默认图标改「力量祝福」 |
| 1.71.19~1.71.20 | 「方案」标签 tooltip 三行说明 + 右键全清绑定 + 左键打印绑定情况 |
| 1.71.23 | 清理四次试错留下的**死探针**（净减 205 行） |

### 🧪 测试与质量

- 断言组从 66 扩到 **102 组**；本次跨度内新增/重写：组 93~102（布局 / 标题子开关 / 小地图图标 /
  宏组对齐 / 图标库右键 / 绑定 / 标签 / 状态打印 / 派发前提 / 方案管理窗）。
- 每个版本都跑了**变异测试**并全部捕获（绑定 6/6、清理 2/2、方案管理窗 **8/8** 等）。
- **一条方法论教训**（v1.71.24 变异救回来的）：**给旧入口留等价别名会让回归悄悄通过** ——
  初版给 `EVAL_HELP_RP_OPEN` 留了别名，结果「调用点改回旧名字」的变异**两次都 SURVIVED**。
  → 删掉旧名 + 补「数窗 / 旧名不存在」两条**结构断言**才是正解。

### ⚠️ 升级注意

- 若你之前绑过方案快捷键而按键不触发，那是 v1.71.21 及更早的形态；**本版开始真正生效**，无需重启客户端。
- 旧的 `Bindings.xml` 已**删除**（该路线被证伪）。

## 🗂 v1.71.24 — 右键功能合并成一个「方案管理」弹窗

**用户需求**：「将这两个功能合并成一个弹窗管理.都是右键触发.」

**合并前**（两个独立弹窗、两套 chrome、功能互不相通）：

| 右键位置 | 打开的窗 | 能做的事 |
| :-- | :-- | :-- |
| 配置窗「一键宏设置」方案按钮 | `EVAL_HELP_RP_*` 重命名窗 | 只能改名 |
| 战斗信息UI 方案按钮 | `EVAL_BIND_*` 绑定窗 | 只能绑键 |

**合并后**（一个窗，两处右键都开它）：

```
┌─ 方案管理：武器战 ─────────────┐
│ ① 方案名称                    │
│   [ 武器战________________ ]  │  ← 输入框 + 金色回声行（EditBox 不渲染也可见）
│ ② 快捷键                      │
│   当前绑定：E                 │
│   [ 点击选择按键…        ▾ ]  │  ← 分类下拉（功能键/数字/字母/鼠标/组合键）
│   该键空闲，可以绑定           │  ← 冲突/空闲实时提示
│  [保存] [清除] [取消]          │
└───────────────────────────────┘
```

**设计要点**：
- **「保存」一次提交两段**：改名 + 绑键一起生效；**两段互不牵连**——名字留空只如实拒绝改名，绑键照常。
- **两处右键都开同一个窗**（左键语义完全不变，仍是激活方案）。
- 保留了 `EditBox 可能不渲染` 的**回声行**保底范式（本客户端老坑）。
- 刷新走**单一刷新点** `pmRefresh()`，写完顺手 `EVAL_WAR_TAB_REFRESH()`（数据变了→界面同步，1.71.1 教训）。

**★一个必须记下的测试教训（变异救回来的）**：

初版我给旧入口留了**等价别名**（`EVAL_HELP_RP_OPEN = EVAL_PM_OPEN`），结果变异测试里
「把调用点改回旧名字」**两次都 SURVIVED** —— 因为旧名照样开同一个窗，**行为断言全绿**。
→ 最终决定**删掉旧入口名**：任何回退都会当场变成「调用 nil」而炸响；并补了两条**结构断言**：

1. `EVAL_HELP_PM_WINDOW_COUNT() == 1`（全插件只剩一个管理弹窗）
2. `type(EVAL_HELP_RP_OPEN) == "nil"` 且 `type(EVAL_BIND_OPEN) == "nil"`（旧名已彻底删除）

**测试**：新增组 102（两处右键同窗 / 窗内两段都在 / [保存] 一次提交两段 / 空名不牵连绑键 /
旧入口已删除 / 数窗 / 两段有序排开且在窗内），**变异 8/8 全捕获**
（另开第二个窗 / 战斗UI 右键丢失 / 只改名 / 只绑键 / 空名字 / 标题回退 / 加回别名 / 两段重叠）。

## 🧹 v1.71.23 — 清理四次试错留下的死探针代码

**背景**：v1.71.22 定案派发路线（接管 `ActionButtonDown/Up`）之后，前三次试错留下的探针已经**没有存在价值**——
更糟的是它们会把后人引向已经被证伪的路线（本项目已有「与代码相反的注释比没有更危险」的教训）。

**删掉的三个探针（全部是「写入型」试验，验的都是死路）**：

| 探针 | 它当年验的路线 | 为什么该删 |
| :-- | :-- | :-- |
| `/eh go bind2` | CLICK 隐藏按钮派发 vs 裸命令名 | 两条都已被实测判死（① 劫持鼠标；② 不派发） |
| `/eh go bind3` | 无尾 CLICK（`CLICK <按钮>`） | 同属已被判死的 CLICK 家族 |
| `/eh go actbar` | 宏 + 动作条链路 | 已被 `/eh go diag` 完全覆盖（diag 只读且读数更全） |

**改掉的 `/eh go bind`**：从「写入型可行性试验（T1 绑 JUMP / T2 绑自定义命令名）」改成**只读现状检查**：

1. **派发前提**：命令表里 `ACTIONBUTTON1~12` 有几条（12 = 前提成立）
2. **接管状态**：`ActionButtonUp` 是否已被我们替换（`EVAL_BIND_ORIG_UP ~= nil`）
3. **逐方案**：每个方案的键 + 占用格（`ACTIONBUTTON<n>`）+ 读回值，没有绑定就如实说没有

**保留 `/eh go diag`** 作为唯一诊断入口（写盘 + `/reload` 后可离线读，读数最全）。

**收益**：`EvalHelp.lua` 净减 **205 行**（+27 / -205）；去掉一处「探针会临时改玩家绑定」的副作用；
`/eh go bind` 现在**永远是安全的只读操作**。

## 🎯 v1.71.22 — 方案快捷键真正生效：接管 ActionButtonUp 派发

**背景**：v1.71.16~1.71.21 连续四个版本都在「怎么让绑定的键真的触发」上试错，用户实测始终不触发。
本版用**对照实验**一次定案，并换到实测可行的派发路线。

**四次试错的完整记录（全部有实弹/实测依据，写进代码注释防止后人重走）**：

| # | 方案 | 结果 |
| :-- | :-- | :-- |
| ① | `CLICK <隐藏按钮>:LeftButton` 派发（vanilla 经典招） | ❌ 客户端把命令串解析岔了：**鼠标左右键的转屏被顶掉**（插件确实被触发 = 链路通、形态错） |
| ② | 裸命令名 `EVAL_GO<i>` | ❌ 绑定表接受、按键不触发（客户端命令表里没有这个名字） |
| ③ | `Bindings.xml` 登记（1.71.21） | ❌ **本客户端根本不读插件的 Bindings.xml** |
| ④ | **`ACTIONBUTTON<n>` + 接管 `ActionButtonDown/Up`** | ✅ 本版实装 |

**③ 的判定证据（两个独立来源，结论一致）**：
- **本机对照实验**：把装有 `Bindings.xml` 的 **ArchiTotem 启用** + **完整重启客户端**，它的
  `CAST_EARTH_TOTEM` **依然不在命令表里**（整表连一条 `CAST_*` 都没有），命令表恒为 **226 行**、重启前后一模一样。
  → 不是「名字写错」，是**文件根本没被读**。而 `SetBinding` 会**照单全收**任何字符串
  （`Keybinds.ini` 里真能看到 `EVAL_GO_PROF_1=E`）→ 按下时查不到执行体 → **静默无反应**。
- **同机同客户端的 unrealUI 插件**源码注释（`modules/actionbar.lua:991`，2026-08-19 实测）：
  「Bindings.xml declarations -- unrealUI's **60 commands were absent from a 225-entry binding table**」，
  它最后也放弃了这条路。同处还记录「常驻键盘 Frame 抢全部输入」这条路**不可用**（连 Enter 聊天都被杀）。

**④ 的可行依据（`/eh go diag` 实测）**：
```
ActionButtonDown=function  ActionButtonUp=function
MultiActionButtonDown=function  MultiActionButtonUp=function
SetOverrideBindingClick=nil  ClearOverrideBindings=nil   ← 这条确认堵死
ACTIONBUTTON 家族=12 条（客户端**自己会派发**）
```
即：客户端按下绑到 `ACTIONBUTTON<n>` 的键时，调的是**全局函数** `ActionButtonDown(index)`/`ActionButtonUp(index)`
—— 而**插件可以替换这两个全局**（unrealUI 就是这么干的）。

**实现要点**：
- **零成本**：不占宏名额（用户实测通用宏位只剩 2 个）、不占动作格、**不抢用户已有的键位**、**游戏运行中立即生效**。
- **格号分配两轮挑**（用户追问「动作条前面都有具体站位了」逼出来的规则）：
  ① 先找「格空 **且** 该格命令没人绑键」的**完全无主**格；② 都没有才退而用「格空但键位被占」的。
  → 绝不悄悄抢走用户已有的 `T`/`R`/`F` 等键。
- **补回组合键守卫**（unrealUI 源码明确警告的代价）：本客户端按**物理键**触发，替换原函数会丢掉客户端
  原有的「组合键保护」→ 按住 Alt/Ctrl/Shift 且该组合另有归属时**不吞**，让给客户端。
- **非占用格原样透传**：只有我们占的格才接管，其余一律交给原函数（绝不改变别的插件/客户端的按键行为）。
- **诚实失败**：客户端若没有 `ActionButtonDown/Up` 全局，`EVAL_BIND_INSTALL()` 返回 false，
  登录时**如实播报**「按键派发装不上（功能不可用）」，绝不假装成功。
- **清除 = 交还格子**：解绑后该格重新透传给客户端；`/eh go diag` 的 `EVAL_BIND_XML_STATUS` 判据整体改写为
  「命令表里有没有 `ACTIONBUTTON1~12`」（这是整条链路唯一的外部前提，精确后缀，`ACTIONBUTTON1X` 不算）。

**测试**：组 98 整体重写（命令名形态 / 格映射 / 存档 / **真机派发**（模拟客户端调 `ActionButtonUp` 验到
`EVAL_GO` 真的被调）/ 非占用格透传 / 组合键守卫 / 清除交还 / 真实弹窗流程），组 101 判据改写；
**变异 6/6 全捕获**（退回旧命令名 / 静默吞按键 / 吞别人的格 / 去掉组合键守卫 / 假装接管成功 / 换键换格）。
→ 顺带删掉了已被证伪的 `Bindings.xml`（它只会继续误导，且没有任何运行时作用）。

## 📦 v1.71.21 — Bindings.xml 随插件自带（用户零配置）+ 登录生效自检

- 用户三问：「Bindings.xml 这个文件是插件帮忙完成的吗?用户零操作配置文件吗? 是否自己绑定完成.用户重启就可以?」——**三个都是**：
  ① 文件**随插件自带**（ addon 目录里的 `Bindings.xml`，12 个命令 `EVAL_GO_PROF_1~12`，用户零配置）；
  ② 绑定**全部在绑定弹窗里完成**（`SetBinding(key, "EVAL_GO_PROF_<i>")` + `SaveBindings`，用户零手动）；
  ③ 用户唯一要做的 = **重启客户端一次**（完全退出再进，不是 /reload；之后按键就走客户端自己的派发）。
- **文档深挖佐证**（`RunBinding` 条目）：派发靠「command has a **matching press/release handler**」——处理器是客户端侧的，
  插件无从注册（`SetConsoleKey` 明写「Registered for compatibility. **Does nothing**」）→ 裸命令名永远不会触发，
  唯一的注册通道 = 启动时命令表；这正是 vanilla `Bindings.xml` 的位置（客户端启动自动加载，**不列 .toc**）。
- **登录自检 `EVAL_BIND_XML_STATUS()`**：数命令表里的 `EVAL_GO_PROF_*`（精确前缀，`EVAL_GO_PROF_1X` 不算）——
  12 = 已生效静默；0 = 装了带 xml 的版本但还没重启 → 聊天框**如实提醒一次**「重启客户端一次即生效」；
  验证口令不变：重启后 `/eh go bind` 的 T0 应显示「以 EVAL_ 开头的命令行数 = 12」。
- **测试**：新增组 101（桩命令表：内建命令/HEADER 行不算 / 12 个全在 / 前缀精确）；**变异 2/2 全捕获**（宽松前缀 / 恒 12）。

## 🖨 v1.71.20 — 左键点「方案」= 打印当前绑定情况（附 /eh go bind3 无尾 CLICK 探针）

- 用户原话：「左键点击<标题方案>.日志打印方案绑定情况.」
- **「方案」标签左键**：聊天框逐行打印「序号. 方案名 = 按键」（`EVAL_BIND_STATUS()`）；
  ★只打印、**不动任何绑定**；没有绑定时如实打印「当前没有自定义绑定」（不是静默什么都不出）；
  tooltip 同步补第 4 行「左键点「方案」= 打印当前绑定情况」（三语言）。
- **绑定派发的边界（用户定）**：「在游戏运行状态无法生效的方案不要考虑」——`Bindings.xml`（需重启客户端）已删除出局；
  裸命令名实测不派发、带尾巴的 CLICK 实测劫持鼠标 → 附 `/eh go bind3` 实弹探针验证**无尾 CLICK**
  （按钮显式 `EnableMouse(false)` + 1x1 挪出屏幕；10 秒倒计时如实报告，自动解绑还原）。
- **测试**：新增组 100 —— 真实逻辑层造两个绑定 → **真实左键**标签 → 聊天逐行「甲 = E」「乙 = F9」都在、绑定原样不动；
  清空后再点 → 如实「没有」；**真实悬停** tooltip 第 4 行在。
- **变异 4/4 全捕获**：左键不打印 / 有绑定不逐行打印 / 没有时不报「没有」 / tooltip 第 4 行没了。

## 🏷 v1.71.19 — 「方案」标签 tooltip 三行说明 + 右键 = 清除全部自定义绑定

- 用户原话（截图红箭头指着「方案」二字）：「方案文字 添加tootlp 提示 右键取消所有自定绑定,左键点击具体的方案,激活方案,右键绑定方案按键,美化说明下.」
- **「方案」标签变成可点按钮**（视觉不变：金色小字）：
  · **悬停**出三行美化说明（三语言）：左键点方案 = 激活该方案 / 右键点方案 = 绑定或修改它的快捷键 / 右键点「方案」= 清除全部自定义绑定（第三行橙色警示）；
  · **右键点它** = `EVAL_BIND_CLEAR_ALL()`：**逐键解绑**（不留下占着键的垃圾绑定）+ 清掉配置表 + `SaveBindings` 存档，如实返回并播报清掉的个数；空表幂等返回 0（连点不炸）；
  · 左键点它什么都不做（左键激活是方案按钮的事，tooltip 里已讲清）。
- **测试**：新增组 99 —— 真实逻辑层先造两个绑定 → 左键标签不动 → **真实右键**标签 → 两个键都被解绑 + 配置清掉 + 存档计数；
  `EVAL_BIND_CLEAR_ALL` 计数与幂等；**真实悬停** tooltip 三行说明逐字在。
- **变异 4/4 全捕获**：右键标签不全清 / 全清不解绑 / 全清不存档 / tooltip 少一行。

## 🔧 v1.71.18 — 绑定派发弃用 CLICK 改裸命令名（修「E 没触发 + 鼠标左右键被劫持」）

- 用户两轮实测：① 绑定 E → 方案成功但按 E 没触发；② 「好像是绑定到鼠标点击，左右键点击原始的屏幕转动效果没了，但是会触发插件」——
  **按键 → 插件代码的链路是通的**，错的是 CLICK 命令串：`"CLICK EVAL_GO_KEY_i:LeftButton"` 被这个客户端解析岔了，
  触发没挂到 E 上、反而挂到了鼠标左右键（还顶掉了 BUTTON1/2 的原始命令）。
- **改法**：派发命令名改**裸命令名** `EVAL_GO<i>` —— 依据三条：T0 探针里客户端内建命令就是 `MoveForward`/`Jump` 这种**驼峰全局函数名**；
  T2 实测绑定表直接接受 `EVAL_GO2`；`EVAL_GO1~12` 全局函数**本来就存在**（Engine.lua 批量生成，零新增函数）。
  ★CLICK 隐藏按钮机制整段删除（前提被证伪的代码不留）。
- **测试**：组 98 断言同步改裸名 + 补**反向哨兵**：`EVAL_BIND_CMD(1~12)` 每一个都必须 `type(_G[...]) == "function"`
  （绑出去的命令名拼错一个字母，客户端派发时就无的放矢）；**变异 3/3 全捕获**（回到 CLICK / 拼错命令名 / 全部绑成 EVAL_GO1）。
- ★用户侧恢复（本版报告里给过）：鼠标左右键若仍失灵，`/run SetBinding("BUTTON1","CAMERAORSELECTORMOVE") SetBinding("BUTTON2","TURNORACTION")` 后重载；
  旧的 E 绑定在重新绑定走弹窗时会被自动替换（换键解旧键判据不变）。

## 🖼 v1.71.17 — 初始默认图标改「力量祝福」（用户定稿）

- 用户日志（右键换图标成功）：「看日志.初始图标换成日志内的」——
  她在图标库亲自挑了 `Spell_Holy_BlessingOfStrength_TEX`（金色拳头，「增益/强化」一眼可辨）当小地图按钮图标，
  现在要求**初始默认**（新装/没挑过时）也用同一枚，而不是 1.71.13 自动挑的那枚。
- **改法**：`EVAL_HELP_MB_PICKICON` 的候选优先级把 `^SPELL_HOLY_BLESSINGOFSTRENGTH` 排最前（压过 扳手 > 工程学 > 齿轮…）；
  ★她在图标库右键挑的（`cfg.mbIcon`）仍然**永远优先**于一切默认（1.71.15 的判据不动）。
- **测试**：组 95 补一条——力量祝福压过扳手（含本客户端 `/Game/..._TEX` 路径形态）；**变异 2/2 全捕获**（删候选 / 排到扳手后面）。

## ⌨ v1.71.16 — 方案快捷键绑定（方案行右键 → 绑定弹窗，下拉选键分键盘/鼠标/组合键）

- 用户路线：先验证（「方案 右键能否弹窗设置 绑定快捷键,先验证」）→ `/eh go bind` 实测：
  **T1** 空闲键绑 JUMP 成功读回（API 可用）；**T2** 绑 `EVAL_GO2` 成功读回（**自定义命令名无需 Bindings.xml 登记**）→ 放行实装。
- **入口**：战斗信息UI 方案切换行，**右键任意方案** → 绑定弹窗（左键仍是切换方案，不被吃掉）。
- **弹窗**（美化版）：金边深底 + 标题栏拖动 + 当前绑定行 + **分类下拉选键**
  （功能键 / 数字键 / 字母键 / 鼠标 BUTTON3-5·滚轮 / 组合键 SHIFT·CTRL·ALT，分类标题行不可点）
  + **冲突提示**（选中键被占用 = 橙色「绑定后替换」，空闲 = 绿）+ [绑定] [清除] [关闭]。
- ★**派发机制 = CLICK 隐藏按钮**（vanilla 经典招）：`SetBinding(key, "CLICK EVAL_GO_KEY_i:LeftButton")` ——
  按下键 = 客户端替我们点那个隐藏命名按钮 → OnClick → `EVAL_GO(i)`（执行并激活方案）；
  ★T2 只证明「命令名能存进绑定表」，“按下真的触发”用实弹探针 `/eh go bind2` 复核（CLICK vs 裸命令名，10s 倒计时如实报告）。
- ★**持久化**：绑定后 `SaveBindings(GetCurrentBindingSet())`（实测 set = 1）——不存就随重登消失；
  ★**换键自动解旧键**（不留「一键两命令」的烂摊子）；空键如实拒。
- **测试**：新增组 98 —— ① 派发命令名纯函数；② 按键清单（分类标题全 locked / 键名全合法 / 键盘鼠标都在）；
  ③ 逻辑层真实流程（绑定 → 换键解旧键 → 清除 + SaveBindings 计数）；④ **真实右键**开弹窗 → 下拉回调选键 →
  点[绑定]写进绑定表；⑤ 左键仍切换方案。
- **变异 6/6 全捕获**：派发丢 CLICK 前缀 / 换键不解旧键 / 绑定后不存档 / 右键不开弹窗 / 下拉回调不收键 / 清除不解绑。

## 🖱 v1.71.15 — 图标库右键 = 设为小地图按钮图标（修「图标调整的不对」）

- 用户截图（红圈圈着小地图按钮，里面是插件自动挑的那枚蓝色图标）：「图标调整的不对.」
- **定案**：她的图标**让她自己挑**，不再由插件代挑 ——
  图标库里**右键**任意一枚 = 立刻设为小地图按钮图标（存 `cfg.mbIcon`，重登不丢；格子 tooltip 加了一行绿色提示「右键 = 设为小地图按钮图标」）
- ★**亲自挑的永远优先**：`cfg.mbIcon` 非空时 `EVAL_HELP_MB_SETICON` 直接用它（宏图标表里有扳手也不换）；
  清掉自定义（`/eh go mbicon reset`）才回到 1.71.13 的自动挑；`/eh go mbicon` 查看当前
- ★左键仍是原来的「打印路径」，不被右键吃掉；空串路径如实拒（不许把按钮贴成空白）
- **测试**：新增组 97 —— ① 真实 SETCUSTOM（写配置真值 + 真控件当场贴图 + 空串拒收）；② SETICON 优先级
  （亲自挑的 > 自动挑，清自定义回自动挑）；③ **真实右键** OnClick（配置与按钮都换）；④ 左键不动配置
- **变异 3/3 全捕获**：SETICON 不看自定义 / 右键分支被删 / SETCUSTOM 不写配置

## 🧹 v1.71.14 — 全局页「一键宏」组底部对齐（修与「状态信息UI」重叠）

- 用户截图（红圈圈着「重扫动作条 + 执行去抖」一组，箭头向下）：「全局配置有重叠,将红色部分底部对齐.」
- **根因**：1.71.12 给「界面」组加了两行子开关后，「状态信息UI」下移到 -222（高 16，下缘 -238），
  而「一键宏」组头还锚在 -212 → 直接叠在勾选框上（截图里「一键宏」三个字压在「状态信息UI」上）。
- **修法**：整组（组头 → 重扫按钮 → 提示 → 执行去抖）改**从底部按钮行向上锚**（底部对齐）——
  去抖行下缘与导航/关闭按钮行上缘留 6px 间距，组内行距不变（20px 逐级向上）。
  ★y 一律从 `NAV_ROW_Y` 推导（底部行是布局单一来源），不再写死 -212；`cfgHeader` 顺带交出真实控件。
- **测试**：新增组 96（全部读**真实控件**的位置）：① 组头与「状态信息UI」不重叠（≥ 勾选框高 16）；
  ② 底部对齐：去抖行下缘 = 底部按钮行上缘 + 6px；③ 组内行距保持 20px；④ 整组在窗口内。
- **变异 3/3 全捕获**：底部间距 6→26 / 组头锚回 -212（再重叠）/ 组内行距 20→24。

## 🖼 v1.71.13 — 小地图配置按钮改图标钮 + tooltip 美化（附 /eh go bind 探针）

- 用户原话（配图：图标库页面，红圈圈着小地图旁的「EH」按钮）：「配置按钮 换图标.尺寸和图标尺寸保持一致,不用设置外边框」「tootip: 全职业一键宏,常用工具箱,世界数据库检索,描述美化下.」
- **按钮 = 一枚宏图标本身**：无边框、无「EH」字、**26×26**（与图标库单格同边长）、图标**占满整钮**；悬停给图标染一层金作反馈
- ★**图标不写死路径**：本客户端宏图标打包在 `Content\Paks`（磁盘取不到），路径只能从 `GetMacroIconInfo` 运行时查 —— 故按**名字前缀 + 优先级**挑：`扳手 > 工程学 > 齿轮 > 小装置 > 书`（「工具箱」气质优先）；挑选逻辑抽成纯函数 `EVAL_HELP_MB_PICKICON`（可注桩直测）
- ★**拿不到图标不许留空白按钮**：接口缺席 / 没有匹配项 → 退回旧的「金框 + 暗底 + EH」样式；后来才贴上图标时把兜底的「EH」字**清空**（OVERLAY 层会压在图标上穿帮）
- ★贴图时机在 `VARIABLES_LOADED`（载入期宏图标接口未必就绪）
- **tooltip 美化**（5 个语言键 × 三语言）：标题行「EvalHelp 全职业施法工具」+ 三行功能（**全职业一键宏**（条件规则引擎）/ **常用工具箱**（自动买卖 · 频道助手）/ **世界数据库检索**（任务 · NPC · 物品））+ 一行操作提示
- **顺带交付**：新探针 `/eh go bind`（请求 ②「方案右键绑快捷键先验证」的配套）——T0 枚举客户端命令表（看有没有 EVAL_ 开头的预留命令名）/ T1 空闲键绑 JUMP 验证 SetBinding 可用 / T2 同键试绑 `EVAL_GO2`（**true = 右键弹窗设快捷键直接可行**；false = 要先在 Bindings.xml 登记 + 客户端重启）；全程不 SaveBindings、试验键用完即还原
- **测试**：新增组 95 —— ① 挑选纯函数（优先级压过表序 / 没扳手退齿轮 / 不沾边如实 nil / 认得本客户端 `/Game/..._TEX` 形态）；② 真按钮 26×26 正方形（读真控件）；③ 真 SETICON：拿不到图标 → 兜底真的建出来；塞进扳手 → 真控件贴上它 + 「EH」字被清空
- **变异 5/5 全捕获**：退回 24×24 / 挑选不看优先级（取表序）/ SETICON 永不贴图 / 贴图后不清 EH 字 / 不建兜底样式

## 🎛 v1.71.12 — 标题改用玩家名 + 「战斗 / 方案」两个子开关

- 用户原话（一条消息里两件事）：「战斗信息UI 再加2个子选项,战斗,方案,分开控制显示和隐藏. 战斗信息 标题换成 用户名字」
- **标题**：改为 `title:SetText(UnitName("player"))`；★取不到名字（nil / 空串）**如实退回**语言包文案 `G_UI_TITLE` —— 直接 `SetText(nil)` 会把标题栏留空，比旧文案更糟
- **两个子开关**（配置窗「全局」页「界面」分组，**缩进 16px** 挂在「战斗信息UI」下面，状态信息UI 顺次下移并与之留更大的分组间距）：
  · **战斗区** = 血条 / 能量条 / 连击点 / 目标条（含右侧距离文本）/ 施法读条 / 挥击计时条 / 状态行；
  · **方案区** = 方案切换行 + 技能图标带（图标带不画时那次自动重扫也一并省掉）。
- ★★**配置语义**：`cfg.ui.subCombat` / `cfg.ui.subScheme`，**nil = 开**（老配置里没有这两个键 → 行为与旧版逐字一致）；只有显式 `false` 才不画
- ★★**「隐藏」= 不画**（本项目既有判据）：布局 y 随之前移、`root:SetHeight(y)` 跟着变小 —— `Hide()` 做不到这两件事（会留下一条空白带）
- ★★**两个区块各自独立**：关掉「战斗」不影响「方案」，反之亦然；两个都关时窗口只剩标题栏（仍保留底部内边距，免得内容贴着边框）
- ★`ui.combatOn / ui.schemeOn` = BUILD 时定下的**真实生效值**，tick 只认它们（不每帧回读配置）；★BUILD 开头**先清掉上一轮的 `ui.*` 引用** —— 否则块外的旧赋值会把「这一轮没建」盖掉，tick 里 `if ui.castBar then` 之类的存在性判据照样成立 → 去写**上一轮的幽灵控件**
- ★tick 里的**普攻判定**（写 `st.autoAttack`，规则引擎要用）**不跟着区块一起被跳过** —— 子开关只决定画不画，不改变状态
- **测试**：新增组 94（全程走**真实入口 + 真实控件**）：① 标题 = `UnitName("player")`（读真 FontString 文本）+ 空串退回文案 + 名字恢复后不粘兜底值；② 配置窗四个**真勾选框**的真实几何（子项缩进 16px、逐级向下、互不重叠、与状态信息UI 之间留更大的分组间距）；③ 点真勾选框 → 真重建 → **控件真的没建**（不是 Hide）+ **帧高真的变小** + 两个开关互不干扰 + 往返一圈帧高回到原值；④ 区块不建时 `EVAL_HELP_UI_TICK()` 不报错
- **变异 8/8 全捕获**：标题退回写死文案 / 忽略子开关 / `ui` 标志说谎（恒 true）/ tick 不按开关分段（nil 控件当场炸）/ 不清上一轮引用 / 子开关不缩进 / 状态信息UI 与子项同间距 / 开关改动不重建

## 📐 v1.71.11 — 方案行：给「拉伸」设上限（修「空的太多了」）

- 用户第二轮截图（红圈圈着一个**只有两个字却占满半行**的按钮）：「空的太多了.」
- **根因**：1.71.10 把每行剩余空间**全部**均摊给该行按钮 —— 一行里只有一个短名时，它就被拉成大空框
- **改法**：每格最多比「名字需要宽」多 **MAX_EXTRA = 24px**（随 `z` 缩放），超出的部分**留在行尾**（宁可行尾留白，也不做空框）
- ★这个上限**是个可调的数**：想要「完全按名宽」就把 `MAX_EXTRA` 设 0（实测该变异不违反任何判据 → 属**观感偏好**、不是缺陷）；想要「整行齐平」就调大
- **测试**：组 93 补两条 —— ① 每格宽落在 `[需要宽, 需要宽+40]`（★**故意不读生产的 cap**：读了它，把 cap 调大的变异会连判据一起改 —— M6 实测就这样 SURVIVED）② 生产上限 `ui.profCap` 必须**是个小数字**（≤32）
- 变异：**M6 捕获**（去掉上限 → 回到空框）/ **M7 等价**（上限设 0 → 只影响观感，如实记为等价变异）

## 📐 v1.71.10 — 战斗信息UI 方案切换行：按名字实测宽 + 自动换行（修「方案溢出宽度」）

- 用户原话：「有些方案 会溢出宽度. 能动态自适应吗?」（截图里第二行方案名互相压字）
- **根因**：方案切换行的按钮宽度原先**按行均分**（同一行所有按钮一样宽）——长名字（如「一键团队驱散」）撑破自己的格子、压在邻居上
- **改法**（四条一起）：
  · 每格宽 = **量宽(名字) + 内边距**，夹在 `[minBW(=34), 整行可用宽]` 之间；
  · 按可用宽**贪心换行**（首行扣掉「方案」标签宽）；
  · 每行剩余空间**均摊**回该行按钮（保住 1.56.0 的「每行填满」观感）；
  · 文字格**显式限宽 + 禁折行**（极长名字在自己的按钮里裁掉，不再压邻居）。
- ★量宽走 ruler FontString（挪出可视区量：本客户端 Hide 过的控件仍可能被绘出）；本机 API 表里没有 `GetStringWidth` → 退化为「中文一字约 9px」的近似
- ★★**方案名 / 数量变了必须重排**：由 `EVAL_WAR_TAB_REFRESH`（用户改方案名的**唯一入口**：改名/增删/导入分享）比对**版式签名**后**整体重建** —— 行数变了帧高与下方技能带一起挪；每 0.15s 的 tick 里**一次都不量宽**（频率防护）
- **测试**：新增组 93（长短悬殊的假方案名）：① 每格宽 ≥ 它自己需要的宽（用户报的「溢出」就是这条）② 长名格真的比短名宽（不是均分）③ 同行不重叠 / 不越出可用宽 / 整块在帧内 ④ 文字格已显式限宽 ⑤ 改名走**真实刷新入口**后版式自动跟着变、长名变多自动换行；新增钩子 `EVAL_TEST_UI_PROF()`（读真实控件几何）与 `EVAL_TEST_UI_SHOWN()`
- **变异 5/5 全捕获**（退回均分 / 去掉换行 / 去掉文字限宽 / 去掉自动重建 / 量宽按字节数）
  ★M3（去掉文字限宽）第一次 **SURVIVED**：桩的 FontString 没被 `SetWidth` 时 `GetWidth` 返回 nil → 断言被跳过（又一次「桩太宽松 → 断言失明」）→ 补一条「文字格必须限宽」才捕获

## ▲ v1.71.9 — 调序按钮改真三角 + 图标库一页 6 → 9 行

- **① 技能列表的调序 / 滚动按钮：`^` / `v` → `▲` / `▼`**（用户截图：「技能列表这两个图标」）——原先的 `^` 在本客户端字体里看着像**一条横线**、`v` 就是普通字母；三角字形与本插件「工具箱 / 数据检索」的滚动按钮**同一套**（那两处早已在用，已验证能显示）
- **② 图标库一页行数 6 → 9**：图标区高度 204 → **306px**（+102），一页 **162 枚**（18 列 × 9 行，原 108 枚）
  - ★★**先算清可用空间再定行数**：网格顶边 -100、配置窗底部两条（开关横排 / `[案例模版][分享][关闭]`）的中线 = -439 → 可用下沿 -420 → `floor((420-100)/34) = 9` 行（末行下缘 -402，余量 18px）
  - ★用户原话是「图标容器高度可以再增加 250」——但 250px 在 460 高的窗口里放不下；照字面硬塞会把最后几行画到**窗口外面**，而**本客户端的越界是静默的**（不报错、只是看不见）→ 取窗口内能容纳的最大值，并如实说明
- **测试**：新增**组 92**（① 读**真实控件文本**验四个箭头，不是读生产常量；② 读**网格真实几何**验「末格下缘不压底部按钮行、不越窗口右缘」）；★图标库翻页组的测试数据 150 → **400 枚**（每页涨到 162 后，150 枚已不足两页）
- **变异 4/4 全捕获**（调序改回 `^` / 只改调序、滚动保留 `v` / 行数回退 6 / 行数贪到 12）；★另外单独验证过**几何闸门**：把行数贪到 12 且短路掉「行数==9」那条断言后，`末格下缘 -504 > -427` 当场变红

## 🧩 v1.71.8 — 案例模版窗：分组分两列 + 组内同行自动换行

- **版式**：组标题与方案按钮**在同一行里依次往右排**，放不下就**换行回本列左边界**；每个组**从新的一行开始**（否则两组挤一行看着像一组）
- **分两列**：列宽 = (窗宽 − 2×边距 − 列间距) / 2（中文 660 → 310）；**组是不拆的最小单位**——按顺序切成左右两段，一个组绝不跨列
- **切点**：遍历所有切点取**两列行数差最小**（★不用「塞到过半就停」的贪心 —— 1.71.5 已经吃过一次亏）
- 实测：11 组排成 **左 8 行 / 右 7 行**，窗口 **660×240**（上一版单栏是 660×180）；分列结果：左[战士 法师 通用法系 盗贼 猎人 骑士] 右[牧师 德鲁伊 术士 萨满 队伍/团队]
- **设计要点**：版式仍是**纯函数** `tplTwoColPlan(W, groups, measure, margin, gap, rowH, colGap)`（位置单一来源，渲染只读这张 plan）；**合成数据**入口喂 8 组 × 9 个宽按钮 → 12+12 行、两列各 36 个
- **判据（分列不变量，读真实控件几何）**：按钮在**本列**列宽内（不是「窗内」）/ 同行相邻留足 GAP（**同 y 的两列各自成行**）/ **组不跨列** / 两列都有内容 / 行数差 ≤2 / 右列起点 = 左列右边界 + 列间距 / 标题落在本列行首
- **测试**：组 91 扩充；**变异 6/6 全捕获**（只排一列 / 组内不换行 / 一个组出现在两列 / 切点按组数对半 / 两列贴着排 / 每组不从新行开始）

## 🧑‍🤝‍🧑 v1.71.7 — 队伍/团队案例模版审计：一律「指定技能 + 队伍/团员条件」

- **审计结论**：17 条队伍/团队模版（一键队伍/团队 治疗·驱散·buff、通用法系补智力·耐力、骑士祝福、牧师/奶德/萨满治疗）原本是「**选取目标行 + 候选者条件**」两行配合 → 改成「**指定技能 + 队伍/团员条件**」**一行自足**
- **依据**：这些条件**自己就会扫人 + 切目标**（命中即记 `st.allyUnit`）→ 少一行，也不会出现「切了目标却没施法」的空切
- **典型用法**：`- 快速治疗 | 队伍血<70 & 能量%>15`、`- 奥术智慧 | 无队伍buff:奥术智慧[职业:法师/术士/…]`、`- 驱散魔法 | 有队伍debuff:魔法 & 能量%>10`
- ★**顺序铁律**：成员类条件**必须写在一行的最前面**——它负责切目标；写在后面 = 后面的条件拿「切之前的目标」求值（静默出错，日志与行为都对不上）
- ★顺带纠正内容错误：萨满「治疗波」门槛改成 `队伍血<40`（desc 写的就是「血线低用大治疗」，旧值 `<70` 会把大治疗砸在 65% 的人身上）
- ★★**审计必须落成可执行的闸门**，否则只是这一次的手工动作。四道闸门 + 端到端：① 模版不许再出现「选取目标:队伍成员/团队成员」② 成员条件必须在行首 ③ 文本里写了几条成员条件、解析后就必须有几条（**静默丢弃 = 那一行变成无条件施法**）④ 成员施法模版的每一行都得自己带成员条件 ⑤ 拿**模版原文**真跑 `EVAL_RULE_RUN`（真扫人、真切目标、技能真打在乙身上）
- **测试**：**变异 11/11 全捕获**

## 🧩 v1.71.6 — 案例模版窗改流式（自动换行）布局

- 组标题与按钮**一起从左往右排、放不下就换行回左边界**（不再「一行一个 / 一列一组」）；按钮宽度 = `FontString:GetStringWidth()` **实测** + 内边距（拿不到退化为「字符数 × 9px」）；窗口高度**由内容算出**（中文 660×180）
- ★**组标题不许孤悬行尾**：换行判断要求「标题 + 该组第一个按钮」能一起放下，否则先把标题换到下一行
- ★★★**真实数据测不出换行**（每组的按钮串刚好放得下 → 删掉换行判断、甚至改成「一行一个」，真实数据下的多数断言照样绿）→ **合成数据**入口 `EVAL_TEST_TPL_PLAN_FAKE` 喂 60 个宽按钮逼出换行路径
- **测试**：**变异 7/7 全捕获**；★判「捕获」要**同时看退出码与断言文本**（`: FAIL` 只覆盖源码检查，行为断言打的是 `ASSERT FAIL`）

## 🧹 v1.71.5 — 案例模版补「一键队伍驱散 / 一键团队驱散」

- 队伍/团队功能组补两条驱散模版，覆盖 **魔法 + 疾病**两条链（牧师 = 驱散魔法 + 祛病术；骑士把两个法术名都换成「净化」；德鲁伊/法师/萨满改成 毒/诅咒 + 自己的解咒法术）
- ★**类型写错一个字就谁都不解，而且不报错** → 补「类型解析体检」断言（`cd.dt == "Magic"`），模版总数 25 → **27 条 / 11 组**
- ★★**贪心分列的教训**：「按顺序塞到过半就停」在 25 条时正好 18/18（看着没问题），**加了 2 条模版就变 22/16** → 改成**遍历所有切点取两列行数差最小**，断言同时钉「选的就是最优切点」
- **测试**：**变异 3/3 全捕获**（删一条驱散模版 / 类型写错字 / 退回贪心分列）

## 🧩 v1.71.4 — 案例模版窗两列改版 + 模版扩到 11 组 25 条

- 窗口**加宽到与配置窗同源**（中文 660 / 西文 800）+ **分两列**；★**按职业组分列**（一个组绝不被拆到两列——拆开看着像少了一组），两列行数自动对半
- 标题上加**金色文字感叹号** + 悬停提示「如果您有好的配置方案，欢迎在插件评论里分享给大家！」（★不用那两枚有固定含义的 mark 图标，避免语义叠加）
- **模版 6 组 → 11 组 25 条**：新增 牧师 / 德鲁伊 / 术士 / 萨满 四组常用一键宏 + **队伍/团队**功能组（一键队伍治疗 / 一键团队治疗 / 一键队伍buff / 一键团队buff）；通用法系补「队伍/团队刷 buff（智力/耐力）」；骑士加按职业区分的 力量/智慧 祝福
- ★★★**「职业过滤」必须同时支持候选者条件**：解析白名单 / 导出（`teamFilterSuffix`）/ 编辑器「职业过滤」格**三处一起**放开（改一处漏两处 = 半个功能）
- ★新增**内容质量断言**（模版是数据，数据的错全是静默的）：名字非空 / 同组不重名 / 首行 `# 方案: X` **等于**模版名 / 至少 1 条技能行；并点名检查用户要的那几条确实在
- ★**抓到一个真盲区**：新加的 5 个数据文件**忘了加进 `test_engine.js` 的装载表** → 新模版一条都没被测，而 `getn(EVAL_IO_TEMPLATES)==6` 的断言**照样绿**
- **测试**：**变异 10/10 全捕获**

## 🛑 v1.71.3 — 停止攻击 / 跟随 / 分享与接收重做 / 频道屏蔽 / 自动购买重做 / 图标体系 / 图标库 Tab / 成员选取器

- **「角色行为」新增 `停止攻击`**：一次点击停掉【读条 + 自动射击 + 魔杖自动重复 + 近战普攻】（≈按 ESC，不占动作条）；★`SpellStopCasting` **一个调用覆盖三类**且【Protected: yes】→ 必须走 `RunScript`（**直调在真机静默无效**）；★`AttackTarget` 是 **Toggles** 语义 → 必须先确认「当前真在挥击」才敢按，否则会把没在打的人打起来；日志如实报账停了哪几类
- **「角色行为」新增 `跟随`**：`跟随`（跟当前目标）/ `跟随:名字`；★API 取证：Movement 分类**只有 `FollowByName` / `FollowUnit` 不是 Protected**（★这也**回顾性解释了**上一轮「移动脉冲」为什么实测无效）；直调、不走 RunScript；真接口无返回值 → 日志如实写明
- ★★★**顺带修掉的老 bug：全角冒号「：」**——中文输入法打出的全角冒号进 Lua 的 `[...]` 字符集只吃掉首字节 → 选取目标 / 物品 / 宠物 / 姿态 / **条件解析全线静默失效**；修法 `colonNorm()` 在**解析入口**归一
- **分享**：删掉「团队」频道（1.12 系里队长/团长聊天走**独立事件**，只注册普通队伍/团队事件的接收端**永远收不到**，且失败静默）；发送频道走**白名单单一真值**；接收端**只保留最新一份 + 同一发送者旧缓冲立即作废 + 四道上限**（在途 4 笔 / 单笔 40 片 / 解码 8192 字节 / 去重表 64 条）+ **提示限频 5s**；弹窗标出来源频道；自己方案的回声自动忽略（★「说」这个例外必须留着，否则没法自测）
- **屏蔽频道进出信息**（工具箱 → 队伍/社交，**默认开**）：判据 = 频道词 **且** 进出词（多要一个频道词是为了不误伤真人聊天）；★用户实测「不生效」→ 两个根因：判据**漏了「进入」** + **载入那刻聊天框还没建好、失败后再也没人重试** → 事件/每帧重试 + `/eh go 频道 装` 手动重装；新增源码检查 `CHAN RETRY WIRING`
- **自动购买重做**：**一拍只下一笔**（全部走 0.3s/笔限频队列）+ **逐笔核对**（连失败 3 次自动停用该条目）+ **每次数量四重夹取**（每次 / 商人一次上限 / 库存 / 还差多少）+ 专用设置窗（启用 | 名称 | 总数 | 每次）；★「购买数量」= **绝对值目标**（背包够了就一件不买）
- **图标体系**：类别下拉带图标（标签/取项/图标同一张表 = 单一来源）、「取消施法」异常标记（白感叹号 + 悬停说明）、分享弹窗美化（标题栏 + 详情底板 + 两按钮以窗口中线对齐）；素材**自包含**拷进 `media/icons/`；新增源码检查 `UI ICON`（纹理路径逐个解析到磁盘核对）
- **第 5 个 Tab「图标库」**：读客户端内置宏图标表（`GetNumMacroIcons` / `GetMacroIconInfo`），按文件名前缀分组、悬停显示路径、可搜索翻页（★**先过滤再分页**）；取不到时状态机如实区分 `noapi` / `empty`
- **「选取目标:」族图标 + 队伍成员选取的如实日志**：11 项各一张专属图标（表里没有的仍回退问号）；选取失败时日志点名「**自身* 条件筛不出队友**」并给出正解（目标血% / 目标buff / 目标debuff）
- **成员选取器：候选者条件 + 按比较符选人**：新增 4 类「候选者」条件（原 8 项一字不动）；条件类型下拉**按技能行过滤**；**选人规则 = 行内第一个比较型条件**（`<` 取最小 / `>` 取最大 / `=` 取最接近 / 无 → 血量最低）；队伍/团员条件加**职业多选 + 小队多选**（★这里的空集 = 不过滤，与「目标职业」空 = 永不满足正好相反）
- **测试**：新增组 77~90（待测试标记改版 / 图标 / 图标库 / 选取目标日志 / 候选者条件 / 成员过滤 / 停止攻击 / 跟随 / 频道屏蔽 / 接收规则 / 自动购买），**变异多组全捕获**

## 🧙 v1.71.2 — 条件归组 + 配置窗整理 + 案例模版外置 + 一批「静默失败」修复

- **施法族条件改名并统一归入「技能状态」组**：施法中 / 施法时间 / 施法剩余时间 ‖ 目标施法中 / 目标施法时间 / 目标施法剩余时间（两侧镜像）
- **配置窗**：底部删掉与开关重复的 [接收] 按钮、隐藏「开关」标题、[分享] 加**指引 tooltip**；开关组改横排一行（4 项）并**下移 40 像素** → 窗口高度同步 420 → 460（★算清楚空间再动：-389 的横排底边在 420 高的窗口里会越界 5px）
- **案例模版拆到 `examples/`**（按职业分文件、由 `.toc` 载入 —— 本客户端没有文件读取 API，「从文件载入」的唯一形式就是列进 .toc）；新增 `EXAMPLES TOC CHECK`（磁盘 ↔ .toc 双向一致 + 顺序 = 选单顺序）
- **修一批「静默失败」**：自动接取任务的**空白日志**（`GetQuestLogTitle` 返回的是**空串**，而 Lua 里空串是真值 → 入库层与播报层各挡一道）、光环下拉里混进「清凉的泉水」这类物品、**未知光环名不得被当成「确定没有」**（骑士虔诚光环实测事故）、光环纹理优先级（学习表 > 动作条代理）、条件类型下拉**最后一组被行池静默截断**（菜单需 54 行、行池只有 48 → 末行改为**如实提示**「还有 N 项未显示」）、EditBox 的「黑块」（`Hide()` 后文字/底条仍会被绘出 → 挪出可视区 + 不透明背板）
- ★**修「方案列表不刷新」**：刷新必须放在**写入点**（`ioImportText`），而不是散落各调用点（分享弹窗就漏调了）
- **测试**：新增 `WINDOW SIZE` / `COMMENT SWALLOW` 等源码检查；多组变异全捕获

---

## 🩺 v1.71.1 — 队友/团员扫描（一键奶/一键驱散）

- **新增行为类型**：`选取目标: 队伍成员` / `选取目标: 团队成员`——**自动扫描**，并用**这一行自己的条件列表**逐个候选过滤（如 `目标血量<60%` / `目标buff:回春术` / `目标debuff:痛苦诅咒`），候选按**血量升序**（最危险优先），命中即切目标并记 `st.allyUnit`
- **新增条件类型「队友/团员状态」**（共 8 项）：`队友血量% / 队友蓝量% / 队友debuff / 队友缺buff` + 团员同名四型；debuff 型带**类型下拉**（任意/魔法/诅咒/毒/疾病），配置方式对齐已有的 目标buff/目标debuff
- **典型用法**：`队友血量%<60 & 就绪` → 找出最该治的人、切过去、治疗落在他身上；`队友debuff(魔法) & 就绪` → 找出中魔法的人、切过去、解他。**单条条件即可自足**，不强制配选取器
- **设计要点**：★**惰性扫描**（`UPDATE_STATE` 绝不扫描——它被战斗信息UI 每 0.15s 调一次；真正扫描在按宏那一轮触发一次并缓存复用）；★**诚实失败**（不在队/团里、队友无蓝、无人符合 → 如实返回 false 并给原因，绝不退回自身数据）；★**UI 预览(dry) 绝不切目标**；★扫描范围只扫 `party1..party4`（官方文档：party 索引只到 4，且团队里 `GetNumPartyMembers` 返回「团人数-1」）
- **数值口径**：`UnitMana` 是主能量且有显示缩放（怒气÷10/幸福÷1000）→ 蓝量条件**先判 `UnitPowerType==0`**，怒气/能量职业跳过而不是当成 0% 蓝
- **测试**：组 66（扫描/缓存复用/选取器过滤/条件自足记人+切目标/dry 不切/端到端一键奶真跑/诚实失败/往返/UI 8 行/三语言）；**变异 33/33 全捕获**
- 另含此前在途未发布的：配置窗加宽 660/800 + 条件「剩余时间」参数；UnrealQuest 依赖 5 状态探测 + 语言来源真 bug 修复 + 技能编辑窗高度回归修复（新增 WINDOW SIZE / COMMENT SWALLOW 两道源码检查）

## 📡 v1.71.0 — 方案分享（聊天频道直发直收）

- **发送**：导入导出窗第二排新增「分享」按钮 → 频道下拉（公会/队伍/团队/说，自动校验在不在该频道）→ 当前方案 hex 编码分片发出
- **接收**：监听聊天事件（免疫学习器同款通道），收齐分片自动弹窗「xxx 分享的方案《名》（N 个技能）→ [导入] [忽略]」
- **技术要点**：SendChatMessage 是 Protected → RunScript 队列绕行；hex 编码防聊天系统吃掉 `|`/引号/链接符；点击发生在自家弹窗按钮上（规避 SetItemRef 未知数）；同方案重复收不重复弹
- **隐私开关**：IO 窗「接收分享:开/关」，关了收消息也不弹窗
- 另见调研结论文档：.dsh/reports/ 内分享可行性分析（SetItemRef 未知数的规避设计）

## ⏱️ v1.69.2 — 任务通知滞后一个状态修复
- **根因**：QUEST_LOG_UPDATE 事件先于任务日志数据落地——事件处理器里同步扫描读到的是【上一状态】，下次事件才把上一变化差分出来，进度/接取通知永远滞后一拍（参照 UnrealQuest `Quest/QuestState.lua` 设计结论：不信任事件时序，轮询重建+事件仅作唤醒）
- **修法**：事件只排期 `tbQScanDue = now + 0.3s`，OnUpdate 调度器 `tbQuestTick` 到点才扫描差分；0.5s 节流改为顺延制（节流中重排到窗口结束，最终状态不丢失）
- **测试**：组 41 改走 `EVAL_TB_TICK`（= 真实 OnUpdate 本体），新增「事件当下不同步扫」断言

## 🧾 v1.69.1 — 自动售卖明细日志
- **要点**：自动售卖每笔成交验证后输出明细：物品名称 ×数量（物品种类/子类），末尾仍保留件数汇总
- **典型用法**：工具箱勾选「自动售卖灰色物品」，与商人对话后聊天窗逐笔显示 `售出：xx xN（种类）`
- **设计亮点**：明细在出售状态机验证**成交后**输出（非尝试时），不虚报；种类读 GetItemInfo 本地缓存，未缓存兜底显示 `?`；wiki 确认本客户端 GetItemInfo 仅 9 返回值**无售价字段**，故不含金额

## 📢 v1.69.0 — 任务通知 + 交接限频（工具箱·任务组拆分）

- **交接 API 限频**：AcceptQuest/CompleteQuest/GetQuestReward 全部进 1.68.2 的限频队列（0.3s 滴出，对话框滴出期内保持打开），与商人动作同节拍防踢线
- **交接日志**：接取/交任务/领奖励各打一条聊天日志（交任务带任务名——QUEST_PROGRESS 时扫日志记录 isComplete==1 的条目）
- **任务进度通知**：QUEST_LOG_UPDATE 0.5s 节流 + 任务日志扫描差分——新出现=接取通知、目标计数增加=进度通知（带目标文本）、交任务=完成通知；首次扫描只建档不刷屏
- **频道可选**：工具箱「任务通知」组新增频道行——关/仅自己/说/队伍 四档下拉；SendChatMessage 是 Protected（wiki 原文）→ RunScript 队列绕行（1.49.3 范式），且进限频队列防刷屏
- **分组调整**：任务 → 自动交接 / 任务通知 两个组
- 测试组 41（通知差分/频道路由/关=静默）+ 组 11 任务断言适配队列化；时间单调递增

## 🧰 v1.68.0 — 配置窗新增「工具箱」Tab

- 独立载入 `Toolbox.lua`（toc 末尾，自绘 UI 不改主程序），配置窗第三个 Tab：分组归类 + 滚动窗口（滚轮/▲▼，为后续扩容准备）
- **商人助手**：自动修理（带花费提示）/ 自动卖灰 / 自动购买指定物品（补足到目标数量）/ 自动丢弃指定物品（仅灰白品质防误删，BAG_UPDATE 0.5s 节流）
- **队伍/社交**：团队就位自动确认（ConfirmReadyCheck）/ 屏蔽公会成员上下线提示（SetCVar guildMemberNotify）
- **任务助手**：自动接取/交付（多奖励留手选）
- 全部功能默认关闭逐项勾选；API 缺失静默跳过；配置存 EVAL_HELP_CONFIG.tb
- 功能集 API 经官方 wiki 全量审计（.dsh/reports/toolbox-api-audit.md）

## 🗡️ v1.67.2 — 盗贼案例模版

- 模版库新增「盗贼」分组（用户实配收录）：`邪恶攻击 | 能量>70` 攒星 → `剔骨 | 能量>50 & 连击>4 & 自身血<30`（血线下 4 星即收）→ `剔骨 | 能量>50 & 连击>=5`（满星终结）
- 与 1.67.0 连击点条配套：5 格实时看星数，终结时机一目了然

## ⭐ v1.67.0 — 战斗UI 连击点条

- 战斗信息UI 能量条下方新增**连击点段**（盗贼/德鲁伊专属，其他职业不占行高）：条状分割为 **5 等宽小块**
- 未激活=浅灰、激活=高亮金填充——0~5 点实时跟随（数据走 st.combo，与「连击点数」条件同源）

## 🖼️ v1.66.0 — 选取目标类图标=目标头像

- **可行性验证**：wiki 确认 `SetPortraitTexture(texture, unit)` 存在（把单位肖像画到 Texture 控件上）——比 Model 控件方案简单得多
- 战斗信息UI 技能带：**选取目标类技能**（最近敌人/最近友方/指定名称…）有目标时图标实时显示**目标头像**，切目标随刷新节拍即变；无目标回退原图标
- 用途直观：补 buff 方案里一眼看到当前选中的友方是谁

## ↩️ v1.65.0 — 接管时序还原（撤销 1.62.0 后移）

- 用户判断正确：1.62.0「接管动作后移到规则评估之后」是基于**误诊**的防御性修改——当时的真凶是 1.64.0 修掉的 `s and pcall(...)` 多返回截断（可用性恒 false），与接管时序无关
- **还原**：接管动作（AttackTarget/UseAction）回到规则评估**之前**——每次按键先补自动攻击（1.62.0 前旧行为回归；法师魔杖不再「只在空闲按键才启动」）
- **安全性**：可用性条件现只信 noMana 资源信号——开弓/进战不会让冲锋类脱战技能变「资源不足」，不会再误杀；状态采集（st.autoAttack）原位不变
- 测试 29/32 组（空方案驱动）继续全绿

## 🩹 v1.64.1 — GetDifficultyColor 兼容垫片

- 用户报红框：`FrameXML/TargetFrame.lua:130: attempt to call global 'GetDifficultyColor' (a nil value)`——客户端 FrameXML 引用该全局函数但未提供（UnrealQuest ClientAPI 注释确认 absent）
- TargetFrame 在本客户端懒加载（首次选目标才加载，晚于插件）→ **插件全局垫片可到达**：按 vanilla 语义返回 {r,g,b,font} 难度色表（红/橙/黄/绿/灰五档，绿区取 GetQuestGreenRange 兜底 10），已有真函数则不覆盖
- 顺带说明自动攻击接管时序（用户排查）：接管动作 1.62.0 起在【规则都没出手】时才补——法师方案火球/寒冰箭恒就绪恒出手，魔杖自然只在空闲按键上启动，「有时触发有时不触发」是设计行为

## 🐛 v1.64.0 — 可用性真凶根治：Lua 多返回截断

- **真凶**（修正 1.62.0 的接管时序结论）：`local oku, u, noMana = s and pcall(IsUsableAction, s.slot)`——`s and pcall(...)` 是逻辑表达式，**多返回被截断只留第一个**：u/noMana 恒 nil → 可用性条件自引入起在游戏内恒 false。共 4 处（可用×2、未排队×2），全部改显式 if 后直调
- **为何这么久没发现**：冒烟测试只覆盖了可用条件的【显示】路径，没覆盖【执行】路径；游戏内每个带 可用 条件的技能（冲锋/撕裂…）全被误杀——用户「手动能放、探针 true、条件跳过」的三连矛盾就是它的指纹（1.62.0 的接管后移保留，作为防御性时序仍然正确）
- **可用性语义落地**：本客户端 usable 第一返回走缓存态误报率高，新判定只信第二返回 **noMana=资源不足**（用户 0 怒气验证过准确性）——资源不足才挡，其余「不可用」交给客户端自拒（1.59.0 模型，静默无副作用）；未排队/已排队检测同时复活
- 测试组 40：资源不足挡 / 缓存 false 无 noMana 放行

## 🩸 v1.63.0 — 可流血改免疫体系驱动

- **用户定新机制**：`可流血` 条件默认 true；目标在免疫体系内、且免疫记录命中**流血技能**（战士撕裂 / 贼割裂·绞袭 / 德斜掠·撕扯，含英文兜底）→ false。手动白/黑名单（EVAL_BLEED_WHITELIST/BLACKLIST）保留为覆盖层
- **删除生物类型硬排除**（元素/机械不可流血）：本客户端 `UnitCreatureType` 返回值不可靠——用户实测「黑暗犬」（恶魔）被误判不可流血，撕裂规则被永久挡死。类型猜测不如免疫实测：放一发撕裂，客户端回「免疫」→ 免疫学习器（1.36.0）自动记下 → 可流血自动转否
- 测试组 39：默认 true / 流血免疫→false / 非流血免疫忽略 / 黑名单仍生效

## 🎯 v1.62.0 — 冲锋疑案真凶：接管时序（接管动作后移）

- **证据三连**（用户提供）：条件「可用」跳过 × 探针 `IsUsableAction(57)=true` × 手动点击可放；存档条件 `{k="usable"}` 无 inv，与探针调用逐字节一致——唯一差异只剩**调用时序**
- **真凶**：EVAL_GO 的自动攻击接管在规则评估**之前**执行 AttackTarget——开弓/进战瞬间，冲锋这类【脱战限定】技能的可用性缓存翻 false；探针/手动点击时没有接管副作用所以一切正常
- **修法**：接管块拆分——状态采集（st.autoAttack）保留原位；**接管动作后移到规则评估之后**，只在本次按键无规则出手时补自动攻击。规则评估时的世界状态=按键瞬间，不再被自家副作用污染
- 测试组 29/32 语义更新（接管动作在无出手时执行，空方案驱动）

## 🔬 v1.61.4 — 可用性疑案结案（跳过原因细分）

- **探针实证**（用户回报 /eh go probe usable 数据）：`IsUsableAction` 判定正常——冲锋 usable=true（含无目标时）；战斗怒吼/断筋/撕裂/英勇打击 usable=false 但全部 noMana=true = **怒气不足的正确判定**（冲锋免费所以不受此影响）。早前那次「冲锋跳过： 可用性」系 /reload 或姿态切换后缓存态短暂滞后，非逻辑 bug
- **跳过原因细分**：可用性判定失败时，调试日志直接显示 `不可用:资源不足`（noMana=true）或 `不可用`——以后不用跑探针也能区分（两处判定路径同改）

## 🚑 v1.61.3 — 条件关系列同换全角竖线（1.61.2 内联漏网）

## 🚑 v1.61.2 — 竖线显示修复²（换全角竖线）

- 1.61.1 用 `||` 转义（官方字面竖线写法），但**本客户端对 `||` 渲染异常**（显示成小方块/空白）——用户实测未生效
- 改用**全角竖线 ｜（U+FF5C）**：FZLBJW 中文字体自带全角字符，视觉与 ASCII 竖线一致，且不是转义符不会被吞。uiEsc() 实现一行替换，覆盖点不变（关系列/条件列/预览/聊天/IO 预览）

## 🚑 v1.61.1 — 条件关系「|」显示修复

- 用户报：条件关系切到 `|`（组间或）后显示为空——**`|` 在 WoW 里是颜色转义起始符**（`|cffRRGGBB`/`|r`），FontString/聊天框把单个 `|` 当无效转义序列吞掉，关系列和预览的 `非战斗 | 就绪` 显示成 `非战斗  就绪`
- 修法：显示层统一 `||` 转义（WoW 的字面竖线写法）——新增 `uiEsc()` 助手，覆盖：编辑窗关系列/条件预览、技能列表条件列、保存/添加/方案列表聊天输出、IO 保底预览区
- 数据层（存储/导入导出文本）不受影响，`|` 仍是组间分隔符；EditBox 原文不转义（复制粘贴保真）

## ↕️ v1.61.0 — 技能行下移按钮

- 技能列表行操作从 [上][编][删] 扩为 **[^][v][编][删]**——新增 v 下移（与相邻下一位交换顺序），^ 为原上移（文本「上」改箭头，两键成对）
- 顺序=优先级（1.59.0 起全部顺序执行），调序即调整尝试先后

## 🔬 v1.60.1 — 可用性探针（/eh go probe usable）

- 用户报「技能可用条件在该放时跳过（冲锋跳过:可用性）」——当前判定用 `IsUsableAction(slot)`，wiki 文档明确它 **Uses a cached usable state**（走缓存态，可能滞后或初始为 false）
- 新探针命令：`/eh go probe usable 冲锋`（不带参数 = 当前方案全部动作条技能）——打印每格的 usable/noMana/inRange/冷却/HasAction 原始值
- 在「该放却跳过」的站位跑一次探针，usable=false 而 inRange=true 即可证实是缓存态误判，后续再定矫正方案（换判定通道或缓存预热）

## 📏 v1.60.0 — 技能列表高度衍生

- 配置窗技能列表从固定 8 行改为**按窗口高度动态计算**（(H-120)/24，420 高 = 12 行）——延伸到底部关闭按钮上方，旧版下半截大片空置
- 下翻滚动按钮位置同步跟随最后一行（旧硬编码 -242 只适配 8 行）；行数变化后 offset 收敛/滚动显隐逻辑不变

## 🎭 v1.59.0 — 姿态下拉自适应 + 去除公共CD终止

- **条件「当前姿态」下拉自适应**：旧版硬编码 1/2/3——法师没有姿态、德鲁伊不止 3 个。改为按角色实际姿态栏动态生成（GetNumShapeshiftForms + GetShapeshiftFormInfo），项带名称（如 `1 战斗姿态`）；无姿态栏职业兜底只给 1
- **执行流程去除公共CD机制**（用户决定）：1.53.0 的「角色技能出手即结束本次按键」删除——现在**按顺序把条件符合的规则全部执行一遍**，能不能放交给游戏内置判定（GCD 中 UseAction 静默失败，无副作用）。规则编排直觉化：排在前面的先尝试，客户端自己决定哪个落地
- 测试组 34 语义更新：GCD 技能之后的规则继续执行（宠物指令照常发出）

## 🚑 v1.58.1 — 布局双修复

- **方案栏删按钮偏移**：配置窗方案列表的 [删] 按钮纵向间距写成了 21px，而方案按钮是 19px——每往下一行 [删] 就相对下沉 2px，方案越多错位越严重（用户截图：第 8 个方案的 [删] 掉到 [+] 行上）。改为同 19px
- **战斗UI 方案第二排宽度异常**：1.56.0 自适应布局对后续行按「该行实际方案数」拉伸填满——只剩 2~3 个方案时按钮被拉得异常宽。改为按「整行容量」定宽：满行仍填满右缘，**不满的行保持同宽左对齐**

## 🔢 v1.58.0 — 时间型条件步进修正

- 时间类数值条件（**距下次攻击 / 自身读条进行 / 自身读条剩余 / 读条已进行 / 读条剩余**）参数区改为：步进 **0.1**、区间 **0.0~10.0**、初始 **0.1**——秒级场景下 ±5/0~300 的通用步进完全不可用（一步就飞出有效域）
- 数值显示一位小数（防 0.30000000004 浮点噪音）；连击（±1/1-5）与其他数值（±5/0-300）不受影响
- 文本导入的任意值（如 距攻击<0.5）不受影响，本修正只约束编辑窗步进与初始值

## ⏲️ v1.57.0 — 条件「距下次攻击」（swingLeft）

- 新增数值比较条件（自身状态组）：**距下次攻击 X 秒**——复用 1.55.0 挥击计时（平砍锚点+攻速+施法推迟），文本 `距攻击>1` / `距攻击<0.5`
- 典型用法：`英勇打击 | 怒气>30 & 距攻击<0.5`（挥击落下前瞬间泄怒，不浪费普攻计时）；`压制 | 可用 & 距攻击>1`（离下次挥击还远优先打技能）
- 无计时数据（还没挥过/无攻速）时条件不满足——不会误判放行；测试组 38 覆盖 回环/比较/无数据

## 🔮 v1.56.1 — 法师案例模版×2 + 导入守卫修复

- 模版库新增法师分组（用户实配收录）：
  - **法师一键**：`选取目标:最近敌人 | 非战斗 & 无目标` 开怪 → `寒冰箭 | 范围内:寒冰箭 & 就绪 & 可攻击` → `火球术 | 可攻击 & 就绪` → `霜甲术 | 能量%>30 & 无buff:霜甲术` → `奥术智慧 | 无buff:奥术智慧`
  - **周围补buff**（通用法系）：`选取目标:最近友方 | 目标非战斗` + `奥术智慧 | 无目标buff:奥术智慧`——1.53.0 并行模型下切友+补 buff 一键完成，挨个补一圈
- **★修复导入守卫丢行**：技能名合理性上限 24 字节（8 汉字）是 1.x 时代定的——带前缀的技能名更长（选取目标：指定名称：嗜血者=39B、物品：弱效巨魔之血药水=36B），导入时这些行被**静默丢弃**（测试组 26 扩充时抓出）；上限提至 48 字节（16 汉字），仍挡说明性文字

## 📐 v1.56.0 — 战斗UI 方案切换行自适应布局

- **宽度对齐**：方案行右缘与上方血/能量/目标条一致（旧版固定 12 等分窄格，方案少时右半空置、方案名字挤压）
- **动态换行**：单按钮最小宽 34px 保证名字可读——首行（带「方案」标签）装不下时自动折到第二行、第三行（12 方案最多 3 行），每行按钮**拉伸填满整行**；窗口高度随行数自动撑高
- 布局在窗口重建/缩放时按当前方案数重新计算

## ⏲️ v1.55.0 — 挥击计时（施法学习同机制）

- **可行性验证**：wiki 确认 `UnitAttackSpeed(unit)` 存在（主手/副手秒每击，含急速修正）；挥击时刻锚点走 `CHAT_MSG_COMBAT_SELF_HITS` 战斗文字事件（「你击中/你爆击X造成…」英文 "You hit/crit"）——与 1.40.0 施法时长自学习同一事件机制，无需 SuperWoW
- **战斗信息UI**：读条下方新增金色挥击细条——进度=已过/攻速，文本=「下次攻击 X.Xs / 攻击就绪」（无数据时空条隐藏）
- **状态信息UI**：新增攻速行「攻速 X.Xs · 距下次攻击 X.Xs（副手 X.Xs）」
- **施法推迟**：读条会推迟挥击——锚点取 max（上次挥击， 读条结束时刻），施法后计时从读条结束起算
- 事件名若与本客户端不符则静默无效（不会炸）；测试组 37 覆盖 中英文锚点/法术不误锚/施法推迟/无锚点=nil

## 🗡️ v1.54.3 — 连击点数参数域修正

- 条件「连击点数」在编辑窗用通用数值步进器（±5、0~300）——但 GetComboPoints 只可能返回 0-5，步进到 8 以上条件永远不可能满足（死条件）
- 修正：连击条件专属步进 ±1、限定 1~5；其他数值条件（怒气/血量/秒数）维持 ±5、0~300 不变

## 🎚️ v1.54.2 — 去抖窗口可配 + 执行追踪器

- 用户反馈偶发「按一下执行两次」——去抖窗口从硬编码 0.2 改为**配置项** `cfg.goDebounce`：配置窗全局 Tab「一键宏」组 **执行去抖 [-] 0.30s [+]**（步进 0.05，范围 0~1.0，0=关闭去抖），默认 0.30s
- 新增 **执行追踪器**：每次 EVAL_GO 调用（含被去抖丢弃的）记录时间戳进 `EVAL_GO_TRACE`（最近 12 条）——**`/eh go trace`** 打印相对间隔毫秒与 执行√/去抖× 标记；现象复现时按两下键 → /eh go trace → 截图发我即可看清重复执行的真实间隔（判定是按下/松开双触发还是队列冲刷）
- 注：去抖只能消除间隔小于窗口的重复；若 trace 显示重复间隔稳定大于窗口，需要换思路（如目标切换幂等化）

## ⏱️ v1.54.1 — 执行去抖（连按堆积治理）

- **问题**（用户实测）：频繁按宏后执行滞后，停止按键后目标还在快速切换——wiki 证实 `/run` 宏在本客户端走 **RunScript pending 队列**（queued，按节奏冲刷）：按得比冲得快就堆积，松手后陈旧调用还在逐个执行
- **治理**：EVAL_GO 入口加 0.2s 执行去抖——间隔不足的重复调用直接丢弃（公共 CD 1.5s，节流完全无感）；队列里堆积的过期调用冲刷时几乎全部命中去抖 → **松手即停**
- 插件无法删除客户端的队列本身，这是行为等效方案：陈旧调用不再产生可见副作用
- 去抖命中时调试日志可见（/eh debug）；EVAL_GO1~12 直触同享；测试组 36（castLog 计数信号——接管自带 2s 节流会干扰 attackTried 信号）

## ✨ v1.54.0 — 光环检查四型（合并+新增）

- **合并**：自身有buff+自身无buff → 「**自身buff检查**」（是/否切换）；目标有debuff+目标无debuff → 「**目标debuff检查**」（层数门槛保留，方向随 是/否）
- **新增**：「**目标buff检查**」（是/否 + 实时下拉列当前目标 buff，●标记）——嗜血/迅捷等敌方增益可编程（驱散时机、爆发避让）；「**自身debuff检查**」（是/否 + 实时下拉列自身 debuff，▲标记）——中了减速/沉默再交章
- 实时项四象限配齐：◆目标debuff ○自身buff ●目标buff ▲自身debuff，名称→纹理照常跨会话学习
- 存量兼容：旧 noBuff/noDebuff 数据引擎照常判定；编辑窗打开自动归并为新格式；文本格式不变（`无buff:名`/`无debuff:名<3` 解析为合并结构，回写文本一致）
- 状态表新增 st.targetBuffs / st.playerDebuffs；测试组 35 覆盖 解析回环/引擎判定/反向/缺失 全路径

## ⚡ v1.53.0 — 并行执行模型（仅角色技能占公共CD）

- 用户定义的语义正式实装：**只有「角色技能」（动作条法术）有公共CD、出手即结束本次按键**；角色行为（攻击/自动射击/射击/取消施法/姿态切换）、宠物行为、选取目标、物品使用**全部可并行**——出手后刷新状态、继续评估后续规则（1.52.1 的选取目标续行推广到全部非 GCD 类型）
- 一次按键的真实效果：`选取目标:最近敌人 + 宠物:攻击 + 物品:治疗药水 + 致死打击` = 切目标、宝宝上、喝药、致死——四件事一键完成，只有最后的法术吃 GCD
- 排序即编排：免费动作放前面、压底放法术；GCD 法术之后的规则本次不再评估（优先级语义不变）
- 回归测试组 34：五连并行链（选目标→宠攻→姿态→攻击→法术全执行）+ GCD 技能终止（其后不执行）

## ⛓️ v1.52.1 — 选取目标不消耗按键（切换成功继续评估）

- **问题**（用户实测）：方案 `选取目标:最近友方 | 目标非战斗` + `奥术智慧 | 无buff:奥术智慧`，每次按宏只切目标永远不放奥术智慧——RULE_RUN「第一条通过的规则出手即结束」，恒过的选取目标规则把按键用尽了
- **修正**：切换目标是瞬发无 GCD 动作，不该消耗一次按键——targetSel 规则出手后 **EVAL_HELP_UPDATE_STATE() 刷新状态（新目标）并继续评估后续规则**；一次按键 = 切目标 + 放技能（仍只有一个 GCD 动作）
- 后续条件按新目标判定（可攻击/目标职业/debuff 等不会拿旧目标误判）；姿态/物品/宠物/取消施法等仍是终止动作（有 GCD/终态语义）
- 回归测试组 33：选目标+施法连发 / 单发选取目标仍计动作 / 条件门控不变

## 📏 v1.52.0 — 自动攻击距离检测（超程降档）

- 「自动攻击」的两个远程档（自动射击/魔杖射击）增加**射程检测**：`IsUsableAction` 不含距离判定（自动射击 8-35 码，贴脸也显示"可用"但放不出）→ 叠加 `IsActionInRange`，明确返回 0=超程时**自动降档**，最终落到近战普攻
- nil（无目标/无法判定）放行交给客户端自己拒；测试组 32 覆盖 超程降档近战 / 在程优先远程 两路径
- 顺带：测试桩发现接管 2s 节流（Engine local）在恒定 GetTime 下会卡死后续断言——测试用时间前移绕过（同 1.40.0 读条学习测试惯例）
