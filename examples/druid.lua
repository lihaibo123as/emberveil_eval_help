-- 案例模版 · 德鲁伊（数据文件：由 EvalHelp.toc 载入）
--   为什么是 .lua 文件而不是 .md：本客户端没有文件读取 API（Lua 沙箱无 io/os），
--   插件运行时读不了自己的目录 → 唯一受支持的「从文件载入」就是列进 .toc 当 Lua 模块加载。
--   格式与导入导出完全一致（md 文本），可直接编辑；顺序 = .toc 里的顺序 = 模版选单顺序。
--   ★姿态行**必须守门**：本插件的「姿态:名称」分支已内建「已在该姿态就跳过」的守卫
--     （德鲁伊重复变豹会**取消**当前形态——这就是那道守卫存在的理由，见 Engine 的 wFindStance 注释）。
EVAL_IO_TEMPLATES = EVAL_IO_TEMPLATES or {}
table.insert(EVAL_IO_TEMPLATES,
  { cls = "德鲁伊", list = {
    { name = "猫德一键输出", desc = "起手变豹 → 选敌 → 能量低先猛虎之怒、连击≥4 凶猛撕咬收尾、其余爪击（撕碎要绕背，一键宏不做站位判断，故用爪击）", text = "# 方案: 猫德一键输出\n\n" ..
      "- 姿态:猎豹形态\n" ..
      "- 选取目标:最近敌人 | 无目标\n" ..
      "- 猛虎之怒 | 能量%<25 & 可攻击\n" ..
      "- 凶猛撕咬 | 连击>=4 & 可攻击\n" ..
      "- 爪击 | 可攻击" },
    -- ★1.71.7 审计：成员类条件写在**行的最前面**——「队伍血<70」自己扫人 + 切目标，
    --   后面的「无目标buff:回春术」才是对**切过去的那个人**求值（顺序反了就会拿旧目标求值）。
    { name = "奶德一键", desc = "扫队伍里血最少的那个人（<70%）→ 没回春就先挂回春 → 治疗之触（蓝不够不硬放）", text = "# 方案: 奶德一键\n\n" ..
      "- 回春术 | 队伍血<70 & 无目标buff:回春术\n" ..
      "- 治疗之触 | 队伍血<70 & 能量%>15" },
  }})
