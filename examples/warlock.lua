-- 案例模版 · 术士（数据文件：由 EvalHelp.toc 载入）
--   为什么是 .lua 文件而不是 .md：本客户端没有文件读取 API（Lua 沙箱无 io/os），
--   插件运行时读不了自己的目录 → 唯一受支持的「从文件载入」就是列进 .toc 当 Lua 模块加载。
--   格式与导入导出完全一致（md 文本），可直接编辑；顺序 = .toc 里的顺序 = 模版选单顺序。
--   ★三条 dot 各自「缺哪个补哪个」（无debuff:名），按顺序即优先级；补完用暗影箭填充。
EVAL_IO_TEMPLATES = EVAL_IO_TEMPLATES or {}
table.insert(EVAL_IO_TEMPLATES,
  { cls = "术士", list = {
    { name = "暗影术士", desc = "暗影三咒：痛苦诅咒/腐蚀术/献祭缺哪个补哪个，暗影箭填充（顺序即优先级）。", text = "# 方案: 暗影术士\n\n" ..
      "- 选取目标:最近敌人 | 无目标\n" ..
      "- 痛苦诅咒 | 无debuff:痛苦诅咒 & 可攻击\n" ..
      "- 腐蚀术 | 无debuff:腐蚀术 & 可攻击\n" ..
      "- 献祭 | 无debuff:献祭 & 可攻击\n" ..
      "- 暗影箭 | 可攻击" },
    { name = "灵魂收割", desc = "敌血两成以下，吸取灵魂收割（击杀顺带补灵魂碎片）；其余情况暗影箭。", text = "# 方案: 灵魂收割\n\n" ..
      "- 选取目标:最近敌人 | 无目标\n" ..
      "- 吸取灵魂 | 目标血%<20 & 可攻击\n" ..
      "- 暗影箭 | 可攻击" },
  }})
