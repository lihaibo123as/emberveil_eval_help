-- 案例模版 · 萨满（数据文件：由 EvalHelp.toc 载入）
--   为什么是 .lua 文件而不是 .md：本客户端没有文件读取 API（Lua 沙箱无 io/os），
--   插件运行时读不了自己的目录 → 唯一受支持的「从文件载入」就是列进 .toc 当 Lua 模块加载。
--   格式与导入导出完全一致（md 文本），可直接编辑；顺序 = .toc 里的顺序 = 模版选单顺序。
--   ★图腾行用「无buff:<图腾名>」判断：图腾给的增益就挂在**你自己的增益条**上（图腾名 = 光环名）。
EVAL_IO_TEMPLATES = EVAL_IO_TEMPLATES or {}
table.insert(EVAL_IO_TEMPLATES,
  { cls = "萨满", list = {
    { name = "元素萨满", desc = "先立图腾（石肤/大地之力，可换成风怒/法力之泉）→ 闪电箭引雷填充。", text = "# 方案: 元素萨满\n\n" ..
      "- 选取目标:最近敌人 | 无目标\n" ..
      "- 石肤图腾 | 无buff:石肤图腾\n" ..
      "- 大地之力图腾 | 无buff:大地之力图腾\n" ..
      "- 闪电箭 | 可攻击" },
    { name = "先祖之泉", desc = "为小队血最少的那个（<70%）施治：低于 40% 用治疗波，否则次级治疗波。★法术名按你实际学的等级改。", text = "# 方案: 先祖之泉\n\n" ..
      "- 治疗波 | 队伍血<40 & 能量%>40\n" ..
      "- 次级治疗波 | 队伍血<70 & 能量%>15" },
  }})
