# EvalHelp —— 全职业施法工具

> 🌍 Languages: **中文** · [English](README_en.md) · [Русский](README_ru.md)（游戏内语言：配置窗右上角国旗切换）

> EmberVeil（1.12.1 / Lua 5.1）**全职业施法辅助插件**：宏正文一行 `/run EVAL_GO()`，规则引擎驱动——**技能五分类**（角色行为/角色技能/宠物行为/目标选取/物品使用）× **49 种条件类型**（分组下拉；成员选取器行另有 4 种「候选者」条件）× 多方案绑定按键，全部可视化配置，不限职业。另附三个内嵌页：**工具箱**（商人 / 社交 / 任务自动化）、**数据检索**（任务 / 物品 / 怪物·NPC + 世界地图标注层，依赖 UnrealQuest）与**图标库**（客户端内置宏图标：分组 / 悬停显示路径 / 搜索翻页）。

| 模块 | 入口 | 一览 |
| :-- | :-- | :-- |
| 🗡️ **通用一键宏** | 宏 `/run EVAL_GO()` | 技能五分类：角色行为（攻击/姿态切换）/技能/宠物指令/目标选取/物品使用；方案技能数不限（列表可滚动）；多方案 ≤12 各绑按键直触 |
| ⚙️ **配置窗口** | 小地图 EH 图标 · `/eh cfg` | 方案/技能/条件全可视化编辑，文本导入导出分享 |
| 📤 **方案分享** | 配置窗「一键宏设置」页 `[分享]` / 聊天链接 | 分片发到 公会/队伍/说（限频防刷屏），对方点 [导入] 才入档；**品阶封皮**（`[品阶秘籍·名]` 可点链接 + 弹窗确认）· **品阶评分**（覆盖 ≥4 大类且 ≥50 分才神级 · 五色）· **头衔抽卡**（5档×15张 · 名号自定义）· 彩蛋「创世者亲临」· 收方按（头衔档×秘籍档）抽奖式回一句角色扮演反应 |
| 🧩 **案例模版** | 配置窗「一键宏设置」页底部 `[案例模版]` | **12 组 35 条**现成方案（按职业分组）→ **分组分两列 + 组内同行自动换行**；点一下即导入，悬停看方案内容 |
| 🧰 **工具箱** | 配置窗 Tab3 | 商人助手（自动修理 / 售卖灰色 / 按名购买·丢弃）+ 队伍社交（就位自动确认 / 屏蔽公会上下线 / **屏蔽「加入·离开频道」通知**）+ 任务自动交接（默认 Shift 按住临时暂停）+ 任务通知频道 |
| 🗺️ **数据检索** | 配置窗 Tab4 | 任务 / 物品 / 怪物·NPC / 对象 四类检索，输入即搜、无限级联下钻；**世界地图标注层** 16 类别随切图实时重绘，条目带坐标可一键定位（依赖 UnrealQuest） |
| 🖼️ **图标库** | 配置窗 Tab5 | 客户端内置宏图标按前缀分组，悬停显示路径、可搜索翻页；另有「本插件在用」一组（直接列出本插件用到的图标） |
| 📊 **战斗信息UI** | `/eh ui` | 血/能量/目标条 + 方案切换行 + 技能图标行（亮金=条件满足，点开编辑） |
| 🔍 **状态信息UI** | `/eh st` | 状态变量实时总览 + 最近释放条件逐项判定 √× |
| 📝 **状态日志** | `/eh` | 聊天框 + 日志文件双写 |

> 📌 持续优化中，欢迎测试反馈！　🐞 [问题反馈 / 建议](https://gitee.com/xeval/emberveil_eval_help.git)（Issues）　🤖 DeepSeek Harness AI 辅助开发（见文末「参与开发」）

## 🏁 里程碑（1.52.0 → 1.75.1）

- **🧭 数据检索新增「任务线」：经典任务线按奖励排序 · 武器优先**（1.75.0）：10-30 级 **22 条经典任务线 / 88 个任务 / 62 件奖励（含 39 件武器）**，全部逐条抓取自 `database.emberveil.org` 并核对；**奖励优先展示**（精良武器排最前：旋风武器 · 维里甘之拳 · 西部荒野法杖 · 新月法杖/翼刃 · 墓碑节杖 · 逃犯弯刀），每条链给**完整上下级步骤**（任务 ID · 等级 · 地区，按接取顺序）；奖励行**悬停出原生物品详情**、步骤行**点击进任务详情**；★抓取/生成脚本**独立在 `quest/`**（`fetch.js` + `chains.js` + `build.js`，与插件**零耦合**，`node quest/build.js` 可重生成）；数据自带 ⇒ **UnrealQuest 缺席时任务线照样能查**；★「地图标注」右侧新增 **[任务线推荐] 弹窗**（★**装备优先**：按可获得等级列出装备 + 真实物品图标 → 点装备**反查来源任务线**与完整步骤 → 一键**跳到数据检索**查这件装备；搜索 + 筛选（装备：等级/类型/阵营 · 任务线：档位/阵营）· 分页 + 滚轮 · 可拖动）。
- **⏱️ 射击计时 + 条件「距下次射击」+ 界面三处补完**（1.74.29 → 1.74.30）：猎人自动射击**有了自己的计时**（探针真机取证定案：法术频道里含「自动射击」的原文 = 锚点、`UnitRangedDamage[1]` = 射速，与近战**两套锚点互不干扰**），新增数值条件「距下次射击」；战斗UI **激活方案格镶品阶色边框**；两个助手的弹窗候选出**原生物品详情**；喂食助手按 `GetPetHappiness()` 给图标镶**快乐度边框**（开心亮绿）；消耗品助手左键不再弹窗。
- **🔔 评级门槛 + 分角色存档 + 稀有提醒转播**（1.74.19 → 1.74.28）：方案评级重做（空技能 0 分 · 条件按类加权 1/2/3 · 单条上限 12 · 覆盖不足 4 大类就封顶 · **神级 ≥ 50 分**）、三个助手配置**分角色存**、案例模版收录用户分享的「神圣风暴」、**稀有提醒转播**（任务插件发现稀有那一刻 → 聊天框报一行：名字按品阶染色、可点 —— **左键切目标 / 右键在地图上标出刷新点**；点名字就是一次目标函数调用，选不中就报错；整块现已提取为 `tools/RareWatch.lua` + 工具箱开关；两个助手的弹窗候选已按物品类型过滤（武器/装备/灰色/材料药草矿物/任务物品 全剔））。
- **🎬 分享体验闭环**（1.74.1 → 1.74.6）：接收/拒绝 × 有/无目标 × 三语言 **40 条场景描述**（头衔按档位染色 · 实测出「色码必须带链接」这条铁律）· 方案**作者/来源**四类 + 方案列表 tooltip（评级 · 来源 · 技能数）· 「不弹窗」**8 条分叉全部落日志** · 彩蛋反应按用户要求**不再接线**（机制与文案保留）· 修「回声忽略」误判（别人发的、你库里已有同款时被误吞）。
- **🧰 工具箱两个助手 + 负面类型**（1.74.5 → 1.74.6）：**猎人助手 → 一键喂食** 与 **消耗品助手**（共用 8×N 图标网格 · 多选 · 用光变灰；未实测的功能在右侧标**黄感叹号「待测试」**）；**自身/目标 debuff 支持「负面类型」**——与队伍/候选者 debuff 同一套语义（名称留空 + 类型 = 「有任意该类型」）。
- **🎉 分享系统重做：封皮 · 品阶 · 头衔 · 彩蛋 · 反应**（1.73.35 → 1.74.0）：85 个提交一次发布 —— 分享封皮行（`[品阶秘籍·名]` 可点链接、点击弹窗确认不自动导入）、品阶评分制（1.74.21 起：**≥50 分且覆盖 ≥4 大类**才神级 · 五色含亮蓝）、**头衔抽卡**（5 档×15 张 · 只升不降 · 名号自定义+名号色）、彩蛋「创世者亲临」（手动方案≥3+神级+头衔满档三条件闸门）+ **角色扮演反应**（按头衔档×秘籍档抽奖式、公会/说/队伍三频道、方案名可点链接开接收页）。
- **🧪 链接/色码的真机取证体系**（1.73.35 → 1.74.0）：自定义链接过服务器、消息真实长度上限 250B、悬停机制、落盘账本探针 —— 真机反复定案出「色码发送规则」（一段色码打头 + **必带链接**、限频 **1 秒/条**、只认 `[EHPF#]` 标识）。
- **🖥 标题栏徽标 + 品阶染色**（1.73.55 → 1.74.0）：战斗信息UI / 状态信息UI 标题栏同一套 —— 玩家名 → 头衔 → 品阶徽标 → 当前方案名；方案行按品阶染色（HUD + 配置窗 + 案例模版同一套品阶判定）。
- **💬 聊天名字：着色 + 右键菜单**（1.73.12 → 1.73.29）：查清「名字在 `arg2`、名字槽**不吃富文本**」后走**方案A**（吞掉客户端那行、自己拼整行）真正染上职业色；右键名字给出**悄悄话 / 邀请 / 目标 / 公会邀请 / 复制名字**（复制名字 = **只把 `/s 名字` 填进输入框、绝不代发**）；菜单半透明窄条 + 高度自适应。
- **🧰 工具箱弹窗规程**（1.73.30 → 1.73.34）：弹窗**层级阶梯**（配置 10 · 编辑 100 · 输入 220 · 工具箱 200 · 下拉 250）、拖动柄必须是 Button、列几何单一来源，并把**滚动条**按规范做齐（滚轮方向单一来源 `EVAL_WHEEL_DIR` · 专用滚动槽不压数据列 · 整数页码 · 边界隐藏箭头）。

## 更新日志

| 版本 | 主题 | 一句话亮点 |
| :-- | :-- | :-- |
| **1.75.0** | 🧭 **数据检索新增「任务线」+「任务线推荐」弹窗（装备优先）**：10-30 级经典任务线 · **先按等级列可获取装备 → 反查要做哪些任务** · 阵营严格三分类（联盟/部落/共有）· **每个任务节点带放大镜**（本插件 `database` 图标，按任务名直达数据检索且**不关窗**）· **滚动区按窗高铺满**（行数现算，不写死）· 装备详情**大图标 + 大标题**（标题/副标题垂直不重叠 · 悬停即出装备信息）· 翻页改**文字按钮 [上一页][下一页]**（底部按钮顺序 **[上页][下页][← 返回]**、整体左对齐 · **两个视图都常显**）· 焦点离开装备信息**正确隐藏** | 22 条链 / 88 任务 / 62 件奖励（**39 件武器**）逐条抓取自 `database.emberveil.org`；★**奖励优先**：精良武器（旋风之斧/剑/锤、维里甘之拳、西部荒野法杖、新月法杖、翼刃、墓碑节杖、逃犯弯刀）排最前，**每条链给完整上下级步骤**（按接取顺序，带等级与地区）；奖励行**悬停出原生物品详情**、步骤行**点击进任务详情**；★过滤下拉新增「任务线」，空关键词直接列出全部链；★抓取/生成脚本**独立在 `quest/`**（`fetch.js`+`chains.js`+`build.js`，与插件零耦合）；★解耦：数据自带，**UnrealQuest 缺席时任务线照样能查** |
| **1.74.30** | ⏱️ **射击计时**（猎人自动射击）+ 新条件「**距下次射击**」+ 消耗品助手左键不再弹窗 | 探针真机实测定案：锚点 = `CHAT_MSG_SPELL_SELF_DAMAGE` 里含「自动射击」的原文（**暴击词形也认**）、射速 = `UnitRangedDamage` 第 1 返回（实测 2.09s）；与近战**两套锚点互不干扰**，战斗UI 金色细条 / 状态UI 行 / 条件「距攻击」按「是否自动射击中」自动切换（文案分「下次射击 / 下次攻击」）；自校准只在**明显偏离**时才修（不把抖动当急速）；★新增数值条件「距下次射击」（文本 `距射击<0.5`，编辑窗初始值 **0**）；★顺带修「切换条件类型把 30.0 带进时间型」的继承 bug；★消耗品助手主图标**左键不再弹面板**（入口收归右键，左键留给拖动） |
| **1.74.29** | 🖼️ 战斗UI「**激活方案格加边框高亮**」+ 两个助手弹窗候选出**原生物品详情** + 喂食助手**快乐度边框** | 方案格挂 4 条 1px 边框（颜色 = 该方案品阶色，算不出退回暖金），换方案即时换格；选择面板候选格悬停先出**客户端原生物品 tooltip**（品质 / 类型 / 「使用：」效果 / 售价）再补帮手的行；喂食助手主图标边框按 `GetPetHappiness()` 现算：**开心=亮绿** · 一般=金 · 不开心=红 · 没宠物=默认金 |
| **1.74.28** | 🧺 两个助手的弹窗候选**按物品类型过滤** + 一条验证命令 | 三级判定（品质 → `GetItemInfo(链接)` → **自建 tooltip 兜底并顺手写进客户端缓存、自愈**）；武器/装备/灰色/**药草矿物材料**/任务物品/容器/箭矢/钥匙/配方 全剔，判不出就不剔；`/eh go 消耗品探针 过滤` 与 `/eh go 喂食探针 过滤` 把每件物品的判定依据一屏摊开 |
| **1.74.27** | 🧹 稀有提醒转播**提取到独立模块** `tools/RareWatch.lua` + **工具箱开关** | 整块搬到 `tools/`（主程序只留两个接线点）；工具箱新增「稀有提醒」组 + 开关，读写走**实现自己的单一来源**（不另存一份），勾完回执一行状态 |
| **1.74.26** | 🧹 点名字**只做一件事**：调目标函数选中它；选不中**就报错** | 按用户定案删掉了所有与任务插件**机制**的耦合（坐标 / 最近刷新点 / 地图投影 / 开图定位 / 距离文案），链接 token 从 `EHRW:areaId:x:y:unitId` 收成 `EHRW:<名字>`；点一下 = 一次 `TargetByName(名字)`，选中报「已选中目标」、选不中报「选不中目标」；源码检查**反向守**不许再出现地图耦合 |
| **1.74.25** | 📏 切目标**文案纠错**（说清是客户端限制 + 报出距离）+ 目标探针 | 实测截图：那只稀有在 **556 码**外，而文案写的是「它没刷」—— 真因是「按名字选目标只认客户端**附近**的单位」（官方文档明文，远处静默失败）；现在改成「选不到「X」：只认附近单位（它现在约 556 码）—— 走近了再点，或右键在地图上定位」；新增 `/eh go 稀有 目标探针`（用当前目标自己的名字验接口，一次定案） |
| **1.74.24** | 🎨 稀有提醒**按品阶染色 + 名字可点**（左键 = 切目标到它 / 右键 = 地图定位刷新点） | 名字按品阶染色（精英 银 / 稀有 蓝 / 稀有精英 紫 / 首领 橙），整条只留一段 8 位色码；名字是链接：**左键 = 把目标切到那只稀有**（TargetByName，前后比对如实报结果），**右键 = 打开世界地图并标出它最近的记录刷新点**；命令 /eh go 稀有 目标|定位|链接 |
| **1.74.23** | 🔔 **稀有提醒转播**（任务插件发现稀有那一刻 → 聊天框一行） | 任务插件 UnrealQuest 的稀有弹窗出现**那一刻**，聊天框立刻报一行：名字 + 品阶（精英/稀有精英/首领/稀有）+ 距最近刷新点码数 + 同刻还有几只；做法是**包住它的弹窗出口**（RareAlert:Show）而不是轮询，装不上时自动退化为读它的公开计数；开关与状态：/eh go 稀有（开/关/试） |
| **1.74.22** | 🧩 案例模版收录「神圣风暴」（骑士 · Rainbow 分享）+ 🎭 模版名称按**艾泽拉斯体系 × 自身品阶**重命名 + 📝 模版支持**作者/备注** + 🔆 神级配色改**亮蓝** | ①把用户配置里的「神圣风暴」（13 技能惩戒骑）收进骑士模版组：文本由插件**自己的导出器**逐字生成（读存档里那份方案表）并过 `export(parse(text)) == text` 往返校验，不是手抄；②**34 条模版全部改名 + 说明重写**，按各条**自身品阶**取艾泽拉斯名号（普通=凡人世界「旅店歇脚 / 急救药水」· 稀有=精锐「肯瑞托法师 / 无冕刺客」· 珍稀=英雄「游侠领主 / 翡翠追猎」· 绝版=传说「地狱咆哮 / 白银之手」；神级的「神圣风暴」原名已是法术名 → **保留原名**）；③模版条目新增可选 `author`/`note` 字段 —— 悬停显示「作者：Rainbow」「备注：来自龙之国度的LM-Rainbow-骑士无私分享」，导入时按该署名记方案来源（没带 author 的模版照旧记「技能学院」，行为不变）；④**神级档配色**由深红 `c00000` 改为**亮蓝 `00bfff`**（`SH_SEAL_TIERS` 唯一来源：配置窗 / 模版窗 / 分享封皮 / 标题徽标**同一套**，改一处全跟）。 |

> 📜 各版本详细说明见 **[CHANGELOG.md](CHANGELOG.md)**；更早历史见 git 提交记录。

## 🧩 案例模版（12 组 35 条现成方案）

> 不必从零配。配置窗 →「一键宏设置」页 → 底部 **`[案例模版]`** → 按职业分组挑一个，**点一下直接导入**。

| 项 | 说明 |
| :-- | :-- |
| **入口** | 配置窗「一键宏设置」页底部 `[案例模版]` 按钮 |
| **内容** | **12 组 35 条**：战士 / 法师 / 通用法系 / 通用 / 盗贼 / 猎人 / 骑士 / 牧师 / 德鲁伊 / 术士 / 萨满 / 队伍·团队 |
| **版式** | 分组分两列 + 组内同行自动换行；**悬停**该条可看它的技能与条件 |
| **导入** | 点一下即导入为方案，之后照常编辑、改名、绑定按键 |

**队伍·团队组**（6 条）是一键奶 / 一键驱散类，一律写成「**指定技能 + 队伍（团员）条件**」——
条件自己扫描全队、按血量挑人、切目标，放完还原原目标，**不需要你手点队友**。

> 💬 有好用的方案欢迎在插件评论里分享；也可以用配置窗的 `[分享]` 直接发到 公会 / 队伍 / 说 频道。

## 界面预览

**配置窗口 · 全局页**（`/eh cfg` 或小地图旁 EH 图标）：日志/界面开关 + 内置帮助说明

![配置窗口·全局页](preview/main.png)

**案例模版窗**（配置窗「一键宏设置」页底部 `[案例模版]`）：12 组 35 条现成方案按职业分组，**分组分两列 + 组内同行自动换行**，点一下即导入、悬停看方案内容

![案例模版窗](preview/skill_tpl.png)

**技能编辑窗**（点 [编] 或技能图标打开）：条件逐行独立配置——条件类型/参数全部下拉选择（49 种条件类型 + 成员选取器行专用的 4 种「候选者」条件，按 自身 / 目标 / 光环 / 队友·团员 / 技能 分组），关系列切 &（组内全过）/ |（组间任一），实时预览

![技能编辑窗](preview/skill_doif.png)

**技能二级下拉 · 四大类**（技能编辑窗的技能行 = `[分类▾][具体项▾]` 并排双下拉，选项配图标）：

- **角色技能**：白名单 + 动作条扫描全技能
- **宠物行为**：8 种宠物指令（不占动作条直调 Pet API），指令图标经宠物动作条自学习
- **目标选取**：9 种切换方式（最近敌人 / 上一目标 / 目标的目标 / 指定名称 / 清除目标…），按宏先切目标再出手
- **物品使用**：背包实时扫描（带图标+数量）+ 自定义名称——消耗品直接使用、装备自动穿上

**光环条件四型**：自身buff/目标debuff/目标buff/自身debuff 检查（是/否切换，实时项 ◆○●▲ 全配齐）——条件「无buff:再生」驱动一键喝药、「无目标buff:奥术智慧」驱动挨个补 buff

**战斗信息UI + 状态信息UI**（`/eh ui` / `/eh st`）：血/能量/目标条 + 方案切换行 + 技能图标行（悬停 tooltip 看触发条件，亮金=条件当前满足，点击图标直开编辑窗）；状态信息窗实时显示全部状态变量 + 最近释放日志（每次出手的技能/目标/条件逐项判定 √×）

**工具箱（Tab3）**：商人助手（自动修理装备 / 自动售卖灰色物品 / 自动购买·丢弃指定物品）+ 队伍社交（团队就位自动确认 / 屏蔽公会成员上下线提示 / **屏蔽「加入·离开频道」通知**）+ 任务自动交接（可选按住 Shift 临时暂停）+ 任务通知频道（关 / 自己 / 说 / 队伍）

![工具箱](preview/tools.png)

> ⚠️ **依赖**：本页数据全部来自 **UnrealQuest** 插件（任务/物品/生物/对象数据库）。未安装或未启用时，本页只会显示一段安装引导，其余控件隐藏。

**数据检索（Tab4）**：任务 / 物品 / 生物·NPC / 对象 四类检索，输入即搜（前 10 项匹配）；条目可无限级联下钻，物品行悬停显示游戏内物品 tooltip

**数据检索 · 详情与下钻**：任务 / 物品 / 怪物的详情——目标与描述、起始 / 结束 NPC、需求物品；每条关联都可点进去继续下钻，带坐标的行右缘有地图图标

![数据检索·任务线装备](preview/quests_item.png)

**数据检索 · 地图标注**：控制行「地图标注(N)」= 总开关 + 类别多选（16 类：采集物 5 + 服务设施 11，含「全部开启 / 全部关闭」快捷），勾选后随地图切换实时绘制

![数据检索·任务线步骤](preview/quests_line.png)

**抓宠帮手（Tab6）**：检索宠物技能（图标 + 各等级列表）→ 详情页列技能简介 / 需求等级 / **驯服来源**（哪个怪、在哪出没）→ 放大镜一键跳「数据检索」查刷新点 —— 从「能学到某技能的宠物」到「去哪抓」一条龙

![抓宠帮手·驯服来源](preview/pet_skill.png)

## 快速上手

1. 把要用的**技能拖上动作条**：插件按动作条识别技能（任意职业、任意技能都能入方案）
2. 游戏内输入 `/eh go rescan` 让插件识别槽位（`/eh go` 查看识别结果）
3. `/eh cfg` →「一键宏设置」页 → `[添加技能]` 配好条件；或直接 `[案例模版]` 按职业挑一个一键导入
4. **绑定按键（推荐，全程点点点）**：右键任意方案（战斗信息UI 方案行 或 配置窗方案栏）→「方案管理」→ 快捷键下拉选一个键 → `[保存]`，按下即执行；**备选**：习惯宏的话，新建宏正文 `/run EVAL_GO()` 拖到按键上（两条路等价）
5. **第一次用？** 输入 `/eh guide` 看四步新手指引（开战斗UI → 配第一个宏 → 绑按键 → 看技能日志），首次进入游戏会自动播报一遍
6. 技能没放出来？开「方案技能日志」（配置窗 全局页）后 ` /eh logdump` 查看每次按键的判定明细

## 安装

**方式一：下载发布包（推荐）** —— [Releases 页面](https://gitee.com/xeval/emberveil_eval_help/releases) 下载最新 `EvalHelp-vX.Y.Z.zip`，解压到 `Interface/AddOns/`（解压后应为 `AddOns/EvalHelp/EvalHelp.toc`）：

```
https://gitee.com/xeval/emberveil_eval_help/releases
```

**方式二：克隆源码** —— `git clone git@gitee.com:xeval/emberveil_eval_help.git`，把整个目录放到 `Interface/AddOns/` 并确保目录名为 `EvalHelp`。

目录结构：

```
Interface/AddOns/
└── EvalHelp/
    ├── EvalHelp.toc     ← 目录规则：Folder/Folder.toc
    ├── EvalHelp.lua
    └── README.md
```

## 异常自救（遇到问题先看这里）

1. **先 `/reload`**：技能不识别、界面异常、刚更新过插件文件——绝大多数情况重载即恢复（配置已存盘不会丢）；
2. 技能换了位置/新拖上动作条 → `/eh go rescan` 重扫；
3. 开了调试看决策过程 → `/eh debug`（每个技能为什么放/为什么不放，逐条件 √×）；
4. 仍未解决 → 到 <https://gitee.com/xeval/emberveil_eval_help.git> 提 Issue（附 `/eh st` 状态截图 + 日志文件更有助于定位）。
5. **分享来的方案有些技能不放**：多半是那个光环在你这台机器上还没学过纹理 —— 现在会自动按名字扫描并学下来；`/eh go tex` 看学习表、`/eh go texdel 名字` 删一条重试、`/eh go texclear` 全清；

## 参与开发（欢迎加入！）

- 仓库：<https://gitee.com/xeval/emberveil_eval_help.git>（`git clone` 后把 `EvalHelp/` 软链或复制到 `Interface/AddOns/` 即可开发调试）；
- **开发指南**：见 `DEVELOPMENT.md`（架构地图 / 本客户端实测 UI 配方 / 规则引擎与方案数据结构 / 提交前必跑的语法检查）；
- **AI 协作记忆**：`CLAUDE.md` 是本项目的 AI 开发记忆体（DeepSeek Harness / Claude Code 等 AI 助手读它即可快速进入上下文）；
- 开发流程约定：每次改动递增版本号（lua 头注释 + `local VERSION` + toc 三处同步），改完跑 `node luacheck.js` 全量语法解析；
- 扩展新职业：技能白名单 `WAR_SKILLS` 目前内置战士 10 技能，其他职业按同构方式加技能表即可（规则引擎/方案/UI 全部职业无关）；
- 本插件由 **DeepSeek Harness AI** 辅助开发——也欢迎你带着 AI 一起来。

## 支持我们

EvalHelp 从始至终都是免费、开源的，今后也会一直是。

如果它恰好帮到了你，而你又愿意鼓励一下作者——可以赞助一点点算力 ☕。这些心意都会用在刀刃上：更细致的功能打磨、更及时的 bug 修复、更多职业的现成模版。

当然，哪怕只是点个 Star、提一条建议、把它推荐给公会里的朋友，对我们来说同样是莫大的支持。谢谢你看到这里。

| 支付宝 | 微信 |
| :---: | :---: |
| ![支付宝收款码](pay/bao.jpg) | ![微信收款码](pay/wei.jpg) |

## 致谢

- UI 实测配方参考：UnrealQuest（ClientAPI.lua）；状态变量设计参考：Cat（TurtleWoW）；最初参考：OneJudge。
- 感谢每一位测试与反馈的玩家。